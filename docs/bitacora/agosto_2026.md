# Bitácora - Agosto 2026

- **14 de Agosto de 2026** *(filtros de selección múltiple en el Tablero de Agendas, [financial_agendas.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/public/financial_agendas.py) [MODIFY], [FinancialAgendasPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/admin/reports/FinancialAgendasPage.jsx) [MODIFY])**:
  - **Pedido del usuario** (sesión de administrador → Closers → Registro Agendas): antes de mejorar la exportación, permitir **elegir varias opciones en cada filtro**, como ya se hacía en Registro Ventas.
  - **Frontend**: los 5 `<select>` de la barra (Estado pre call, Estado post call, Closer, Fuente, Call Confirmer) pasan a ser píldoras desplegables con casillas (`MultiSelectPill`, local a la página, con el mismo estilo del resto de la barra: resumen "N seleccionados", atajo "Limpiar" y cierre al hacer clic afuera). El valor se sigue guardando como **string separado por comas** —igual que en Registro Ventas— así que los filtros ya persistidos en `localStorage` (`filters_financial_agendas`) siguen siendo válidos sin migración: un valor viejo como `"Show Up"` se lee como una selección de un solo elemento.
  - **Backend** (`GET /public/financial-agendas`): se extrajo el armado de la consulta a `_build_agenda_queries()` (fechas + búsqueda + estado + closer + fuente + call confirmer + orden), que ahora parte cada parámetro por comas y filtra con `IN` en vez de `==`. `Sin Asignar` conserva su caso especial (encargado nulo o vacío) y ahora se puede **combinar** con nombres reales. El estado post call se sigue filtrando en memoria porque es calculado, pero contra un conjunto de valores. Mandar un solo valor se comporta exactamente igual que antes, así que el resto de consumidores del endpoint (Triaje, Registro Ventas, `AttributionModal`, Tablero de llamadas) no se ven afectados.
  - **Verificado en el navegador** (sesión de operador simulando al admin Santiago, datos reales de la base local): los conteos de las selecciones múltiples suman exactamente igual que los individuales — Show Up 23 + No Show 35 = 58 combinados; Jean Carlo 64 + Marlon Closer 10 = 74; Pendiente 18 + Confirmado 26 = 44; Elias 65 + Sebastian 4 = 69; `Sin Asignar` 17 + Dari 107 = 124 (el total del período). `npm run build` compila sin errores.

- **13 de Agosto de 2026** *(continuación — la causa de fondo: los modales del mazo nunca se desmontaban al cerrarse, [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **El usuario reportó que el seguimiento seguía sin desaparecer** después del arreglo anterior. Reproducido en pantalla: la lista **sí se actualizaba** (el registro salía del listado), pero el **modal del lead quedaba pegado encima**, opaco y mostrando el mismo lead que se acababa de procesar. Desde el punto de vista del closer, el seguimiento "no desapareció"; y como el overlay ocupa toda la pantalla y captura los clics, la única salida era F5.
  - **No era del seguimiento ni del dev server**: cerrar el modal con la "×" dejaba el mismo overlay pegado, y se reprodujo igual en el **build de producción** servido por Flask. Pasaba con todos los modales de la pantalla (se verificó también "Nueva agenda").
  - **Causa**: `AnimatePresence` con un `<div>` plano como hijo directo y el `motion.div` anidado adentro. Con framer-motion 12.29.2 + React 18.3.1, ese hijo nunca recibe el "safe to remove" y el nodo se queda montado para siempre. Se probó la forma documentada (hijo directo `motion.div` con `key`): la animación de salida corre —el overlay queda en `opacity: 0`— pero **tampoco se desmonta**, así que seguía tragándose todos los clics, ahora encima de forma invisible. Los tres `AnimatePresence` que sí están bien usados (listas de tarjetas, hijos `motion` con `key` y sin `motion` anidado adentro) funcionan correctamente: filtrando la búsqueda, las 10 tarjetas salen del DOM como corresponde. Quedan como están.
  - **Arreglo**: los 8 overlays de la pantalla se renderizan condicionalmente, sin `AnimatePresence`. Se conserva la animación de entrada; se pierde solo el fundido de salida de 0,2s. Se eligió esto antes que pelear con la librería porque un modal que no cierra deja la app inusable, y el desmontaje tiene que ser determinista en dev y en producción.
  - **Alcance**: se arreglaron los 8 de `CloserWorkflowPage` (el del lead, decisor, prompt de venta, declaración de venta, motivo, nueva agenda, referido manual, selector de agenda y celebración). El mismo patrón aparece en otros 31 archivos del frontend (57 usos de `AnimatePresence` en total) y probablemente tenga el mismo problema — queda pendiente revisarlos.
  - **Verificado en pantalla**: en dev, completar el seguimiento de Erick Andaverde cierra el modal y baja la lista de 36 a 35 sin recargar; abrir y cancelar "Nueva agenda" y "Referido manual" ya no deja overlays en el DOM. En el build de producción recién compilado, abrir el modal de un lead y cerrarlo con "×" lo saca del DOM (antes quedaba con `opacity: 1` para siempre). `npm run build` sin errores.

- **13 de Agosto de 2026** *(continuación — el seguimiento completado desaparece de la lista sin recargar la página, [SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Reportado por el usuario**: "en la pestaña de seguimientos, al completar un seguimiento, el registro no desaparece, hay que recargar la página".
  - **Causa**: la pestaña de seguimientos es el único panel del mazo que **no se alimenta del estado `agendas`**. `SeguimientosPane` pide sus propios datos a `/closer/followups/today`, `/pool`, `/pool-counts` y `/goal`, y su `useEffect` solo dependía de `selectedDate`. Todas las acciones del modal (`saveSeguimientoReport`, `handleConfirmFollowUp`, `handleMarkFollowUpDone`, descartes, ventas) terminan llamando a `fetchAgendas()`, que recarga el mazo pero **no le dice nada al panel**: guardaba bien en la base, y la lista en pantalla seguía siendo la de antes hasta el próximo montaje del componente (o sea, hasta recargar la página).
  - **Arreglo**: `fetchAgendas` sube un contador `seguimientosRefreshKey` que el panel recibe como prop y usa en las dependencias de sus dos cargas. Se eligió meterlo **en `fetchAgendas` y no en cada handler** porque ese es el punto por el que ya pasan todas las mutaciones del mazo: cualquier acción futura hereda el refresco sin acordarse de agregarlo. La única llamada que no lo sube es la del `useEffect` de cambio de pestaña/día, donde el panel recién se monta y ya está pidiendo sus datos por su cuenta.
  - **Sin parpadeo**: la recarga por acción no vuelve a mostrar "Cargando seguimientos...". El spinner de pantalla completa (y el del pool) solo aparece cuando todavía no hay nada que mostrar — primer render o cambio de día —; en una recarga por acción la lista se reemplaza en silencio.
  - **Verificado en pantalla** (sesión simulada como closer Jean Carlo, base local): con 43 seguimientos asignados, completar el de "Aline Despaux" programando el intento 2 lo saca de la lista al instante, sin recargar. El grupo pasa de "Llamadas no tomadas · 21" a · 20, el total de 43 a 42 y el aviso de atrasados de 36 a 35. En la pestaña de red se ve una sola tanda de los tres endpoints después del `POST /closer/deck/5957`, y la consola queda sin errores.

- **13 de Agosto de 2026** *(continuación — el closer ya puede corregir los datos del lead desde el mazo, nuevo [LeadEditModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/LeadEditModal.jsx), [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: "revisá que el closer tenga cómo corregir fácilmente la información del lead (nombre, teléfono, correo, fecha y hora de la llamada)".
  - **Auditoría: no lo tenía**. `PATCH /closer/customers/<id>` existe desde hace tiempo y acepta `full_name`, `email`, `phone` e `instagram` con rol `closer`, pero **ningún componente del mazo lo llamaba** — confirmado por grep en todo el frontend: los únicos usos eran `SaleDetailModal` (flujo de ventas) y `ClientsPage`, que solo lee. Lo único que el closer podía cambiar era la fecha/hora, y solo desde flujos puntuales como "agendar 2ª llamada" o reprogramar; si un lead entraba con el nombre mal escrito o un teléfono con typo, no había forma de arreglarlo desde el mazo.
  - **`LeadEditModal`**: lápiz en la cabecera del modal de detalle del lead, al lado de la papelera. Edita nombre, teléfono, correo, instagram y fecha/hora de la llamada. Manda **solo lo que cambió**, así corregir un teléfono no reescribe el resto de la ficha, y son dos endpoints porque son dos entidades (contacto → cliente, fecha → cita). Respeta `can_edit === false`: en un lead de otro closer el botón no aparece, igual que el de eliminar.
  - **Detalles que evitan sorpresas**: el backend descarta el mail si no tiene `@` (lo guarda como `NULL`), así que el modal valida antes de mandar en vez de dejar creer que se guardó; la hora se aclara que es la de la zona horaria del closer y que mover la agenda de un lead confirmado hay que avisárselo; al guardar se cierra el modal de detalle y se recarga el mazo, para que el lead no quede leyéndose de la copia vieja.
  - **Verificado contra la base local** (recién sincronizada desde producción): el ISO exacto que manda el modal (`2026-08-20T15:30:00.000Z`) lo parsea bien `datetime.fromisoformat` tras quitar la `Z`; `update_client` normaliza como se espera (recorta espacios, pasa el mail a minúsculas, saca el `@` del instagram); un mail sin `@` efectivamente queda en `None`; y el registro se restauró a sus valores originales al terminar la prueba. `npm run build` sin errores.

- **13 de Agosto de 2026** *(continuación — el aviso de reportes faltantes dice CUÁLES son y lleva directo a cada día, [closer_dashboard_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_dashboard_service.py) [MODIFY], nuevo [MissingDaysPicker.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/MissingDaysPicker.jsx), [dataIssues.js](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/dataIssues.js) [MODIFY], [DataIssuesPanel.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/DataIssuesPanel.jsx) [MODIFY], [FixIssueModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/FixIssueModal.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: "al hacer click en ir al reporte del día, me muestre los días que hay que revisar para ir directo a ese día".
  - **Qué faltaba**: el aviso decía "faltan 11 reportes" pero no cuáles. El closer tenía que ir probando fecha por fecha en el selector del reporte hasta dar con los días sin enviar.
  - **Backend**: `_reports_coverage` ya recorría el período día por día para contar; ahora además devuelve `dias_faltantes` con las fechas concretas (por closer en `detalle`, y a nivel raíz cuando el dashboard está acotado a UN closer — con "todos los closers" queda vacío porque cada uno debe los suyos y no hay una lista única que tenga sentido). Un closer siempre entra acotado a sí mismo, así que siempre la recibe.
  - **`MissingDaysPicker`**: chips con día de la semana y fecha ("mié 13 ago"), 8 visibles y el resto detrás de un "+N más". Cada chip abre la pestaña del reporte **ya posicionada en esa fecha**. Aparecen tanto en la tarjeta del aviso como dentro del modal de pasos.
  - **`onNavigate` pasa a aceptar opciones**: `onNavigate('report', { date })` — `CloserWorkflowPage` setea `reportDate` antes de cambiar de pestaña, así el reporte abre en el día pedido en vez de en hoy.
  - **Detalle de zona horaria**: las fechas se parsean a mano y no con `new Date('YYYY-MM-DD')`, que las interpreta como UTC y en zonas al oeste de Greenwich muestra el día anterior — el mismo error que ya se corrigió en agosto en `CloserWorkflowPage`.
  - **Verificado contra producción**: para Jean Carlo en agosto devuelve los 11 días exactos (`2026-08-01` a `05`, `08` a `13`), que son los que efectivamente no tienen reporte, y con "todos los closers" la lista viene vacía como corresponde. `npm run build` sin errores.

- **13 de Agosto de 2026** *(el script de sincronización no copiaba 9 tablas y dejaba la limpieza a medias, [actualizar_db.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/scripts/actualizar_db.py) [MODIFY])**:
  - **Encontrado al ejecutar la sincronización pedida por el usuario**: la copia terminó "con éxito" pero `installment_plans` quedó con 11 filas contra 21 en producción. La lista `modelos` del script se fue quedando atrás respecto de `app/models`: **9 modelos nunca se sincronizaban** — `InstallmentPlan` (toda la deuda del dashboard), `FeatureToggle` (el toggle del bloqueo del reporte, agregado ayer), `CloserAlias`, `CommentNotification`, `ClientMergeLog`, `WorkshopEvent`, `WorkshopLead`, `LandingSession`. Verificado por diff entre `app.models.__all__` y los imports del script; ahora la diferencia es cero.
  - **Bug de la limpieza — el `rollback` deshacía borrados ajenos**: el bucle de limpieza borraba modelo por modelo con un solo `commit()` al final, y cada `except` hacía `db.session.rollback()`. Como la transacción era una sola, ese rollback **deshacía todos los borrados acumulados hasta ese momento**, no solo el que falló. Al agregar los modelos nuevos salió a la luz: `landing_sessions` y `workshop_leads` no existían en local (migraciones sin aplicar), sus borrados fallaron, y la copia posterior murió con `UNIQUE constraint failed: notifications.id` sobre una tabla que se creía vacía. Ahora se hace commit por modelo, así una tabla faltante solo se saltea a sí misma.
  - **El `print` del except tapaba el error real**: los mensajes de SQLAlchemy incluyen las filas que fallaron, y ahí vienen emojis y acentos de datos reales. En la consola de Windows (cp1252) el propio `print` reventaba con `UnicodeEncodeError` y el traceback que se veía era el del print, no el del fallo. Nuevo helper `safe()` que codifica con `errors='replace'`.
  - **Resultado**: `flask db upgrade` en local (3 migraciones pendientes: `workshop_leads`, su dedupe, y `landing_sessions`) y re-sincronización completa sin una sola advertencia — 4.875 clientes, 3.285 citas, 842 ventas, 189 reportes diarios, 21 planes de cuotas, 14 workshop leads y 11 landing sessions.

- **13 de Agosto de 2026** *(continuación — los datos que no cierran explican cómo arreglarlos y llevan al lugar donde se arreglan, nuevos [dataIssues.js](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/dataIssues.js), [DataIssuesPanel.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/DataIssuesPanel.jsx) y [FixIssueModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/FixIssueModal.jsx), [CloserDashboard.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/CloserDashboard.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: "donde se vea un dato erróneo debes poner un mensaje explicando la acción que hay que hacer para solucionarlo, incluso si puedes poner un enlace directo al lugar donde el closer puede solucionar el problema, mejor, si es necesario crear modales momentáneos".
  - **`detectIssues(data)` — 5 problemas detectados** sobre el payload que ya llega, sin tocar el backend: close rate por encima del 100%, días sin reporte diario, más agendas que slots, llamadas asistidas sin ninguna oferta marcada, y pagos con un tipo que el parser no reconoce.
  - **Regla explícita de qué entra**: solo problemas de **captura de datos** con una acción clara. Un pitch rate bajo o un show rate bajo NO entran aunque estén en rojo — pueden ser performance real, y marcarlos como "dato erróneo" enseñaría a ignorar los avisos. Los problemas de negocio siguen en "Qué mirar esta semana".
  - **Cada aviso trae 4 cosas**: qué pasa, **por qué** pasa (la explicación de fuentes que el usuario pidió mantener), qué datos ensucia, y el arreglo. Botón primario que **navega directo** a la pestaña donde se corrige (`onNavigate` lo pasa `CloserWorkflowPage` con su propio `setActiveView`, así que "Ir al reporte del día" abre esa pestaña de verdad) y botón secundario "Cómo lo arreglo" que abre un **modal con los pasos numerados** y el mismo CTA al final.
  - **Panel arriba de todo**, antes de los KPIs: si el embudo está incompleto, leer los KPIs de abajo lleva a conclusiones falsas. Cuando no hay nada roto la tarjeta sigue estando pero en verde ("los datos del período cierran") — que el espacio esté siempre reservado hace que la ausencia de problemas también sea una señal.
  - **Verificado con los payloads reales de producción**: agosto global dispara 2 avisos (29 reportes faltantes + 95 agendas contra 58 slots); filtrado a Jean Carlo dispara el close rate de 120% en rojo con su acción; un caso con asistencias y cero presentaciones dispara el aviso de la oferta sin marcar; y un período sano no dispara ninguno. `npm run build` sin errores.
  - **No verificado en pantalla**: la app pide login y no tengo credenciales.

- **13 de Agosto de 2026** *(continuación — rediseño de "Calidad de la llamada": de 6 anillos sueltos a la cadena real de la llamada, [PerformanceQuality.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceQuality.jsx) [MODIFY], [CloserDashboard.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/CloserDashboard.jsx) [MODIFY])**:
  - **Pedido del usuario**: mejorar visualmente la sección "Calidad de la llamada".
  - **Qué tenía mal**: 6 anillos SVG idénticos en una fila, todos del mismo tamaño y sin ningún número atrás. Un 54% y un 0% se veían igual de importantes; no se sabía sobre cuántas llamadas estaba calculado ninguno (un 100% sobre 2 llamadas se ve idéntico a uno sobre 40); y no había forma de saber si un 54% era bueno o malo.
  - **Ahora se lee como la cadena que es**, en dos grupos: **Llegar a la llamada** (confirmation rate, show rate, show s/ confirmada) y **Convertir la llamada** (pitch rate, close s/ llamada, close s/ presentación). Es el mismo orden del embudo, así que se ve dónde se corta.
  - **Cada tasa muestra ahora**: el porcentaje grande coloreado por salud, un chip de estado (bien / a mejorar / crítico / imposible), **la fracción real detrás** ("54 de 100 agendas" — el dato que más faltaba), una barra con la marca de referencia, una línea de qué significa en una frase, y el tooltip de fuente y fórmula. Los numeradores/denominadores se derivan del embudo y de las confirmaciones que ya vienen en el payload: no hizo falta tocar el backend.
  - **"Lo que más te frena"**: se calcula sobre la salud *relativa al umbral*, no sobre el porcentaje crudo — un 45% de close rate es excelente y un 45% de show rate es un problema, así que compararlos en bruto habría señalado siempre al close rate como el peor. La tarjeta ganadora se marca con borde rojo y un rótulo "eslabón más débil", y se resume arriba a la derecha.
  - **Umbrales a la vista pero honestos**: la marca "▲ ref." de cada barra lleva al pie la aclaración de que es el umbral que ya usaban las alertas del sistema para colorear, no una meta asignada a nadie.
  - **Bug menor encontrado de paso**: `border-base-hover`, usado en 3 lugares del dashboard, mapea a `--color-border-hover`, una variable que no existe en el tema — ese hover nunca hizo nada. En las tarjetas nuevas se usa el color de marca; los otros 2 usos quedan como estaban (fuera del alcance de este cambio).
  - `npm run build` sin errores. **No verificado en pantalla**: la app pide login y no tengo credenciales.

- **13 de Agosto de 2026** *(cada dato del dashboard explica de dónde sale: catálogo de procedencia + tooltips, nuevos [metricSources.js](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/metricSources.js), [MetricTip.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/MetricTip.jsx) y [DataSourceLegend.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/DataSourceLegend.jsx), + los 6 paneles del dashboard [MODIFY])**:
  - **Pedido del usuario**: "quiero que cada dato que se vea en el dashboard tenga un tooltip que explique de dónde viene ese dato".
  - **`metricSources.js` — un catálogo, no tooltips sueltos**: ~45 métricas, cada una con su fuente, su fórmula exacta y la nota de qué mirar. Está centralizado a propósito: el mismo dato aparece en varias tarjetas (el close rate está en los KPIs, en Calidad de la llamada y en el ranking) y si la explicación vive en cada componente se desincronizan en cuanto cambie un cálculo del backend.
  - **Las cuatro fuentes quedan explícitas**, porque su mezcla es la causa de casi todos los números raros: `reporte` (CloserDailyReport, solo existe los días que el closer reportó), `agendas` (Appointment, no depende del reporte), `ventas` (FinancialSale, cubre el período completo) y `cuotas` (InstallmentPlan, saldo histórico sin filtro de período). Cada tooltip lleva su badge de color y los cálculos que cruzan dos fuentes lo dicen — "show s/ confirmada", por ejemplo, aclara que divide asistencias del reporte por confirmadas de las agendas.
  - **`MetricTip`**: tooltip por portal a `<body>` (las tarjetas tienen `overflow-hidden` y scroll propio, ahí un tooltip absoluto queda recortado), con tokens de tema en vez de los slate fijos de `StatTooltip` — se ve igual en claro y oscuro. Se abre con hover, con foco de teclado y con click, que es lo que lo hace usable en touch. Se cierra con Escape o al scrollear, y se voltea hacia abajo si el dato está en la primera fila.
  - **`DataSourceLegend`**: franja colapsable arriba de todo con las cuatro fuentes explicadas en una línea cada una, y el conteo de reportes sin enviar si los hay. Colapsada por defecto para no robarle espacio a los KPIs.
  - **Umbrales centralizados**: los `good`/`warn` de cada tasa salen de `metricSources.js` y son **los mismos que ya usaban las alertas del backend y las tablas** (show 55/40, pitch 70, close 40) — no son metas nuevas inventadas, solo dejaron de estar repetidas como cadenas de ternarios en cada tarjeta.
  - `npm run build` sin errores. **No verificado en pantalla**: la app pide login y no tengo credenciales.

- **12 de Agosto de 2026** *(continuación — las confirmaciones se separan entre las del día/período y las de próximas agendas, [closer_dashboard_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_dashboard_service.py) [MODIFY], [closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY], [PerformanceFunnel.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceFunnel.jsx) [MODIFY], [CloserDashboard.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/CloserDashboard.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: "las confirmaciones que se deben reflejar en el dashboard y del reporte diario deben verse por separado, las del día actual del reporte y las de las próximas agendas".
  - **Reporte diario**: `get_daily_activity_summary` devolvía un solo `confirmados` — todas las citas que el closer tocó ese día y quedaron en 'Confirmado', sin importar para cuándo era la cita. Ahora se separa por la **fecha de la cita** (no por cuándo se la tocó) en `confirmados_hoy` y `confirmados_proximos`, y el resumen en vivo muestra dos tarjetas en vez de una. Con datos reales de Jean Carlo, la distinción es enorme: el 6/8 confirmó 11 citas y **solo 3 eran llamadas de ese día, las otras 8 eran agendas futuras**.
  - **Dashboard — «Confirmadas» del embudo era un número inventado**: se calculaba como `agendas − canceladas − reprogramadas`, que no es una confirmación sino "todo lo que no se cayó". Daba **97,9% de confirmation rate en agosto** (y un 50,5% de show sobre confirmada como consecuencia). Nuevo `_confirmations(closer_id, start, end)` que cuenta de `Appointment` las agendas del período que llegaron a `result = 'Confirmado'` — `result` se queda en 'Confirmado' después de la llamada (el resultado real vive en `closer_result`), así que sirve igual para agendas ya pasadas.
  - **Números reales tras el cambio**: agosto 54 de 100 agendas confirmadas (**54,0%**, antes 97,9%) y show sobre confirmada 87,0% (antes 50,5%); julio 70 de 152 (46,1%). El embudo queda monótono y creíble: 95 agendas → 54 confirmadas → 47 asistencias.
  - **Tarjeta "Confirmaciones"** al lado del embudo, con los dos números separados: las del período (54 de 100) y las de **agendas posteriores al período ya confirmadas** (6 de 16 por venir) — pipeline hacia adelante que no pertenece a este embudo. Mezclarlas infla el confirmation rate con trabajo cuya llamada todavía no pasó: en julio eran 60 confirmaciones de agendas futuras, casi tantas como las 70 del propio mes.
  - **Un solo confirmation rate en pantalla**: el anillo pasa a usar el mismo numerador y denominador que la tarjeta, para no mostrar 56,8% y 54,0% uno al lado del otro solo porque uno dividía por las agendas de los reportes diarios y el otro por las reales.
  - **Verificado contra producción** (agosto y julio, agregado y por closer) y `npm run build` sin errores.

- **12 de Agosto de 2026** *(continuación — se suspende el bloqueo del reporte diario por trabajo atrasado, ahora gobernado por un toggle, [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], nuevo [ReportBacklogTogglePanel.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/operations/ReportBacklogTogglePanel.jsx), [OperationsSettingsPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/operations/settings/OperationsSettingsPage.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: "si el closer no hace los seguimientos no le permite hacer su reporte diario, vamos a suspender eso de forma momentánea, en unos días lo volvemos a activar cuando el closer se ponga al día".
  - **Por qué un toggle y no borrar el bloqueo**: es explícitamente temporal ("en unos días lo volvemos a activar"). Una constante en código habría necesitado un deploy para revertirlo, así que se maneja como `FeatureToggle` con la clave `bloqueo_reporte_backlog`, igual que el toggle ya existente de la auditoría de leads. **Sin fila en la tabla = suspendido** (mismo criterio `bool(toggle and toggle.is_active)` que usa el resto del proyecto), así que queda apagado al desplegar sin tener que escribir nada en producción — verificado: la tabla `feature_toggles` está vacía hoy.
  - **Nuevo panel** en Operaciones → Configuración → "Bloqueo del Reporte": switch de un click con la explicación de qué traba exactamente (confirmaciones nunca gestionadas, llamadas confirmadas sin resultado, seguimientos sin realizar) y quién lo cambió por última vez. Es lo que hay que tocar para reactivarlo en unos días.
  - **El atraso se sigue mostrando igual**: `get_previous_days_pending` no cambia y la tarjeta de la pestaña "Reporte del día" sigue listando el detalle — pero en ámbar y como recordatorio ("podés enviar el reporte igual mientras te ponés al día, pero esto sigue contando como atraso") en vez de en rojo bloqueando. El botón de enviar deja de estar deshabilitado. `GET /closer/deck/daily-report` devuelve `backlog_blocks_report` para que el frontend sepa en cuál de los dos modos está, en vez de decidirlo por su cuenta.
  - El admin seguía y sigue exento del bloqueo. `npm run build` sin errores.

- **12 de Agosto de 2026** *(continuación — bugfix: la columna "Reportes" del ranking repetía el mismo número global en todas las filas, [closer_dashboard_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_dashboard_service.py) [MODIFY], [PerformanceRanking.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceRanking.jsx) [MODIFY])**:
  - **Encontrado revisando el resto de los datos del dashboard**: cada fila del ranking mostraba `reports_productivity.al_dia_pct` de su propio `get_comprehensive_stats`, pero ese bloque **se calcula sobre todos los closers activos sin filtrar por `closer_id`** (ver el `User.query.filter_by(role='closer', is_active=True)` de `get_comprehensive_stats`). Resultado: el mismo porcentaje global repetido en las 3 filas, leyéndose como si fuera de cada closer. Hoy no se notaba porque da 0% para todos (nadie reporta desde el 9/8).
  - **Fix**: la columna pasa a mostrar la cobertura de reportes **de ese closer en el período** (`_reports_coverage`), que es la que corresponde al resto de la tabla, y se renombra a "Reportes enviados". Verificado contra producción: agosto da 16,7% / 33,3% / 8,3% y julio 80,6% / 80,6% / 6,5% — tres números distintos por fila, y siguen siendo correctos al filtrar el dashboard por un closer puntual.

- **12 de Agosto de 2026** *(continuación — la deuda separa lo vencido de lo por vencer y recupera los nombres perdidos, [closer_dashboard_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_dashboard_service.py) [MODIFY], [PerformanceMoney.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceMoney.jsx) [MODIFY], [PerformanceKpis.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceKpis.jsx) [MODIFY])**:
  - **Pedido del usuario**: "la deuda no sé si está bien calculada".
  - **Auditada contra producción**: el cálculo en sí es correcto — suma los `InstallmentPlan` con `estado != 'pagado'`, que es el cronograma real de cobro. Los 4.825 de hoy cuadran fila por fila con los 19 planes pendientes de la base. Lo que estaba mal era **cómo se presentaba**.
  - **Faltaba separar vencido de por vencer**: un total grande no dice nada por sí solo. Puede ser un problema de cobranza o simplemente un plan de cuotas recién firmado con todo su cronograma a futuro — que es exactamente el caso actual: **de los 4.825, $0 están vencidos**, las 19 cuotas vencen entre septiembre y noviembre. Ahora `_pending_collections` devuelve `{total, vencido, por_vencer, count, count_vencido}`, cada fila trae su fecha de vencimiento y los días de atraso, y la lista ordena lo vencido primero. La alerta de deuda pasa a dispararse por lo vencido (antes: `total > cash × 2`, que se prendía por un cronograma futuro perfectamente sano).
  - **Nombres perdidos**: 5 planes tienen `client_id` nulo (creados antes de ese campo) y salían como "Sin nombre", inservible para ir a cobrar. Ahora el nombre cae al cliente de la cita que originó el plan.
  - **Hallazgo para el usuario, NO tocado**: con los nombres visibles queda a la vista que **1.550 de los 4.825 (32%) son planes de prueba** — "Kerwin Prueba" (3 cuotas de 300) y "Prueba" (2 de 325), ambos de citas de prueba del closer Sebastian. Son datos de producción: no se borran sin que el usuario lo decida.
  - **Verificado**: totales recalculados contra producción (19 cuotas, 4.825 total, 0 vencido) y `npm run build` sin errores.

- **12 de Agosto de 2026** *(continuación — se explica cómo se cuentan los "slots disponibles" y se avisa cuando el número es imposible, [closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: "la cantidad de slots totales creo que la están escribiendo mal los closers, no puede haber más agendas que slots disponibles, ponles un mensaje para que entiendan cómo escribir ese número".
  - **Confirmado en producción**: pasa de verdad y seguido. Reportes reales de agosto — Jean Carlo el 6/8 puso 7 slots con 8 agendas; Sebastian el 6/8 puso 11 con 21, y el 9/8 puso 11 con 34. La lectura más probable es que estén escribiendo los cupos **que quedaron libres** en vez de la capacidad total, que es lo que el embudo necesita como primer escalón.
  - **Texto explicativo** en la tarjeta "Slots disponibles": la fórmula (`cupos ocupados + cupos que quedaron libres`), que es capacidad total y no los huecos que sobraron, que un cupo agendado sigue contando, que nunca puede ser menor que las agendas del día, y un ejemplo cerrado ("abriste 10, se agendaron 6, quedaron 4 → escribís 10, no 4 ni 6").
  - **Aviso en vivo**: `get_daily_activity_summary` devuelve `agendas_del_dia` (citas cuya hora cae en el día, en cualquier estado). Con eso la tarjeta muestra el piso concreto ("ese día tenés N agendas: los slots tienen que ser N o más") y, si el número escrito queda por debajo, el input se pone ámbar con el motivo. **No bloquea el envío**: una agenda puede entrar fuera de los cupos configurados (un referido manual, por ejemplo), así que es un aviso, no una validación dura.
  - **Verificado**: `npm run build` sin errores.

- **12 de Agosto de 2026** *(continuación — sección propia para analizar las señas, [closer_dashboard_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_dashboard_service.py) [MODIFY], nuevo [PerformanceSenas.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceSenas.jsx), [PerformanceQuality.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceQuality.jsx) [MODIFY], [CloserDashboard.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/CloserDashboard.jsx) [MODIFY])**:
  - **Pedido del usuario**: "debemos analizar las señas por separado, es muy importante ver el porcentaje de señas por presentación y por llamada, las conversiones de seña a pago completo y a split pay".
  - **Qué había**: 3 anillos sueltos ("Conversión de Señas") dentro de "Calidad de la llamada" — solo Oferta → Seña, un "Seña → Venta real" sin decir a qué venta, y el close rate promesa. Ni cuántas señas eran, ni cuánto sumaban, ni la tasa sobre llamadas.
  - **Sección "Señas · reservas, no ventas"** (nueva, entre Calidad de la llamada y Dinero), en dos tarjetas: *cuántas se consiguen* (cantidad, cash total, seña promedio, % sobre presentaciones y % sobre llamadas asistidas — los dos denominadores, cada uno con su base a la vista — más el close rate con señas al pie) y *en qué terminan* (a pago completo / a Split Pay / todavía sin pago, cada uno con cantidad y porcentaje, y la conversión total). Los 3 anillos viejos salen de "Calidad de la llamada".
  - **Backend**: `conversion_senas` (3 porcentajes) se reemplaza por un bloque `senas` completo, alimentado por el desglose por destino agregado en `deposit_conversions` y por el nuevo `deposit_rate_llamada`.
  - **Verificado contra producción**: julio marca 17 señas por 1.662,50 (97,79 promedio), 32,7% sobre presentaciones y 28,8% sobre llamadas, 2 convertidas — **las dos a Split Pay, ninguna a pago completo** — y 15 todavía sin pago. Agosto: 2 señas, ambas pendientes. `npm run build` sin errores.

- **12 de Agosto de 2026** *(continuación — "Composición del cash" nombra las señas por su nombre y muestra la cantidad de pagos de cada categoría, [closer_dashboard_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_dashboard_service.py) [MODIFY], [PerformanceMoney.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceMoney.jsx) [MODIFY])**:
  - **Pedido del usuario**: "en Composición del cash los Depositos / reserva son señas, y pon la cantidad de pagos de esos datos".
  - **Qué cambió**: la fila "Depósitos / reservas" pasa a llamarse **Señas** (`cash_mix.depositos` → `cash_mix.senas`), y cada categoría deja de ser un número suelto: ahora es `{cash, count}` y se muestran las dos cosas. Sin el conteo no se puede distinguir "poco cash porque hubo pocos pagos" de "poco cash porque los pagos fueron chicos" — con datos reales de julio, las 17 señas suman 1.662,50 (97,79 por seña) contra 18 cuotas que suman 6.891.
  - Cada fila suma una aclaración de qué es (PIF + Split Pay / de ventas ya cerradas / reservas, todavía no son venta / clientes que ya habían comprado) y aparece una quinta categoría **"Sin clasificar"** con los pagos cuyo `tipo_pago` el parser no reconoce — se oculta sola cuando está en cero, que es el caso hoy en julio y agosto.

- **12 de Agosto de 2026** *(continuación — el dashboard explica de dónde salen los close rates imposibles: cobertura de reportes diarios, [closer_dashboard_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_dashboard_service.py) [MODIFY], [PerformanceKpis.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceKpis.jsx) [MODIFY], [PerformanceFunnel.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceFunnel.jsx) [MODIFY], [PerformanceRanking.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/CloserDashboard.jsx) [MODIFY])**:
  - **Por qué**: arreglada la clasificación de ventas, Jean Carlo seguía marcando **120%** de close rate s/ presentación en agosto. Investigado contra producción: no queda ningún error de cálculo — el desbalance es de **fuentes de datos**. Las ventas se leen de `FinancialSale` (cubre el período entero, haya reporte o no); las agendas, asistencias y presentaciones se leen de `CloserDailyReport` (solo los días que el closer efectivamente reportó). Jean Carlo **envió 2 de 12 reportes en agosto**: 6 ventas reales contra 5 presentaciones reportadas. Un día sin reporte le baja el denominador sin tocarle el numerador.
  - **`_reports_coverage(closer_id, start, end, active_closers)`** (nuevo): días del período con reporte enviado vs. días esperados, por closer y agregado. Corta contra hoy — los días futuros del mes en curso no cuentan como faltantes. Se expone como `reports_coverage` en el payload del dashboard, y cada fila del ranking suma `presentaciones` y `reportes_faltantes`.
  - **Alertas**: cuando hay más ventas que presentaciones, "Qué mirar esta semana" abre con una alerta roja que dice el número exacto de cada lado y cuántos reportes faltan, en vez de dejar el porcentaje absurdo sin explicación. Si no hay close rate imposible pero sí reportes faltantes, queda una alerta ámbar de cobertura.
  - **En pantalla**: la tarjeta del close rate se pone ámbar con el motivo cuando pasa de 100%; el pie del embudo aclara qué paso sale de qué fuente y muestra "N de M reportes enviados (X%)"; el ranking marca en rojo los close rates >100% con el detalle al pasar el mouse y suma una columna "Sin enviar".
  - **Verificado contra producción**: agosto marca 10 reportes faltantes para Jean Carlo, 8 para Sebastian y 11 para Marlon — la alerta y la tarjeta ámbar aparecen con esos números. `npm run build` sin errores.

- **12 de Agosto de 2026** *(bugfix de datos: solo PIF y Split Pay cuentan como venta, y la clasificación de `tipo_pago` pasa a usar el parser canónico, [closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY])**:
  - **Pedido del usuario**: en el dashboard de closers, "Close rate s/ presentación" marcaba **140% este mes**, imposible. Su diagnóstico: se estaban contando todos los pagos como venta cuando las únicas ventas reales son **PIF/Completo** (paga todo el programa de una) y **Split/Parcial** (primer pago que abre las cuotas). Lo mismo para el ticket promedio.
  - **Confirmado contra producción**: el 140% era de Jean Carlo — 7 "ventas" sobre 5 presentaciones reportadas en agosto. De esas 7, una era `RR - Renovacion` (cash de un cliente que ya había comprado, no una venta nueva): `total_sales` sumaba `pif + split + upsell + renovacion`.
  - **Segundo bug, más silencioso — la clasificación de `tipo_pago` era una cadena de `in` sobre el string crudo**: `'parcial'` (la etiqueta real de todos los Split Pay en la base: `RR - Parcial`, `AL - parcial`, …) **no matcheaba ninguna rama** y solo terminaba contada como split por caer en el `else` final. Ese mismo `else` se tragaba también cualquier etiqueta desconocida — `Ace Learner` (16 filas históricas) y `Desconocido` (4) se reportaban como Split Pays que nunca existieron.
  - **Fix**: la clasificación pasa a `SheetsService.parse_tipo_pago`, el parser canónico que ya usaban el alta de ventas y el reporte diario (normaliza acentos, mayúsculas y el prefijo "Con "). Se agrega un bucket `otros` para lo no reconocible: suma al cash total, no a las ventas — si crece, hay etiquetas nuevas que normalizar en el origen (Ventas_DB). `total_sales = pif + split`, nada más. Mismo criterio aplicado en `compute_daily_report_fields` (el reporte diario tenía el mismo `else: key='split'`).
  - **Ticket promedio y programas**: el filtro era por descarte ("todo lo que no sea seña ni cuota"), así que upsells, renovaciones y filas sin tipo reconocible entraban al promedio. Ahora solo entran `completo` y `parcial`. Además el programa se toma del prefijo real (`RR - …`) en vez de buscar `"AL"` dentro del string crudo — bug concreto: `"PARCIAL"` **contiene** `"AL"`, así que las 85 ventas históricas etiquetadas solo `Parcial` (sin prefijo) quedaban todas atribuidas a Ace Learners.
  - **Conversión de señas**: se separa en qué termina cada seña — a **pago completo** o a **split** — en vez de un único "convertida". Y se agrega el corte `date >= fecha de la seña`, que arregla un falso positivo real: antes valía como conversión una venta **anterior** a la seña (un cliente que compró meses atrás y ahora dejó una seña de otro programa contaba como convertido de entrada). Se expone `deposit_rate_llamada` (señas sobre llamadas asistidas) junto al ya existente `offer_to_deposit` (sobre presentaciones).
  - **Verificado contra la base de producción (solo lectura)**: agosto pasa de `close_rate_presentacion` 53.3% → 46.7% global y de 140% → 120% en Jean Carlo (el 20% que sobra **no** es un bug de cálculo: le faltan reportes diarios, ver la entrada siguiente). Ticket promedio de agosto 733.75 → 767.14, que cuadra a mano con las 7 ventas reales del mes (5.370/7). Conversión de señas de julio: 17 señas, 2 convertidas, ambas a split, 0 a PIF.

- **12 de Agosto de 2026** *(feature: el closer ve los días de retraso de cada seguimiento, [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [MODIFY])**:
  - **Pedido del usuario**: que en la pestaña "③ Seguimientos" el closer pueda ver los días de retraso que tiene en cada seguimiento.
  - **Qué faltaba**: la fila de seguimiento solo mostraba "Call hace Nd" (`days_since_call`, días desde la **llamada**), que no dice nada del compromiso incumplido — un seguimiento agendado para el 8 y no hecho puede tener la call hace 212 días (caso real en la base: Juan Mejía) o hace 5, sin relación con cuánto hace que se debería haber contactado. La lista "📅 Asignados para hoy" mezcla los seguimientos de hoy con todos los arrastrados de días anteriores (filtra `fecha_seguimiento <= selected_date`), así que sin este dato el closer no podía distinguir uno de otro ni priorizar.
  - **Backend — `dias_retraso` (nuevo campo en el payload)**: helper `_dias_retraso(fecha_seguimiento, reference_date)` = días entre la fecha en que estaba agendado el seguimiento y la fecha de referencia. Positivo = atrasado, `0` = toca hoy, negativo = todavía no le toca, `None` cuando la cita no tiene fecha asignada (todo el "📥 Pool sin fecha") o cuando el valor guardado no es una fecha ISO válida — `Appointment.fecha_seguimiento` es una columna de texto, así que el parseo va con `try/except` en vez de asumir formato. Se expone desde `_serialize` y también desde `_cerrada_pool_items` (que arma sus dicts a mano, sin pasar por `_serialize`).
  - **Referencia = el día que el closer está mirando, no `date.today()`**: `get_today_grouped` le pasa a `_serialize` la fecha seleccionada en la pantalla, así que si el closer revisa un día pasado ve el retraso que había **ese** día, coherente con la lista que se le muestra (que ya está filtrada por esa misma fecha). El pool sin fecha cae a hoy por defecto, irrelevante porque ahí `fecha_seguimiento` es `NULL`.
  - **Frontend**: chip de retraso al principio de la fila de cada seguimiento, coloreado por gravedad para poder priorizar de un vistazo — ámbar de 1 a 6 días, rojo de 7 en adelante ("⏰ 9d de retraso"), violeta "Para hoy" cuando toca hoy, y gris "En Nd" para los agendados a futuro (aparecen en el pool de cerradas). El encabezado de "📅 Asignados para hoy" suma un contador resumen ("3 atrasados · hasta 19d") que solo se muestra si hay alguno. El orden de la lista ya era el correcto (`fecha_seguimiento` ascendente = lo más atrasado primero), no hizo falta tocarlo.
  - **Verificado contra la base local real** sobre los 3 closers activos: 39 seguimientos del día con `dias_retraso` coherente al 100% con `hoy - fecha_seguimiento` (0 incoherencias, 0 `None` inesperados), rangos reales de 0 a 19 días — y los mismos leads con `days_since_call` completamente distinto (Juan Mejía: 2 días de retraso contra 212 desde la call), que es exactamente la confusión que este campo elimina. Con fecha de referencia pasada (`2026-08-05`) el retraso baja como corresponde (un seguimiento del 3 pasa de 9 a 2 días). Pool sin fecha: los 57 items de `no_tomada` devuelven `dias_retraso: None` sin excepción; en el pool de cerradas aparecen tanto atrasados (5) como futuros (-27), ambos renderizados. `_dias_retraso` con texto no-fecha, cadena vacía y `None` devuelve `None` sin romper. `npm run build` sin errores.

- **11 de Agosto de 2026** *(continuación — recuperar el formulario de n8n cruzando teléfono/correo/instagram cuando el cliente vinculado a la cita no tiene respuestas propias, [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**, tras el fix anterior de esta misma sesión (mostrar `Client.form_data` en la pestaña "Formulario"): seguía viendo "Este lead no completó el formulario de calificación" en la mayoría de los leads, pese a que — según el usuario — Calendly no deja agendar sin responder ese formulario, así que en teoría todos deberían tenerlo. Pidió cruzar por teléfono, correo o instagram para recuperar la respuesta si quedó guardada en otro registro.
  - **`_recover_form_data_by_contact(client)` (nuevo, en `app/api/closer.py`)**: cuando el `Client` vinculado a la cita no tiene respuestas útiles, busca otro `Client` con el mismo correo (exacto, sin mayúsculas), el mismo instagram (sin `@`, sin mayúsculas) o el mismo teléfono — comparando solo los **últimos 8 dígitos** del teléfono (`_normalize_phone_digits`) para tolerar que un lado tenga el código de país y el otro no, o distinto formato de espacios/guiones/`+`. Si hay varios candidatos con respuestas útiles, se queda con el de `submitted_at` más reciente. Solo se activa con `recover_form=True`, pasado únicamente desde `GET /closer/deck/card/<id>` (la vista de un lead puntual en el modal) — **no** se activa en el listado masivo del mazo (`GET /deck`), para no multiplicar esta búsqueda extra por cada tarjeta cuando se listan muchas citas a la vez.
  - **Investigación previa, antes de tocar código**: se midió el alcance real contra la base local completa (3272 citas con cliente) para entender si el problema era realmente de vinculación. Resultado: **339 ya tenían respuestas útiles directamente** (el fix anterior), pero de las **2933 restantes, solo 2 son recuperables** cruzando teléfono/correo/instagram contra cualquier otro `Client` de la base — el resto (2931) simplemente no tiene ningún registro en todo el sistema con respuestas de calificación reales para ese contacto. Se verificó además que la columna `Client.form_data` casi nunca es `NULL` a nivel de base (4804/4805), pero en la enorme mayoría de los casos guarda el literal JSON `null` sin ninguna respuesta real — de ahí que el chequeo de "¿respondió o no?" tenga que mirar si hay claves de respuesta con valor, no solo si la columna existe.
  - **Conclusión importante para el usuario**: la falta de respuestas visibles **no es mayormente un problema de vinculación** dentro de NeurOPS (ese cruce solo rescata un puñado de casos) — la enorme mayoría de las citas nunca tuvieron ninguna sumisión de formulario registrada en el sistema para ese contacto, en ningún cliente. Si en la práctica todos los leads responden el formulario antes de agendar en Calendly, el corte más probable está en el webhook de n8n que llama a `POST /public/financial-agendas/form` (no siempre se dispara, o falla silenciosamente para la mayoría de los leads) — algo fuera del alcance de este repositorio, a revisar directamente en el flujo de n8n/Calendly.
  - **Aviso en pantalla cuando SÍ se recupera**: para no hacerle creer al closer que esas son las respuestas propias del lead sin más, la pestaña "Formulario" ahora muestra un aviso ("⚠ Recuperado de otro registro con el mismo teléfono/instagram/correo — verificá que corresponda a este lead") cuando el backend marca `form_data_recovered: true`.
  - **Verificado end-to-end vía HTTP real** (Flask test client con JWT de un closer real): ubicada una cita real (`Appointment #5882`, cliente `Lysander Diaz`) sin form propio pero recuperable por teléfono — `GET /closer/deck/card/5882` devuelve las respuestas reales del formulario recuperado (`examen`, `profesion`, `formacion`, etc.) con `form_data_recovered: true`. `npm run build` sin errores.
  - **Reportado por el usuario**: en la esquina superior del modal de detalle de lead, el botón que elimina al prospecto se veía como un signo "+".
  - **Diagnóstico**: el botón usaba el ícono `X` de `lucide-react` rotado 45° (`<X className="rotate-45" />`) para simular visualmente una papelera/cruz de eliminar — a esa rotación, una X se ve como un "+", no como una cruz de "eliminar".
  - **Corrección**: se reemplaza por el ícono `Trash2` (papelera) de `lucide-react`, sin rotación. Único punto del archivo con este patrón (`rotate-45` no aparece en ningún otro lugar).

- **11 de Agosto de 2026** *(bugfix: las respuestas del formulario de n8n no se veían en la pestaña "Formulario" del modal de lead, [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Reportado por el usuario**: las respuestas del formulario que llegan desde n8n no se ven en el modal del lead — la pestaña "Formulario" decía que el lead no había respondido nada.
  - **Diagnóstico (dos orígenes distintos de "formulario" que nadie había cruzado)**: las respuestas de n8n se guardan en `Client.form_data` (columna JSON suelta, escrita por `POST /public/financial-agendas/form`), mientras que la pestaña "Formulario" del modal solo leía `survey_answers`, que sale de la tabla `SurveyAnswer` — la encuesta propia de la página de reserva, un origen completamente distinto. Como casi ningún lead pasa por esa encuesta interna, la pestaña mostraba "No hay respuestas a la encuesta registradas." aunque el lead sí hubiera respondido el formulario de n8n. Confirmado contra la base local: **433 clientes tienen `form_data` real** (dict no vacío) contra apenas **16 filas en toda la tabla `survey_answers`**; verificado sobre citas reales (clientes 3322/3329/3440) que `_format_appointment_for_deck` devolvía `survey_answers: []` para leads con formulario completo. La sección de la pestaña que sí estaba etiquetada "n8n" solo mostraba Instagram/Fuente/Setter, nunca las respuestas.
  - **Corrección backend**: `_format_appointment_for_deck` (lo que consume el modal vía `GET /closer/deck/card/<id>`) ahora expone también `form_data`. Se normaliza a `{}` cuando el cliente no existe o el JSON no es un dict — necesario porque la gran mayoría de los `Client.form_data` guardan el literal `null`, no un objeto.
  - **Corrección frontend**: la pestaña "Formulario" ahora renderiza las respuestas de `form_data` con etiquetas legibles (`FORM_DATA_LABELS`, las mismas que ya usa `FormsManagementPage` para este mismo JSON, para que closer y operaciones lean lo mismo), ocultando los campos que no son respuestas (nombre/teléfono/instagram/fuente/`submitted_at`: datos de contacto ya visibles en la ficha, o metadata). Los campos que n8n mande y no estén en el mapa se muestran igual con la clave formateada, para no perder preguntas nuevas si cambia el formulario. La sección "Encuesta de cita" (`SurveyAnswer`) se mantiene como bloque aparte pero **solo se muestra si realmente tiene respuestas** — antes se renderizaba siempre con un tag "✓ completada" fijo, que quedaba contradiciendo al propio mensaje de "no hay respuestas" justo debajo.
  - **Verificado contra datos reales**: backend probado sobre los 3 clientes con formulario (devuelven todas sus claves) y sobre 400 citas seguidas sin ninguna excepción, confirmando que los leads sin formulario caen a `{}` limpio. La lógica de filtrado del frontend se corrió aparte contra los `form_data` reales de la base: cliente 3440 pasa de 0 a 7 respuestas visibles, 3329 a 5, 3322 a 4; casos borde (`null`, `{}`, `undefined`, solo campos ocultos, valores en blanco) devuelven lista vacía sin romper, y un campo inventado que no está en el mapa igual se muestra. `npm run build` sin errores.
  - **Nota de coordinación**: otra sesión estaba editando en paralelo este mismo `CloserWorkflowPage.jsx` (cambio del ícono del botón "Eliminar lead" a `Trash2`) y la bitácora. Se commiteó **solo lo de esta pasada**, armando el índice a mano (`git hash-object` + `git update-index`) para dejar el cambio del ícono y su entrada de bitácora intactos en el árbol de trabajo, sin tocarlos ni arrastrarlos a este commit.

- **11 de Agosto de 2026** *(continuación — se elimina `followup_reminder_sent_pm_at`, la columna que quedó sin uso tras la cola de recordatorios, [booking.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/booking.py) [MODIFY], nueva migración `b40bb66b18bc`)**:
  - **Continuación de la deuda anotada en la entrada anterior**: al reemplazar las dos franjas fijas de recordatorios (10am/4pm, la vieja `CloserFollowUpService.REMINDER_SLOTS`) por la cola con goteo de 2 por hora, `Appointment.followup_reminder_sent_pm_at` (el campo de la franja de las 4pm) dejó de leerse y escribirse — la cola usa únicamente `followup_reminder_sent_at`. En esa pasada se dejó la columna a propósito para no mezclar una migración destructiva con un cambio de comportamiento; acá se completa la limpieza.
  - **Verificación previa a tocar nada**: confirmado por grep que la única referencia en todo el código (backend y frontend) era la propia definición de la columna en el modelo — ningún servicio, endpoint ni componente la leía. `REMINDER_SLOTS` tampoco tiene ya ninguna referencia viva.
  - **Cadena de Alembic revisada antes de generar la migración** (era el riesgo principal, por las sesiones que venían trabajando en paralelo sobre este repo): `flask db heads` devolvió **un solo head** (`6be4ac5883e0`), sin bifurcación, y la base local ya estaba en esa misma revisión — así que la nueva migración `b40bb66b18bc` cuelga limpio de ahí sin dejar dos ramas.
  - **Migración**: `batch_alter_table` (necesario para SQLite en local, inocuo en PostgreSQL) con `drop_column` en el `upgrade`. En el `downgrade` se cambió el `sa.DATETIME()` que había autogenerado Alembic (tipo del dialecto SQLite, por correrse contra la base local) por `sa.DateTime()`, igual que la migración `772f47fdf24d` que creó la columna, para que el rollback también aplique en producción.
  - **Verificado en local**: `flask db upgrade` aplicó sin errores; la tabla `appointments` pasó de 35 a 34 columnas con las 3272 filas intactas, y `followup_reminder_sent_at` (el campo que sí usa la cola) conserva sus 40 valores no nulos. `flask db check` posterior devuelve "No new upgrade operations detected" — modelo y base en sincronía, sin drift. Smoke test del flujo real con el envío de WhatsApp mockeado: `send_due_reminders`, `get_pool_counts` y `get_today_grouped` siguen devolviendo lo mismo que antes de la migración.
  - **Dato perdido a conciencia**: la columna tenía 40 timestamps de recordatorios de la franja de las 4pm ya enviados. Es información sin ningún uso posterior (solo servía para no reenviar dentro del mismo día, y esa lógica ahora vive en `followup_reminder_sent_at`), así que se descarta con el drop.

- **11 de Agosto de 2026** *(continuación — cola de recordatorios de seguimiento con goteo de 2 por hora, en vez de dos avalanchas diarias, [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [reminder_scheduler.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/reminder_scheduler.py) [MODIFY], [closer_followups.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer_followups.py) [MODIFY], [booking.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/booking.py) [MODIFY])**:
  - **Pedido del usuario**: una cola de seguimientos para los recordatorios a los closers, que no se envíen todos a la misma hora sino **2 por hora**, para recordarles constantemente que tienen seguimientos por hacer.
  - **Cómo funcionaba antes**: `send_due_reminders` mandaba **un mensaje de WhatsApp por CADA seguimiento pendiente**, todos de golpe, en dos franjas fijas del día (10am y 4pm locales del closer, `REMINDER_SLOTS`, cada una con su propio campo en `Appointment` para no pisarse). Un closer con 30 pendientes recibía 30 mensajes seguidos a las 10am y otros 30 a las 4pm — el patrón exacto que se ignora por saturación.
  - **Implementación (cola con goteo)**: `REMINDER_SLOTS` se elimina y se reemplaza por `REMINDERS_PER_HOUR = 2`, `REMINDER_START_HOUR = 9`, `REMINDER_END_HOUR = 20`. Los candidatos siguen siendo los mismos (seguimientos vencidos + de hoy, mismo criterio que `get_today_grouped`), pero ahora se agrupan **por closer** y se aplican tres reglas: (1) solo dentro del horario laboral local de cada closer (`User.timezone`, mismo patrón que `_user_day_bounds_utc` — un closer en otro huso no recibe avisos a la hora de otro); (2) como mucho 2 en la **última hora**, con ventana deslizante sobre `followup_reminder_sent_at` en vez de por hora de reloj, para que el ritmo no dependa de cuándo caiga el tick del scheduler ni se rompa en los bordes de hora; (3) cada cita se avisa una sola vez por día, y la cola se ordena por urgencia (`fecha_seguimiento` ascendente, lo más vencido primero) — así el goteo va **rotando por leads distintos** en vez de repetir siempre el mismo.
  - **Simplificación de contrato**: desaparece el parámetro `slot` de `send_due_reminders`, del scheduler interno (que ahora hace una sola llamada por tick en vez de una por franja) y del endpoint de cron `GET /followups/cron/send-reminders`. El scheduler sigue corriendo cada 15 minutos: correrlo de más es inofensivo, el propio goteo decide si toca mandar algo. La respuesta ahora incluye `throttled` (cuántos quedaron en cola esperando su turno) y renombra `gated_before_hour` → `gated_outside_hours`.
  - **Verificado con reloj controlado** (monkeypatch de `datetime.now` sobre el módulo + `WhatchimpService.send_followup_reminder` mockeado, para no mandar ni un solo WhatsApp real) sobre un closer aislado con 5 seguimientos vencidos sintéticos en la base local: **07:05 y 08:05 → 0 enviados** (fuera de horario); **09:05 → 2** (los dos más vencidos); **10:05 → 2** (los dos siguientes, rota correctamente); **11:05 → 1** (el último); **12:05 → 0** (nada más pendiente ese día); **20:05 y 21:05 → 0** (fuera de horario). Re-corridas dentro de la misma hora (a los :25) → 0 extra en todos los casos. Total exacto: 5 mensajes para 5 seguimientos, sin duplicados. También verificado sin aislar (datos reales de la base local, 3 closers con pendientes): 6 mensajes en la primera corrida = 2 por closer, priorizando los más vencidos de cada uno. Todos los datos sintéticos eliminados y los `followup_reminder_sent_at` preexistentes restaurados a su valor original tras las pruebas (verificado: 40 antes y 40 después).
  - **Deuda menor dejada a propósito**: `Appointment.followup_reminder_sent_pm_at` (el campo de la vieja franja de las 4pm) queda sin uso. No se dropeó en este mismo cambio para no meter una migración destructiva junto a un cambio de comportamiento, y porque había sesiones trabajando en paralelo sobre este repo (riesgo de bifurcar la cadena de Alembic con dos heads). Queda anotado en el modelo y como tarea aparte.

- **11 de Agosto de 2026** *(feature: reasignar un lead a otro closer directamente desde el modal de detalle, [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: que los closers puedan cambiarse fácilmente de lead — que el responsable de un lead pase a ser otro closer — sin depender de un admin.
  - **`PATCH /closer/appointments/<id>/reassign` (nuevo)**: recibe `closer_id` (destino), valida que exista, sea rol `closer` y esté activo, y que no sea ya el dueño actual (400 con mensaje claro en ambos casos de rechazo). Cualquier closer puede reasignar cualquier lead a cualquier otro closer activo — mismo criterio permisivo ya establecido para el resto del flujo ("cualquier closer puede editar cualquier lead", sesión del 5 de agosto), pensado para un pase de mano rápido entre compañeros sin fricción. Deja un `ClientComment` de trazabilidad ("Lead reasignado de X a Y (por Z)").
  - **Backend, datos expuestos**: `_format_appointment_for_deck` (usado por `GET /closer/deck/card/<id>`, que se llama automáticamente al abrir cualquier lead) y `CloserFollowUpService._serialize`/`_cerrada_pool_items` ahora incluyen `closer_id`/`closer_name` — necesario para que el modal sepa a quién pertenece el lead actualmente y arme el selector de destino sin ofrecer al dueño actual como opción.
  - **Frontend**: en la ficha compartida de los 4 modales de etapa (debajo del `idcard`, antes de "Ver historial completo"), nueva fila "Responsable: {closer}" con un botón "Cambiar de closer" que despliega un `<select>` con los closers activos (reusa `teamMembers`, ya cargado para el feature de menciones — ahora se precarga al abrir cualquier lead, no solo en seguimientos) excluyendo al dueño actual; al elegir uno, llama al endpoint nuevo y actualiza el modal en caliente sin cerrarlo.
  - **Verificado**: `npm run build` sin errores. Backend probado con Flask test client autenticado como Jean Carlo contra la base local real (sesión forzada manualmente vía `session_transaction`, con `WTF_CSRF_ENABLED=False` y `session_protection=None` solo para poder simular el login en este entorno de prueba puntual — el flujo real de la app sigue protegido por CSRF/sesión fuerte como siempre): reasignación exitosa (200, `closer_id`/`closer_name` correctos, `ClientComment` creado con el texto esperado), closer inexistente/inactivo (400), mismo closer ya dueño (400), `closer_id` faltante (400), cita inexistente (404). Datos de prueba revertidos tras la verificación. No se pudo probar el click-through visual en el navegador en esta sesión por no contar con credenciales de un closer real para iniciar sesión — verificado en su lugar a nivel de código (JSX bien formado, build limpio) y de datos reales vía backend.

- **10 de Agosto de 2026** *(continuación — pool "Llamadas no tomadas" no incluía agendas vencidas sin procesar, y el filtro de fecha de la call era acumulativo en vez de por rango, [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [MODIFY])**:
  - **Reportado por el usuario**, sobre "📥 Pool sin fecha asignada" → "Llamadas no tomadas": (1) el filtro de "Fecha de la call" (Últimos 14 / 30 / +30 días) "no funciona correctamente"; (2) el pool debería incluir también las agendas vencidas (fecha ya pasada) que siguen en estado "Pendiente" — hoy solo entran ahí las citas que el closer ya reportó explícitamente como No Show/Cancelado, no las que simplemente nunca procesó.
  - **Diagnóstico #1 (bug real, confirmado contra la base local)**: `matches_days` en `CloserFollowUpService.get_pool` (duplicado en la rama 'cerrada' y en la rama normal) trataba los 3 rangos como acumulativos: "14" = `d<=14`, pero "30" también era `d<=30` (superset de "14"), así que cambiar de "Últimos 14 días" a "Últimos 30 días" solo agregaba resultados, nunca sacaba nada — para el usuario se sentía como que el filtro "no cambiaba" lo que ya veía. Verificado contra datos reales: antes del fix, con Jean Carlo, "14"→1 item y "30"→10 items (los mismos 1 + 9 más); after fix, "14"→29, "30" (ahora 15-30 días)→16, sin solape, sumando exacto al total sin filtro.
  - **Diagnóstico #2**: `CloserFollowUpService._base_query`/`_effective_tipo` solo derivaban `'no_tomada'` de citas con `seguimiento_tipo` explícito o `closer_result` en (No Show/Cancelado/Cancelada) — una cita vencida que el closer simplemente nunca tocó (`closer_result` sigue en `'Pendiente'`/vacío, `closer_processed=False`) quedaba invisible para todo el flujo de seguimientos hasta que el barrido automático de 30+ días (`CloserService.archive_stale_backlog`) la archivara como "Lead Perdido" — un hueco de hasta 29 días donde esas citas no aparecían en ningún lado del pipeline de seguimiento.
  - **Corrección**: `_effective_tipo` ahora también clasifica como `'no_tomada'` cualquier cita con `closer_result` en `('', 'pendiente')`, `closer_processed=False` y `start_time` ya en el pasado (mismo criterio de datos que `archive_stale_backlog`, sin esperar los 30 días de ese barrido de mantenimiento) — y `_base_query` se amplía con la misma condición para que esas filas pasen el filtro SQL inicial. No afecta "Asignados para hoy" ni los recordatorios de WhatsApp (`send_due_reminders`): ambos ya filtran además por `fecha_seguimiento` asignada, que estas citas nunca tienen. El filtro de días se corrige a rangos excluyentes: "14" (0-14), "30" ahora es 15-30 (antes 0-30), "+30" sin cambios — label del selector actualizado a "Entre 15 y 30 días" para reflejarlo.
  - **Verificado contra la base local real**: pool `no_tomada` de Jean Carlo pasó de 15 a 50 items (35 agendas vencidas-pendientes nuevas incorporadas); `get_today_grouped` (sección "Asignados para hoy") sin cambios, como se esperaba. `npm run build` sin errores.
  - **Nota de coordinación**: había otra sesión trabajando en paralelo sobre Auditoría de Leads (feature toggle + `CloserLeadsAudit.jsx`, con cambios sin commitear en `CloserWorkflowPage.jsx`) — se commiteó únicamente lo tocado en esta sesión (`closer_followup_service.py`, `SeguimientosPane.jsx`) para no interferir con ese trabajo en curso.

- **10 de Agosto de 2026** *(continuación — nuevo modelo `FeatureToggle` para activar/desactivar funciones temporales desde Operaciones, nuevo [crm.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/crm.py) [MODIFY], migración `6be4ac5883e0`)**:
  - **Pedido del usuario**: agregar una pestaña temporal de auditoría de leads en el panel del closer, que Operaciones pueda activar y desactivar a voluntad — no es una función permanente, así que necesitaba un mecanismo genérico de encendido/apagado que no existía todavía en el proyecto (se revisó `AlertRule.is_active`, `Pipeline.is_active`, `UserViewSetting` como precedentes, ninguno servía tal cual para un toggle global simple).
  - **`FeatureToggle` (nuevo, en `app/models/crm.py`)**: tabla simple `key` (único), `is_active`, `updated_by_id`, `updated_at` — un registro global por funcionalidad (no por closer), pensado para reutilizarse en futuros interruptores de este mismo tipo sin tener que crear una tabla nueva cada vez. Registrado en `app/models/__init__.py`.
  - Migración generada con `flask db migrate` y aplicada con `flask db upgrade` en local, siguiendo la pauta obligatoria del proyecto al tocar modelos.
  - **Primer uso**: base para la pestaña "🗂️ Auditoría" del closer, activable con la clave `closer_leads_audit` (ver commits siguientes de esta misma sesión).

- **10 de Agosto de 2026** *(continuación — endpoints de Operaciones para leer/actualizar un `FeatureToggle`, [admin.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/admin.py) [MODIFY])**:
  - **`GET /api/admin/feature-toggles/<key>`** (devuelve `is_active` en `false` por defecto si la clave nunca se creó, sin 404 — así el frontend no necesita manejar un caso especial de "nunca configurado") y **`PATCH /api/admin/feature-toggles/<key>`** (get-or-create + actualiza `is_active` y `updated_by_id`), ambos protegidos con `@operator_required` (mismo decorador ya usado en el resto de endpoints administrativos de este archivo — permite `admin` y `operator`).

- **10 de Agosto de 2026** *(continuación — endpoints de datos para la nueva pestaña "🗂️ Auditoría" del closer, [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
  - **`GET /closer/leads-audit/status`**: cualquier closer autenticado puede consultarlo — devuelve `{enabled}` leyendo el `FeatureToggle` de clave `closer_leads_audit`, para que el frontend decida si mostrar el botón de la pestaña nueva en el dock.
  - **`GET /closer/leads-audit?month=YYYY-MM`**: si el toggle está apagado, devuelve `403` para un closer (un admin/operator sí puede consultarlo siempre, para poder auditar sin depender del toggle). Devuelve todos los `Client` con alguna `Appointment` de ese closer dentro del mes pedido (por `start_time`; mes actual si no se manda `month`) — para cada uno, **todas** sus agendas de ese closer (`id`, `closer_result`, `created_at`, `start_time`, sin acotar al mes, para que el closer vea el historial completo del lead aunque tenga citas de otros meses) y todos sus pagos (`Payment` vía `Enrollment.closer_id`, con `amount`, `date`, `payment_type`, `status`). Un admin/operator puede pasar `closer_id` para auditar a cualquier closer puntual.
  - **No se creó ningún endpoint de escritura nuevo**: la edición de esta pestaña reutiliza los mismos endpoints que ya usa el flujo normal del closer — `PATCH /closer/appointments/<id>` (fecha de reunión), `POST /closer/appointments/<id>/process` (estado/`closer_result`) y `PUT /closer/payments/<id>` (monto/fecha/estado de un pago) — para no duplicar validaciones de negocio ya existentes.
  - **Verificado**: `python -m py_compile` sin errores; `create_app()` real registra las 4 rutas nuevas (`/api/admin/feature-toggles/<key>` GET/PATCH, `/api/closer/leads-audit/status`, `/api/closer/leads-audit`) sin romper el resto de la app.

- **10 de Agosto de 2026** *(continuación — switch en el panel de Operaciones para activar/desactivar la Auditoría de Leads, nuevo [LeadsAuditTogglePanel.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/operations/LeadsAuditTogglePanel.jsx), [OperationsSettingsPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/operations/settings/OperationsSettingsPage.jsx) [MODIFY])**:
  - Nueva sección "Auditoría de Leads" en el sidebar del Panel de Control Técnico (mismo patrón `activeSection` ya usado por el resto de secciones de este archivo). El panel llama a `GET`/`PATCH /admin/feature-toggles/closer_leads_audit` (commits anteriores de esta sesión) con un switch simple y muestra cuándo y por quién se cambió por última vez.

- **10 de Agosto de 2026** *(continuación — pestaña "🗂️ Auditoría" en el panel del closer, nuevo [CloserLeadsAudit.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/audit/CloserLeadsAudit.jsx), [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - Cierre de la feature pedida: pestaña temporal junto a "📈 Dashboard" en el dock del closer, visible únicamente si `GET /closer/leads-audit/status` devuelve `enabled: true` (consultado al montar el panel). Extiende el ternario de vistas existente (`activeView`: `'inbox'`/`'report'`/`'auditoria'`/`'dashboard'`).
  - **`CloserLeadsAudit.jsx` (nuevo, patrón "embedded" igual a `CloserDashboard.jsx`)**: selector de mes (`input type="month"`), lista de leads del closer logueado con sus agendas (estado, fecha de creación, fecha de reunión) y pagos, cada uno expandible/colapsable. Editar una agenda llama a `PATCH /closer/appointments/<id>` (fecha) y `POST /closer/appointments/<id>/process` (estado); editar un pago llama a `PUT /closer/payments/<id>` — mismos endpoints que ya usa el flujo normal del closer, sin lógica de guardado duplicada. Si el toggle se apaga mientras la pestaña está abierta, muestra un mensaje claro en vez de pantalla en blanco (lee el `403` del backend).
  - **Verificado end-to-end vía HTTP real** (Flask test client, JWT de usuarios reales — Mario Opera como operator, Jean Carlo como closer real; el `PATCH` del toggle se probó actualizando el registro directamente en la sesión de base de datos con la misma lógica exacta del endpoint, ya que la validación CSRF de este proyecto está atada a la cookie de sesión de un login de navegador real, no reproducible limpiamente desde un script): toggle activado → `GET /closer/leads-audit/status` de Jean Carlo devuelve `enabled: true`; `GET /closer/leads-audit` (mes actual, agosto 2026) devuelve sus 47 leads reales con agendas y pagos anidados correctamente (ej. cita `#6070`, `closer_result: 'Pendiente'`, fechas de creación/reunión reales); mes sin datos (`2020-01`) devuelve lista vacía sin error; toggle desactivado → mismo endpoint devuelve `403` con el mensaje esperado para el closer, mientras que un operator/admin sigue accediendo normalmente pasando `closer_id`. `npm run build` del frontend sin errores.
  - **No verificado visualmente en navegador esta sesión**: no se contaba con credenciales reales de un usuario para iniciar sesión desde la UI (a diferencia de otras sesiones donde ya había una sesión de Jean Carlo simulada activa); la verificación de UI queda pendiente de que el usuario confirme el flujo completo (activar el toggle desde Operaciones → ver aparecer la pestaña → editar una agenda/pago) en su navegador real.

- **10 de Agosto de 2026** *(bugfix: un closer no podía ver los valores de sus KPIs en el Dashboard de Performance, [index.css](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/index.css) [MODIFY])**:
  - **Reportado por un closer real** (con captura): en `Dashboard de Performance`, todas las tarjetas de KPI (Ventas cerradas, Ticket promedio, Deuda total, close rates, seguimientos) mostraban la etiqueta y el badge de variación, pero el número del valor no se veía — como si estuviera en blanco. El usuario simuló la sesión del closer (impersonación) y a él se lo veía bien, así que sospechaba de Safari en modo oscuro pero no estaba seguro.
  - **Diagnóstico, reproducido de forma determinística en el navegador** (sin login, montando directamente un `<div class="bg-surface"><p class="text-base">` como lo hace `Card`/`PerformanceKpis`): con el tema por defecto de la app ("Elegant" + variante "glass", exactamente la configuración de cualquier closer que nunca tocó los ajustes de apariencia), el fondo de la tarjeta resolvía a `rgba(10, 14, 61, 0.85)` (navy oscuro) y el color del texto del valor a `rgb(10, 14, 61)` — **el mismo color exacto**, texto invisible sobre su propio fondo. Causa raíz: `--color-text-base` (la variable detrás de la clase de Tailwind `text-base`, usada para el valor numérico de cada KPI) se define una única vez en `:root` como alias fijo de `--text-main` (pensado para el fondo de la PÁGINA). Las tarjetas (`.bg-surface`, `.glass-panel`, `.theme-sidebar`) sí sobreescriben `--text-main` localmente para verse bien contra su propio fondo (a veces oscuro aunque el tema general sea "claro"), pero nunca sobreescribían también `--color-text-base` — así que cualquier `text-base` dentro de una tarjeta seguía usando el color de texto de la página, no el de la tarjeta. En los temas oscuros ("Custom Pro") ambos valores coinciden en blanco por casualidad, ocultando el bug — por eso nadie lo había notado antes y por eso la impersonación del usuario (con su propio tema ya personalizado guardado en su navegador, ya que el tema vive en `localStorage` por navegador, no en la cuenta) no lo reprodujo. No tiene relación con Safari ni con el modo oscuro del sistema operativo.
  - **Corrección**: se agrega `--color-text-base: var(--text-on-card);` junto a cada `--text-main: var(--text-on-card);` ya existente en `.glass-panel`, `.theme-sidebar` y `.bg-surface` — así la clase `text-base` de Tailwind sigue el color de texto correcto de la tarjeta en vez del de la página, en cualquier combinación de tema/variante.
  - **Verificado en el navegador** (sin necesidad de login, montando el mismo patrón HTML/CSS que usa la app real): antes del fix, tema "Elegant"+glass daba tarjeta `rgba(10,14,61,.85)` con texto `rgb(10,14,61)` (invisible); después del fix, texto `rgb(255,255,255)` (blanco, legible). Verificado también "Elegant"+solid (`rgb(10,14,61)` de fondo, texto blanco tras el fix) y "Custom Pro"+glass (dark) sin regresión (seguía dando texto blanco como antes). `npm run build` sin errores.

- **9 de Agosto de 2026** *(continuación — corrección aplicada también en PRODUCCIÓN: pago histórico de Andrés Simón + 386 citas mal archivadas como "Lead Perdido")*:
  - **Pedido del usuario**: repetir en producción el registro del pago histórico de Andrés Simón (ver entrada anterior de hoy), y de paso corregir también en producción cualquier otro lead en la misma situación (cita archivada automáticamente como perdida pese a tener un pago real) — "corrige la agenda de los leads que tienen un pago realizado, porque eso significa que esas agendas tuvieron asistencia".
  - **`scripts/registrar_pago_historico.py`** corrido contra producción (`DATABASE_URL` apuntando a `DATABASE_PRODUCTION` por esta vez, mismo patrón que ya usa `actualizar_db.py`) para Andrés Simón: mismo resultado que en local (`FinancialSale #57609`, `Enrollment #507`, `Payment #653`), verificado con `SalesConsistencyService.validate_next_payment_type` directo contra producción — `renovacion` pasó a estar permitido.
  - **Alcance real del problema de archivado, encontrado al generalizar**: nuevo `scripts/corregir_archivados_con_pago.py` (dry-run primero) — de 2867 citas archivadas automáticamente como "Lead Perdido" por `CloserService.archive_stale_backlog` (30+ días sin confirmar/procesar dentro de la app), **385 en local / 386 en producción** correspondían a clientes que en realidad SÍ tenían un pago real registrado (`FinancialSale`) — mucho más grande de lo esperado para un caso puntual. Confirmado el alcance con el usuario antes de aplicar el lote completo.
  - **Corrección aplicada**: esas citas pasan de `closer_result='Lead Perdido'` a `'Show up'` (si pagó, asistió), con una nota explicando el motivo y un `ClientComment` de trazabilidad — sin disparar ninguna automatización. Aplicado primero en local (385 corregidas, verificado que las 2482 restantes son backlog genuino sin pago) y luego en producción (386 corregidas) — la corrida contra producción tomó varios minutos por la latencia de red de las ~2867×2 consultas individuales contra la base remota, se corrió en segundo plano.
  - **Nota operativa**: las consultas de verificación posteriores contra producción (conteos/lookups puntuales) empezaron a tardar/fallar por timeout tras la corrida en lote — se tomó como suficiente la confirmación de éxito que el propio script imprimió (`"[OK] 386 cita(s) corregida(s)"`) en vez de insistir con más consultas de verificación contra una conexión visiblemente saturada en ese momento.
  - **Pendiente**: la tarea de auditoría lanzada en paralelo (de solo-lectura, en una sesión aparte) sobre el mismo problema en local queda sin objeto una vez aplicada esta corrección — puede cerrarse o usarse para una segunda pasada de verificación independiente.

- **9 de Agosto de 2026** *(continuación — el error al crear una agenda en un horario ya ocupado no explicaba la causa, [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Reportado por el usuario**: al intentar crear una agenda ("＋ Nueva agenda") en un horario que el closer ya tenía ocupado, el sistema mostraba un error pero no explicaba la causa.
  - **Diagnóstico**: dos problemas compuestos. (1) El backend (`POST /closer/appointments`) delegaba en `BookingService.create_appointment`, que devuelve `None` tanto si hay un choque de horario como si el cliente no existe — el endpoint traducía cualquiera de los dos casos al mismo mensaje ambiguo: *"Ya existe una cita agendada en este horario para este closer o no se pudo crear la agenda"* (un "o" que no distingue cuál pasó). (2) Más grave: el frontend ni siquiera mostraba ese mensaje — el `catch` de `handleCreateAgenda` tenía hardcodeado un genérico `"Error al crear la agenda"` sin leer nunca `err.response.data.error`, así que el mensaje específico del backend (por ambiguo que fuera) nunca llegaba a la pantalla del closer.
  - **Corrección**: el endpoint ahora hace el chequeo de choque de horario ANTES de intentar crear la cita (mismo criterio exacto que ya usaba `BookingService.create_appointment` internamente), y si encuentra una cita en conflicto arma un mensaje específico con el nombre del lead y la hora exacta: *"Ya tenés una agenda con {nombre} a esa misma hora (dd/mm/aaaa HH:MM) — elegí otro horario."* (`409`, con `conflicting_appointment_id` incluido). Se agregó también un mensaje distinto para cliente no encontrado, y el mensaje genérico de respaldo ahora deja claro que es un caso inesperado en vez de reciclar el mismo texto ambiguo. El frontend ahora sí lee `err.response?.data?.error` y lo muestra en el toast.
  - **Verificado end-to-end**: reproducido el choque real (cita de prueba a una hora, intento de crear una segunda cita para el mismo closer a esa misma hora) vía HTTP directo y en el navegador real (sesión de Jean Carlo) — el toast mostró exactamente *"Ya tenés una agenda con Test UI Conflicto QA a esa misma hora (16/08/2026 15:00) — elegí otro horario."* Datos de prueba eliminados. `npm run build` sin errores.
  - **Fuera de alcance esta vez**: se detectaron ~20 puntos más en `CloserWorkflowPage.jsx` con el mismo patrón (toast genérico que descarta el mensaje específico del backend) — no se tocaron por no ser lo reportado; queda como candidato a una revisión más amplia del manejo de errores si se vuelve a pedir.

- **9 de Agosto de 2026** *(continuación — Andrés Simón no podía renovar porque su pago Completo de enero nunca quedó registrado, y nuevo script para registrar pagos históricos sin disparar automatizaciones, nuevo [scripts/registrar_pago_historico.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/scripts/registrar_pago_historico.py))**:
  - **Reportado por el usuario**: un closer no podía registrar una Renovación para el lead "Andrés Simón" — el sistema decía que no encontraba ningún pago previo, pese a que el usuario había visto en Discord que pagó el programa Completo el 16 de enero.
  - **Primer paso (pedido explícito del usuario)**: se corrió `python scripts/actualizar_db.py` para refrescar la base local completa desde producción, por si el dato simplemente no había llegado a esta copia local. Confirmado que **no era un problema de base local desactualizada**: tras el refresh (837 `FinancialSale` sincronizadas, antes 831), seguía sin existir ninguna fila de `FinancialSale` para este cliente bajo ningún cruce (nombre, email, teléfono) — el pago nunca se declaró dentro del CRM en absoluto, se cerró y comunicó por fuera del sistema (Discord/Sheets manual).
  - **Diagnóstico adicional encontrado de paso**: la cita de este lead (`Appointment` #2855, 10 de enero) estaba marcada `closer_result='Lead Perdido'` con la nota `"[Sistema] Archivado automáticamente: sin confirmar ni procesar tras 30+ días desde su fecha."` — el barrido automático de backlog (`CloserService.archive_stale_backlog`) lo había dado por perdido porque nunca se procesó DENTRO de la app, aunque en la realidad sí convirtió. Corregido manualmente a `closer_result='Show up'` con una nota explicando el porqué, más un `ClientComment` dejando trazabilidad de la corrección.
  - **`scripts/registrar_pago_historico.py` (nuevo)**: script reutilizable para registrar pagos históricos de leads viejos directamente en la base de datos (crea `FinancialSale` + su espejo en `Enrollment`/`Payment`, mismo criterio que ya usa `SheetsService._sync_enrollment_payment` para pagos declarados normalmente) **sin disparar ninguna automatización** — no llama a `SheetsService.post_to_sheets` (así que no hace POST a Google Sheets), no dispara el webhook de n8n hacia Discord, no dispara la notificación de "seña convertida a venta". Respeta la misma validación de secuencia de pagos que el flujo normal (`SalesConsistencyService.validate_next_payment_type`) salvo que se pase `--force` explícitamente. Corre en modo dry-run por defecto (muestra qué haría sin guardar nada); requiere `--apply` para escribir de verdad. No crea clientes nuevos a propósito (requiere `--client-id` de un `Client` ya existente) — es para poner al día un lead que ya está en el sistema, no para dar de alta gente nueva.
  - **Aplicado para Andrés Simón** (`client_id=3276`, confirmado con el usuario: programa RR, $1500, Stripe, 16 de enero de 2026): `FinancialSale #57609`, `Enrollment #507` (Residency Roadmap v3), `Payment #653`.
  - **Verificado end-to-end vía HTTP real** (`GET /closer/sales/client-state?client_id=3276&programa=RR`, autenticado como Jean Carlo — el mismo endpoint que consulta el modal de "Declarar Venta" del closer): antes del backfill, `renovacion`/`upsell` no estaban permitidos; después, `allowed_types.renovacion.ok = true` y `allowed_types.upsell.ok = true`, con `has_full_payment=true` y `balance_remaining=0.0` — el closer ya puede registrar la renovación normalmente desde la app.
  - **Nota para investigar en otra pasada**: el archivado automático de backlog (`archive_stale_backlog`) puede estar marcando "Lead Perdido" a otros leads viejos que en realidad sí convirtieron por fuera del flujo normal de la app — no se investigó a fondo el alcance real de este caso más allá de Andrés Simón en esta pasada.

- **9 de Agosto de 2026** *(continuación — bugfix crítico: las agendas confirmadas desaparecían solas de la columna "Confirmado", [booking_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/booking_service.py) [MODIFY])**:
  - **Reportado por un closer real**: agendas que marcaba como "Confirmado" desaparecían de esa columna del pipeline sin que él hiciera nada.
  - **Diagnóstico**: `BookingService.sync_financial_agenda_to_appointment` (que resincroniza `Appointment.result` desde el `estado` de la hoja de cálculo — se dispara por cada edición individual vía el webhook de n8n, y también en lote sobre los últimos 14 días vía `POST /public/financial-agendas/sync-appointments`) sobreescribía `Appointment.result` incondicionalmente con lo que dijera la hoja, sin importar que el closer ya hubiera avanzado el pipeline de confirmación DENTRO de NeurOPS (`por_confirmar` → `conversando` → `Confirmado`, vía el modal de confirmación). Como la hoja la mantiene el Call Confirmer por separado y normalmente no se actualiza en tiempo real cuando el closer confirma desde la app, cualquier resync (uno de los dos disparadores de arriba) volvía a escribir el valor viejo de la hoja (típicamente "Pendiente"/"Agendado"), revirtiendo la confirmación silenciosamente. Ya existía esta misma protección del lado de LECTURA (`_format_appointment_for_deck` en `app/api/closer.py`, `CLOSER_CONFIRM_STAGES`) para decidir qué mostrar en el mazo, pero nunca se había aplicado también al GUARDAR — por eso el dato en base quedaba realmente revertido, no era solo un problema de visualización.
  - **Corrección**: se agrega la misma protección a `sync_financial_agenda_to_appointment` — si `Appointment.result` ya está en una etapa que el closer gestiona directamente (`por_confirmar`/`conversando`/`confirmado`), un valor entrante de la hoja en ese mismo grupo (pendiente/agendado/confirmado/sin respuesta/contactado) ya no lo pisa. `Cancelado`/`Reagendado` siguen aplicándose siempre sin importar el estado previo — son señales reales de un evento externo (el cliente canceló/reagendó), no un valor por defecto desactualizado de la hoja.
  - **Verificado con datos reales/sintéticos**: (1) cita marcada `Confirmado`, resync con la hoja en `Pendiente` → se mantiene `Confirmado` (antes del fix se revertía); (2) la misma cita, resync con la hoja en `Cancelado` → sí pasa a `Cancelado` (los eventos externos genuinos se siguen respetando); (3) cita en `conversando`, resync con la hoja en `Pendiente` → se mantiene `conversando`; (4) cita sin ningún estado propio del closer (`Pendiente`), resync con la hoja en `Confirmado` → sí pasa a `Confirmado` normalmente (el flujo normal para agendas nuevas no se rompió). Datos de prueba eliminados tras la verificación.

- **9 de Agosto de 2026** *(continuación — bugfix crítico: "Descartar lead" no hacía nada tras un No Show, presentación sin cierre o sin presentación, [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Reportado por un closer real**: intentó descartar a un lead por "Bloqueó / desapareció" (tras marcar No Show) y el sistema "no lo dejó" — confirmaba el descarte y no pasaba nada, el lead seguía igual.
  - **Diagnóstico**: `handleConfirmReason` (el handler central que procesa TODOS los modales de "motivo/confirmación" del árbol de decisión) maneja los `actionType` `'cancel'`, `'reschedule'`, `'no_lead'` y `'confirm_discard'` — pero a los pasos "Descartar lead" de los pasos **No Show** (`lost_no_show`), **Perdido tras presentación** (`lost_after_pres`) y **Sin presentación** (`lost_no_pres`) nunca se les agregó su rama correspondiente. El modal de confirmación se armaba y mostraba perfecto (título, descripción, motivo específico), pero al confirmar, `handleConfirmReason` no encontraba ningún `if`/`else if` que matcheara esos 3 `actionType` — la función no hacía NADA salvo cerrar el modal: ni llamaba a la API, ni mostraba error, fallaba en silencio total. Los otros 2 puntos de descarte ("Descartar lead" en el paso de confirmación pre-llamada, y "Marcar No Lead" al cancelar) sí funcionaban porque sí tenían su rama.
  - **Corrección**: se agregó la rama faltante para los 3 `actionType`, llamando a `POST /closer/appointments/<id>/process` con `status: 'Lead Perdido'` (a diferencia de `'No Lead'`, que usan los descartes PRE-llamada para prospectos nunca calificados — acá el lead sí llegó al funnel de cierre, así que corresponde el estado "perdido" que el backend ya soporta desde antes en `CloserService.process_agenda`, solo que nunca se llamaba desde estos 3 caminos). Además, para el caso `lost_no_show`, si el closer había elegido un motivo puntual del listado (ej. "Bloqueó / desapareció") antes de abrir el modal de confirmación, ese motivo ahora se antepone al comentario libre en el texto guardado (antes se perdía silenciosamente — solo se guardaba el comentario libre, igual que ya hace el camino de "Programar seguimiento" para el mismo paso).
  - **Verificado end-to-end contra la base local real**: cliente y cita de prueba creados, simulado el flujo completo como Jean Carlo (No show → Bloqueó/desapareció → Descartar lead → confirmar) — antes del fix, `Appointment.closer_result` quedaba en `'Pendiente'` sin cambios pese a "confirmar" el descarte; después del fix, pasa correctamente a `closer_result='Lead Perdido'`, `closer_processed=True`, `seguimiento_realizado=True`, y se crea el `ClientComment` con el motivo. Datos de prueba eliminados tras la verificación. `npm run build` sin errores.

- **9 de Agosto de 2026** *(continuación — bloquear el envío del reporte diario con backlog atrasado, y sugerir el valor de slots del reporte anterior, [closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY], [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: (1) impedir que un closer envíe su reporte diario si tiene llamadas sin registrar, seguimientos asignados sin realizar o confirmaciones pendientes de días ANTERIORES sin resolver; (2) agilizar el campo "Slots disponibles" del reporte recordando el último valor cargado (o un default), para no tener que reescribir la misma cifra todos los días.
  - **`CloserService.get_previous_days_pending(closer_id, day_local)` (nuevo)**: cuenta trabajo atrasado de ANTES del día que se está reportando (no incluye el propio día) en 3 categorías mutuamente excluyentes — `confirmaciones_pendientes` (citas de días anteriores que nunca llegaron a `result == 'Confirmado'`), `llamadas_sin_registrar` (sí se confirmaron pero nunca se reportó su resultado real) y `seguimientos_sin_realizar` (`fecha_seguimiento` vencida, `seguimiento_realizado` en falso) — mismo criterio de datos que ya usan las pestañas ①/②/③ del mazo (`GET /closer/deck/counts`), solo que acotado a ANTES del día reportado en vez de "desde hoy en adelante" (ese es el flujo normal de agendas futuras por confirmar, no backlog).
  - **Bloqueo real, no solo advertencia**: `POST /closer/deck/daily-report` calcula este pendiente antes de guardar nada y devuelve `409` con el desglose si `total > 0` — no hay forma de saltárselo desde el frontend (el `admin` real, sin impersonar, queda exento para poder destrabar una situación puntual). `GET /closer/deck/daily-report` también expone `pending_previous_days` para que el frontend muestre el bloqueo proactivamente antes de intentar enviar.
  - **Frontend**: banner rojo bloqueante ("🚫 No podés enviar el reporte...") con el desglose de las 3 categorías y un botón directo a la bandeja; el botón "Enviar reporte al sistema" queda deshabilitado mientras haya pendientes (con tooltip explicando por qué), y si de todas formas el backend lo rechaza (409, ej. por datos desactualizados en pantalla), el error se muestra igual y el banner se actualiza con el desglose real recién recibido.
  - **Slots sugeridos**: `GET /closer/deck/daily-report` ahora también devuelve `slots_last_value` (el `slots` del reporte guardado más reciente de CUALQUIER día anterior del mismo closer). Si el día que se está viendo todavía no tiene su propio reporte, el input se prellena con ese valor (editable, marcado visualmente como "Sugerido a partir de tu último reporte — editalo si hoy cambió") en vez de arrancar vacío.
  - **Verificado**: `get_previous_days_pending` corrido contra la base local real — Jean Carlo (31 confirmaciones + 1-2 llamadas + 2 seguimientos atrasados, total >0), Sebastian y Marlon Closer con backlog real también; un usuario sin citas como closer_id devuelve `{total: 0}` limpio. `POST /closer/deck/daily-report` probado vía Flask test client autenticado como Jean Carlo real: devuelve `409` con el desglose exacto sin guardar nada. **Verificado en el navegador** (sesión de Jean Carlo): el banner rojo aparece con "31 confirmación(es) nunca gestionada(s), 1 llamada(s) confirmada(s) sin registrar su resultado, 2 seguimiento(s) asignado(s) sin realizar"; el botón "Enviar reporte al sistema" está `disabled` con el tooltip correcto; el input de slots se precarga con "0" marcado como sugerido. `npm run build` sin errores.

- **9 de Agosto de 2026** *(continuación — simplificar la ficha de los modales de Lead, [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: le gustó el teléfono junto al nombre en la cabecera, pero pidió quitar Instagram, Fuente y Teléfono del `idcard` (la grilla de campos debajo de la cabecera) — quedaba redundante con el teléfono ya visible arriba.
  - **Corrección**: el `idcard` ahora solo muestra Examen, Grupo, Fecha agendamiento, Fecha de ingreso (cuando aplica) y Estado. El teléfono se mantiene únicamente como link de WhatsApp debajo del nombre, en la cabecera.
  - **Verificado**: `npm run build` sin errores; confirmado en el navegador (sesión de Jean Carlo) que la ficha ya no muestra Instagram/Fuente/Teléfono y que el link de WhatsApp junto al nombre sigue intacto.

- **9 de Agosto de 2026** *(continuación — número de teléfono visible en los modales de Lead para contactar durante el seguimiento, [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [MODIFY])**:
  - **Pedido del usuario**: en los modales del Lead, junto al nombre, la fecha de agendamiento y la fecha de ingreso, quería ver el número de teléfono para poder contactar al cliente al momento de hacerle seguimiento.
  - **Implementación**: se agrega el teléfono en dos lugares de la ficha compartida de los 4 modales de etapa — como link de WhatsApp justo debajo del nombre en la cabecera (mismo formato `wa.me` + mensaje de saludo ya usado en el mazo de `CloserDeckPage.jsx`), y como campo "☎ Teléfono" en el `idcard`, junto a "Fecha agendamiento"/"Fecha de ingreso". Los flujos de confirmación/llamada ya traían el teléfono (`_format_appointment_for_deck` ya lo devolvía); se agregó también a `CloserFollowUpService._serialize` (seguimientos tomada/no_tomada) y a `_cerrada_pool_items`/`get_client_lead_stage` (llamada cerrada), y se propagó en `SeguimientosPane.openLead` y en la construcción del lead de búsqueda para la etapa "cerrada", para que esté disponible sin importar por qué entrada se abrió el modal.
  - **Verificado en el navegador** (sesión de Jean Carlo): buscando un lead se ve "57 319 5231640" como link debajo de "Reporte de llamada" y de nuevo junto a "Fecha agendamiento" en la ficha; el `href` generado es `https://wa.me/573195231640?text=Hola Sebastian Paraban, te saluda tu asesor de NeurOPS. ¿Cómo estás?` (número limpio de espacios/símbolos, mensaje pre-armado). `npm run build` sin errores.

- **9 de Agosto de 2026** *(continuación — los botones del pool sin fecha asignada no se distinguían al estar seleccionados, [SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [MODIFY])**:
  - **Reportado por el usuario**: en "📥 Pool sin fecha asignada" (pestaña Seguimientos), casi no se notaba cuál de los 3 botones (Llamadas no tomadas/tomadas/cerradas) estaba seleccionado.
  - **Diagnóstico**: el estilo activo usaba `bg-violet-650/10` — `violet-650` no es un tono válido de la paleta de Tailwind de este proyecto (no está en la paleta por defecto ni extendido en `tailwind.config.cjs`), así que esa clase nunca generaba ningún CSS real; solo quedaba el borde `border-violet-500/50` (válido, pero muy sutil a 50% de opacidad sobre fondo oscuro) como única señal visual.
  - **Corrección**: el estado activo ahora usa el mismo color por tipo que ya usan sus chips (`rose`/`amber`/`emerald`, vía el `TIPOS[tipo].cls` existente) con fondo, borde y anillo (`ring-2`) bien visibles — ej. "Llamadas cerradas" activo se ve verde esmeralda en vez de violeta genérico —, más el texto del label y el contador en blanco cuando está activo (antes gris en los tres estados). Se agregó `aria-pressed` para accesibilidad.
  - **Verificado**: `npm run build` sin errores; en el navegador (sesión de Jean Carlo) se confirmó que al clickear "Llamadas cerradas" el `className` del botón cambia correctamente a `bg-emerald-500/15 border-emerald-400 ring-2 ring-emerald-500/30` con `aria-pressed="true"`, y que esa regla CSS existe en la hoja de estilos con los valores esperados (`rgba(16,185,129,.15)`, etc.) y coincide con el elemento (`Element.matches`). No se pudo confirmar el color renderizado en pantalla vía `getComputedStyle` en esta sesión — devolvía el valor inactivo pese a que la clase y la regla CSS eran correctas, consistente con la misma limitación de composición de frames de este entorno que ya impide tomar capturas de pantalla (ver notas anteriores de hoy); pendiente que el usuario confirme visualmente en su navegador.

- **9 de Agosto de 2026** *(continuación — "Ver historial completo del cliente" dejaba el modal de etapa abierto detrás, [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Reportado por el usuario**, probando el cambio anterior de esta misma sesión: al abrir un modal de etapa (confirmación/llamada/seguimiento/cobranza) desde la búsqueda y clickear "Ver historial completo del cliente →", el resumen (`ClientHistoryModal`) se abría correctamente pero el modal de etapa que estaba detrás no se cerraba — quedaban los dos superpuestos.
  - **Diagnóstico**: el botón solo hacía `setHistoryClientId(...)`, sin nunca limpiar `selectedLead` — a diferencia del resto de los puntos donde se abre `ClientHistoryModal` (ej. `handleRegisterSaleFromHistory`), que sí lo hacían.
  - **Corrección**: el botón ahora limpia `selectedLead` (`setSelectedLead(null)`) en el mismo click antes de fijar `historyClientId`.
  - **Nota de verificación**: no se pudo confirmar visualmente en el navegador de esta sesión que el modal de etapa desaparece de la pantalla — el panel de preview de este entorno no compone frames (mismo problema que impide tomar capturas de pantalla en esta sesión), y las animaciones de salida de Framer Motion (`AnimatePresence`/`exit`) dependen de `requestAnimationFrame` para completarse y desmontar el nodo; sin composición de frames esas animaciones nunca "terminan", así que el nodo puede quedar en el DOM aunque el estado de React ya haya cambiado correctamente. Se confirmó por inspección de DOM que, tras el fix, el código se ejecuta (build sin errores, sesión recargada con el código nuevo), pero la confirmación visual real queda pendiente de una prueba del usuario en su navegador normal.

- **9 de Agosto de 2026** *(el modal que abre la búsqueda global del closer ahora depende de la etapa real del lead, en vez de mostrar siempre el resumen genérico, [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [MODIFY])**:
  - **Pedido del usuario**: al buscar un lead en la barra superior del closer, dejar de mostrar siempre el resumen general (`ClientHistoryModal`) y en su lugar abrir el modal correspondiente a la etapa real del lead: **Llamada sin tomar** (aún no confirmada → modal de confirmación; ya pasó la fecha sin reportar → modal de "¿qué pasó con la llamada?"), **Llamada tomada** (asistió pero no cerró → seguimiento en curso hasta cerrar o descartar), o **Llamada cerrada** (ya compró algo → seguimiento de cobranza/renovación/upsell). También pidió que el pool "Llamadas cerradas" de la pestaña Seguimientos abra ese mismo modal de seguimiento en vez del resumen, que el modal de confirmación deje elegir entre varias agendas si el lead tiene más de una, y que se vea la fecha de agendamiento (siempre) y la fecha de ingreso del cliente (cuando es una llamada cerrada).
  - **Diagnóstico**: los tres modales de etapa ya existían y estaban completamente construidos (`modalStep` 'confirm' para confirmación, 'root'→árbol de decisión para reportar la llamada, 'seg' para seguimiento de llamada tomada/no tomada, 'segventa' para cobranza de llamada cerrada — con tabla de cuotas y "¿qué pasó con el cobro?") — el problema era exclusivamente de enrutamiento: `handleSelectSearchResult` siempre abría `ClientHistoryModal` para cualquier lead con `Client` propio, y `SeguimientosPane` hacía lo mismo para el pool "cerrada" (`openLead`, con un comentario explícito documentando esa decisión), dejando `segventa` sin ninguna forma real de alcanzarlo desde ahí pese a estar terminado.
  - **`CloserFollowUpService.get_client_lead_stage(client_id)` (nuevo)**: clasifica un cliente en `'cerrada'` (tiene algún `FinancialSale`, vía el mismo `_client_has_sale` ya usado para derivar seguimientos "tomada"), `'confirm'` (tiene agenda(s) futura(s) sin procesar por el closer — devuelve la lista completa si hay más de una, para el selector), `'call'` (la fecha ya pasó y no se reportó qué pasó), `'seg'` (llamada tomada/no tomada con seguimiento pendiente, mismo criterio que `_effective_tipo`) o `'none'` (sin citas). Expuesto en `GET /closer/leads/<client_id>/stage`.
  - **Frontend**: `handleSelectSearchResult` consulta ese endpoint y enruta con `handleSelectLead` (reusando el árbol de decisión, formularios y guardado ya existentes) según la etapa: si hay varias agendas por confirmar, se abre un selector nuevo ("Elegí la agenda") que lista todas para elegir sobre cuál marcar el estado; si es `'cerrada'`, se arma el mismo objeto de lead que ya usa el pool de Seguimientos (deuda, programa, próxima cuota) para abrir directo el modal de cobranza. `SeguimientosPane.openLead` ya no desvía "cerrada" a `ClientHistoryModal` — ahora pasa por `onOpenLead` como los demás tipos. `handleSelectLead` se ajustó para que la etapa explícita (`lead.fase`) mande sobre la pestaña actualmente abierta (`activeStep`) — necesario porque, a diferencia de las tarjetas del mazo (que siempre pertenecen a la pestaña donde viven), un resultado de búsqueda puede corresponder a una etapa distinta de la pestaña que el closer tenga abierta en ese momento.
  - **`ClientHistoryModal` no se eliminó**: sigue disponible como acceso secundario ("Ver historial completo del cliente →", nuevo botón en la ficha compartida de todos los modales de etapa) para cuando el closer sí quiera ver el panorama completo (todas las agendas, ventas, pagos) — solo dejó de ser lo que abre por defecto.
  - **Fecha de agendamiento / fecha de ingreso**: la ficha compartida (`idcard`) de los 4 modales de etapa ahora muestra "Fecha agendamiento" (`start_time`/`call_date`, siempre) y, solo cuando existe, "Fecha de ingreso" (`Enrollment.enrollment_date` más reciente del cliente — no existía como campo expuesto en ningún endpoint del closer hasta ahora). Se agregó a `_format_appointment_for_deck`, a `CloserFollowUpService._serialize`/`_cerrada_pool_items`, y al nuevo `get_client_lead_stage`.
  - **Verificado end-to-end**: `get_client_lead_stage` corrido contra la base local real — un cliente con venta real (Sebastian Paraban, `client_id` 5729) clasificó correctamente como `'cerrada'` con `enrollment_date` real (09 ene 2026); un cliente duplicado con el mismo nombre pero sin venta (`client_id` 3275) clasificó como `'call'`; con dos citas futuras sintéticas creadas temporalmente sobre un cliente de prueba se confirmó `'confirm'` con las dos agendas listadas (datos de prueba eliminados después). **En el navegador real** (sesión simulada de Jean Carlo): buscar al cliente sin venta abrió el árbol de decisión de llamada ("Reporte de llamada") con "Fecha agendamiento: 08 ene 2026" visible; buscar al cliente con venta abrió el modal de cobranza ("Seguimiento de Cliente. Ya cerró la venta...") con "Programa: Residency Roadmap", "Deuda pendiente: $0" y "✓ Fecha de ingreso: 08 ene 2026" (solo visible en este caso, como se pidió); buscar un cliente de prueba con dos agendas futuras abrió el selector "Elegí la agenda" listando ambas fechas, y elegir una abrió el modal de confirmación anclado a esa cita puntual. `npm run build` sin errores.
  - **Hallazgo sin resolver esta pasada, fuera de alcance**: al probar en el navegador real se detectó que el botón "×" de cierre del modal de detalle de lead (patrón `.ov.on` + `AnimatePresence`, código preexistente sin tocar en esta pasada) no desmontaba el modal de forma confiable ni con click real ni con `.click()` vía JS — mismo síntoma reproducido en el selector de agendas nuevo, que usa el mismo patrón exacto. Otros botones simples de la misma página (ej. cambiar de pestaña) sí respondieron con normalidad al mismo mecanismo de click, así que parece acotado a este patrón de modal específico, no a un problema general de entrega de clicks. No se pudo aislar la causa con las herramientas disponibles en esta sesión (podría ser un bug real o una limitación del entorno de pruebas remoto — el mismo problema de panel de preview documentado repetidamente en sesiones anteriores). Se dejó como tarea aparte para investigar con un click manual real antes de decidir si amerita arreglo.

- **6 de Agosto de 2026** *(continuación — un pago en un programa distinto daba 409 por una cuota de otro programa, [installment.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/installment.py) [MODIFY], [installment_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/installment_service.py) [MODIFY], [closer_installments.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer_installments.py) [MODIFY], migración `75aea94ff0e5`)**:
  - **Reportado por un closer real** (con captura del error): al declarar un primer pago (Parcial) a un cliente que ya tenía una cuota registrada, el sistema devolvía 409 y no dejaba crear el plan.
  - **Diagnóstico**: `InstallmentPlan` se identificaba solo por `client_id`, sin ningún concepto de programa (AL/RR/SI). Reproducido exacto contra un caso real de la base local: un cliente con un plan de RR (1 cuota pagada, 3 pendientes) — la validación de secuencia de pago correctamente permitía un Parcial nuevo en AL (sin historial previo en ese programa), pero al crear el plan de cuotas, `InstallmentService.create_plan` encontraba el plan de RR con una cuota pagada y rechazaba TODO, sin importar que fuera un programa completamente distinto.
  - **Corrección**: `InstallmentPlan.programa_code` (nuevo, nullable — los planes históricos quedan sin programa asociado, no se puede reconstruir retroactivamente con certeza). `create_plan`/`get_plan`/`get_plan_by_client` ahora filtran por `(client_id, programa_code)` en vez de solo `client_id`. `GET /closer/installments/<id>` sigue devolviendo cuotas de todos los programas cuando no se pide uno específico (pantalla general de seguimiento de cobro), y se acota al programa activo solo cuando el modal de venta necesita elegir qué cuota se está pagando de ESE programa puntual.
  - **Verificado**: reproducción exacta del bug contra datos reales (cliente con plan RR ya pagado parcialmente) — antes, crear un plan AL fallaba con 409; después, se crea correctamente y el plan de RR queda intacto sin tocarse (confirmado leyendo sus 4 cuotas antes/después). `npm run build` sin errores. Datos de prueba eliminados tras la verificación.

- **6 de Agosto de 2026** *(continuación — fechas de cobro de cuotas editables al crear el plan, [installment_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/installment_service.py) [MODIFY], [closer_installments.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer_installments.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [ClientHistoryModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/shared/ClientHistoryModal.jsx) [MODIFY])**:
  - **Pedido del usuario**: "esas fechas deben ser modificables, para que el closer ponga cuándo le va a cobrar cada cuota a cada cliente, al momento de registrar el primer pago" — el armador de plan (tanto en Declarar Venta al declarar un Parcial, como en el "crear plan al vuelo" agregado hace un momento) mostraba una tabla de vista previa con fechas fijas (+1/+2/+3 meses automático), editables recién después de creado el plan, no en el momento de armarlo.
  - **Implementación**: `InstallmentService.create_plan` acepta un `fechas` opcional (lista, una por cuota en orden) que sobreescribe el vencimiento automático — una fecha faltante o inválida en la lista cae al cálculo automático para esa cuota puntual, sin romper el comportamiento existente cuando no se manda nada. `POST /closer/installments` pasa el parámetro tal cual. En el frontend, tanto la tabla de "Plan de Cuotas" del modal de Declarar Venta como el formulario "Crear plan" del resumen del cliente ahora tienen un input de fecha editable por cuota, precargado con la fecha automática pero modificable antes de guardar.
  - **Verificado end-to-end vía HTTP real** contra `POST /closer/installments` con fechas mixtas (dos custom + una `null`): las custom se respetaron tal cual, la `null` cayó correctamente al cálculo automático. Datos de prueba eliminados. `npm run build` sin errores.

- **6 de Agosto de 2026** *(continuación — armar plan de cuotas al vuelo desde el resumen del cliente, [client_cleanup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/client_cleanup_service.py) [MODIFY], [ClientHistoryModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/shared/ClientHistoryModal.jsx) [MODIFY], [SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: "si no tiene plan de cuotas debe permitir crearlo" — el fix anterior (mostrar "Debe \$X · sin plan de cuotas") era una etiqueta muerta sin ninguna acción posible.
  - **Implementación**: `GET /closer/clients/<id>/full-history` expone `client.total_amount`/`client.deuda`. En `ClientHistoryModal`, si "Cuotas por cobrar" está vacía pero hay deuda real, aparece un formulario compacto (elegir número de cuotas, ver el monto de cada una) que llama al mismo `POST /closer/installments` que ya usa el armador de plan del modal de Declarar Venta — pasando el saldo adeudado como total y 0 como cobrado hoy. Clickear un item de "Llamadas cerradas" en el pool de Seguimientos ahora abre directamente este resumen del cliente (antes abría el árbol de decisión de llamada, que no aplicaba a este tipo) — el recordatorio de cobro de la pasada anterior ahora lleva directo a poder armar el plan, no solo a verlo.
  - **Verificado end-to-end vía HTTP real**: sobre un cliente real de Jean Carlo que debía \$2500 sin plan, se crearon 3 cuotas mensuales de \$833.33/\$833.33/\$833.34 con fechas escalonadas mes a mes; datos de prueba eliminados después. `npm run build` sin errores.

- **6 de Agosto de 2026** *(continuación — clientes con deuda sin plan de cuotas se veían "sin cuotas pendientes", [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [MODIFY])**:
  - **Reportado por el usuario**: "todos los que deben dinero [deberían] tener cuotas pendientes", pero el pool de Llamadas cerradas mostraba "Sin cuotas pendientes" para casi todos.
  - **Diagnóstico**: `proxima_cuota` dependía exclusivamente de que existiera un `InstallmentPlan` real, y ese plan solo se crea si el Parcial se declaró pasando por el armador de cronograma específico del modal de venta — la gran mayoría de Parciales históricos nunca pasaron por ahí (11 planes en total en toda la base local), así que un cliente podía deber dinero real (`_client_debt`, vía Enrollment/Payment) sin tener jamás un plan, quedando invisible para el recordatorio pese a deber.
  - **Corrección**: cuando no hay `InstallmentPlan` pero sí deuda real (>$0.01), se sintetiza un `proxima_cuota` marcado `sin_plan: true` con el monto adeudado (sin fecha, porque no hay ninguna definida). Orden de urgencia del pool: vencidas > próximas a vencer > deuda sin plan armado > al día. El frontend distingue "Debe $X · sin plan de cuotas" de "Cuota vencida"/"Cobrar cuota" (con fecha real) y de "Al día".
  - **Verificado**: de los 70 clientes cerrados de Jean Carlo, 15 pasaron de "sin cuotas pendientes" a mostrar su deuda real como recordatorio. `npm run build` sin errores.
  - **Hallazgo de rendimiento sin corregir esta pasada**: `_cerrada_pool_items` tarda varios segundos (llegó a superar el timeout de 2 minutos en pruebas por consola, aunque en la práctica vía HTTP es más rápido) por un patrón N+1: `_client_debt`/`_client_program_code` recorren `Enrollment`/`Payment` con una consulta por cliente, multiplicado por los ~70 clientes del pool. No se optimizó en esta pasada (no fue lo pedido); queda documentado como candidato a revisar si el pool crece o el tiempo de carga se vuelve notorio para los closers.

- **6 de Agosto de 2026** *(continuación de la sesión del 5 — marcar cuota como pagada no reportaba a Discord, [client_cleanup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/client_cleanup_service.py) [MODIFY], [ClientHistoryModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/shared/ClientHistoryModal.jsx) [MODIFY])**:
  - **Reportado por el usuario** probando la sección de cuotas recién agregada al historial del cliente: al marcar una cuota como pagada, esperaba que se reportara a Discord vía n8n igual que cualquier venta declarada normalmente — no estaba pasando.
  - **Diagnóstico**: el botón "Marcar como pagada" (agregado en la pasada anterior) solo llamaba a `PATCH /closer/installments/cuota/<id>`, que únicamente actualiza `InstallmentPlan.estado` en la base local — nunca pasaba por `SheetsService.post_to_sheets` (que crea el `FinancialSale`, sincroniza Enrollment/Payment, y dispara `_trigger_n8n_webhook` hacia Discord), a diferencia del flujo normal de "Declarar Venta" con tipo Cuota, que sí hace ambas cosas en ese orden.
  - **Corrección**: el botón ahora hace primero `POST /sheets/push?tabla=Ventas_DB` con un payload `"{programa_code} - Cuota"` (mismo formato que usa el resto del sistema) y, solo si esa venta se registra correctamente, hace el `PATCH` que marca la cuota como pagada — mismo orden que ya usa el flujo existente. Se expuso `client.programa_code` en `GET /closer/clients/<id>/full-history` (reutilizando `CloserFollowUpService._client_program_code`) para que el frontend pueda armar el `tipo_pago` correcto.
  - **Precaución de verificación**: `SheetsService.post_to_sheets` pega contra una URL real de Google Apps Script hardcodeada (no gateada por ninguna variable de entorno local) — se verificó con `requests.post` mockeado para no tocar la hoja de cálculo real en ningún momento. Confirmado: (1) un cliente sin Parcial previo es rechazado correctamente por la validación de secuencia, no crea nada; (2) un cliente con Parcial real sí crea el `FinancialSale` (conteo antes/después) y hubiera llegado al POST real de no estar mockeado. Datos de prueba eliminados en ambos casos. El webhook de n8n en sí (`N8N_WEBHOOK_URL`/`VENTAS_WEBHOOK`) no está configurado en este entorno local, así que no se pudo verificar que efectivamente llegue a Discord — solo que el código lo dispara correctamente cuando esas variables sí están configuradas (mismo mecanismo ya usado y confiado por "Declarar Venta").

- **5 de Agosto de 2026** *(continuación — recordatorios de cuotas en Llamadas cerradas y búsqueda global abriendo el resumen del cliente, [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [MODIFY], [ClientHistoryModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/shared/ClientHistoryModal.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido 1**: los recordatorios para cobrar cuotas deberían vivir en el pool "Llamadas cerradas" de Seguimientos, no solo pasivamente en el historial del cliente (feature de la pasada anterior). `_cerrada_pool_items` ahora resuelve la próxima cuota pendiente de cada cliente (`InstallmentPlan` más próxima a vencer) como `proxima_cuota`, y el pool completo se ordena por urgencia: vencidas primero (las más atrasadas arriba), luego las próximas a vencer, al final los sin cuota pendiente. `SeguimientosPane` muestra esa fecha directamente en la tarjeta ("Cuota vencida"/"Cobrar cuota" con fecha y monto) en vez del "Asignar fecha" genérico que no aplicaba acá. Verificado inyectando temporalmente una cuota vencida sobre un cliente real: subió al primer lugar de la lista.
  - **Pedido 2**: confirmó que la búsqueda ya no muestra duplicados (fix de la pasada anterior), pero al abrir un resultado seguía llevando a la última agenda puntual en vez de al cliente. `handleSelectSearchResult` ahora abre `ClientHistoryModal` (resumen: agendas, ventas, cuotas) como comportamiento principal; el ícono secundario de cada fila pasa a ser un atajo para quien igual quiera la última agenda directamente. Desde el resumen ahora se puede: abrir cualquier agenda listada con un click, marcar una cuota como pagada (además de reprogramar su fecha), y un botón "Registrar venta / pago" que abre el modal de Declarar Venta de siempre, precargado con los datos del cliente y anclado a su agenda más reciente.
  - **Verificado**: `npm run build` sin errores; backend probado con inyección/limpieza de datos de prueba contra la base local. No se pudo verificar visualmente en el navegador en esta pasada (mismo problema de panel de preview de toda la sesión).

- **5 de Agosto de 2026** *(continuación — cuotas pendientes visibles y reprogramables desde el historial del cliente, [client_cleanup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/client_cleanup_service.py) [MODIFY], [closer_installments.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer_installments.py) [MODIFY], [ClientHistoryModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/shared/ClientHistoryModal.jsx) [MODIFY])**:
  - **Pedido del usuario**: en el resumen del cliente (recién agregado a la búsqueda global) quería ver, además de agendas y ventas, las cuotas pendientes por cobrar con su fecha — y poder modificar esa fecha, de cara a un futuro recordatorio para el closer.
  - **Implementación**: `GET /closer/clients/<id>/full-history` ahora incluye `installments` (`InstallmentService.get_plan_by_client`, que ya existía en el servicio pero no estaba conectado a esta vista). El modal muestra cada cuota con badge de estado (pendiente/vencido/pagado — ya calculado por `InstallmentPlan.to_dict`), monto, y un selector de fecha editable que llama al `PATCH /closer/installments/cuota/<id>` ya existente (solo le faltaba estar expuesto desde el historial). Se alineó `_can_access` de `closer_installments.py` con la política de "cualquier closer puede editar" del resto de esta sesión — antes solo el closer dueño de la cita podía tocar el plan de cuotas de un cliente.
  - **Verificado end-to-end vía HTTP real**: cuotas de un cliente real aparecen correctamente en el historial; un closer distinto del dueño de la cita reprogramó la fecha de una cuota real (200 OK), confirmado el cambio y revertido después.
  - **Fuera de alcance esta vez**: no se implementó un recordatorio proactivo (push/notificación automática al closer cuando una cuota vence) — solo quedó resuelta la visibilidad pasiva (el badge de estado, visible cada vez que se abre el historial) y la posibilidad de reprogramar. Un recordatorio activo requeriría diseñar dónde/cómo se le avisa al closer (¿en el dock del mazo, como badge de conteo? ¿en el reporte diario?) — queda como propuesta para una futura sesión, a definir con el usuario.

- **5 de Agosto de 2026** *(continuación — clientes duplicados en búsqueda, permisos entre closers, pool de cerradas, y un bug de raíz que bloqueaba declarar Cuotas, [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [closer_followups.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer_followups.py) [MODIFY], [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [sales_consistency_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/sales_consistency_service.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], nuevo [ClientHistoryModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/shared/ClientHistoryModal.jsx))**:
  - **Búsqueda global mostraba clientes duplicados**: `/closer/leads/search` deduplicaba sus resultados de `FinancialAgenda` contra los de `Client`, pero nunca deduplicaba las propias filas de `Client` entre sí. Se agrega dedup por email/instagram/últimos 8 dígitos del teléfono normalizados sobre los resultados de `Client` mismos — la deduplicación automática (`ClientDedupService`) no atrapa el 100% de los duplicados reales, así que esto sigue siendo necesario del lado de la búsqueda.
  - **Cualquier closer puede editar cualquier lead** (decisión explícita del usuario: bloquear por dueño solo generaba fricción sin aislamiento real necesario entre closers). Se remueve la restricción por `closer_id` en los endpoints de escritura del flujo (`POST /closer/deck/<id>`, `PATCH /closer/appointments/<id>`, `.../stage`, `.../outcome`, `POST /closer/followups/<id>/schedule`, `GET /closer/deck/card/<id>`). Si el dueño actual está inactivo se reasigna al closer que lo trabaja (igual que la pasada anterior); si sigue activo, se edita sin quitárselo de su pipeline. Nota: esto revierte una feature de "solo lectura para lead ajeno" que una sesión paralela había completado minutos antes (`can_edit`/`owner_closer_name`) — ahora siempre `can_edit=true`, con `owner_closer_name` puramente informativo.
  - **Pool "Llamadas cerradas" casi siempre vacío**: reportado por el usuario ("no hay ningún registro para poder agregarle seguimiento... deberían estar ahí las ventas que hizo el closer"). `seguimiento_tipo='cerrada'` solo se etiquetaba si el closer pasaba por el flujo específico de "cobro pendiente" al declarar la venta — la inmensa mayoría de ventas normales nunca calificaban. Nuevo `CloserFollowUpService._cerrada_pool_items`: resuelve directamente desde `FinancialSale` (cruzando por `CloserService._resolve_sale_identifiers`) todos los clientes a los que el closer le vendió algo, con deuda y programa. Jean Carlo pasó de 0 a 70 clientes visibles.
  - **Hallazgo de datos que explica varios bugs de golpe**: `FinancialSale.client_id` está `NULL` en 829 de 830 filas de esta base local, pese a que la bitácora del 5 de agosto documenta un backfill "830/830" — o esta copia local es anterior a ese backfill, o se resetió después. Esto rompía en silencio cualquier consulta que dependiera de esa columna:
    - El pool de "Llamadas cerradas" (arriba).
    - **Bug reportado por un closer real, bloqueante**: al intentar declarar una Cuota, el sistema decía "no tiene un Parcial registrado" aunque sí lo tenía — `SalesConsistencyService.get_client_payment_state` filtraba `FinancialSale` solo por `client_id`. Se agregó, en ambos sitios, un cruce por email/instagram/teléfono normalizados (mismo criterio que `BookingService.find_or_create_client`) como respaldo — verificado end-to-end vía HTTP real: `has_first_payment` pasó de `False` a `True` y Cuota de bloqueada a permitida para un cliente real con un Parcial ya declarado.
    - **Pendiente para una futura sesión**: correr (o re-correr) `scripts/backfill_financial_sale_client_id.py` sobre esta base para que `client_id` quede poblado de verdad, en vez de depender del cruce por email/instagram/teléfono en cada consulta — el cruce es un respaldo razonable pero más lento y menos preciso que la FK ya resuelta.
  - **Historial de cliente accesible desde la búsqueda**: se extrajo el modal de historial (agendas, ventas, inscripciones/pagos) que ya existía embebido en `ClientCleanupPane` a `components/shared/ClientHistoryModal.jsx` (mismo endpoint `GET /closer/clients/<id>/full-history`, sin cambios), y se agregó un botón de historial en cada resultado de la búsqueda global — antes solo se podía abrir la última cita puntual.
  - **Verificado**: todos los fixes de backend probados vía HTTP real (Flask test client) o consulta directa contra la base local, con datos de prueba limpiados después. `npm run build` sin errores. No se pudo verificar nada visualmente en el navegador en esta pasada (mismo problema de panel de preview de esta sesión).
  - **Incidente de datos evitado a tiempo**: durante la prueba end-to-end de "cualquier closer puede editar", un test automatizado sobrescribió `closer_notes` de una cita real (id 2838) con texto de prueba; no había ningún `LeadEventLog` previo del que recuperar el valor original, así que se limpió a `NULL` en vez de dejar el texto de prueba — no se puede garantizar que coincida exactamente con el estado anterior, pero es preferible a dejar contaminación de prueba permanente.

- **5 de Agosto de 2026** *(continuación — leads de closers dados de baja quedaban huérfanos en Seguimientos, [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [closer_followups.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer_followups.py) [MODIFY], [SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [MODIFY])**:
  - **Aclaración del usuario** a la pregunta dejada pendiente en la entrada anterior: "las que no tienen a nadie asignado" se refería a leads de closers que ya no trabajan ahí (dados de baja), no a un `closer_id` literalmente nulo.
  - **Confirmado contra la base local**: 955 `Appointment` tienen `closer_id` apuntando a un `User` con `is_active=False` — invisibles hasta ahora para cualquier closer activo, porque `CloserFollowUpService._base_query` filtraba estrictamente `closer_id == closer_id` sin excepción.
  - **Corrección**: `_base_query` ahora incluye, además de las propias del closer, las de closers con `role='closer'` e `is_active=False`. Los 3 endpoints de escritura del flujo de seguimiento (`POST /closer/deck/<id>`, `PATCH /closer/appointments/<id>`, `POST /closer/followups/<id>/schedule`) reclaman automáticamente el lead para el primer closer activo que lo procesa, en vez de devolver `403` solo porque el `closer_id` de la fila no coincide con el suyo — antes de este fix, aunque el lead ya apareciera en el pool, el closer no hubiera podido ni abrirlo ni guardar nada sobre él. Se agregó un chip "De {closer} (baja)" en las tarjetas del pool para dejar claro que es un lead heredado.
  - **Verificado extremo a extremo** contra la base local: se marcó temporalmente un `Appointment` real de un closer inactivo (Guillermo) como elegible para el pool de seguimientos, se confirmó que aparecía en el conteo de Jean Carlo (closer activo: 15→16 en "no_tomada"), y se revirtió el estado de prueba.
  - **Nota operativa — incidente evitado a tiempo**: al hacer `git add app/api/closer.py` para commitear el fix de la meta diaria (entrada anterior), se arrastró sin querer un cambio sin commitear de una sesión paralela en el mismo archivo (`can_edit`/`owner_closer_name` en `get_closer_deck_card`, para abrir en solo lectura un lead de otro closer) — quedó mezclado en el commit `824e973` en vez del suyo propio. Se resolvió solo: esa sesión commiteó su parte del frontend después (`10a2104`/`5ba2485`), y el feature quedó completo y funcional igual, solo con la atribución del commit del backend cambiada. Para el resto de esta pasada se revisó `git diff` archivo por archivo antes de cada `git add` para no repetirlo.

- **5 de Agosto de 2026** *(continuación — meta diaria de seguimientos contaba toda la historia, [closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
  - **Reportado por el usuario**: en la pestaña de Seguimientos, "Objetivo de seguimientos del día" mostraba "196 de 50 · Meta cumplida" en un día en el que no había hecho ningún seguimiento.
  - **Diagnóstico**: `CloserFollowUpService.get_daily_goal_progress` calculaba `start_dt`/`end_dt` del día seleccionado pero nunca los usaba para filtrar la query — contaba `seguimiento_realizado == True` sobre TODA la historia del closer, sin ningún filtro de fecha.
  - **Corrección**: se reescribe usando `Appointment.last_contact_at` (agregado en una pasada anterior de esta misma sesión) dentro de la ventana del día en la zona horaria del closer — mismo criterio que `follow_ups_closed` en `compute_daily_report_fields`, así que la meta diaria y el resumen del reporte ahora son consistentes entre sí. Verificado contra la base local: pasó de `196/50` a `0/50` para el mismo closer en el mismo día.
  - **Pedido del usuario, en investigación**: que el "Pool sin fecha asignada" muestre también las agendas sin nadie asignado, no solo las del closer actual. Al investigarlo se confirmó (con un `IntegrityError` real al probarlo) que `Appointment.closer_id` es `NOT NULL` a nivel de base de datos — un lead verdaderamente "sin asignar" no puede existir como fila en `Appointment`, así que la lectura literal de "sin nadie asignado" es estructuralmente imposible con el esquema actual. Se revirtió el intento inicial de agregar `closer_id IS NULL` al pool/permisos (código muerto: esa condición nunca puede ser verdadera). Pendiente aclarar con el usuario qué quiere decir exactamente con "sin nadie asignado" antes de construir algo — ver pregunta hecha en el chat.
  - **Bug real encontrado de paso, sí corregido**: `POST /closer/deck/referrals/manual` sí intentaba crear un `Appointment` con `closer_id=None` cuando el referido lo creaba un admin/setter (no un closer) — eso hubiera crasheado con `IntegrityError` → `400` en vez de guardar el referido, silenciosamente. Ahora hereda el `closer_id` del lead que lo refirió, con el mismo fallback que ya usa `BookingService.sync_financial_agenda_to_appointment` si ese lead tampoco tiene closer. Verificado con un referido de prueba creado como admin: antes crasheaba, ahora se crea y hereda correctamente el closer del referente (creado y eliminado en la misma verificación).
  - **Nota operativa**: se detectó en vivo una sesión paralela editando `CloserWorkflowPage.jsx` (agregando una vista de solo lectura para leads de otro closer, campos `can_edit`/`owner_closer_name`) — se dejó su diff intacto sin commitear, sin overlap con los archivos de esta pasada.

- **5 de Agosto de 2026** *(continuación — el resumen en vivo del reporte daba números absurdos, [closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Reportado por el usuario**, probando en local con la sesión real de Jean Carlo: el resumen en vivo recién agregado mostraba `0` en conversando pero `65` confirmados y `42` show ups en un solo día — "absurdo". Pidió además ver pendientes y no vio nada de ventas en el resumen.
  - **Diagnóstico**: `get_daily_activity_summary` contaba citas por `Appointment.updated_at` dentro de la ventana del día — pero ese campo lo toca *cualquier* guardado de la fila, no solo una acción real del closer. La sospecha (confirmada): el webhook de n8n `/public/financial-agendas` resincroniza agendas en lote llamando a `sync_financial_agenda_to_appointment` por cada item, lo que reescribe `updated_at` de citas que el closer nunca tocó hoy — inflando el conteo con ruido de sincronización de fondo.
  - **Corrección**: se reemplaza la fuente por `LeadEventLog` — cada guardado real desde `POST /closer/deck/<id>` ya escribe un evento con el `user_id` real del closer (`BookingService.log_lead_event`, existente desde antes). Ahora se cuentan citas *distintas* con al menos un evento de ese closer específico hoy (no eventos — una misma cita puede guardarse varias veces en el día), agrupadas por su estado actual. Verificado contra la misma base local con el mismo cliente: confirmados bajó de 65 a 1, show ups de 42 a 0.
  - **Agregado**: ventas del día (cantidad + efectivo cobrado, reutilizando `compute_daily_report_fields` para no duplicar la clasificación de tipo de pago) y pendientes por confirmar/llamar (solo tiene sentido para hoy — mismo criterio que las pestañas "① Confirmaciones"/"② Llamadas" del mazo) se suman al panel de resumen.
  - **Nota honesta dejada documentada en el código**: `LeadEventLog` tampoco distingue trabajo real de sesiones de desarrollo que actuaron "como" el closer (mismo `user_id`) — en este entorno local compartido, donde varias sesiones (incluida esta) verifican features "simulando a Jean Carlo", esos números pueden seguir inflados por pruebas hechas el mismo día. No hay forma de que ningún criterio basado en timestamps distinga eso; es una limitación del entorno de desarrollo, no del código.
  - **Verificado**: `npm run build` sin errores; `get_daily_activity_summary` corrido dos veces contra la base local (antes/después del fix) mostrando la corrección real de los números; smoke test end-to-end vía Flask test client autenticado como closer real. No se pudo probar visualmente en el navegador en esta pasada (mismo problema de panel de preview reportado en entradas anteriores de hoy).

- **5 de Agosto de 2026** *(continuación — resumen en vivo en el reporte diario, [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
  - **Pedido del usuario**: la pestaña "Reporte del día" seguía preguntando cosas que el sistema ya sabe por el trabajo del closer en su bandeja ("Llamadas tomadas hoy" era solo un display, y "¿A cuántas les pediste/dieron referido?" eran 2 inputs manuales). En su lugar, pidió un resumen tipo dashboard con datos reales — conversando, confirmados, show ups, reagendas, seguimientos configurados, seguimientos marcados como hechos — para poder verificar ANTES de enviar que el reporte tiene sentido, corregir desde la bandeja si algo no cuadra, y volver a chequear.
  - **`CloserService.get_daily_activity_summary(closer_id, day_local)` (nuevo)**: cuenta, sobre las citas tocadas ese día (mismo criterio `Appointment.updated_at` dentro de la ventana del día ya usado para "backlog resuelto hoy" en `compute_daily_report_fields`, no la fecha original de la cita — así reportar un día anterior sigue reflejando lo que se hizo ese día): `conversando`/`confirmados` (por `Appointment.result`), `show_ups` (`closer_result == 'Show up'`), `reagendas` (`is_rescheduled` o resultado reagendado/a), `seguimientos_configurados` (`fecha_seguimiento` fijada y aún pendiente), `seguimientos_hechos` (reutiliza `last_contact_at` de la pasada anterior de hoy) y `referidos_capturados` (agendas nuevas hoy con `origin LIKE 'Referido de%'`). Expuesto en `GET /closer/deck/daily-report` como `"activity"`.
  - **Frontend**: se reemplazó el panel "Referidos del día" (con sus 2 inputs numéricos) por un grid de 7 tarjetas de estadística en vivo con botón "Actualizar" — se recarga solo al reentrar a la pestaña o cambiar de fecha (mismo `useEffect` que ya consultaba el estado del reporte).
  - **`POST /closer/deck/daily-report`**: dejó de leer `referrals_sourced`/`referrals_scheduled` del body — ahora los calcula el propio backend a partir de `referidos_capturados`. Se usa el mismo número real para ambos ("Pedidos" y "Concretados" en el reporte de Discord) porque no hay ninguna señal automática que distinga cuántos se *pidieron* de cuántos se *concretaron* — se prefirió reportar el único dato real disponible en vez de inventar un "pedidos" o dejarlo en `0` al lado de un número real (mismo criterio ya documentado para `slots`).
  - **Verificado**: `npm run build` sin errores; `get_daily_activity_summary` corrido contra la base local sin excepciones; smoke test end-to-end vía Flask test client autenticado como closer real (Jean Carlo) contra `GET /api/closer/deck/daily-report` — confirma que la clave `"activity"` llega con los 7 conteos. No se pudo probar en el navegador real en esta pasada (mismo problema de panel de preview de esta sesión) — pendiente confirmar visualmente que el grid de estadísticas se ve bien y que "Actualizar" refresca tras editar algo en la bandeja.

- **5 de Agosto de 2026** *(continuación — reportes atrasados y datos reales de seguimientos/recuperaciones, [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY], [booking.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/booking.py) [MODIFY], [closer_report.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/closer_report.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], migraciones `0995e4725f90`/`3a05775ef49e`)**:
  - **Pedido del usuario**: mejorar el reporte diario de los closers en dos frentes — (1) poder reportar días anteriores, no solo hoy; (2) asegurar que todos los campos que se mandan a Discord estén llenos con datos reales de lo que el closer completa en su bandeja (agendas, estados, ventas), y que sea fácil de completar.
  - **(1) Reportar días anteriores**: `POST /closer/deck/daily-report` usaba `date.today()` fijo — no había forma de armar el reporte de un día que se pasó por alto. Ahora acepta `date` opcional ('YYYY-MM-DD', capado a hoy — nunca futuro) vía `_resolve_report_date`, y reenviar el mismo día actualiza el reporte existente (mismo `UniqueConstraint(closer_id, date)`) en vez de duplicarlo. Nuevo `GET /closer/deck/daily-report?date=` para que el frontend sepa si un día ya fue reportado y precargue lo que ya se había escrito (referidos/reflexión) — necesitó `CloserDailyReport.created_at` (no existía) para poder mostrar cuándo se envió.
  - **Frontend**: selector de fecha en la pestaña "Reporte" (tope en hoy). Se separó el estado "reporte de HOY enviado" (`todayReportSent`, decora el dock flotante con el check "✓ Reporte enviado" — eso siempre habla de hoy, sin importar qué día se esté mirando) del estado del día seleccionado (`reportSent`, controla el contenido de la pestaña). El botón ya no desaparece tras el primer envío: pasa a "Actualizar y reenviar reporte" para poder corregir referidos/reflexión de un día ya reportado.
  - **(2) Campos en cero fijo, encontrados durante la auditoría pedida**: `follow_ups_sent`, `follow_ups_replied`, `recoveries_contacted`, `recoveries_replied`, `recoveries_scheduled` estaban hardcodeados en `0` en `CloserService.compute_daily_report_fields` desde que se implementó el reporte real (4 de agosto) — las secciones "SEGUIMIENTOS" y "RECUPERACIONES" del reporte de Discord siempre llegaban en blanco. Además, `follow_ups_closed` (que sí se calculaba) tenía un bug propio: filtraba por `Appointment.fecha_seguimiento LIKE 'hoy%'`, pero ese campo se limpia a `NULL` justo al cerrar un seguimiento (ver flujo `saveSeguimientoReport`), así que un cierre de hoy nunca hubiera matcheado — subcontaba en silencio.
  - **Corrección**: `Appointment.last_contact_outcome`/`last_contact_at` (nuevos, migración `3a05775ef49e`) — se pueblan en `POST /closer/deck/<id>` cuando el payload trae `contact_result` (el resultado que el closer ya elegía en el árbol de "¿Qué pasó con este contacto?": `no_resp`/`contesto`/`agendo`/`cerro`/`pago`, ahora también enviado desde el frontend en las 4 ramas de `saveSeguimientoReport`). Se usa un timestamp propio en vez de reutilizar `updated_at` porque cualquier otra edición de la cita también lo toca, y hubiera contaminado el conteo. `compute_daily_report_fields` clasifica cada contacto del día por su tipo efectivo (`CloserFollowUpService._effective_tipo`: `tomada` → cuenta como Seguimiento, `no_tomada` → cuenta como Recuperación) y cuenta contactados/respondidos (`last_contact_outcome != 'no_resp'`) y, para recuperación, agendados (`== 'agendo'`). `follow_ups_closed` pasa a usar la misma ventana de `last_contact_at` en vez del `LIKE` roto.
  - **Nota**: `slots` sigue en `0` a propósito — no hay ninguna señal persistida de "slots configurados" en el sistema; queda documentado como decisión deliberada (mejor un 0 honesto que inventar un número), no un campo pendiente de arreglar.
  - **Verificado**: `npm run build` compila sin errores; los archivos backend parsean sin errores de sintaxis; `compute_daily_report_fields` corrido contra la base local sin excepciones; smoke test end-to-end vía Flask test client autenticado como closer real (Jean Carlo) contra `GET`/`POST /api/closer/deck/daily-report` para el día de ayer — reporte creado, `GET` posterior confirma `sent=true` con los datos de referidos/reflexión enviados: datos de prueba eliminados después. No se pudo verificar en el navegador en esta pasada (mismo problema de panel de preview reportado en entradas anteriores de hoy) — pendiente probar en vivo el selector de fecha y el botón "Actualizar y reenviar reporte".

- **5 de Agosto de 2026** *(continuación — referido como "agenda pendiente" y campo de grupo de Calendly, [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [client.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/client.py) [MODIFY], [financial.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/financial.py) [MODIFY], [financial_agendas.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/public/financial_agendas.py) [MODIFY], [booking_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/booking_service.py) [MODIFY], [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], migración `796839c55ce8`)**:
  - **Pedido del usuario (1)**: al agregar un referido no hay forma de poner una fecha real de cita, y confirma que eso está bien — la idea es que igual quede registrado como una "agenda" visible para el closer (aunque no exista una cita real todavía), como un primer seguimiento: el closer debe contactar a esa persona para acordar una fecha real y luego reagendarla.
  - **Diagnóstico**: el backend de `/closer/deck/referrals/manual` (fix de la pasada anterior de esta sesión) ya crea un `Appointment` real con `start_time=datetime.utcnow()` para que entre al mismo pipeline de confirmación que cualquier otra agenda — eso ya funcionaba. El problema encontrado: la tarjeta del Kanban (`renderKanbanCard`) mostraba esa marca de tiempo literal ("Hoy · 15:47"), dando la impresión de que había una cita coordinada a esa hora exacta, cuando en realidad es solo un valor técnico para que la fila entre al pipeline.
  - **Corrección**: toda tarjeta cuyo `origin` empiece con `"Referido de"` ahora muestra "Por agendar" en el badge de fecha/hora, más una etiqueta violeta "Contactar y acordar fecha" — sigue siendo una agenda real (con sus mismos botones "Registrar contacto"/"Confirmar asistencia" y la posibilidad de reagendarla), solo que ya no sugiere una hora falsa.
  - **Pedido del usuario (2)**: n8n ahora manda un campo nuevo `grupo` (minúsculas, ej. `"grupo 1"`/`"grupo 2"`/`"grupo 3"`) en el JSON de agendamiento vía Calendly. Pide guardarlo por cliente en la base de datos para que los closers puedan verlo; si un cliente no tiene grupo asignado (por ser previo a este cambio), debe mostrarse "Grupo sin asignar" en vez de inventar un valor.
  - **Implementación**: `Client.grupo` y `FinancialAgenda.grupo` (String, nullable) nuevos. `POST /public/financial-agendas` (webhook de n8n) extrae `item.get('grupo')` y lo guarda tanto en la `FinancialAgenda` como, vía `BookingService.find_or_create_client(..., grupo=...)` → `sync_financial_agenda_to_appointment`, en el `Client` asociado — sin pisar un grupo ya asignado si el cliente ya existía con uno. Expuesto en el mazo del closer (`_format_appointment_for_deck`, `/closer/leads/search`) como `"grupo": cliente.grupo or "Grupo sin asignar"`, y mostrado en la ficha del lead (`idcard`) junto a Instagram/Fuente/Examen/Estado.
  - **Verificado**: `npm run build` compila sin errores; los 5 archivos backend tocados parsean sin errores de sintaxis; migración `796839c55ce8` aplicada en local (`flask db upgrade`) y confirmada por ORM (`Client.grupo`/`FinancialAgenda.grupo` accesibles). No se pudo verificar en vivo en el navegador en esta pasada — el panel de navegador de la sesión no cargó la preview (mismo problema reportado en la entrada anterior de hoy); pendiente de una prueba manual en el mazo real, en particular confirmar cómo llega el payload real de n8n (el nombre exacto de la clave `grupo` y que no traiga mayúsculas/espacios inesperados) antes de dar el mapeo por completamente cerrado.

- **5 de Agosto de 2026** *(continuación — validación de contacto en Nueva Agenda/Referido Manual y fix de modales superpuestos en Declarar Venta, [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
  - **Pedido del usuario**: revisar los formularios de "Nueva agenda" y "Referido manual" (faltaban datos — teléfono e Instagram debían ser obligatorios, y a preguntar se definió que el correo también); y revisar que al abrir el modal de "Declarar Venta"/registrar pago, cualquier otro modal abierto se cierre automáticamente para poder interactuar con el formulario de venta.
  - **Formularios**: el modal "Nueva agenda" mostraba el asterisco de obligatorio en Instagram pero `handleCreateAgenda` nunca lo validaba, y ni siquiera tenía campos de teléfono/correo en el formulario. El modal "Referido manual" tenía un único input de texto libre ("Instagram o teléfono", opcional) que el backend clasificaba por heurística (si tiene `@` o no es solo dígitos, es Instagram; si no, teléfono) — no había forma de capturar ambos ni el correo. Se agregaron los campos `phone`/`email` a `newAgendaForm` y se separó el campo único `contact` de `manualRefForm` en `instagram`/`phone`/`email`, los tres ahora obligatorios en ambos modales (con validación de formato de correo vía un helper `isValidEmail` nuevo) antes de poder enviar.
  - **Backend** (`POST /closer/deck/referrals/manual`): ahora acepta `phone`/`instagram`/`email` como campos estructurados (se los pasa directo a `BookingService.find_or_create_client`) en vez de depender únicamente de la heurística sobre `contact`. Esa heurística se conservó como *fallback* porque un segundo llamador real y distinto (la captura rápida "¿Le pediste referidos?" dentro del árbol de decisión de un seguimiento, en `saveSeguimientoReport`) sigue enviando un único campo `contacto` libre y sin obligar nada — es una herramienta más liviana durante una llamada en vivo, deliberadamente no se le exigieron los 3 campos porque excede el alcance de lo pedido.
  - **Modales superpuestos en Declarar Venta**: encontradas dos causas compuestas. (1) Al confirmar "Sí, hubo venta" desde dentro del modal de detalle de un lead (`modalStep === 'venta'`), se abría el modal de venta (`saleModalOpen`, z-index 100) sin cerrar el modal de detalle del lead (`selectedLead`, clase `.ov`, z-index 1000) — el modal de detalle, con mayor z-index, quedaba tapando/bloqueando el formulario de venta detrás. (2) En los 3 puntos donde se abre el modal de venta completo, el código setea `salePrompt.apptId` (usado como referencia de datos, ej. para cargar el plan de cuotas existente) *junto con* `saleModalOpen(true)` en el mismo clic — pero el prompt intermedio "¿Se cerró la venta?" se renderizaba solo con la condición `salePrompt.apptId &&`, sin excluir el caso en que el modal de venta completo ya estuviera abierto, así que ambos modales (mismo z-index 100, mismo overlay fijo a pantalla completa) podían quedar montados a la vez.
  - **Corrección**: se cierra `selectedLead` (`setSelectedLead(null)`) en el mismo clic que abre el modal de venta desde el detalle del lead; y la condición de render del prompt intermedio pasó a `salePrompt.apptId && !saleModalOpen`, ocultándolo automáticamente en cuanto el modal de venta completo está abierto (sin afectar el flujo donde el prompt se muestra solo, sin el modal de venta, ej. al marcar "Show up" desde la tarjeta).
  - **Verificado**: `npm run build` compila sin errores; `app/api/closer.py` parsea sin errores de sintaxis. No se pudo verificar en vivo en el navegador en esta pasada (el panel de navegador de la sesión no estaba disponible/visible para cargar la preview) — pendiente de una verificación manual en el mazo real antes de confiar ciegamente en el fix visual.

- **5 de Agosto de 2026** *(continuación — bugfix crítico: registrar una Cuota recreaba el plan completo desde cero)*:
  - **Reportado por el usuario**: al registrar el pago de una Cuota para un cliente que ya tenía un Parcial (y por lo tanto un plan de cuotas ya armado), el modal "Declarar Venta" seguía mostrando el armador "Plan de Cuotas" pidiendo definir de nuevo el número de cuotas, en vez de reconocer que ya existía un plan y dejar elegir cuál cuota planificada se estaba cobrando (adelantada o atrasada).
  - **Diagnóstico**: dos problemas compuestos. (1) `InstallmentPlan` se vinculaba únicamente por `appointment_id` (la cita puntual donde se declaró el Parcial) — si el pago de la Cuota se reportaba desde una cita distinta del mismo cliente (ej. una llamada de cobro posterior), la búsqueda del plan por esa cita nunca lo encontraba. (2) El frontend, ante `tipo_pago_simple !== 'completo'`, siempre renderizaba el armador de plan nuevo y, al guardar, llamaba incondicionalmente a `POST /closer/installments`, cuyo `InstallmentService.create_plan` **borraba y recreaba todas las cuotas desde cero** (`InstallmentPlan.query.filter_by(appointment_id=...).delete()`) sin verificar si ya había cuotas pagadas — perdiendo ese historial silenciosamente.
  - **Corrección**:
    - `InstallmentPlan.client_id` (nueva columna, migración `1cd1bf9f0ed8`, backfill de los 11 planes existentes desde `appointment.client_id`): el plan ahora pertenece al cliente, no a una cita puntual. `InstallmentService.get_plan(appointment_id)` resuelve el cliente de esa cita y devuelve **su** plan completo, sin importar desde qué cita se esté consultando.
    - `InstallmentService.create_plan` ahora recibe `client_id` y **se niega a borrar/recrear un plan si ya tiene alguna cuota en estado `pagado`** (devuelve `None`; el endpoint responde `409` con un mensaje explicando que hay que marcar la cuota correspondiente en vez de redefinir el plan). Solo permite reemplazar un plan que sigue 100% pendiente (ej. el closer corrigió el número de cuotas antes de cobrar la primera).
    - **Frontend**: al declarar una `Cuota`, el modal ahora busca el plan existente del cliente (`GET /closer/installments/<apptId>`, ya resuelto por cliente) y, si lo encuentra, muestra una tabla de sus cuotas (pendiente/vencida/pagada) para elegir cuál se está pagando — cualquiera, no solo la siguiente en orden, para poder adelantar una futura o saldar una atrasada — en vez del armador de plan nuevo. Al guardar, se llama a `PATCH /closer/installments/cuota/<id>` marcándola `pagado` con el monto ingresado, en vez de `POST /closer/installments`. El armador de plan nuevo se conserva sin cambios para `Parcial` (primera vez que se define el cronograma) y como respaldo si un `Cuota` no encuentra ningún plan previo (dato histórico incompleto), avisando explícitamente que se creará uno nuevo.
  - **Verificado end-to-end** (nivel servicio directo y HTTP real contra el backend, simulando a Jean Carlo): plan de 3 cuotas de $100 creado desde una cita; cuota 1 marcada pagada; intento de recrear el plan desde la misma cita devuelve `409` con el motivo; consultado desde una **segunda cita distinta del mismo cliente**, el plan se ve intacto (cuota 1 pagada, 2 y 3 pendientes con sus montos y fechas originales) — confirma que el bug original (recrear el plan) está resuelto y que el plan ahora es visible sin importar desde qué cita se consulte. `npm run build` compila sin errores. Datos de prueba eliminados tras la verificación.
  - **Nota operativa**: durante la verificación se encontró el backend Flask compartido (proceso de otra sesión) sirviendo código desactualizado en `closer_installments.py`/`installment_service.py` pese a los cambios ya guardados en disco — el auto-reload de Flask se había quedado atrás. Se resolvió sin reiniciar el proceso (solo re-tocando los archivos para forzar la detección del cambio); no afecta a producción, es un artefacto del entorno de desarrollo local compartido entre sesiones.

- **5 de Agosto de 2026** *(continuación — sistema de lotes diarios para el backlog)*:
  - **Sistema de "Lote Diario" Gamificado para Procesar el Backlog de "② Llamadas" ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Pedido del usuario**: en vez de enfrentar de una todo el backlog de "② Llamadas" (que puede ser grande — 17 para Jean Carlo en este momento), presentarlo en lotes pequeños (~10) de forma "didáctica"/tipo juego: notificar "tenés N leads atrasados", ofrecer un lote aleatorio, felicitar al completarlo y ofrecer otro. La idea explícita del usuario: con ~10 al día alcanza para mantener el sistema razonablemente al día.
    - **Diseño elegido (bajo riesgo, reutiliza todo lo existente)**: en vez de construir un endpoint nuevo o interceptar cada uno de los múltiples puntos donde el árbol de decisión de una llamada puede terminar (asistió/no show/cancelado/reagendó, cada uno con sus propias sub-ramas), el "modo lote" es una capa de filtrado 100% client-side sobre los datos que "② Llamadas" ya trae: se eligen `BATCH_SIZE=10` ids al azar (Fisher-Yates) de la lista ya cargada, y el progreso se deriva solo de comparar cuántos de esos ids siguen presentes en la lista tras cada refetch — sin necesidad de enganchar ningún callback de guardado. El closer sigue usando exactamente el mismo flujo de tarjeta→modal→árbol de decisión→guardar que ya existía, sin ningún cambio ahí.
    - **UI agregada**: banner de notificación ("Tenés N leads atrasados por procesar... Procesar lote de 10") cuando hay backlog y no se está en modo lote; banner de progreso ("Lote en progreso: X de 10 completados" con barra) mientras se procesa; banner de felicitación ("🎉 ¡Lote completado!") con botón para pedir otro lote o terminar por hoy, cuando el lote se vacía.
    - **Verificado end-to-end en el navegador**, simulando la sesión real de Jean Carlo (17 llamadas atrasadas): el banner de notificación mostró correctamente "Tenés 17 leads atrasados por procesar"; al pedir un lote se armó una lista de exactamente 10 citas al azar con el banner "0 de 10 completados"; se procesó una cita real por el flujo normal (No Show → motivo → programar seguimiento → guardar) y el banner actualizó a "2 de 10 completados" con la lista bajando a 8 citas — la matemática de progreso coincide exactamente con la lista real en todo momento.
    - **Nota**: se detectó una sesión de Claude Code en paralelo (terminal) trabajando sobre el mismo repositorio durante esta pasada, atacando hallazgos documentados el 4/5 de agosto (columnas de Renovación/Upsell en `CloserDailyReport`, `client_id` en `FinancialSale`, deduplicación de clientes, `offer_presented` en `Appointment`) — se verificó que no hay solapamiento de archivos con este cambio antes de commitear, y se dejaron sus archivos en progreso sin tocar.

- **5 de Agosto de 2026** *(continuación — proyecto de consistencia de ventas por cliente)*:
  - **Contexto**: el usuario reportó que al declarar una Cuota para un cliente que ya había pagado un Parcial, el formulario volvía a pedir el monto total del programa desde cero, y que "Cuota" se podía declarar sin que existiera un Parcial previo. Definió las reglas de secuencia de pago (Seña→Parcial/Completo únicamente; Cuota solo después de Parcial; Renovación/Upsell solo con el programa completamente pagado, con opción de liquidar el saldo restante junto con la renovación/upsell), pidió deduplicación automática y silenciosa de clientes, y que las métricas de conversión de señas (por oferta y por asistencia) lleguen a los dashboards de admin y closer — todo documentado en el plan de esta sesión.
  - **Fase 0 (backbone de datos)**: se agregó `financial_sales.client_id` (FK a `clients.id`, antes `FinancialSale` no tenía ninguna relación con `Client` — se cruzaba por coincidencia difusa de email/instagram/teléfono en cada consulta), `appointments.offer_presented` (booleano, para poder medir conversión de señas por oferta presentada — la pregunta ya existía en el árbol de decisión del mazo pero nunca se guardaba), columnas `renewal_*`/`upsell_*` en `CloserDailyReport` (mismo patrón de 4 campos ya usado para PIF/Split/Seña/Cuota), y la tabla `client_merge_logs` para auditar fusiones automáticas de clientes duplicados. Migración `c174c6b25233` generada con `flask db migrate` y aplicada en local.
  - **`SheetsService.post_to_sheets`** (tabla `Ventas_DB`) ahora resuelve/crea el `Client` con `BookingService.create_or_update_client` **antes** de crear el `FinancialSale` (antes esa resolución solo ocurría, en paralelo y por separado, dentro del espejo a `Enrollment`/`Payment`), y setea `sale.client_id`. Se extrajo `SheetsService.parse_tipo_pago`/`resolve_program` como helpers reutilizables (antes el parseo de `"CODE - tipo"` y la resolución del programa activo estaban duplicados/inline dentro de `_sync_enrollment_payment`) para que la próxima fase (validación de secuencia) los reutilice sin repetir lógica.
  - **Backfill histórico** (`scripts/backfill_financial_sale_client_id.py`, dry-run primero): de las 830 `FinancialSale` sin `client_id` en la base local, las 830 se resolvieron exitosamente contra `Client` (0 sin datos suficientes) y quedaron vinculadas tras correr con `--apply`. Verificado con consulta directa: `830/830` ventas con `client_id`, muestra revisada coincide (`Sebastian Paraban -> Sebastian Paraban`).
  - **Nota para el usuario**: el modo "dry-run" del script de backfill no es 100% no-destructivo — `BookingService.create_or_update_client` hace `commit()` por su cuenta al crear/completar un `Client` (mismo comportamiento que en el resto del sistema), así que incluso sin `--apply` puede crear o completar filas de `Client`; lo único que el dry-run evita es escribir `FinancialSale.client_id`. Quedó documentado en el propio script.
  - **Fase 1 (deduplicación automática de clientes)**: nuevo `app/services/client_dedup_service.py` (`find_duplicate_groups`/`merge_clients`/`run_full_dedup`), reutilizando el `UnionFind` ya existente en `attribution_service.py`. **Hallazgo de seguridad durante el desarrollo**: la primera versión (coincidencia simple por email/instagram/teléfono normalizados) fusionó por error a 8 personas claramente distintas en un solo grupo — causa raíz: el valor de instagram `"."` (usado como placeholder de "sin instagram" en varios registros) se trataba como una coincidencia real, encadenando por transitividad a desconocidos entre sí. Un segundo caso encontrado en la misma revisión manual: un número de teléfono de pruebas (`54 387 611-9789`) reciclado en 15 cuentas `Prueba`/`Test`/`Workshop Test` de Santiago habría fusionado registros de prueba con clientes reales si alguno hubiera compartido ese número por coincidencia.
    - **Correcciones aplicadas antes de fusionar nada en firme**: (1) lista negra de valores placeholder para instagram (`.`, `-`, `no hay`, etc.) y longitud mínima; (2) una clave normalizada (email/instagram/teléfono) compartida por **más de 2** clientes distintos se descarta como señal de fusión (dato placeholder/compartido, no duplicado real); (3) instagram y teléfono (señales más débiles que un email exacto) solo fusionan si además el nombre normalizado coincide (o es genérico/vacío) — evita que un registro con datos cruzados encadene por transitividad a dos personas reales distintas (caso detectado: "Andres Buhler" y "Cindy Restrepo" casi quedan fusionados por compartir instagram/teléfono con un tercer registro corrupto).
    - **Verificado tras las correcciones**: 288 grupos reales / 295 clientes fusionados (bajó de los 466 iniciales, todos falsos positivos filtrados), tamaño máximo de grupo 3 (antes había un grupo de 8). Revisión manual de una muestra de ~15 grupos confirmó mismo nombre + mismo teléfono con variación de email — duplicados genuinos. Post-fusión: `0` `FinancialSale`/`Appointment` huérfanos (`client_id` nulo), `0` emails duplicados reales entre clientes con email no nulo.
    - **Endpoint admin** `GET`/`POST /api/admin/tools/client-dedup` (mismo patrón que `backlog-cleanup`, `operator_required`): `GET` = dry run, `POST` = ejecuta.
    - **Hacia adelante**: `BookingService.create_or_update_client` ya no se detiene en la primera coincidencia — busca por las 3 vías, y si detecta que apuntan a clientes YA duplicados entre sí, los fusiona en el momento (mismas reglas de seguridad de arriba) antes de continuar, en vez de crear un cuarto registro fragmentado. Verificado con pruebas puntuales contra la base local: cliente nuevo se crea normal, mismo email reutiliza el existente, datos de una persona distinta no se fusionan por error, y el caso real de fusión (email apunta a un cliente, instagram con nombre compatible apunta a otro) fusiona correctamente y registra el `ClientMergeLog`.
  - **Fase 2 (validación de secuencia de pagos + autocompletar saldo pendiente)**: nuevo `app/services/sales_consistency_service.py` (`get_client_payment_state`/`validate_next_payment_type`/`get_allowed_types`), integrado como bloqueo duro dentro de `SheetsService.post_to_sheets` (tabla `Ventas_DB`) justo después de resolver el `Client`, antes de guardar la venta — devuelve `{"status": "error", "message": <motivo explicable>}` en vez de guardar, para que el closer pueda corregir el tipo de pago.
    - **Hallazgo de datos durante el desarrollo**: `FinancialSale.tipo_pago` tiene formatos históricos muy inconsistentes (`"Con Seña"`, `"RR - Con seña"`, `"RR -Parcial"` sin espacio, `"Al - Parcial"` en minúscula, y **cientos de filas sin ningún prefijo de programa** — `"Parcial"`, `"Cuota"`, `"Renovación"` a secas). El parser original (`SheetsService.parse_tipo_pago`, que solo dividía por `" - "` exacto) habría dejado esas ventas invisibles para la validación, bloqueando por error a clientes con historial real. Se reescribió como `_extract_tipo_keyword` (normaliza acentos/mayúsculas y reconoce `"Con X"` como `"X"`) + `parse_tipo_pago` tolerante a separador con/sin espacios y código en minúscula. Para ventas sin prefijo de programa, `get_client_payment_state` las asigna al programa consultado solo si el cliente no tiene evidencia de estar inscrito en otro programa distinto (evita contaminar cruzando programas).
    - **Nuevo endpoint** `GET /api/closer/sales/client-state?client_id=&programa=` (con fallback por email/instagram/teléfono si no hay `client_id` aún): devuelve cuánto pagó, cuánto falta y qué tipos de pago corresponden a continuación.
    - **Frontend** (`CloserWorkflowPage.jsx`): el selector de Tipo de Pago del modal "Declarar Venta" deshabilita las opciones que romperían la secuencia (con el motivo en tooltip y debajo del selector); si el tipo elegido por defecto queda inhabilitado al cargar el estado, salta automáticamente al primer tipo permitido; panel informativo "Ya pagó $X de $Y — saldo restante $Z"; `precio_total`/`monto` se autocompletan con el precio del programa y el saldo restante en vez de quedar vacíos. Para Renovación/Upsell con saldo pendiente, aparece un toggle "Incluir el pago del saldo pendiente junto con esta venta" que, al confirmar, envía primero una Cuota liquidando el saldo y solo si eso funciona continúa con la venta principal.
    - **Verificado end-to-end contra la base de datos local y vía HTTP real** (simulando a Jean Carlo desde el navegador): cliente con Parcial de $250/$500 en AL → solo Cuota permitida, con motivo explicado para las demás; cliente nuevo → solo Completo/Parcial/Seña permitidas; intento real de declarar una Seña para un cliente de prueba con Parcial ya registrado devolvió `HTTP 500` con el motivo exacto y **no creó ninguna fila** en `FinancialSale`; el mismo cliente con Cuota sí se guardó correctamente. En el navegador: selector deshabilitando las 5 opciones no válidas, panel de saldo mostrando "$200.00 de $500.00 — saldo restante $300.00", salto automático a "Cuotas", y autocompletado confirmado (`Precio Total=500`, `Cobrado Hoy=300`).
    - **⚠️ Nota de precaución para el usuario**: durante esta verificación, el intento de prueba con un tipo de pago válido (Cuota) sí pasó por el endpoint real `/api/sheets/push`, que reenvía incondicionalmente a Google Sheets (no hay forma de desactivar solo esa parte — el flag `enviar_webhook` únicamente frena el webhook de n8n, no el POST a Sheets). Quedó una fila de prueba en la pestaña `Ventas_DB` (`"Prueba Secuencia Validacion" / "AL - Cuota" / $50`) — mismo tipo de aviso que ya quedó documentado el 4 de agosto para este mismo endpoint. Los registros locales de prueba fueron eliminados tras la verificación.
  - **Fase 3 (persistir "oferta presentada" + métricas de conversión de señas)**: se agregó `Appointment.offer_presented` (migración de la Fase 0) y se cablea igual que el ya existente `with_decision_maker` — capturado en `sessionForm` al responder "¿Se hizo la presentación de la oferta?" en el árbol de decisión, y enviado a `POST /closer/deck/<id>` desde el paso "Guardar Seguimiento" y desde el flujo de "Declarar Venta" (que hasta ahora nunca tocaba estos dos campos en absoluto). Backend: `app/api/closer.py` (handler de `/closer/deck/<id>`) y `CloserService.process_agenda` (handler de `/closer/appointments/<id>/process`) ahora aceptan y guardan `offer_presented` con la misma lógica que ya usan para `with_decision_maker`.
    - **Hallazgo durante el desarrollo**: al trazar cómo `with_decision_maker` llega hoy al backend desde el árbol de decisión, se confirmó que la función `saveLlamadaReport` (que en teoría lo enviaría) **nunca se invoca en ningún lado** — es código muerto. El único punto real donde el árbol persiste algo es el paso "Guardar Seguimiento" (`modalStep === 'follow'`), que hasta esta fase tampoco incluía `with_decision_maker` ni `offer_presented` en su payload. Además, se detectó que los 3 flujos de "Lead perdido" tras la oferta (`actionType` `lost_after_pres`/`lost_no_pres`/`lost_no_show`) abren el modal de motivo pero `handleConfirmReason` no tiene ninguna rama para esos `actionType` — confirmar esos modales hoy no llama a ningún endpoint. **No se corrigió en esta pasada** (excede el alcance de "persistir oferta presentada"); queda como hallazgo para decisión del usuario.
    - `CloserService.compute_daily_report_fields` y `get_comprehensive_stats` ahora calculan `offers_made` real (antes fijo en 0) contando `Appointment.offer_presented is True` sobre las llamadas asistidas, igual patrón que ya usaban para `decision_makers`.
    - El balde de ventas de `compute_daily_report_fields` se separó de 4 a 6 tipos (se agregaron `renewal`/`upsell`, usando las columnas nuevas de `CloserDailyReport` de la Fase 0) reutilizando el parser robusto `SheetsService.parse_tipo_pago` de la Fase 2 en vez de la clasificación por substring que tenía antes.
    - **Admin** (`CloserPerformanceTab.jsx`): se agregaron 3 tarjetas nuevas en "Agendas y Asistencias" — Oferta → Seña, Seña → Venta Real y Close Rate Promesa — todas ya calculadas hace tiempo en el backend (`get_comprehensive_stats`) pero **nunca consumidas por ningún componente del frontend** hasta ahora (confirmado por grep: cero matches previos).
    - **Closer** (`CloserDashboard.jsx` / `PerformanceQuality.jsx`): nueva sub-sección "Conversión de Señas" con los mismos 3 anillos, alimentada por un bloque nuevo `conversion_senas` agregado a `CloserDashboardService._build_period_block` (el endpoint `/closer/performance-dashboard` no tenía ninguna métrica de señas hasta ahora).
    - **Verificado**: `compute_daily_report_fields`/`get_performance_data` corren sin excepciones tras los cambios (`env/Scripts/python.exe` directo contra la base local); `offer_presented`/`with_decision_maker` confirmados persistiendo correctamente vía `CloserService.process_agenda` con una cita de prueba (creada y eliminada en la misma verificación). En el navegador, simulando a Jean Carlo: el Dashboard renderiza la nueva sección "Conversión de Señas" (Oferta → Seña 0%, Seña → Venta Real 0%, Close con Señas 100%) sin errores, junto con el resto de tarjetas ya existentes. `npm run build` compila sin errores.
  - **Fase 4 (cola de limpieza diaria del closer)**: nuevo `app/services/client_cleanup_service.py::get_cleanup_queue(closer_id, limit=10)`, que reutiliza `SalesConsistencyService` (Fase 2) contra el historial real de cada cliente del closer para detectar 4 tipos de problema, priorizados por severidad: (1) secuencia de pagos inconsistente (Cuota sin Parcial, o Renovación/Upsell con saldo pendiente), (2) llamada con oferta presentada (`Show up` + `offer_presented=True`, de la Fase 3) sin venta ni `fecha_seguimiento_cobro` registrada, (3) cliente fusionado en los últimos 7 días (`ClientMergeLog` de la Fase 1), (4) descuadre entre `Enrollment/Payment` y la suma de `FinancialSale` del mismo cliente (mirror desincronizado). Nuevos endpoints `GET /closer/cleanup-queue` y `GET /closer/clients/<id>/full-history` (agendas + ventas + inscripciones/pagos completos de un cliente en una sola vista).
    - **Frontend**: nueva pestaña "🧹 Limpieza" en el dock inferior del mazo (mismo nivel que Bandeja/Reporte/Dashboard), `ClientCleanupPane.jsx` reutilizando el patrón visual de tarjeta de `SeguimientosPane.jsx`/`SeguimientoRow` (motivo + detalle en vez del árbol de decisión de llamada, que no aplica acá), con un modal de "Historial completo del cliente" al hacer clic en cualquiera.
    - **Verificado end-to-end contra la base de datos local** (simulando a Jean Carlo desde el navegador): la cola trajo 10 clientes reales con secuencias de pago inconsistentes (tiempo de cómputo ~4.3s para su cartera completa). Al abrir el historial de "Karla Contreras" (primer resultado), el modal mostró sus 5 ventas reales ordenadas por fecha — confirmando en vivo el patrón exacto que motivó todo este proyecto: una `Seña` el 28/5 seguida de 4 `Cuota` entre el 6/6 y el 19/7, **sin ningún `Parcial` de por medio** — evidencia real de que el hallazgo original del usuario no era un caso aislado. `npm run build` compila sin errores.
  - **Cierre del proyecto**: las 4 fases quedaron implementadas, verificadas contra datos reales y commiteadas por separado. Pendiente para una futura sesión (fuera de alcance de lo pedido esta vez, documentado como hallazgo): los flujos "Lead perdido" tras la oferta (`lost_after_pres`/`lost_no_pres`/`lost_no_show`) no persisten nada al confirmar el motivo (bug preexistente, no introducido en esta sesión).

- **5 de Agosto de 2026** *(continuación — "Total" del cliente pasa de ser por programa a un campo único por cliente)*:
  - **Base de Datos ([actualizar_db.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/scripts/actualizar_db.py) [EXECUTE])**: sincronización de solo lectura desde producción ejecutada al inicio de la sesión para partir de datos reales (4,682 `clients`, 830 `financial_sales`, 372 `enrollments`, entre otras tablas).
  - **Pedido del usuario**: el monto total que debe pagar un lead dejó de estar implícito en el precio del `Program` (global, por programa) y pasa a ser un campo `Total` propio de cada `Client` — se autoasigna según el programa del primer pago (AL $1000 / RR $1500 / SI $2000) pero queda editable en cada pago siguiente que se registre para ese cliente, no fijo para siempre.
  - **`Client.total_amount` ([client.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/client.py) [MODIFY], migración `5d084533d803` [MODIFY])**: nueva columna `Float` nullable en `clients`. Aplicada en local con `flask db upgrade`.
  - **`SalesConsistencyService.get_client_payment_state` ([sales_consistency_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/sales_consistency_service.py) [MODIFY])**: el `program_price` que alimenta saldo pendiente/tipos de pago permitidos ya no viene de `Program.price` (precio global desactualizado — la fila activa real de RR es $1500 pero AL/SI no coinciden con los nuevos defaults pedidos) sino de `Client.total_amount` si el cliente ya tiene uno guardado, o si no del nuevo diccionario `PROGRAM_DEFAULT_TOTALS = {'AL': 1000, 'RR': 1500, 'SI': 2000}`.
  - **`SheetsService.post_to_sheets` (tabla `Ventas_DB`) ([sheets_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/sheets_service.py) [MODIFY])**: al registrar cualquier pago, si el payload trae `precio_total` (o es un pago `Completo`, donde el total es el monto mismo) se guarda como el nuevo `Client.total_amount` — así queda modificable en cada pago, tal como se pidió. Si el cliente aún no tiene ninguno guardado y no llegó `precio_total`, se autoasigna el default del programa.
  - **Frontend ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**: `buildSalePayload` ahora incluye `precio_total` (antes solo se usaba localmente para armar el plan de cuotas, nunca viajaba al backend — el total nunca se habría podido persistir sin este cambio). El campo "Precio Total" del modal "Declarar Venta" ahora se autocompleta con el default/total del cliente para **cualquier** cliente al abrir el modal (antes solo se autocompletaba si ya tenía pagos previos registrados — un cliente nuevo veía el campo vacío en vez del default por programa).
  - **Bug propio detectado y corregido durante la verificación**: al mover el cálculo de `program_price` fuera del import original, `get_client_payment_state` se quedó sin el `from app.services.sheets_service import SheetsService` que el resto de la función seguía necesitando (`parse_tipo_pago`) — cualquier segundo pago sobre el mismo cliente fallaba con `NameError: name 'SheetsService' is not defined` (silenciado por el `except Exception` genérico de `post_to_sheets`, que lo reportaba como "Error local de base de datos"). Detectado por un test end-to-end propio (no reportado por el usuario) antes de dar el cambio por terminado.
  - **Verificado end-to-end contra la base de datos local** (cliente de prueba creado y eliminado en la misma verificación): consulta de estado de pago de un cliente sin historial devuelve el default correcto por programa (AL→$1000); registrar una Seña sin `precio_total` explícito autoasigna $1500 (RR) al cliente; un segundo pago (Parcial) con `precio_total=1800` actualiza el `Client.total_amount` a 1800; una consulta posterior de estado de pago ya refleja 1800 en vez del default. `npm run build` compila sin errores.

- **5 de Agosto de 2026**:
  - **El Reporte Diario Ahora Cuenta el Backlog Atrasado Procesado Hoy ([booking.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/booking.py) [MODIFY], migración `a91421981706`, [closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY])**:
    - **Pedido del usuario**: que el reporte diario refleje todo lo que el closer hace en el sistema, incluyendo ir actualizando trabajo atrasado en el tiempo.
    - **Contexto**: hallazgo pendiente documentado el 4 de agosto — `compute_daily_report_fields` solo contaba llamadas cuya fecha *original* (`start_time`) caía hoy; si el closer procesaba hoy un lote de agendas atrasadas de días/meses anteriores (ahora visibles gracias al fix de "② Llamadas"), ese trabajo no se reflejaba en el reporte de Discord del día. No había forma de saber "cuándo" se procesó una cita vieja porque `Appointment` no tenía ningún timestamp de modificación.
    - **Corrección**: se agregó `Appointment.updated_at` (con `onupdate=datetime.utcnow`, migración `a91421981706`) y se amplió `compute_daily_report_fields` para incluir, además de las citas agendadas hoy, cualquier cita de un día anterior que el closer haya efectivamente procesado hoy (`closer_processed=True` y `updated_at` dentro de la ventana de hoy) — se suma a los mismos contadores de llamadas (atendidas/no show/etc.) según su resultado real, sin necesidad de un campo nuevo en `CloserDailyReport`.
    - **Verificado end-to-end** contra la base de datos local: se procesó una cita de backlog real de Jean Carlo agendada originalmente el 10 de julio (marcada "No Show" hoy), se generó el reporte del día, y `first_call_no_show` la contó correctamente junto con las citas de hoy — confirmando que `updated_at` se actualiza automáticamente al guardar y que el reporte ya no ignora el trabajo de limpieza de backlog.

  - **Corrección de Datos: Zona Horaria de Sebastián y Marlon Closer**:
    - **Reportado por el usuario**: "Sebastian y marlon estan en venezuela" — sus cuentas tenían `timezone='America/La_Paz'` (Bolivia), no `America/Caracas` (Venezuela).
    - **Corrección**: se actualizó `User.timezone` a `America/Caracas` para ambos directamente en la base de datos. Ambas zonas comparten el mismo offset (UTC-4, sin horario de verano en ninguna de las dos), así que no cambia ningún cálculo de fecha/hora existente — es una corrección de exactitud de cara a futuro (por si alguna cambia de política de DST) y evita depender de que vuelvan a loguearse para que el auto-sync de zona horaria del login lo corrija solo.
  - **Bugfix: "① Confirmaciones" Volvía a Mostrar Vencidas, Duplicando con "② Llamadas" ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Reportado por el usuario**: "Las vencidas no deben aparecer en confirmaciones, esa parte esta funcionando bien [refiriéndose a que ahora aparecen correctamente en Llamadas]" — confirmando que había que revertir la ventana de 30 días agregada el 4 de agosto a "① Confirmaciones", ya que el fix de "② Llamadas" de esa misma fecha volvió redundante (y ahora duplicada) esa cobertura.
    - **Corrección**: `step=confirmations` (y su contador en `/deck/counts`) vuelve a filtrar solo `start_time >= inicio de hoy`, sin el límite inferior de 30 días. El backlog de vencidas vive exclusivamente en "② Llamadas" de ahora en adelante — evita que la misma cita aparezca en ambas pestañas.
    - **Verificado end-to-end** contra la base de datos local, simulando a Jean Carlo: "① Confirmaciones" pasó de 19 (con vencidas coladas) a 0 citas (ninguna vencida, todas migraron correctamente a "② Llamadas" que mantiene sus 16).
  - **Pulido Visual del Árbol de Decisión de Llamadas: Estado "Seleccionado" y Barra de Progreso ([index.css](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/index.css) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Contexto**: recorriendo el árbol de decisión completo del reporte de llamada (Asistió → Con decisor → Con presentación → Declarar Venta) para verificar en vivo el fix de "② Llamadas" del 4 de agosto (backlog visible, agrupado por mes — ese fix ya estaba commiteado; este pulido es aparte).
    - **Cambio**: se reemplazaron las clases Tailwind ad-hoc de los botones de opción (`option()`) por las clases reutilizables `.opt`/`.opt.sel[data-t=...]` (ya definidas para el estado seleccionado por tipo: ok/no/bad/info/pink), y se agregó una barra de progreso visual del pipeline de confirmación (`.pipe`/`.pstep.done`/`.pstep.cur`) mostrando en qué paso del árbol está el closer. Los encabezados de pregunta (`.q > h4`) ahora muestran una insignia "obligatorio" que cambia a "✓ listo" cuando el campo ya se completó, en vez de solo texto estático.
    - **Verificado**: `npm run build` compila sin errores.
    - **Hallazgos encontrados durante esta pasada de prueba en vivo, no corregidos todavía**: (1) el tipo de pago "Cuota" se puede declarar sin validar que exista un "Parcial" previo para ese cliente; (2) la búsqueda global del mazo (`/closer/leads/search`) devuelve leads de cualquier closer del sistema, pero al seleccionar uno que pertenece a otro closer la API responde 403 sin explicar el motivo, mostrando un error genérico; (3) el reporte diario automático (`compute_daily_report_fields`, del 4 de agosto) agrupa Renovación y Upsell dentro del balde "Parcial/Split" (inflándolo) porque `CloserDailyReport` nunca tuvo columnas dedicadas para esos dos tipos, a diferencia del dashboard de admin que sí los separa correctamente.
  - **Mismo Pulido Visual Extendido a la Banda de Confirmación del Modal de Lead ([index.css](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/index.css) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])** *(sesión en paralelo con la entrada anterior, mismo archivo — convergieron en la misma solución)*:
    - **Reportado por el usuario**: comparando contra el prototipo `closer-workspace-v7.html`, el modal de lead no se veía igual — sobre todo la banda que muestra en qué etapa de confirmación está el lead (Por confirmar/Conversando/Confirmado), y la tipografía de títulos/opciones dentro del modal.
    - **Diagnóstico**: el paso `confirm` del modal (banda de estado de confirmación) todavía usaba una versión hecha a mano con clases de Tailwind sueltas — nunca se había migrado a `.pipe`/`.pstep`/`.q`/`.opt`, a diferencia de otros pasos del árbol de decisión que sí las usaban pero (según diagnosticó la entrada anterior) tampoco tenían su CSS definido.
    - **Corrección**: se reescribió el paso `confirm` para usar `.pipe`/`.pstep.done`/`.pstep.cur` (banda de 3 etapas con el paso actual resaltado en degradé y check en los completados) y `.q.req`/`<span class="num">` en sus encabezados, quitando el texto "(Requerido)" hardcodeado ahora que `.q.req::after` lo agrega automáticamente como insignia. Limpieza adicional: se normalizaron a `className="num"` los 6 badges numéricos idénticos que quedaban en otros pasos del árbol (antes con una clase Tailwind larga y repetida).
    - **Verificado end-to-end en vivo contra datos reales**: sesión simulada de Jean Carlo con una cita de prueba en estado "Por confirmar" — se abrió el modal, se confirmó por estilos computados que la banda renderiza 3 pasos con el actual resaltado en degradé navy→rosa, que `.q.req` muestra la insignia "obligatorio" (y pasa a "✓ listo" al completar el campo), que el badge numerado mide 22×22px con el mismo degradé, y que el botón de opción "Sí, conversando" queda con `data-t="info"` y clase `.sel` al elegirlo (la regla CSS del color de selección por tipo se confirmó válida y coincidente contra el elemento, aunque no se pudo capturar el color ya renderizado porque el panel del navegador no estaba visible en pantalla durante la prueba — el motor no recompone frames en ese estado). Cita y cliente de prueba eliminados tras la verificación.
    - **Nota operativa de la sesión**: se detectó en vivo una línea duplicada (`}, [filteredAgendas, activeStep]);`) que rompía la compilación de `CloserWorkflowPage.jsx`, dejada por la edición concurrente de la otra sesión al insertar el sistema de "lote diario"; se resolvió sola segundos después (la otra sesión la corrigió) antes de que hiciera falta tocarla. Se agregó `"autoPort": true` a la config `frontend` de `.claude/launch.json` para que ambas sesiones puedan levantar su propio servidor de desarrollo en paralelo sin chocar por el puerto 5173.

- **4 de Agosto de 2026** *(continuación — Llamadas atascadas invisibles y su efecto en cascada sobre Seguimientos)*:
  - **Bugfix Crítico: "② Llamadas" Excluía Cualquier Cita Atascada en un Estado Intermedio de Confirmación, y eso Vaciaba el Pool de Seguimientos ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Reportado por el usuario**: pidió que "Llamadas" muestre todas las agendas pendientes de reportar tras la llamada (agrupadas por mes si son muchas), y preguntó por qué el pool de Seguimientos de Jean Carlo mostraba solo 15-27 agendas cuando debería haber muchas más.
    - **Diagnóstico (una sola causa raíz para ambos síntomas)**: `GET /api/closer/deck?step=calls` exigía `Appointment.result` ya en `'Confirmado'`/`'2TH Call'`/etc. — es decir, que el closer hubiera avanzado explícitamente la confirmación hasta ese estado terminal antes de que la cita calificara para "Llamadas". Cualquier cita atascada en un estado intermedio (`'Contactado'`, `'conversando'`, `'Pendiente'`, o directamente sin ningún contacto) quedaba invisible en Llamadas para siempre en cuanto pasaba su horario, sin importar que la llamada ya hubiera ocurrido (o no). Medido contra datos reales: de 21 citas sin procesar de Jean Carlo, solo 2 calificaban para Llamadas; las otras 19 (result `None`/`Contactado`/`Pendiente`/`conversando`) quedaban atrapadas sin ninguna vía para reportar qué pasó. Y como `CloserFollowUpService._base_query` (pool de Seguimientos) exige que `closer_result` ya sea un valor terminal (`No Show`/`Cancelado`/`Show up`) para poder derivar la categoría del seguimiento, esas mismas 19 citas —al nunca poder pasar por Llamadas para reportar su resultado real— tampoco podían calificar jamás para Seguimientos. Una sola causa, dos síntomas.
    - **Corrección**: se amplió el filtro de `step=calls` (y su contador en `/deck/counts`) para incluir toda cita cuyo horario ya llegó (o es de hoy), sin procesar (`closer_processed=False`), sin importar en qué estado intermedio haya quedado la confirmación — excluyendo únicamente `Cancelada`/`Reagendada`, que pertenecen a la pestaña "Reagendar". No se tocó `CloserFollowUpService` (su lógica de derivación ya era correcta, solo estaba famélica de citas con `closer_result` terminal).
    - **Verificado end-to-end** contra la base de datos local, simulando la sesión real de Jean Carlo: Llamadas pasó de 2 a 19 citas visibles (incluye las 3 originalmente reportadas como faltantes en el bugfix de Confirmaciones del 4 de agosto). Se reportaron 3 citas del backlog recién visible (2 No Show, 1 Show up) y el pool de Seguimientos reaccionó de inmediato: `no_tomada` 16→18, `tomada` 29→30 — confirmando que la cadena completa (Llamadas → reportar resultado → Seguimientos) ahora fluye correctamente sin tocar la lógica de Seguimientos.
    - **Frontend — agrupación por mes y "bandeja limpia"**: cuando "② Llamadas" tiene más de 10 citas pendientes, se agrupan por mes/año (mes más reciente primero) con encabezado y contador por grupo, en vez de una lista plana difícil de recorrer; con 10 o menos se mantiene la lista simple de siempre. Estado vacío actualizado a "👏 Ninguna llamada pendiente de reportar. ¡Bandeja limpia!" (antes decía genéricamente "No tienes agendas programadas", sin el tono de felicitación que sí tiene Confirmaciones). Verificado visualmente en el navegador contra los datos reales de Jean Carlo: "16 Citas en Lista" agrupadas correctamente en "Agosto De 2026" (6) y "Julio De 2026" (10).
    - **Zonas horarias**: re-verificado el cálculo de límites de día por closer (`_user_day_bounds_utc`) contra Jean Carlo (`America/Mexico_City`, UTC-6) y Sebastián/Marlon Closer (`America/La_Paz`, UTC-4) — los rangos UTC calculados para "hoy" de cada uno son correctos y consistentes con el resto del trabajo de husos horarios de esta sesión; no se encontraron regresiones.
    - **Pendiente / hallazgos para decisión del usuario**: (1) con este cambio, una cita vencida sin procesar puede aparecer simultáneamente en "① Confirmaciones" y "② Llamadas" (antes eran mutuamente excluyentes en la práctica) — no se resolvió la posible duplicación visual porque no fue pedido explícitamente y tocar el límite superior de Confirmaciones otra vez, recién corregido, agregaba riesgo; (2) el reporte diario (`compute_daily_report_fields`) solo cuenta llamadas cuya fecha *original* cae hoy — si un closer procesa hoy un lote de agendas atrasadas de días anteriores (como este mismo fix ahora permite), ese trabajo de limpieza no queda reflejado en el reporte de Discord de hoy. No hay forma confiable de saber "cuándo" se procesó una cita vieja sin agregar una columna `updated_at` a `Appointment` (o registrar un evento en `LeadEventLog` desde `process_agenda`, que hoy no lo hace) — cambio de mayor alcance, no incluido en esta pasada.

- **4 de Agosto de 2026** *(continuación — auditoría completa del flujo de venta del closer)*:
  - **Hallazgo crítico: el reporte diario a Discord estaba completamente desconectado ([closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY], [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Pedido del usuario**: auditoría completa del flujo del closer (agendas → confirmación → llamada → venta → seguimiento) pidiendo específicamente que el reporte diario de Discord refleje datos reales extraídos de completar las agendas en la app, no de re-tipeo manual, y que las señas no cuenten como venta en las métricas.
    - **Diagnóstico**: el botón "Enviar reporte al sistema" del mazo (`CloserWorkflowPage.jsx`) era puramente cosmético — `onClick={() => { setReportSent(true); toast.success(...) }}`, sin ninguna llamada al backend. Mientras tanto el sistema viejo de reporte (`CloserDailyReport`, con ~30 campos: llamadas, decision makers, ventas por tipo, seguimientos, referidos) seguía existiendo y siendo lo que alimenta tanto `_trigger_closer_report_discord` como la sección "Agendas"/"Seguimientos" del dashboard de admin (`CloserService.get_comprehensive_stats`) — pero nada en la interfaz real que usa el closer escribía ahí desde el rediseño v6/v7 del mazo. Resultado: no solo Discord dejó de recibir reportes, sino que **todo el bloque de Agendas/Llamadas/Seguimientos del dashboard de admin ha estado congelado/vacío** desde entonces, silenciosamente, sin que se notara (las tarjetas muestran "0%" en vez de un error visible).
    - **Segundo hallazgo, bug confirmado**: en `get_comprehensive_stats`, `total_sales` sumaba `final_deposit_count` (señas) junto con PIF/Split — contradice directamente que la seña es una promesa de compra, no una venta.
    - **Arquitectura de ventas aclarada**: el modal "Declarar Venta" del mazo sí escribe datos reales al instante (vía `POST /sheets/push?tabla=Ventas_DB` → `SheetsService.post_to_sheets` crea un registro local en `FinancialSale` inmediatamente, antes de replicar a Google Sheets) —o sea que las métricas de ventas del dashboard de admin (que leen de `FinancialSale`) sí reflejan la actividad real del closer. Lo que **no** se actualiza con "Declarar Venta" es `Enrollment`/`Payment` (el sistema de cuotas e inscripciones), que alimenta el dashboard personal del closer dentro de su propio mazo — quedó documentado como hallazgo separado para una próxima pasada.
    - **Corrección — `CloserService.compute_daily_report_fields(closer_id, day_local)` (nuevo)**: calcula todos los campos de `CloserDailyReport` a partir de datos reales — llamadas 1ª/2ª (scheduled/attended/no_show/canceled/rescheduled) desde `Appointment.closer_result`, decision makers desde `Appointment.with_decision_maker` (campo real, ya existente), ventas por tipo (PIF/Split/Seña/Cuota, con o sin cuenta "in call") desde `FinancialSale` del día — excluyendo explícitamente la seña de "ventas". **No fabrica** `slots` ni `offers_made`: no existe hoy ninguna señal persistida de "oferta presentada" (se pregunta en el árbol de decisión del mazo — "¿Se hizo la presentación de la oferta?" — pero la respuesta nunca se guarda en la base de datos, se descarta). Quedan en 0 explícitamente en vez de inventar un número.
    - **Corrección — `total_sales` ya no incluye señas**: se separó `total_sales` (ventas reales: PIF + Split + Upsell + Renovación) de `total_sales_with_deposits` (para el "close rate promesa", que sí incluye señas como compromiso de compra). Se agregó el porcentaje `offer_to_deposit` (ofertas → señas) que faltaba junto al ya existente `deposit_conversion_rate` (señas → venta convertida). El dashboard de admin (`CloserPerformanceTab.jsx`) ya calculaba estos mismos conceptos correctamente por su cuenta en el frontend (no dependía del campo contaminado), así que el impacto visible es menor ahí, pero corrige el contrato del backend para cualquier otro consumidor.
    - **Nuevo endpoint `POST /api/closer/deck/daily-report`** (autenticado, closer/admin): calcula el reporte del día del closer autenticado, recibe del cliente solo lo que el sistema no puede saber por sí solo (referidos pedidos/dados, reflexión diaria — ya se capturaban en el formulario pero tampoco se enviaban a ningún lado), guarda/actualiza `CloserDailyReport` y dispara `_trigger_closer_report_discord` (función existente, sin cambios, reutilizada tal cual).
    - **Frontend**: el botón "Enviar reporte al sistema" ahora llama a este endpoint con estado de carga y manejo de error, en vez de solo simular el envío.
    - **Verificado end-to-end contra la base de datos local** con el cliente de pruebas de Flask (login + suplantación reales, no mocks): `POST /api/closer/deck/daily-report` como Jean Carlo devolvió 200, creó el registro `CloserDailyReport` correspondiente al día calendario correcto en su zona horaria, con `first_call_scheduled=1` (coincide con su agenda real de prueba), referidos y reflexión guardados tal cual se enviaron, e intentó el envío a Discord (correctamente no-op: sin webhook configurado en este entorno local). La verificación del clic real en el botón desde el navegador quedó bloqueada por fricción de la herramienta de automatización en esta sesión (no relacionada al código); `npm run build` compila sin errores.
    - **Pendiente / hallazgos para decisión del usuario**: (1) `Enrollment`/`Payment` no se actualiza al declarar venta — el dashboard personal del closer queda desactualizado; (2) no hay forma de persistir "¿se presentó la oferta?" pese a que se pregunta en el flujo — `offers_made` y las tarjetas "Pitch Rate"/"Ofertas → X" del dashboard de admin seguirán en 0% hasta agregar esa persistencia; (3) `slots` (capacidad configurada) tampoco se deriva de ningún dato real hoy.
  - **Fix: Declarar Venta no actualizaba Enrollment/Payment (dashboard personal del closer desactualizado) ([sheets_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/sheets_service.py) [MODIFY])**:
    - **Contexto**: hallazgo documentado en la entrada anterior — el usuario confirmó reparar `Enrollment`/`Payment` en paralelo a `FinancialSale` (menor riesgo que migrar todas las métricas de admin, que ya funcionan bien sobre `FinancialSale`).
    - **Corrección — `SheetsService._sync_enrollment_payment(payload, sale)` (nuevo)**: al declarar una venta (`POST /sheets/push?tabla=Ventas_DB`), además de crear el `FinancialSale` (sin cambios en esa ruta), ahora también: resuelve el programa activo desde el código en `tipo_pago` (RR→Residency Roadmap, AL→Ace Learner, SI→Specialist Iniciative), resuelve/crea el `Client` reutilizando `BookingService.create_or_update_client` (mismo cruce inteligente por email/instagram/teléfono ya usado en el resto del sistema), resuelve el closer por `email_vendedor`, y crea el `Payment` correspondiente (mapeando completo→full, parcial→first_payment, Seña→down_payment, Cuota→installment, Renovación→renewal, Upsell→upsell) sobre el `Enrollment` (creado si no existía). No reemplaza `FinancialSale` como fuente de las métricas de admin — es un espejo adicional, con manejo de errores no-fatal (si algo no encaja, la venta ya guardada en `FinancialSale` no se pierde).
    - **Verificado**: probado directamente contra la base de datos local (sin pasar por el endpoint HTTP para no volver a escribir en Google Sheets — ver nota de precaución abajo) con las 3 combinaciones de programa/tipo de pago (`AL - parcial`, `SI - Seña`, `RR - Cuota`): los 3 `Enrollment` quedaron con el programa correcto (`Ace Learner v3`, `Specialist Iniciative v1`, `Residency Roadmap v3`) y el `Payment` con el tipo mapeado correctamente (`first_payment`, `down_payment`, `installment`) y el monto exacto. Un primer intento de prueba sí pasó por el endpoint real (`RR - completo`) y confirmó el flujo completo end-to-end, incluyendo la creación correcta del `Enrollment`/`Payment`; los registros de prueba (locales) fueron eliminados tras la verificación.
    - **⚠️ Nota de precaución para el usuario**: la primera prueba de este fix pasó por el endpoint real `/api/sheets/push`, que además de guardar localmente reenvía la fila a Google Sheets (pestaña "Ventas_DB") incondicionalmente. Quedó una fila de prueba ahí, claramente marcada (`Cliente Prueba RR Completo` / `prueba.rr.completo@example.com`) — conviene revisarla y borrarla si te preocupa que quede en el Sheet real.

- **4 de Agosto de 2026**:
  - **Bugfix Crítico: Citas Nunca Confirmadas Desaparecían para Siempre de "① Confirmaciones" al Pasar su Fecha ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Pedido del usuario**: auditar la sesión del Closer porque "a veces no aparecen agendas que deben aparecer", con sospecha de que el problema era de zonas horarias.
    - **Reproducido en vivo**: usando la simulación de sesión del operador (Kerwin) sobre el closer Jean Carlo (único closer activo en un huso horario distinto, `America/Mexico_City`), se confirmó contra datos reales que 3 citas suyas (`Erick Ramirez`, `Gabriela Belen Laso`, `Uriel Yalez`, todas de los días previos, nunca confirmadas: `result` en `'Pendiente'`/`None`) no aparecían en ninguna pestaña del mazo.
    - **Diagnóstico real**: `GET /api/closer/deck?step=confirmations` (y su contador en `/deck/counts`) filtraba con `Appointment.start_time >= start_utc` usando el inicio del día seleccionado (por defecto "hoy", en la zona horaria del closer) **como límite inferior únicamente**. Una cita que nunca llegó a confirmarse (`result` sigue en `'Pendiente'`/`None`) deja de cumplir esa condición apenas pasa su fecha calendario — y tampoco califica para "② Llamadas" (que exige `result` ya en `Confirmado`/`2TH Call`/etc.), quedando invisible en el mazo del closer para siempre, sin ningún indicio de que existía. Mismo patrón de bug ya corregido el 1 de agosto para "Llamadas" (que sí incluía vencidas), pero nunca aplicado a "Confirmaciones". El origen no es estrictamente de huso horario (el cálculo de "hoy" por closer ya era correcto desde el fix del 1 de agosto) sino del límite de fecha en sí: cualquier confirmación pendiente, en cualquier zona horaria, se pierde en cuanto cruza la medianoche local sin ser atendida.
    - **Alcance real del backlog** (medido contra datos reales antes de corregir): al quitar el límite de fecha por completo (primer intento), Jean Carlo pasó de ver 1 confirmación a 214 (la más vieja del 16 de diciembre de 2025), y **Marlon Closer a 1,630** (la más vieja de septiembre de 2024) — evidencia de que el bug lleva meses activo y de que un límite ilimitado no es viable: inundaría la cola en vivo del closer con leads muertos de hace más de un año, enterrando lo realmente urgente (la cita de hoy quedaba en el puesto 214).
    - **Corrección**: se acotó el límite inferior a una ventana móvil de 30 días atrás (respecto a la fecha seleccionada) en vez de quitarlo por completo, sin límite superior (se sigue viendo "hoy" y el futuro, comportamiento previo intacto). Aplicado tanto en `GET /deck?step=confirmations` como en `GET /deck/counts` para que el contador de la pestaña coincida con la lista. Con la ventana de 30 días: Jean Carlo pasó a ver 18 confirmaciones (incluye las 3 originalmente reportadas como faltantes), Sebastián 10, Marlon Closer 7 — números manejables que ya no esconden nada reciente.
    - **Verificado end-to-end** contra la base de datos local, simulando la sesión real de Jean Carlo desde el navegador: las 3 citas originalmente invisibles (`6022`, `6028`, `6012`) ahora aparecen en "① Confirmaciones"; "② Llamadas" y "③ Seguimientos" no se vieron afectados (se verificó que sus conteos siguen coincidiendo con la base de datos). Un conteo transitorio incorrecto observado durante la prueba (`/deck/counts` devolviendo 0 llamadas mientras `/deck?step=calls` sí devolvía 2) resultó ser un artefacto del auto-reload del servidor Flask en modo debug entre ediciones sucesivas del archivo, no un bug de código: se resolvió solo tras el último reload limpio y se confirmó estable con múltiples peticiones frescas.
    - **Pendiente / hallazgo para decisión del usuario**: el backlog histórico completo (214 en Jean Carlo, 1,630 en Marlon Closer, la mayoría de hace meses/años) sigue existiendo en la base de datos y ahora es visible solo si el closer navega manualmente más de 30 días atrás con el selector de fecha. Queda pendiente decidir si se requiere una herramienta de limpieza/archivado masivo (ej. marcar automáticamente como "Lead Perdido" tras N días sin confirmar) — no se tomó esa decisión unilateralmente por ser un cambio de negocio, no un bugfix.
  - **Seguridad: Dos Endpoints del Closer sin Control de Acceso Correcto ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Contexto**: auditoría de seguridad solicitada por el usuario sobre la sesión del Closer, en paralelo al bugfix de Confirmaciones de esta misma fecha.
    - **Hallazgo 1 (crítico)**: `POST /api/closer/reset-appointments` estaba permitido para el rol `closer` (no solo `admin`), y pese a que el comentario del código decía "lo aplicaremos a las del usuario autenticado para evitar desastres globales", la implementación real ejecutaba `Appointment.query.all()` sin ningún filtro por `closer_id` — es decir, **cualquier closer autenticado podía resetear `result`/`last_stage` de absolutamente todas las citas del sistema** (de todos los closers) a `Nueva`/`Terminada` con una sola petición. No está conectado a ningún botón del frontend (se buscó exhaustivamente); es un endpoint de pruebas olvidado, activo y explotable en producción vía API directa.
    - **Hallazgo 2**: `PATCH /api/closer/events/<id>` (configura duración/buffer de los eventos de agendamiento, compartidos por todo el equipo) no tenía ningún chequeo de rol más allá de `@login_required` — cualquier usuario autenticado, sin importar su rol (setter, triage, call_confirmer, etc.), podía modificarlo.
    - **Corrección**: `reset-appointments` restringido a `admin` únicamente; `PATCH /events/<id>` restringido a `closer`/`admin` (único consumidor real confirmado: `SettingsPage.jsx` del closer, para configurar sus propios tiempos de llamada).
    - **Verificado** con el cliente de pruebas de Flask, simulando sesiones reales vía login + impersonación (no mocks): un closer (`Jean Carlo`) recibe `403` en `reset-appointments` y `200` en `PATCH /events/1` (permitido); un setter (`Elias`) recibe `403` en ambos. Protección CSRF de la app confirmada activa de paso (`400 "CSRF token is missing"` sin el token correspondiente).
  - **Herramienta de Limpieza Automática del Backlog de Confirmaciones ([closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY], [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [admin.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/admin.py) [MODIFY], [DatabaseTools.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/admin/dashboard/components/DatabaseTools.jsx) [MODIFY])**:
    - **Pedido del usuario**: resolver el hallazgo pendiente del bugfix de Confirmaciones (arriba) — el backlog histórico (214 en Jean Carlo, 1,630 en Marlon Closer) sigue en la base de datos, solo visible navegando manualmente más de 30 días atrás.
    - **`CloserService.archive_stale_backlog(days=30, dry_run=False)` (nuevo)**: archiva citas nunca confirmadas (`closer_result` en `Pendiente`/`None`/vacío, `closer_processed=False`) cuya fecha ya pasó hace más de `days` días, marcándolas como `closer_result='Lead Perdido'` (mismo estado terminal que usa un closer al descartar manualmente un lead vía `CloserService.process_agenda`, no un estado inventado), `closer_processed=True`, `seguimiento_realizado=True`, y una nota en `closer_notes` indicando el archivado automático. No borra ningún registro, solo lo saca de las colas activas del closer — reversible por un admin editando el registro si hiciera falta.
    - **Automático, sin agregar infraestructura nueva**: el proyecto no tiene scheduler/cron (`requirements.txt` no incluye APScheduler/Celery). En vez de agregar esa complejidad, se dispara oportunistamente desde `GET /api/closer/deck/counts` (el endpoint más frecuentado del mazo del closer, llamado en casi cualquier interacción), con un guard en memoria de proceso que limita la ejecución real a una vez cada 6 horas (`_maybe_auto_archive_backlog` en `closer.py`) — en el caso normal (nada que archivar) es una consulta barata.
    - **Herramienta manual** (`GET`/`POST /api/admin/tools/backlog-cleanup`, admin/operador): `GET` hace *dry run* (previsualiza cuántas se archivarían sin modificar nada), `POST` ejecuta de verdad. Tarjeta nueva "Backlog de Confirmaciones sin Atender" en Operaciones Críticas → DB Tools, con umbral de días configurable, botón "Revisar Cuántas Hay" (preview) y "Archivar Backlog" (con `window.confirm`, deshabilitado hasta hacer preview).
    - **Verificado end-to-end contra la base de datos local (ejecución real, no simulada)**: preview inicial devolvió 2,868 citas elegibles en todo el sistema; se ejecutó el archivado real vía el endpoint POST — las 2,868 quedaron con `closer_result='Lead Perdido'`, `closer_processed=True` y la nota del sistema. Se confirmó que las 3 citas del bugfix original (`6022`, `6028`, `6012`, dentro de la ventana de 30 días) NO se tocaron. Conteos posteriores por closer: Jean Carlo 18 pendientes / 196 archivadas, Sebastián 10 / 3, Marlon Closer 7 / 1,623. Botón "Revisar Cuántas Hay" probado en el navegador tras la limpieza real, confirmando "No hay citas pendientes de archivar con ese umbral". `npm run build` del frontend compila sin errores.
  - **Bugfix Visual: Texto Casi Invisible en los Temas Claros "Elegant Blue" y "Clean Mac" ([index.css](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/index.css) [MODIFY])**:
    - **Reportado por el usuario**: "los temas... están funcionando muy mal", tras notar (tratando de cambiar el tema desde el selector rápido del Dock) que el selector parecía no reaccionar.
    - **Primer hallazgo (interacción)**: el selector de temas del Dock (`Dock.jsx`) usa un dropdown que solo se activa con `group-hover` (`pointer-events-none` hasta que el mouse está encima del contenedor). Un clic simulado directo sin pasar primero por un hover explícito no activa el dropdown y el clic no llega a ningún botón — reproducido con la herramienta de automatización del navegador. No se tocó este mecanismo (funciona con mouse real en desktop; queda documentado como posible punto débil en touch/mobile, ver "Pendiente" abajo).
    - **Hallazgo real (el que causaba "se ve muy mal")**: una vez que el tema sí cambiaba, el contenido se volvía prácticamente invisible. Diagnóstico por inspección de estilos computados (no visual): `body { color: var(--text-main) }`, y en las 4 combinaciones de temas claros (`Elegant Blue`/`Clean Mac` × `Glass`/`Solid`) `--text-main` se resuelve a través de `var(--brand-navy)` en los bloques de estilo `[data-theme-style="glass"]` y `[data-theme-style="solid"]` — bloques diseñados asumiendo que `--brand-navy` es siempre un tono oscuro (como lo es correctamente en `:root` `#0a0e3d` y en `.theme-custom` `#020617`, únicos otros dos lugares donde se define). Pero `.theme-elegant` y `.theme-clean` redefinían `--brand-navy` a `#f8fafc`/`#ffffff` (casi blanco) — comentario original `/* Light BG Base */` revela que se confundió el propósito de la variable con el color de fondo. Resultado medido: texto del body casi blanco sobre fondo claro en las 4 combinaciones (`rgb(248,250,252)` sobre gradiente celeste, o `#ffffff` sobre `#f5f5f7`), y en modo Solid además las tarjetas (`--bg-card`, también derivado de `--brand-navy`) se volvían casi blancas con texto blanco encima (`--text-on-card: #ffffff` fijo) — doble invisibilidad.
    - **Corrección**: se eliminaron las redefiniciones de `--brand-navy` en `.theme-elegant` y `.theme-clean` (2 líneas), dejando que hereden el valor oscuro correcto ya definido en `:root`. No se tocó ningún bloque consumidor (`--text-main`, `--bg-card`, `--bg-sidebar`, `--btn-text`) — todos vuelven a recibir el valor oscuro que siempre esperaron.
    - **Verificado**: inspección de estilos computados en las 4 combinaciones tras el fix — texto `rgb(10,14,61)` (navy oscuro) sobre fondos claros en las 4, y en modo Solid tarjetas navy oscuro con texto blanco encima (contraste correcto, coincide con el diseño "Dark Surfaces on Light BG" del comentario original del CSS). Confirmado visualmente con captura de pantalla real en el navegador (Elegant Blue/Glass). `npm run build` compila sin errores.
    - **Pendiente / no cubierto en esta pasada**: el selector rápido de temas del Dock (`group-hover`) no tiene fallback para dispositivos táctiles (sin estado `:hover`, el dropdown nunca se vuelve clickeable) — el selector completo en Ajustes (`ThemeSelector.jsx`, sin hover-gating) sí funciona en cualquier dispositivo. No se modificó el mecanismo del Dock por no tener evidencia de que sea la causa reportada (el usuario probaba desde escritorio) y por ser un cambio de mayor alcance (rediseño de esa UI); queda señalado por si el uso real es mayormente desde celular.
  - **Bugfix Visual: Campos del Login Ilegibles en Modo Claro ([LoginPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/auth/LoginPage.jsx) [MODIFY])**:
    - **Reportado por el usuario**: "aun hay errores en el modo claro, en el login es muy molesto cuando abre en modo claro" — tras el bugfix anterior del texto del body, quedaba un problema distinto y específico del login.
    - **Diagnóstico**: los inputs de usuario/contraseña usaban `bg-main` (fondo = `var(--bg-app)`, el fondo claro de toda la página) y `text-base` (color = `var(--text-main)`), pero están anidados dentro de la tarjeta `bg-surface` del login. `.bg-surface` reasigna localmente `--text-main: var(--text-on-card)` para que el contenido que vive DENTRO de la tarjeta (de fondo oscuro en Glass/Solid) se lea en blanco — pero esa reasignación de la variable CSS se hereda a TODOS los descendientes, incluidos los inputs que usan `bg-main` (fondo claro, no el fondo oscuro de la tarjeta). Resultado: un input con fondo claro (el gradiente de la página filtrándose) y texto blanco encima — ilegible. Mismo problema en las etiquetas "Usuario"/"Contraseña" (`text-muted`, gris oscuro fijo, sin contexto de tarjeta) sobre el fondo oscuro de la tarjeta — también de bajo contraste. Se confirmó el mismo patrón (`bg-main`/`text-base` dentro de una tarjeta `bg-surface`) en al menos un modal más (`TeamManagementPage.jsx`, "Nuevo Miembro") — es un problema estructural más amplio, no exclusivo del login.
    - **Corrección (acotada al login, según lo pedido)**: los inputs pasan a usar `.input-theme` (clase ya definida en `index.css`, con `--bg-input`/`--text-input` — variables dedicadas que si están correctamente pensadas para "dentro de una tarjeta" en las 4 combinaciones de tema/variante, y que nadie usaba en el código pese a existir). Las etiquetas e íconos pasan de `text-muted` a `var(--text-on-card)` con opacidad reducida (60%), en vez de la variable global `--text-muted` que no tiene contexto de tarjeta.
    - **Verificado**: inspección de estilos computados en Glass y Solid tras el fix — input con fondo `rgba(255,255,255,0.1)` (overlay claro sobre la tarjeta oscura) y texto blanco, tarjeta de fondo `rgb(10,14,61)` (navy), contraste correcto en ambas variantes. `npm run build` compila sin errores.
    - **Pendiente / no cubierto en esta pasada**: el mismo patrón (`bg-main`/`text-base` anidado en `bg-surface`/`glass-panel`) probablemente se repite en otros formularios del sistema (se confirmó al menos en el modal "Nuevo Miembro" de Gestión de Equipo) — no se corrigió de forma global por ser un cambio de mayor alcance (tocaría muchos archivos sin poder verificar visualmente cada uno) y por no haber sido reportado explícitamente fuera del login.
  - **Fix: Los Closers No Tenían Forma de Cerrar Sesión desde su Espacio de Trabajo ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Reportado por el usuario**: "veo que los closers no tienen como cerrar sesión desde los cambios que hicimos en esa vista".
    - **Diagnóstico**: el rediseño v6/v7 del mazo del closer (`CloserWorkflowPage.jsx`) reemplazó por completo el layout compartido (`MainLayout`/`Dock.jsx`, que sí incluye "Log Out") por un header y un dock inferior propios y autocontenidos (`top-v6`/`dock-v6`), sin ningún botón de cierre de sesión — la ruta `/closer/deck` renderiza `CloserWorkflowPage` de forma standalone, sin ese layout compartido. Mismo problema confirmado en `SetterWorkflowPage.jsx` (fuera de alcance de este pedido, que fue específicamente sobre closers).
    - **Corrección**: se agregó un botón de cerrar sesión (ícono `LogOut`, con confirmación `window.confirm`) junto al nombre/avatar del closer en el header, usando `logout()` del mismo `AuthContext` que ya usa el resto de la app — sin depender de ningún dropdown por hover (para evitar el mismo problema de interacción ya documentado en el selector de temas del Dock).
    - **Verificado end-to-end** simulando la sesión real de Jean Carlo desde el navegador: el botón dispara el diálogo de confirmación nativo y, al confirmar, cierra sesión y redirige correctamente a `/login`.
    - **Pendiente / hallazgo para decisión del usuario**: `SetterWorkflowPage.jsx` (mismo rediseño, mismo patrón standalone) tiene el mismo problema — los setters tampoco pueden cerrar sesión desde su vista. No se corrigió por no haber sido pedido explícitamente.

- **1 de Agosto de 2026**:
  - **Bugfix: HTTP 400 por Restricción de 4 Días al Agendar Citas Futuras ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Reportado por el usuario**: error HTTP 400 en producción al intentar crear agendas a más de 4 días en el futuro.
    - **Diagnóstico**: el endpoint `POST /api/closer/appointments` mantenía un límite preexistente que bloqueaba a usuarios no administradores si `start_time.date() > date.today() + 4 días`, lanzando `{"error": "Solo puedes agendar para los próximos 4 días"}` con código HTTP 400.
    - **Corrección**: Se removió dicha restricción en la ruta de creación de citas del closer/setter para permitir agendar libremente en cualquier fecha futura, y se fortaleció el parsing de fechas ISO tolerando múltiples formatos y separadores de tiempo.
    - **Verificado end-to-end**: prueba de agendamiento a 7 días en el futuro respondiendo HTTP 201 con ID de cita y evento de Google Calendar generados exitosamente.
  - **Bugfix: Endpoint Inexistente para Crear Referidos Manuales ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Reportado por el usuario**: imposibilidad de crear clientes referidos desde el modal de la UI del closer.
    - **Diagnóstico**: `CloserWorkflowPage.jsx` enviaba `POST /api/closer/deck/referrals/manual` al intentar crear un referido colgado de un prospecto existente, pero dicha ruta no existía en el backend Flask, resultando en error HTTP 404 / 405.
    - **Corrección**: Se creó el endpoint `@bp.route('/deck/referrals/manual', methods=['POST'])` en `app/api/closer.py` que:
      1. Resuelve el cliente origen (`from_lead_id`).
      2. Clasifica el contacto del referido (Instagram con `@` o número telefónico).
      3. Busca o crea el cliente referido de forma limpia usando `find_or_create_client`.
      4. Genera la nueva cita del referido asociada al closer con origen `Referido de <Cliente Origen>`.
      5. Registra una nota trazable en la ficha del cliente que otorgó la referencia.
    - **Verificado end-to-end**: prueba automatizada confirmando la creación limpia del referido, cita y notas con respuesta HTTP 201.
  - **Bugfix: Pestaña "Llamadas" Excluía Segundas Agendas/Llamadas y Fallback de Programación de Seguimiento ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Reportado por el usuario (Nota de voz)**:
      1. Al agendar una 2ª llamada para hoy y pasarla por el proceso de confirmación, la cita no aparecía en la pestaña "② Llamadas".
      2. Solicitar que siempre se pida / programe un seguimiento cada vez que sea posible para mantener activa la rotación de prospectos.
    - **Diagnóstico**:
      1. En `app/api/closer.py`, el filtro del endpoint `GET /api/closer/deck?step=calls` y `GET /api/closer/deck/counts` requería estrictamente `result == 'Confirmado'` y `closer_result` en `['Pendiente', None, '']`. Las segundas llamadas se crean con `result='2TH Call'` y `closer_result='2da call'`, lo que las descartaba de la pestaña "Llamadas" pese a estar confirmadas para hoy. Además, `end_utc` usaba la medianoche estricta UTC, excluyendo citas nocturnas locales (UTC-4/5/6).
      2. En el frontend, al abrir o reportar la interacción con un lead, la fecha de seguimiento no venía pre-llenada por defecto, dejando la oportunidad de guardarlo sin fecha asignada (`null`).
    - **Corrección**:
      - `closer.py`: Se amplió el filtro de `step=calls` y `deck/counts` para incluir estados de 2ª llamada (`2TH Call`, `2da call`, `2da Call`, `Segunda llamada`) tanto en `result` como en `closer_result`. Se agregó un margen de +12h en `end_utc` para incluir correctamente las citas de la noche local.
      - `CloserWorkflowPage.jsx`: Se pre-llena por defecto la fecha de seguimiento (`fecha_seguimiento`) para el día de mañana (`Date.now() + 86400000`) al inicializar cualquier reporte de lead y se garantiza su fallback automático al guardar, asegurando que todo prospecto procesado quede con un seguimiento futuro programado.
    - **Verificado end-to-end**: consulta de backend confirmando la devolución correcta de llamadas y segundas agendas en `step=calls` y en el conteo de la pestaña.
  - **Bugfix: HTTP 400 al Crear Agenda para un Cliente/Lead Existente en la Base de Datos ([booking_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/booking_service.py) [MODIFY], [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Reportado por el usuario**: error 400 al intentar crear una agenda (`POST /api/closer/appointments`) para un usuario que ya existe en la base de datos.
    - **Diagnóstico**:
      1. `BookingService.create_or_update_client` solo buscaba por coincidencia exacta de `email`. Si el cliente existía con variaciones de espacios/mayúsculas o sin email pero con el mismo Instagram/teléfono, no lo encontraba e intentaba insertar un registro duplicado, fallando la restricción `unique=True` de `Client.email` con error de base de datos.
      2. Si el agendamiento fallaba o devolvía `None` por conflicto de horario, el endpoint en `closer.py` intentaba acceder a `appt.id`, lanzando `AttributeError` capturado como HTTP 400 genérico.
      3. Si `lead_id` correspondía a la tabla `Lead` (ManyChat) en vez de `Client`, la resolución fallaba con `AttributeError`.
    - **Corrección**:
      - `BookingService.create_or_update_client` ahora normaliza los datos y ejecuta búsqueda inteligente cruzada por email (limpio, minúsculas), Instagram (normalizado sin `@`) y teléfono (últimos 8 dígitos), reutilizando el cliente existente sin duplicarlo.
      - `BookingService.create_appointment` realiza fallback a la tabla `Lead` en caso de recibir un `lead_id` de ManyChat/Marketing.
      - `app/api/closer.py` valida explícitamente `if not appt:` retornando un error HTTP 400 claro ("Ya existe una cita agendada en este horario para este closer...") y añade la sincronización automática con `FinancialAgenda`.
    - **Verificado end-to-end**: ejecuciones de prueba con datos reales verificando la resolución de clientes existentes por Instagram (ej. cliente `ID=3329`) y correo normalizado, sin generar errores 400 ni duplicados en base de datos.
  - **Restyling del Modal de Declarar Venta y Plan de Cuotas Real (Prototipo `closer-workspace-v7.html`)**:
    - **Reportado por el usuario**: al probar el sistema de seguimientos, el modal de declarar venta seguía con el tema visual viejo (índigo/esmeralda) en vez del violeta/rosa del resto del workspace v6/v7, y faltaba la sección para configurar los próximos pagos (plan de cuotas) de una venta parcial.
    - **Restyling ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**: se reemplazaron los acentos `indigo-*` por `violet-*` en todo el modal de declarar venta (líneas ~3042–3439), sin tocar el resto del archivo donde `indigo` se usa en otro flujo no reportado como problema.
    - **Nuevo Modelo y Migración ([installment.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/installment.py) [NEW], migración `649861242f3c`)**:
      - `InstallmentPlan`: cuotas individuales (número, monto, fecha de vencimiento, estado pagado/pendiente, fecha de pago) ligadas a la `Appointment` que originó la venta — no a `Enrollment` (sistema legado sin datos recientes) ni a `FinancialSale` (registro plano sin relación estructurada a un cronograma).
    - **Nuevo Servicio y Endpoints ([installment_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/installment_service.py) [NEW], [closer_installments.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer_installments.py) [NEW])**:
      - `POST /api/closer/installments`: genera el cronograma (saldo dividido en N cuotas mensuales, igual al `plan()`/`plan2()` del prototipo), reemplazando cualquier plan previo de la misma cita.
      - `GET /api/closer/installments/<appointment_id>`: lista las cuotas de un lead, marcando dinámicamente como "vencido" cualquier pendiente cuya fecha ya pasó.
      - `PATCH /api/closer/installments/cuota/<id>`: edita monto/fecha o marca una cuota como pagada.
    - **Frontend ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
      - Paso 2 del modal de venta: cuando el tipo de pago no es "Completo", se agregó el campo "Precio Total" (antes solo existía "Monto Cobrado", sin forma de saber el precio total del programa) y una sección "Plan de Cuotas" con cantidad de cuotas configurable y una tabla en vivo (monto/fecha/estado) calculada igual que el prototipo. Se guarda automáticamente al declarar la venta.
      - En el seguimiento de cobro de un cliente ya cerrado (`segventa`), se agregó la tabla real de cuotas con botón "Marcar pagada" por fila, reemplazando la ausencia total de esta información.
    - **Verificado end-to-end** contra datos reales: creación de un plan de 3 cuotas sobre saldo de $2000 (montos $666.67/$666.67/$666.66, fechas mensuales correctas), marcar una cuota como pagada, y relectura del plan reflejando el cambio.
  - **Frontend: Sistema de Categorización de Seguimientos (continuación) — Pestaña "③ Seguimientos" Reagrupada**:
    - **Nuevo Componente ([SeguimientosPane.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/SeguimientosPane.jsx) [NEW])**:
      - Reemplaza la lista genérica compartida con "Llamadas" cuando `activeStep==='seguimientos'`, replicando la estructura del prototipo v7: barra de meta diaria (seguimientos hechos vs. meta de 50), sección "Asignados para hoy" agrupada en las 3 categorías (📵 No tomada / 🎤 Tomada / 💰 Cerrada, cada una con sus leads y subestado), y sección "Pool sin fecha asignada" con 3 botones de categoría y un explorador con filtros reales (días desde la llamada; para "Cerrada" además programa y estado de deuda).
      - Cada fila muestra chips con datos reales: subestado, fuente, días desde la llamada original, y para "Cerrada" el programa y la deuda pendiente calculada.
    - **Wiring en ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
      - Se corrigió `saveSeguimientoReport`: antes, al elegir "Programar Seguimiento N+1" (continuar la cadencia), se enviaba `seguimiento_realizado: true` junto con la nueva fecha — marcando el seguimiento como resuelto y sacándolo de circulación en el mismo movimiento en que se suponía debía seguir vivo para el próximo intento. Ahora se distingue explícitamente "continuar cadencia" (no se marca realizado, se incrementa `seguimiento_intento`) de "cerrar/completar" (si se marca realizado).
      - Se conectaron los 3 puntos donde se crea un seguimiento por primera vez (No Show, Cancelación, Sin presentación/Sin cierre) para persistir `seguimiento_tipo`/`seguimiento_sub`/`seguimiento_intento=1`, y el flujo de seguimiento de cobro post-venta para categorizarlo como `cerrada`.
      - **Bugfix: `segventa` (seguimiento de cliente ya cerrado) mostraba datos financieros inventados** — el panel tenía hardcodeado `Ya pagó: $1,850 USD` y `Debe: $0 USD` de forma fija para cualquier cliente, heredado literalmente del mock del prototipo y nunca reemplazado por datos reales. Se reemplazó por la deuda real y el programa real ya calculados en el backend (`Enrollment`/`Payment`/`FinancialSale`), pasados desde `SeguimientosPane` al abrir el lead.
    - **Verificado end-to-end** contra datos reales: reportar No Show → programar seguimiento (aparece en "Asignados para hoy", intento 1) → continuar cadencia (intento 2, sigue sin marcar realizado, nueva fecha) → confirmando que ya no aparece en el pool (por tener fecha asignada). Todos los archivos modificados compilan sin errores.
    - **Alcance no cubierto** (fuera de esta pasada, por tamaño/ambigüedad): el filtro "Etapa (formulario)" del explorador del prototipo (requiere heurísticas de texto libre sobre respuestas de encuesta, no bien definidas) y el botón de eliminar seguimiento por fila (acción destructiva no solicitada explícitamente).
  - **Backend: Sistema de Categorización de Seguimientos (Prototipo `closer-workspace-v7.html`, pestaña "③ Seguimientos")**:
    - **Hallazgo**: el usuario pidió revisar la pestaña Seguimientos porque no reflejaba las 3 categorías del prototipo (Llamadas no tomadas / tomadas / cerradas). Se confirmó que el modelo `Appointment` no tenía ningún campo para distinguir esa categoría — `fecha_seguimiento` y `seguimiento_realizado` existían, pero nada rastreaba el "tipo" (no_tomada/tomada/cerrada), el "sub" (motivo/subestado) ni el número de intento (1 a 4), por lo que la información ya se perdía en el momento de programar el seguimiento.
    - **Modelo y Migración ([booking.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/booking.py) [MODIFY], migración `083021249e46`)**:
      - Se agregaron `seguimiento_tipo` (String), `seguimiento_sub` (String) y `seguimiento_intento` (Integer, default 1) a `Appointment`. Migración generada con `flask db migrate` y aplicada con `flask db upgrade`.
    - **Nuevo Servicio ([closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [NEW])**:
      - `CADENCIA=[0,3,7,14]` y `META_DIARIA=50`, idénticos al prototipo (la "deuda de burpees" del prototipo resultó ser simplemente `max(0, meta - hechos)`, un valor derivado de datos reales — no un contador aparte que hubiera que inventar).
      - `get_today_grouped`: seguimientos asignados hasta la fecha seleccionada (inclusive vencidos), agrupados por tipo.
      - `get_pool` / `get_pool_counts`: seguimientos sin fecha asignada ("pool"), con filtros reales por sub-motivo, días desde la llamada original y — para tipo `cerrada` — programa y estado de deuda.
      - `_client_debt`: deuda real del cliente (`Program.price` menos pagos completados vía `Enrollment`/`Payment`), mismo patrón ya usado en el dashboard de performance del closer.
      - `_client_program_code`: resuelve el programa (AL/RR/SI) desde la venta oficial más reciente del cliente en `FinancialSale`, igual que el resto del sistema.
      - `get_daily_goal_progress`: seguimientos realizados en la fecha seleccionada vs. la meta diaria.
    - **Nuevo Endpoint ([closer_followups.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer_followups.py) [NEW], [__init__.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY])**:
      - `GET /api/closer/followups/today`, `GET /api/closer/followups/pool`, `GET /api/closer/followups/pool-counts`, `GET /api/closer/followups/goal`, `POST /api/closer/followups/<id>/schedule` — blueprint nuevo y separado, mismo criterio de no seguir engordando `closer.py`/`closer_service.py`.
    - **Verificado end-to-end** contra datos reales: se programó un seguimiento tipo `cerrada` sobre una cita con inscripción real y devolvió correctamente la deuda (`$400.0`) y el programa (`Residency Roadmap`) calculados desde `Enrollment`/`Payment`/`FinancialSale`; se programó un `no_tomada` y apareció agrupado correctamente en `/followups/today`. Registros de prueba restaurados a su estado original.
    - **Pendiente**: falta la interfaz de frontend (pestaña Seguimientos reagrupada + explorador del pool + wiring del flujo de programación desde el reporte de llamada) — se implementa a continuación.
  - **Bugfix: Pestaña "Llamadas" Vacía — Excluía Confirmadas Vencidas de Días Anteriores ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Reportado por el usuario**: pidió que la pestaña "Llamadas" funcione tal cual el prototipo `closer-workspace-v7.html`.
    - **Diagnóstico**: `paneCall` en el prototipo describe explícitamente el criterio como *"Llamadas sin reportar, de hoy hacia atrás"* (incluye vencidas de días anteriores, con badge "Vencida" para diferenciarlas). El backend (`GET /api/closer/deck?step=calls` y `GET /api/closer/deck/counts`) filtraba en cambio por la fecha **exacta** seleccionada (`start_time >= inicio_del_día AND <= fin_del_día`), excluyendo cualquier cita ya confirmada pero sin reportar de días previos. Se verificó contra datos reales: existían citas `Confirmado` con `closer_result='Pendiente'` de días anteriores (ej. 10 de julio) que nunca iban a aparecer en "Llamadas" bajo el filtro viejo, sin importar la fecha seleccionada.
    - **Corrección**: se quitó el límite inferior de fecha en ambos endpoints, dejando solo `start_time <= fin del día seleccionado` — mostrando así todas las llamadas confirmadas y sin reportar hasta la fecha elegida (inclusive vencidas), igual que el prototipo.
    - **Verificado end-to-end**: antes del fix, "Llamadas" devolvía 0 registros para los 3 closers activos en el día de hoy pese a haber datos reales pendientes; después del fix, Jean Carlo ve 1 y Sebastián ve 3 llamadas confirmadas sin reportar de fechas anteriores, con los contadores del badge de la pestaña coincidiendo con la lista.
  - **Bugfix: Botón "Guardar Reporte" Desaparecía al Elegir "Sí, confirmó" en la Etapa "Conversando" ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Reportado por el usuario**: al pasar un lead a "Conversando" y luego intentar confirmar asistencia, no aparecía el botón para guardar y pasarlo a "Confirmado".
    - **Diagnóstico**: la condición que decide qué vista del paso de confirmación mostrar (`sessionForm.confirm_status === 'Confirmado' || currentKey === 'confirmado' ? (...)`) mezclaba el estado **ya guardado** del lead (`currentKey`, derivado de `selectedLead.result`) con la **selección pendiente todavía sin guardar** en el formulario (`sessionForm.confirm_status`). Apenas el closer hacía clic en la opción "Sí, confirmó" (que solo actualiza el estado local del formulario), la vista saltaba inmediatamente a la pantalla de "lead ya confirmado" (con únicamente Reagendar/Descartar), que no incluye ni el textarea de nota ni el botón "Guardar Reporte" — antes de que el usuario llegara a guardar nada. Bug preexistente, no introducido en esta sesión, pero bloqueaba exactamente el flujo que se estaba probando.
    - **Corrección**: la vista ahora depende únicamente del estado real y ya persistido del lead (`currentKey === 'confirmado'`). La opción elegida se sigue marcando visualmente como seleccionada (fix de ayer), pero el formulario con el botón "Guardar Reporte" permanece visible hasta que el closer efectivamente guarda.
  - **Sincronización de Base de Datos Local desde Producción**:
    - **Base de Datos ([actualizar_db.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/scripts/actualizar_db.py) [EXECUTE])**:
      - Se ejecutó el script de sincronización (solo lectura de producción → escritura local, no toca producción en ningún momento) para que el entorno local refleje el estado real de producción antes de seguir probando el mazo del closer.
      - Se detuvieron primero todos los procesos duplicados de `run.py` (recurrentes en esta sesión) para evitar bloqueos de SQLite durante la limpieza y reescritura de tablas.
      - Sincronizados 3,177 `appointments`, 3,700 `financial_agendas`, 4,485 `clients`, 829 `financial_sales`, 11,454 `manychat_leads`, entre otras tablas. Pipeline de normalización de closers ejecutado automáticamente sin cambios pendientes.
      - Se reinició el backend en limpio tras la sincronización y se verificó que el pipeline de confirmaciones devuelve datos reales para los 3 closers activos en la fecha actual.
  - **Bugfix Crítico #3 (el real): El Mazo Aterrizaba en una Pestaña Inexistente (`step=agendas`) y Mostraba Cero Tarjetas ([App.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/App.jsx), [LoginPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/auth/LoginPage.jsx), [OperatorControls.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/modals/OperatorControls.jsx), [useDockNavigation.js](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/hooks/useDockNavigation.js), [TeamManagementPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/admin/team/TeamManagementPage.jsx) [MODIFY])**:
    - **Reportado por el usuario**: después de un hard refresh, "se ve el mazo pero sin tarjetas" — no era un crash de React (el shell/tabs renderizaban bien) ni un problema de backend (todos los endpoints seguían devolviendo datos reales, verificado end-to-end en la entrada anterior).
    - **Diagnóstico real**: el rediseño v6/v7 del mazo del closer reorganizó la navegación en 3 pestañas con nomenclatura nueva (`step=confirmations`, `step=calls`, `step=seguimientos` — ver los 3 botones "① Confirmaciones / ② Llamadas / ③ Seguimientos" en `CloserWorkflowPage.jsx`), pero **5 lugares distintos del código siguen redirigiendo al closer a la URL vieja `/closer/deck?step=agendas`**, un valor de `step` que la UI actual no reconoce en absoluto: login normal (`LoginPage.jsx`), el fallback de `ProtectedRoute` en `App.jsx`, ambos flujos de simulación de sesión (`OperatorControls.jsx` del atajo 'W', y el botón de `TeamManagementPage.jsx` corregido ayer — que apuntaba a la URL vieja `/closer/deck?step=agendas` en vez de una inexistente, pero esa URL vieja tampoco sirve en la UI actual), y el ítem "Agendas del Día" del Dock genérico de `MainLayout` (`useDockNavigation.js`). El backend sí soporta `step=agendas` (devuelve todas las citas del día sin filtrar), pero el frontend solo tiene JSX para renderizar tarjetas cuando `activeStep === 'confirmations'` — para cualquier otro valor no reconocido (incluido `'agendas'`), cae en la rama "Renderizado clásico de Lista para Llamadas y Seguimientos", que a su vez solo pinta contenido cuando `activeStep` es literalmente `'calls'` o `'seguimientos'`. Con `step=agendas` ninguna rama aplica: se ve el armazón completo del mazo (pestañas, buscador, dock) pero la columna central queda vacía — exactamente el síntoma reportado, y probablemente presente desde que se hizo el rediseño de pestañas, no algo introducido esta sesión.
    - **Corrección**: se cambiaron las 5 redirecciones de `step=agendas` a `step=confirmations` en todos los puntos de entrada al mazo del closer (login, fallback de ruta protegida, ambos flujos de suplantación de sesión, y el ítem del Dock genérico).
    - **Verificado**: búsqueda exhaustiva confirmando cero referencias restantes a `step=agendas` en todo el frontend; los 5 archivos modificados compilan sin errores.
  - **Bugfix Crítico #2: El Estado del Pipeline de Confirmaciones se Pisaba con el Estado (Desactualizado) de FinancialAgenda ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Reportado por el usuario probando en local** después del fix anterior: procesó 2 agendas de "Por confirmar" y no aparecieron en la etapa correspondiente (ni en Conversando ni en Confirmado).
    - **Diagnóstico real** (el fix de `closer_processed` era necesario pero no suficiente): `_format_appointment_for_deck` — la función que serializa cada cita para el mazo — nunca devuelve `Appointment.result` tal cual. En su lugar recalcula un `confirmer_result` buscando una `FinancialAgenda` que matchee por email/instagram en una ventana de ±12h, y si la encuentra con un `estado` reconocido (pendiente/agendado/confirmado/sin respuesta/contactado/cancelada/reagendada), **ese valor pisa incondicionalmente** lo que el Closer acababa de guardar vía `saveConfirmReport`. Como la gran mayoría de las citas tienen una `FinancialAgenda` asociada con `estado='Pendiente'` (el estado por defecto que deja Triage), el pipeline de confirmaciones del Closer (`por_confirmar`/`conversando`/`confirmado`) quedaba invisible: se guardaba correctamente en `Appointment.result`, pero el GET del mazo lo volvía a mostrar como `'Pendiente'` — y `'Pendiente'` no matchea ninguna de las 3 columnas del Kanban del Closer más que "Por confirmar" (que es donde ya no debería estar), causando exactamente el síntoma reportado.
    - **Verificado empíricamente contra una cita real de la BD local** (id 6022, "Erick Ramirez", con `FinancialAgenda.estado='pendiente'` asociada): antes del fix, `_format_appointment_for_deck` devolvía `'Pendiente'` aunque `Appointment.result` estuviera en `'conversando'`. Después del fix, devuelve el valor correcto para los 3 estados del pipeline del Closer.
    - **Corrección**: se protegieron los 3 valores propios del pipeline de confirmaciones del Closer (`por_confirmar`, `conversando`, `confirmado`) para que, una vez que el Closer tomó acción sobre el lead, ese estado sea autoritativo y la búsqueda en `FinancialAgenda` se salte por completo (mismo criterio que ya existía para proteger estados terminales del Closer como `Show up`/`No Show`).
    - **Verificación end-to-end completa**: se simuló el flujo real (`POST /api/closer/deck/<id>` con `confirm_status='conversando'` seguido de `GET /api/closer/deck?step=confirmations`) contra la cita real de prueba — el lead aparece correctamente con `result: 'conversando'` en la respuesta que consume el Kanban. Se restauró el registro a su estado original.
  - **Bugfix Crítico: Leads Desaparecían del Pipeline de Confirmaciones al Guardar ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Diagnóstico**: en `POST /api/closer/deck/<id>`, la condición para NO marcar la cita como procesada (`appt.closer_processed = True`) solo contemplaba el caso de enviar `confirm_status` como **único** campo del payload (`len(data) == 1`). Pero `saveConfirmReport` (agregado/ajustado ayer) siempre envía `confirm_status` **junto con** `closer_notes` (para guardar la nota del proceso), por lo que el payload tenía 2 claves y caía siempre en la rama `else`, marcando la cita como `closer_processed = True`. Como varias consultas del mazo (`GET /api/closer/deck`) filtran explícitamente por `Appointment.closer_processed == False`, el lead desaparecía por completo de las 3 columnas del Kanban de confirmaciones (Por confirmar / Conversando / Confirmado) apenas se guardaba, en vez de moverse a la etapa correspondiente.
    - **Corrección**: se amplió la condición para reconocer `confirm_status` y/o `closer_notes` juntos como "actualización rápida de confirmación" (no marca como procesada), dejando intacto el comportamiento para cualquier otro campo (reportes de llamada, cambios de resultado, etc., que sí deben marcar la cita como procesada).
    - **Verificado** invocando el endpoint directamente contra la base de datos local: antes del fix, `closer_processed` pasaba a `True`; después del fix, permanece en `False` y `result` se actualiza correctamente al nuevo estado. Se restauró el registro de prueba usado a su estado original tras la verificación.
  - **Bugfix: Feedback visual ausente en los botones de "¿Ya hubo contacto?"**: los botones de opción del paso de confirmación (`option()`) no mostraban ningún estado de "seleccionado" al hacer clic — dado que además el guardado ahora requiere elegir una opción explícitamente (ver entrada de ayer sobre el mínimo de 10 caracteres), esto hacía parecer que el botón "no funcionaba" al no dar ninguna señal visual de que el clic se había registrado. Se agregó un estado `selected` opcional al helper `option()` (anillo violeta + ícono de check) y se conectó a `sessionForm.confirm_status` en los botones del paso de confirmación.
  - **Bugfix: HTTP 405 al Crear Nueva Agenda desde el Mazo del Closer ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Reportado por el usuario en producción** (`work.thelearnation.com`): `POST /api/closer/deck/new` devolvía 405, mismo patrón de bug que la tarea de reprogramación de citas de ayer (ruta inexistente que cae en el catch-all del SPA, que solo acepta GET).
    - **Diagnóstico**: `handleCreateAgenda` llamaba a `/closer/deck/new` (nunca existió como ruta) en vez del endpoint real de creación, `POST /api/closer/appointments`, y además enviaba los campos del lead (`lead_name`, `instagram`) sueltos en el payload en vez de anidados en `client_data`, que es lo que `BookingService.create_or_update_client` espera.
    - **Corrección**: se apuntó la llamada a `/closer/appointments` con el payload correcto (`start_time`, `origin`, `client_data: {name, instagram}`).
    - **Hallazgo adicional**: `create_appointment` en el backend ignoraba por completo el campo `origin` recibido y siempre hardcodeaba `'Manual Closer'`, por lo que el selector de origen del modal "Nueva Agenda" (Setter/Workshop/VSL/etc., heredado del prototipo v7) nunca tenía efecto real. Se corrigió para respetar `data.get('origin')` con el mismo fallback por defecto.
    - **Verificado** invocando el endpoint directamente: creación exitosa (201) con el `origin` correctamente persistido; se eliminó el registro de prueba creado para la verificación.
  - **Bugfix: HTTP 405 al Eliminar una Agenda desde el Mazo del Closer ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Reportado por el usuario en producción** (`work.thelearnation.com`): al probar la creación de agendas (ya corregida hoy), intentó eliminar la agenda de prueba recién creada y `DELETE /api/closer/deck/<id>` devolvía 405, mismo patrón de ruta faltante que los otros bugs 405 de hoy.
    - **Diagnóstico**: `handleDeleteLead` en `CloserWorkflowPage.jsx` llama `api.delete('/closer/deck/${leadId}')`, pero `app/api/closer.py` solo tenía registrado `@bp.route('/deck/<int:appt_id>', methods=['POST'])` para esa URL — Flask/Werkzeug reconoce la ruta pero no el método, devolviendo 405 automáticamente en vez de caer al catch-all del SPA.
    - **Corrección**: se agregó `@bp.route('/deck/<int:appt_id>', methods=['DELETE'])`, con los mismos permisos del `POST` vecino (closer/admin/setter, closer restringido a sus propias citas). Antes de borrar la cita: desvincula (sin borrar) las `SurveyAnswer` asociadas —su FK a `Appointment` no tiene cascade de borrado y en producción (con constraints de FK reales, a diferencia de SQLite local) hubiera fallado con un error de integridad— y borra el evento de Google Calendar asociado si existe. `InstallmentPlan` y `LeadEventLog` ya tenían `cascade='all, delete-orphan'` en el modelo, así que se limpian solos al borrar la cita.
    - **Verificado end-to-end** contra la base de datos local: se creó una cita de prueba con un plan de cuotas y una respuesta de encuesta colgando, se ejecutó la misma lógica del endpoint, y se confirmó el borrado limpio sin errores de integridad ni registros huérfanos. Registro de prueba eliminado tras la verificación.
  - **Bugfix: HTTP 400 al Crear una Agenda en un Horario Ocupado por una Cita ya Resuelta como "No Lead" ([booking_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/booking_service.py) [MODIFY])**:
    - **Reportado por el usuario en producción** (`work.thelearnation.com`): creó una agenda de demostración, la marcó como "No Lead", y al volver a crear una agenda en el mismo horario recibió `{"error": "Ya existe una cita agendada en este horario para este closer o no se pudo crear la agenda"}` (HTTP 400).
    - **Diagnóstico**: marcar una cita como "No Lead" (o cualquier otro resultado del closer: Show up, No Show, Lead Perdido) solo actualiza `Appointment.closer_result` vía `CloserService.process_agenda` — nunca toca `Appointment.result`, el campo que usa el chequeo de conflicto de horario en `BookingService.create_appointment` (excluía únicamente `result in ('Cancelada', 'Reprogramada')`). El mismo problema existía en `get_available_slots_utc`, usado para calcular los horarios disponibles del selector. Como resultado, una cita ya resuelta y cerrada por el closer seguía "ocupando" su horario para siempre, bloqueando cualquier intento de agendar una cita nueva en ese mismo slot para ese closer.
    - **Corrección**: se agregó `Appointment.closer_processed == False` como condición adicional en ambos chequeos (creación de cita y cálculo de slots disponibles), de forma que una cita ya procesada por el closer (cualquier resultado terminal) deje de contar como conflicto de horario.
    - **Verificado end-to-end** contra la base de datos local: se reprodujo el escenario exacto reportado (crear cita → marcar "No Lead" → crear una segunda cita en el mismo horario y closer) confirmando que antes del fix la segunda creación fallaba (`None` retornado por conflicto) y después del fix se crea correctamente. Registros de prueba eliminados tras la verificación.
  - **Timezone Consistente en Backend (UTC real) y Frontend (hora local del navegador) + Recordatorio Pre-Llamada ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [closer_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY], [triage.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/triage.py) [MODIFY], [workshop.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/workshop.py) [MODIFY], [auth.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/auth.py) [MODIFY], [admin.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/admin.py) [MODIFY], [AuthContext.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/contexts/AuthContext.jsx) [MODIFY], [datetime.js](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/utils/datetime.js) [NEW], [booking.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/models/booking.py) [MODIFY], migración `ba6cd6f7fb5e`, [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Pedido del usuario**: al marcar un lead como "Sí, conversando" o "No responde aún" en el paso de confirmación, poder configurar un recordatorio de seguimiento de llamada por tomar, antes de la llamada, para no olvidarse de escribirle. Al investigar dónde encajaba el date-picker de ese recordatorio, se detectó que el manejo de fecha/hora del mazo del closer estaba roto de forma sistemática, y el usuario pidió corregirlo de fondo: "que en el backend se use la hora UTC y que se calcule para cada usuario según la zona horaria del navegador de cada usuario".
    - **Diagnóstico del bug de fondo**: el frontend armaba strings de hora local del navegador (`` `${date}T${time}:00` ``) y los mandaba crudos a `start_time`/`reschedule_date` como si ya fueran UTC (creación y reagenda de citas en `CloserWorkflowPage.jsx`); al mostrar una cita hacía el camino inverso también sin convertir (`new Date(isoSinZ)` interpreta un ISO sin sufijo `Z` como hora local, no UTC) — ambos bugs se "compensaban" por casualidad solo cuando la misma cita se creaba y se veía desde el mismo navegador, pero rompían cualquier cálculo real del backend. Además, varios endpoints (`/deck`, `/deck/counts` en `closer.py`; el bloque de cumplimiento de reportes diarios en `closer_service.py`; `triage.py`; `workshop.py`) usaban una zona horaria hardcodeada (`America/La_Paz`) en vez de la del usuario (`User.timezone`, ya existente pero subutilizado) — con un parche de "+12h" como workaround en el filtro de fechas del mazo. Se confirmó con datos reales que el bug tenía impacto real, no solo teórico: el closer Jean Carlo tiene `timezone='America/Mexico_City'` (UTC-6), distinta a la mayoría del equipo en `America/La_Paz` (UTC-4).
    - **Corrección backend**: se agregó el helper `_user_day_bounds_utc()` en `closer.py` (mismo patrón que ya usaba correctamente `booking_service.py._process_slot`: `pytz.timezone(...).localize(...).astimezone(UTC)`) y se reemplazó el parche de "+12h" y los cálculos sin localizar en `/deck` y `/deck/counts`. En `closer_service.py`, el cálculo de "hoy"/"ayer" para el cumplimiento de reportes se movió *dentro* del loop de closers activos, usando la timezone de cada uno en vez de una única zona global. `triage.py` y `workshop.py` ahora usan `current_user.timezone` en vez de la zona hardcodeada.
    - **Auto-sincronización de `User.timezone`**: en cada login exitoso, el frontend manda la zona detectada del navegador (`Intl.DateTimeFormat().resolvedOptions().timeZone`) y el backend actualiza `User.timezone` (decisión explícita del usuario: siempre sincronizar, aunque eso implique pisar una zona fijada manualmente por un admin en el login siguiente del closer). Se cerró además el gap de `PUT /admin/users/<id>`, que permitía fijar la timezone solo al crear un usuario, no al editarlo después.
    - **Utilidad centralizada de fecha (nueva, `frontend/src/utils/datetime.js`)**: primera carpeta `utils/` del frontend; sin dependencias nuevas (no hay date-fns/dayjs/moment/luxon instalados), usa el API nativo `Date`/`Intl`. `localInputsToUtcIso()` para enviar, `parseUtcIso()`/`splitLocalDateTime()` para mostrar (anteponiendo `Z` al ISO recibido del backend antes de construir el `Date`, ya que `datetime.isoformat()` de un valor naive-UTC no incluye el sufijo). Se reemplazaron los 4 puntos de envío y los 3 de visualización en `CloserWorkflowPage.jsx` que tenían el bug.
    - **Recordatorio pre-llamada (nuevo campo `Appointment.pre_call_reminder_at`, DateTime UTC real)**: distinto del sistema de Seguimientos ya existente (que es para leads cuya llamada ya pasó). Al elegir "Sí, conversando" o "No responde aún" en el paso de confirmación, aparece un checkbox opcional "Recordarme escribirle antes de la llamada" con un `datetime-local` prellenado por default a 2 horas antes de la cita (editable), enviado en el mismo `POST /api/closer/deck/<id>` que ya hace `saveConfirmReport` sin marcar la cita como procesada. Se muestra como badge en la tarjeta del Kanban de confirmaciones: rojo "Recordatorio vencido" si ya pasó, ámbar "Recordatorio hoy HH:mm" si es el día calendario local actual — puramente calculado al refrescar el mazo, mismo patrón que el badge "Vencida" ya existente en la pestaña Llamadas; sin cron, sin email, sin push (fuera de alcance de esta pasada, documentado explícitamente).
    - **Verificado end-to-end** contra la base de datos local, incluyendo pruebas HTTP reales con `test_client`: `_user_day_bounds_utc` produce límites correctos para closers en `America/La_Paz` y `America/Mexico_City` (incluyendo el caso límite de una cita a las 23:50 hora local); el login sincroniza `timezone` solo en autenticación exitosa (no la toca si la contraseña es incorrecta); el flujo completo `POST /auth/login` → `POST /api/closer/deck/<id>` con `pre_call_reminder_at` → `GET /api/closer/deck?step=confirmations` guarda y devuelve el recordatorio correctamente sin marcar la cita como procesada. `npm run build` del frontend compila sin errores. Todos los registros y credenciales de prueba restaurados a su estado original tras la verificación.
  - **Bugfix: Cita Creada "Para Hoy" Aparecía en Confirmaciones pero no en Llamadas — Defaults de Fecha Calculados en UTC en vez de Local ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [datetime.js](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/utils/datetime.js) [MODIFY])**:
    - **Reportado por el usuario probando con el cliente de prueba "Kerwin Prueba"**: creó una agenda para el mismo día y apareció correctamente en la pestaña "① Confirmaciones", pero nunca apareció en "② Llamadas" aunque ya estaba confirmada.
    - **Diagnóstico**: el modal "Nueva Agenda" (`newAgendaForm`) prellenaba su campo de fecha con `new Date().toISOString().split('T')[0]` — la fecha en **UTC**, no la fecha calendario **local** del closer. En zonas UTC negativas (el closer de prueba está en `America/La_Paz`, UTC-4) eso ya es "mañana" varias horas antes de medianoche local: a las 21:00 hora local ya es la 1am UTC del día siguiente. El closer probó a las 21:00 locales, dejó la fecha en su valor por defecto (sin notar que ya mostraba el día siguiente) y puso hora 22:00/23:00, así que la cita quedó agendada para *mañana* a las 22-23h, no para *hoy*. La pestaña "Confirmaciones" no tiene límite superior de fecha (muestra todo desde hoy en adelante), por eso la cita sí aparecía ahí — pero "Llamadas" sí filtra hasta el fin del día de hoy, y correctamente la excluía por no ser, en efecto, una cita de hoy.
    - **Alcance del bug**: el mismo patrón (`new Date().toISOString().split('T')[0]` o `new Date(Date.now() + N*86400000).toISOString().split('T')[0]`) se repetía en 13 puntos más de `CloserWorkflowPage.jsx`: el reset del formulario de "Nueva Agenda" tras crear, el default de fecha del formulario de venta (4 puntos), los 3 botones de cadencia de seguimiento (+1/+3/+7/+14 días) y su comparación de estado seleccionado, el fallback de fecha de seguimiento al guardar, y el cronograma de cuotas (fechas de vencimiento mensuales) — todos calculando "hoy" o "+N días" en UTC en vez de en la fecha calendario local del navegador.
    - **Corrección**: se agregaron `toLocalDateStr()`, `localToday()` y `localDateFromNow(dias)` a `frontend/src/utils/datetime.js` (reutilizando `splitLocalDateTime` ya existente) y se reemplazaron los 14 puntos afectados en `CloserWorkflowPage.jsx`.
    - **Verificado**: se simuló en Node el momento exacto reportado (21:00 hora local en UTC-4) confirmando que el patrón viejo devolvía la fecha de mañana y el nuevo devuelve hoy correctamente; `npm run build` compila sin errores. Se eliminaron las 2 citas de prueba creadas con la fecha incorrecta durante el diagnóstico. Base de datos local resincronizada desde producción con `scripts/actualizar_db.py` antes de investigar, sin bloqueos de SQLite.
  - **Bugfix: Pool de Seguimientos sin Fecha Siempre Vacío ([closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY])**:
    - **Pedido del usuario**: hacer que funcione el pool de seguimientos sin fecha en la pestaña "③ Seguimientos" — debe mostrar las agendas que están en la etapa correspondiente (no tomada/tomada/cerrada) para poder asignarles seguimiento.
    - **Diagnóstico**: `_base_query` exigía `Appointment.seguimiento_tipo` ya etiquetado para que una cita contara como "en el pool". Ese campo solo se setea cuando el closer pasa explícitamente por la pantalla de "programar seguimiento" — si en cambio cierra el reporte de la llamada marcándola "No Show"/"Cancelado" sin llegar a esa pantalla (el camino más común), la cita queda invisible para siempre, tanto en el pool como en los contadores. Confirmado contra datos reales: 23 citas con `closer_result='No Show'` y 77 con `'Show up'`, ninguna con `seguimiento_tipo` asignado — el pool devolvía 0 resultados en las 3 categorías.
    - **Corrección**: se agregó `_effective_tipo()`, que deriva la categoría desde `closer_result` cuando la cita nunca fue etiquetada explícitamente: `No Show`/`Cancelado`/`Cancelada` → `no_tomada`; `Show up` → `tomada`, pero solo si el cliente todavía no tiene una venta cerrada en `FinancialSale` (26 de los 77 `Show up` ya habían cerrado — se excluyen correctamente, no son seguimientos pendientes). `cerrada` deliberadamente **no** se deriva: sigue atada solo al flujo explícito de declarar una venta con cobro pendiente (`fecha_seguimiento_cobro`), no a "cualquier cliente que alguna vez compró". `get_pool`, `get_pool_counts` y `get_today_grouped` ahora usan el tipo efectivo en vez del campo crudo.
    - **Verificado end-to-end** contra datos reales: el pool pasó de devolver 0 a devolver 44 `no_tomada` + 38 `tomada`; se tomó un lead derivado del pool, se le asignó fecha de seguimiento con la misma lógica de `schedule_followup`, se confirmó que sale del pool y aparece correctamente en "Asignados para hoy" en la fecha asignada, y se restauró el registro a su estado original (sin tag explícito) tras la verificación.
  - **Bugfix: Lead ya "Confirmado" Abría el Proceso de Confirmación en vez del Reporte de Llamada ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Pedido del usuario**: las agendas que están en la etapa "Confirmado" (tercera columna de la pestaña Confirmaciones), al abrirlas, deberían mostrar las mismas opciones que cuando se abren desde la pestaña "② Llamadas" (reportar el resultado de la llamada: Asistió/No show/Canceló/Reagendó), no las opciones de "¿ya hubo contacto?".
    - **Diagnóstico**: `handleSelectLead` decidía el paso inicial del modal únicamente por la pestaña activa (`activeStep === 'confirmations'` → siempre `modalStep = 'confirm'`), sin distinguir en qué columna del kanban estaba realmente el lead. Un lead en "Confirmado" no tiene nada más que confirmar, pero el modal lo seguía mandando al árbol de "¿Ya hubo contacto?" en vez de al reporte real de la llamada.
    - **Corrección**: se agregó una verificación adicional del `result` normalizado del lead — si es `'confirmado'`, el modal abre directo en `modalStep = 'root'` (el mismo árbol de reporte de llamada que usa la pestaña Llamadas). Se agregó el estado `modalFlowLabel` para que el título del modal también refleje el flujo correcto en cada caso, ya que antes se derivaba únicamente de la pestaña activa (insuficiente para distinguir este caso nuevo).
    - **Verificado**: `npm run build` compila sin errores; revisión manual del árbol de decisiones confirmando que un lead con `result='Confirmado'` abierto desde Confirmaciones activa el mismo `modalStep` y título que un lead abierto desde Llamadas.
  - **Reparación completa del Modal de Seguimientos según el Prototipo v7 ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Reportado por el usuario**: "el modal de los seguimientos no está funcionando", pidiendo replicar el comportamiento de `closer-workspace-v7.html` (prototipo de referencia en la raíz del repo, sin trackear en git). Se hizo un diff funcional exhaustivo entre ambas implementaciones antes de tocar código.
    - **Bug más grave — "Cerrar Seguimiento" era un botón completamente muerto**: abría un `reasonModal` con `actionType: 'close_follow_up'`, pero `handleConfirmReason` solo manejaba `cancel`/`reschedule`/`no_lead`/`confirm_discard` — no existía ninguna rama para `close_follow_up`. Al confirmar el motivo, el modal se cerraba y no pasaba absolutamente nada: sin llamada a la API, sin toast, el seguimiento seguía exactamente igual. Casi con certeza el disparador directo del reporte del usuario.
    - **Segundo bug — el botón "Completar Seguimiento" no exigía elegir el siguiente paso**: dependía solo de `notes` + `result`; un closer podía escribir la nota, marcar "No respondió" y guardar sin elegir "Programar Seguimiento" ni "Cerrar Seguimiento" — caía en un `else` que marcaba `seguimiento_realizado: true` sin fecha, cerrando el seguimiento en silencio sin que el closer lo pidiera.
    - **Tercer bug — "Cerró la venta"/"Pagó" saltaban todo el flujo sin guardar nada**: abrían el modal de venta *inmediatamente* al hacer clic en la opción, sin pasar por `saveSeguimientoReport` — la nota escrita y el resultado se descartaban y el seguimiento original nunca quedaba marcado como resuelto.
    - **Gaps de paridad con el prototipo**: faltaba el campo "Modalidad" (Mensaje/Llamada, requerido antes de guardar un seguimiento no-cerrado), faltaba la fecha del próximo seguimiento de cobro en `segventa` para "No respondió"/"Estamos conversando" (el backend recibía un default silencioso de "mañana" sin que el closer lo controlara), y faltaban por completo el flujo de solicitud de referidos tras contacto humano real y el sistema de menciones de equipo en las notas.
    - **Corrección**:
      - "Cerrar Seguimiento" pasa a ser un paso inline dentro del mismo formulario (4 motivos fijos como botones, igual que el prototipo), reemplazando la dependencia del `reasonModal` roto.
      - El botón final queda deshabilitado (`canComplete`) hasta que se elige explícitamente el siguiente paso — "Programar Seguimiento" con fecha, o "Cerrar Seguimiento" con motivo — salvo que el resultado sea "Cerró la venta"/"Agendó"/"Pagó", que transicionan directo (igual que el prototipo).
      - "Cerró la venta"/"Pagó" ahora solo seleccionan la opción; `saveSeguimientoReport` persiste el cierre del seguimiento (`seguimiento_realizado: true`) **antes** de abrir el modal de venta/cobro, con el texto dinámico del botón reflejando la transición ("Continuar al reporte de venta →" / "Continuar al registro de cobro →").
      - Se agregó el campo Modalidad (toggle multi-selección Mensaje/Llamada) en `'seg'`, y el campo de fecha del próximo cobro (requerido para No respondió/Conversando) en `'segventa'`.
      - Se agregó el paso de referidos (se activa tras contacto humano real: Contestó/Agendó/Cerró/Pagó): pregunta si se pidieron, y si dejó datos, filas dinámicas nombre+contacto — cada fila con contacto llama a `POST /deck/referrals/manual` (endpoint ya existente, reusado tal cual) creando cliente + agenda nueva con origen `Referido de <nombre>`. Referidos sin datos de contacto se anotan en `closer_notes` en vez de fabricar una cita sintética (el prototipo usa un array mock en memoria sin esa restricción; adaptación deliberada al esquema relacional real).
      - Se agregó el sistema de menciones: nuevo endpoint `GET /api/closer/team-members` (closer/setter activos) poblando chips debajo de las notas que insertan `@usuario` al hacer clic.
    - **Verificado end-to-end** contra la base de datos local con `test_client` real: (1) el flujo de "Cerrar Seguimiento" (antes muerto) ahora persiste `seguimiento_realizado=true` correctamente; (2) el cierre del seguimiento al elegir "Cerró la venta" se persiste antes de la venta (antes se perdía por completo); (3) un referido con contacto crea cliente + agenda nueva vía el endpoint reusado, confirmado con `origin='Referido de <nombre>'`. `npm run build` compila sin errores. Todos los registros de prueba y la contraseña del closer de prueba restaurados tras la verificación.
  - **Bugfix: HTTP 400 "The CSRF token has expired" al Guardar Formularios Largos ([api.js](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/services/api.js) [MODIFY])**:
    - **Reportado por el usuario probando en local**: al completar el nuevo paso de referidos del modal de Seguimientos (elegir "No se lo pedí" y guardar), `POST /api/closer/deck/5999` devolvió 400. El cuerpo real de la respuesta (obtenido por el usuario desde DevTools) era la página HTML de error por defecto de Flask-WTF: *"The CSRF token has expired."*
    - **Diagnóstico**: `frontend/src/services/api.js` pedía el token CSRF **una sola vez** al cargar la página (`fetchCsrfToken`) y lo cacheaba para siempre en una variable de módulo, sin refrescarlo ni reintentar nunca. Pasado el `WTF_CSRF_TIME_LIMIT` por defecto de Flask-WTF (1 hora), cualquier `POST`/`PUT`/`DELETE`/`PATCH` empezaba a fallar con este 400 sin ninguna recuperación posible salvo recargar la página manualmente — un bug preexistente, no introducido por el trabajo de esta sesión, pero que el nuevo paso de referidos hizo mucho más fácil de encontrar al alargar el tiempo que un closer pasa completando el mismo formulario.
    - **Corrección**: el interceptor de respuesta de axios ahora detecta específicamente este 400 (status 400 + cuerpo de texto conteniendo "CSRF"), descarta el token cacheado, pide uno nuevo vía `fetchCsrfToken(true)`, y reintenta la petición original una sola vez con el token fresco (protegido con un flag `_csrfRetried` para evitar loops) — en vez de fallar silenciosamente.
    - **Verificado**: `npm run build` compila sin errores. Verificación funcional completa requiere un navegador real con el token efectivamente vencido (no reproducible con `test_client` de Flask, que no simula el ciclo de vida del token en el cliente); se documenta la lógica del interceptor como revisada por código, pendiente de confirmación visual del usuario tras el próximo deploy.

- **5 de Agosto de 2026** *(continuación — bugfix: buscador global rompía con 403 al abrir la ficha de un lead de otro closer)*:
  - **Reportado por el usuario en producción**: al usar el buscador por nombre del mazo, seleccionar un resultado y abrir su ficha, la consola mostraba `AxiosError: Request failed with status code 403` al pedir `GET /closer/deck/card/<id>`. En local no se reproducía.
  - **Diagnóstico**: `/closer/leads/search` (el buscador global, [closer.py:134](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py)) busca por nombre/email/instagram/teléfono **sin filtrar por dueño** — a propósito, para poder ubicar cualquier lead del sistema. Pero al seleccionar un resultado, `handleSelectSearchResult` pedía la ficha completa a `/closer/deck/card/<id>` ([closer.py:2060](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py)), que sí exige que la cita sea del closer que la pide (o admin) — devolvía `403 Forbidden` si la cita ya tenía asignado a **otro** closer distinto. En local esto no se veía porque las pruebas se hacían con cuenta admin (que salta esa validación) o contra citas todavía sin asignar; en producción, con closers reales trabajando su propia cartera, el buscador global encuentra leads de cualquier closer y la mayoría ya tienen dueño.
  - **Decisión de negocio** (consultada al usuario antes de tocar el control de acceso): en vez de mantener el bloqueo duro, un closer puede abrir la ficha de un lead de otro closer en **modo solo lectura** — la puede consultar, pero no editarla ni declarar ventas sobre ella. Consistente con que el buscador global ya expone leads de todos.
  - **Corrección**:
    - `GET /closer/deck/card/<id>` ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY]) ya no devuelve `403` cuando la cita es de otro closer — sirve la ficha igual, agregando `can_edit: false` y `owner_closer_name` a la respuesta. El auto-reclamo de citas sin dueño o de origen ManyChat se mantiene sin cambios (eso no era el bug).
    - **Frontend** ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY]): cuando `selectedLead.can_edit === false`, el modal muestra un aviso ("Este lead pertenece a X — solo podés consultarlo") y oculta/reemplaza todo lo que mutaría datos ajenos: el botón de eliminar lead, el árbol de decisión de la pestaña "Acción" (reportar llamada, declarar venta) queda reemplazado por un mensaje explicando la restricción, y el formulario de "Agregar nota rápida" de la pestaña "Setter" se oculta. Las pestañas de solo consulta (Formulario, notas del setter, encuesta) siguen visibles sin cambios.
  - **Bug propio detectado y corregido durante la verificación**: la primera versión de la corrección no se pudo probar end-to-end vía sesión HTTP real por un problema de configuración del harness de pruebas local (`login_user` de Flask-Login no persistía en `test_request_context`, no relacionado con el código de producción) — se verificó en su lugar simulando las 3 combinaciones reales de rol/dueño (closer viendo cita ajena, closer viendo la propia, admin viendo cualquiera) contra usuarios y una cita reales de la base local, confirmando que `can_edit` da el resultado esperado en los 3 casos.
  - **Verificado**: los 3 escenarios de control de acceso dan el resultado esperado contra datos reales (viewer≠owner → `can_edit=False`; owner → `True`; admin → `True`); `npm run build` compila sin errores. Cliente y cita de prueba eliminados tras la verificación.

- **6 de Agosto de 2026**:
  - **Bugfix: "Deuda Total Pendiente" en el Dashboard de Performance Siempre Daba $0 ([closer_dashboard_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_dashboard_service.py) [MODIFY])**:
    - **Reportado por el usuario**: comparando `/closer/dashboard` (dashboard real de performance, la implementación de `closer-dashboard-v3.html` en este proyecto) contra el mockup, notó que "Deuda total pendiente" siempre mostraba $0 pese a haber cuotas pendientes reales.
    - **Diagnóstico**: `_pending_collections` calculaba la deuda como `Program.price - suma de Payment.amount` sobre el modelo `Enrollment`/`Payment` — el sistema legado de matrícula documentado como "sin datos recientes" en `installment.py`. El cronograma de cobro real que usa todo el flujo de venta del closer (crear plan de cuotas, marcar cuotas como pagadas, editar fechas de vencimiento — ver entradas del 31 de julio y 5 de agosto) vive en `InstallmentPlan`, una tabla completamente distinta que el dashboard nunca consultaba. Confirmado contra la base local: existen 10 cuotas `pendiente` por $2.805 en `installment_plans`, mientras que `Enrollment`/`Payment` no tiene datos que las reflejen.
    - **Corrección**: se reescribió `_pending_collections` para leer de `InstallmentPlan` (cuotas con `estado != 'pagado'`), uniendo con `Appointment` para filtrar por closer cuando corresponde (mismo patrón de acceso ya usado en el resto del dashboard) y con `Client` para el nombre. Se agregó un mapeo `programa_code` → nombre completo (AL/RR/SI, igual que en `closer_service.py` y el frontend) para las filas de la tabla. Se corrigió también el subtítulo de la card "Cuotas por cobrar" en el frontend, que afirmaba (ya incorrectamente) que no había fecha de vencimiento registrada.
    - **Verificado**: primero se llamó a `CloserDashboardService.get_performance_data` directamente contra la base de datos local (sin credenciales de prueba a mano en ese momento) — antes del fix `deuda_total_pendiente` no reflejaba las cuotas reales; después del fix devuelve $2.805, coincidiendo exactamente con la suma real de `installment_plans.monto` en estado pendiente. Verificación de UI completada después contra el navegador real (el usuario compartió credenciales de prueba, ver entrada siguiente).
  - **Feature: Filtros de Período "Últimos 30 días" y "Mes pasado" en el Dashboard de Performance ([closer_dashboard_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_dashboard_service.py) [MODIFY], [PerformanceFilters.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceFilters.jsx) [MODIFY], [performanceUtils.js](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/performanceUtils.js) [MODIFY])**:
    - **Pedido del usuario**: agregar botones para filtrar el dashboard de performance por "el mes pasado" y por "últimos 30 días", además de los períodos ya existentes (Hoy / 7 días / Este mes / 90 días).
    - **Implementación**: `_range_for_period` (backend) gana dos ramas nuevas — `30d` (hoy - 29 días hasta hoy) y `mes_pasado` (primer al último día del mes calendario anterior); el cálculo de rango de comparación (`_comparison_range`) ya era genérico por cantidad de días, así que ambos períodos nuevos heredan automáticamente comparación contra "período anterior" y "mismo rango del mes anterior" sin lógica adicional. En el frontend se agregaron los botones al selector de período y sus etiquetas a `PERIOD_LABELS`.
    - **Verificado**: se llamó a `get_performance_data` con `period='30d'` y `period='mes_pasado'` contra la base local, confirmando que las fechas resultantes son correctas (30d: 2026-07-08 a 2026-08-06; mes_pasado: 2026-07-01 a 2026-07-31, con comparación contra junio). Verificación de UI completada después contra el navegador real (ver entrada siguiente).
  - **Verificación de UI en navegador real (usuario compartió credenciales de prueba: `Kerwin` / `Kerwin`, rol operador sin acceso directo al dashboard de closer)**: se usó la función "Simular" del panel de Gestión de Equipo para entrar como Jean Carlo (closer) y se navegó a `/closer/dashboard`. Confirmado visualmente: "Deuda total pendiente" muestra $930 (4 cuotas pendientes reales de Anailys Gonzalez, antes $0); los botones "30 días" y "Mes pasado" aparecen en el selector de período y al hacer clic en "Mes pasado" recalculan correctamente todo el dashboard (cash collected $11.316, 8 ventas, embudo completo con datos reales, nota "· el mes pasado vs. el período anterior"). Sin cambios persistidos durante la verificación.
  - **Feature: Pedir "Slots Disponibles" a mano en la pestaña Reporte (único dato que no sale de la Bandeja) ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Pedido del usuario**: revisando la imagen que llega a Discord con el reporte diario, notó que "Total Agendas" muestra un "% disp." (porcentaje de slots disponibles) que en la práctica siempre da un número sin sentido, porque nadie carga cuántos slots tenía el closer ese día. Pidió auditar la pestaña "Reporte" del mazo: que la mayoría de los campos sigan saliendo automáticamente de la Bandeja (como ya pasa hoy) y que el único dato que de verdad no se pueda derivar de ahí se pida explícitamente ahí mismo.
    - **Diagnóstico**: confirmado que `CloserService.compute_daily_report_fields` ya documentaba el hueco en su propio docstring ("No fabrica valores para 'slots': no existe ninguna señal persistida... Queda en 0") — es honestamente el único campo de todo el reporte automático que no tiene ninguna fuente real en el sistema (a diferencia de `offers_made`, que sí se deriva de `Appointment.offer_presented`, o de referidos, que se derivan de `Appointment.origin`). El endpoint `POST /closer/deck/daily-report` guardaba ese `0` fabricado directo en `CloserDailyReport.slots` sin que el closer pudiera corregirlo, y la pestaña "Reporte" del mazo (que desde la sesión anterior — ver bitácora — quedó 100% automática salvo la reflexión) no tenía ningún input para este campo. Ese `slots=0` mal guardado es justo lo que después usa `_prepare_report_data` ([closer.py (public)](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/public/closer.py)) para calcular el "% disp." de la imagen de Discord — con `slots_totales=0`, `safe_percent` siempre da `0%`, sin importar cuántas agendas reales hubo.
    - **Corrección**:
      - `POST /closer/deck/daily-report`: acepta un `slots` opcional en el payload y lo aplica sobre `computed['slots']` después de calcular todo lo demás automático — el único campo del reporte que no viene de `compute_daily_report_fields`.
      - `GET /closer/deck/daily-report`: ahora devuelve `slots` (el guardado en el reporte de ese día, o `null` si todavía no se envió ninguno) para que el frontend pueda precargar el input al reabrir/editar un día ya reportado, igual que ya hacía con la reflexión.
      - **Frontend**: nueva sección "Slots disponibles" en la pestaña Reporte, con un input numérico y una nota explicando por qué es el único campo manual. El botón "Enviar reporte al sistema" ahora valida que el campo no esté vacío antes de enviar (mensaje: "Ingresá los slots disponibles del día — es el único dato que no se calcula solo").
    - **Verificado end-to-end contra la base de datos local**: `compute_daily_report_fields` sigue devolviendo `slots=0` crudo (sin tocar esa función, es información honesta); simulando el override que ahora hace `send_daily_report` con `slots=12`, se guardó correctamente en `CloserDailyReport.slots`; una consulta posterior (simulando el `GET` que precarga el input) devolvió `12`; y `_prepare_report_data` (la función que arma los datos de la imagen a Discord) reflejó `general.slots = 12` en vez de `0` — confirma que el "% disp." de la imagen ahora va a calcularse sobre un número real. `npm run build` compila sin errores. Reporte de prueba eliminado tras la verificación.
    - **Nota operativa**: sesión con múltiples instancias de Claude Code trabajando en paralelo sobre este mismo repositorio compartido — antes de tocar `app/api/closer.py` se verificó con `git status`/`git diff` que no había cambios sin commitear de otra sesión en ese archivo en ese momento, para no mezclar código ajeno en este commit (ver hallazgo de la entrada anterior, donde sí pasó por accidente).

- **6 de Agosto de 2026** *(continuación)*:
  - **Bugfix: El Plan de Cuotas No Descontaba lo que el Cliente Ya Había Pagado Antes ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])**:
    - **Reportado por el usuario**: al registrar un pago, la parte de "configurar el plan de cuotas" no descontaba del total lo que el cliente ya había pagado antes.
    - **Diagnóstico**: `handleRegisterSale`, al armar el cronograma de cuotas, llamaba a `POST /closer/installments` con `total = saleForm.precio_total` (el Total del cliente, ver feature de esta misma sesión anterior) y `cobrado_hoy = saleForm.monto` — **solo el monto de la transacción puntual que se está declarando ahora mismo**, ignorando por completo `saleClientState.total_paid` (lo que el cliente ya había pagado en ventas anteriores, ej. una Seña). `InstallmentService.create_plan` calcula el saldo a financiar como `total - cobrado_hoy`, así que cualquier pago previo quedaba invisible para ese cálculo: un cliente que debía $1500, ya había pagado $300 y hoy paga $500 terminaba con $1000 a financiar en cuotas en vez de los $700 reales — se le cobraban de más los $300 ya pagados. Mismo bug replicado en el preview de "Saldo a Financiar" y en la tabla de cuotas que se arma antes de enviar (ambos mostraban el número mal calculado antes de que el closer llegara a confirmar).
    - **Corrección**: los 3 lugares (el POST real y los 2 previews) ahora calculan `cobrado_hoy`/`now` como `saleClientState.total_paid + monto de hoy`, en vez de solo el monto de hoy.
    - **Verificado end-to-end contra la base de datos local**: simulando el escenario reportado (total=$1500, $300 pagados antes, $500 pagados hoy, 2 cuotas), `InstallmentService.create_plan` con el `cobrado_hoy` corregido (800) generó 2 cuotas de $350 cada una ($700 total) — antes del fix hubiera dado $1000 a financiar ($500 cada cuota). `npm run build` compila sin errores. Datos de prueba eliminados tras la verificación.
  - **Feature: Seguimientos Hechos/Por Hacer y Programar uno Nuevo desde la Búsqueda Global ([client_cleanup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/client_cleanup_service.py) [MODIFY], [ClientHistoryModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/shared/ClientHistoryModal.jsx) [MODIFY])**:
    - **Pedido del usuario**: al buscar un lead/cliente por la barra superior, también se deberían ver sus seguimientos hechos y por hacer, más una opción para agregar uno nuevo a futuro.
    - **Contexto**: desde una sesión anterior de hoy, la búsqueda global ya no abre directo la última cita — abre `ClientHistoryModal`, el resumen completo del cliente (agendas, cuotas, ventas). Ese resumen no mostraba nada de seguimientos: para verlos había que abrir cada agenda puntual por separado y revisar sus campos `fecha_seguimiento`/`seguimiento_realizado` uno por uno.
    - **Corrección**: `ClientCleanupService.get_client_full_history` ahora expone en cada agenda del cliente los campos de seguimiento que ya existían en el modelo (`fecha_seguimiento`, `seguimiento_realizado`, `seguimiento_tipo`, `seguimiento_sub`, `last_contact_at`, `last_contact_outcome`) — no hubo que agregar ninguna columna nueva, solo exponer lo que ya se guarda desde la pestaña Seguimientos de la Bandeja. El modal separa "Por hacer" (`fecha_seguimiento` seteada y `seguimiento_realizado=false`) de "Hechos" (`seguimiento_realizado=true` o con `last_contact_at` registrado), y agrega un formulario para programar el próximo contacto (fecha + botón), reusando `POST /closer/deck/<id>` — el mismo endpoint que ya usa el árbol de decisión de llamada para esto, sin duplicar lógica.
    - **Verificado end-to-end contra la base de datos local**: creada una cita de prueba con `fecha_seguimiento`/`seguimiento_sub` seteados, `get_client_full_history` la expuso correctamente con esos campos; simulado un contacto (`last_contact_outcome='contesto'`, `last_contact_at` seteado), la siguiente consulta reflejó el cambio. `npm run build` compila sin errores. Cliente y cita de prueba eliminados tras la verificación.
  - **Feature: Se Quita la Pestaña "Limpieza" del Mazo ([CloserWorkflowPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], `ClientCleanupPane.jsx` [DELETE])**:
    - **Pedido del usuario**: quitar la pestaña "🧹 Limpieza" de `/closer/deck` — ya no es necesaria.
    - **Corrección**: se quitó el botón del dock flotante, la rama de render (`activeView === 'cleanup'`) y el import de `ClientCleanupPane`. Como ese componente quedó sin ninguna otra referencia en el frontend, se eliminó el archivo en vez de dejarlo como código muerto. El backend (`GET /closer/cleanup-queue`, `ClientCleanupService`) no se tocó — no se pidió remover la funcionalidad, solo el acceso desde esta pestaña.
    - **Verificado en navegador real**: simulando a Jean Carlo (closer) desde el panel de Gestión de Equipo, el dock flotante del mazo muestra únicamente "📥 Bandeja", "📊 Reporte del día" y "📈 Dashboard" — sin "🧹 Limpieza" y sin errores en consola. `npm run build` compila sin errores.
  - **Feature: Registrar un Seguimiento Ya Hecho desde la Búsqueda Global, no Solo Programar uno Futuro ([ClientHistoryModal.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/components/shared/ClientHistoryModal.jsx) [MODIFY])**:
    - **Pedido del usuario**: en el resumen del cliente que abre la búsqueda global (agregado en la sesión anterior de hoy), además de poder programar un próximo seguimiento, quería poder registrar uno que **ya pasó** — ej. le escribió al cliente por WhatsApp fuera del mazo y quiere dejarlo anotado sin tener que abrir el árbol de decisión completo de una llamada.
    - **Corrección**: nuevo `LogDoneFollowUpForm` junto al `AddFollowUpForm` ya existente — selector de resultado (No respondió / Contestó / Agendó / Cerró la venta / Pagó, mismas 5 opciones que usa el árbol de decisión) + nota opcional, que llama a `POST /closer/deck/<id>` con `contact_result` (mismo campo que ya usa `saveSeguimientoReport` para esto, sin tocar backend) — el endpoint ya seteaba `last_contact_outcome`/`last_contact_at=ahora` con ese campo, solo faltaba un punto de entrada fuera del árbol de decisión completo. La lista "Hechos" se actualiza al instante sin recargar.
    - **Verificado end-to-end en navegador real** (simulando a Jean Carlo): buscado "Saul" en la barra superior, abierto el resumen de "Saul Logreira" desde los resultados, registrado un seguimiento hecho con resultado "Contestó" y una nota — la sección "Hechos" pasó de `(0)` a `(1)` mostrando la fecha y "CONTESTÓ" sin recargar la página; confirmado en la base de datos que `last_contact_outcome`/`last_contact_at`/`closer_notes` quedaron guardados correctamente. Cita de prueba restaurada a su estado original tras la verificación. `npm run build` compila sin errores.
  - **Bugfix: El Contador de "③ Seguimientos" en la Bandeja Siempre Marcaba 0 ([closer.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])**:
    - **Reportado por el usuario**: en la pestaña de Seguimientos de la Bandeja, el número siempre decía cero — debería decir cuántos seguimientos hay para hacer hoy.
    - **Diagnóstico**: `GET /closer/deck/counts` (el endpoint que alimenta ese badge, "X/50" del header, y el título de la pestaña) contaba seguimientos con `Appointment.fecha_seguimiento LIKE 'hoy%'` — coincidencia **exacta** con la fecha de hoy, sin derivar el tipo efectivo del seguimiento. La pestaña ③ Seguimientos en sí (sección "Asignados para hoy") usa una fuente distinta y ya corregida en una sesión anterior (`CloserFollowUpService.get_today_grouped`, ver bitácora del 5 de agosto): vencidos + de hoy (`fecha_seguimiento <= hoy`), con `_effective_tipo` para derivar la categoría cuando el closer nunca etiquetó la cita explícitamente. Confirmado contra datos reales de Jean Carlo: 2 seguimientos reales asignados para hoy (uno vencido del 24/7, otro del 3/8) que la pestaña ya mostraba correctamente, pero el contador nunca los veía por no ser fecha exacta de hoy — como la inmensa mayoría del trabajo pendiente real es backlog atrasado, el contador terminaba en 0 casi siempre.
    - **Corrección**: `get_closer_deck_counts` ahora reutiliza `CloserFollowUpService.get_today_grouped` (la misma fuente de la pestaña) en vez de su propia query ad-hoc — mismo criterio, sin duplicar lógica.
    - **Verificado en navegador real**: simulando a Jean Carlo, el badge "③ Seguimientos" pasó de `0` a `2`, igual que "💬 2/50 SEGUIMIENTOS" del header — coincidiendo exactamente con los 2 items que muestra "📅 Asignados para hoy" al abrir la pestaña. `npm run build` compila sin errores (cambio 100% backend).

- **6 de Agosto de 2026** *(continuación — recordatorios de seguimiento por WhatsApp vía Whatchimp)*:
  - **Pedido del usuario**: avisarle a cada closer por WhatsApp cuándo tiene que hacer un seguimiento, usando Whatchimp (plantilla ya aprobada `421566`, número de negocio `1250095971516745`) con 4 variables (closer, tipo de seguimiento, nombre del lead, teléfono del lead) enviadas al número de WhatsApp del closer. Dio los 3 números de los closers activos (Jean Carlo, Sebastián, Marlon) y confirmó que `WHATCHIMP_API_KEY` ya está en el `.env`.
  - **`WhatchimpService`** (nuevo, [whatchimp_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/whatchimp_service.py) [CREATE]): arma y envía el POST a la plantilla de Whatchimp tal cual el curl que pasó el usuario, con `normalize_phone` (solo dígitos, sin `+`/espacios — necesario para que el número del lead quede en el formato que arma un link directo al chat) igual patrón que ya usa `TwoChatService` para lo mismo.
  - **Número de WhatsApp del closer**: en vez de crear un campo nuevo, se reutilizó `User.two_chat_number` — ya existía en el modelo y ya era editable desde Gestión de Equipo (`admin.py`), pero no lo consumía nada todavía. Sembrados los 3 números dados por el usuario con un script one-off (no migración de datos): Jean Carlo, Sebastián y Marlon Closer. Cualquier closer nuevo solo necesita que un admin le cargue el número ahí, sin tocar código.
  - **`CloserFollowUpService.send_due_reminders`** ([closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY]): recorre exactamente los mismos seguimientos que ya ve la pestaña ③ Seguimientos en "Asignados para hoy" (`get_today_grouped` — vencidos + de hoy, tipo efectivo derivado, arreglado en la entrada anterior de hoy) y le manda un WhatsApp a cada closer por cada uno que todavía no tenga aviso enviado en el día. Nueva columna `Appointment.followup_reminder_sent_at` (migración `ca66e36b5bca`) evita reenviar el mismo aviso si el cron externo se llama varias veces al día — se resetea solo al cambiar de día calendario.
  - **Bug propio encontrado y corregido durante la verificación**: la primera versión marcaba `followup_reminder_sent_at = datetime.utcnow()` pero comparaba contra `date.today()` (hora **local** del servidor) — cerca de medianoche UTC ambas fechas quedan en días de calendario distintos, así que la protección contra reenvío fallaba siempre y cada corrida del cron habría vuelto a mandar el mismo mensaje. Se corrigió a `datetime.now()` (local, consistente con `date.today()` y con el resto de `CloserFollowUpService`, que ya trabaja con fechas locales en string).
  - **Nuevo endpoint** `GET /api/closer/followups/cron/send-reminders?token=...` ([closer_followups.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer_followups.py) [MODIFY]) — mismo patrón de `CRON_SECRET` que ya usa `/api/sheets/cron-sync`, para que un cron externo lo dispare periódicamente. Sin `@login_required` a propósito (un cron no tiene sesión); el token es la única protección.
  - **Verificado con la base de datos local, sin enviar mensajes reales** (mockeando `WhatchimpService.send_followup_reminder` para no spamear los WhatsApp reales de los closers durante la prueba): primera corrida detectó y "envió" a los 21 seguimientos pendientes reales de los 3 closers con número configurado; segunda corrida el mismo día devolvió `sent: 0, already_sent_today: 21` — confirma que la protección contra reenvío funciona tras el fix de zona horaria. Autenticación del endpoint probada con token incorrecto (401), sin token (401) y token correcto (200). Los 21 registros de prueba se restauraron a `followup_reminder_sent_at = None` después de verificar, para que el cron real (una vez configurado) sí mande los avisos reales.
  - **Pendiente para el usuario**: este endpoint no se dispara solo — hace falta configurar un cron externo (mismo mecanismo que ya deben tener para `/api/sheets/cron-sync`) que llame periódicamente a `GET /api/closer/followups/cron/send-reminders?token=<CRON_SECRET>` (ej. cada 30-60 min). No se probó el envío real contra la API de Whatchimp (hubiera mandado un WhatsApp real a un closer) — antes de activar el cron en producción, conviene mandar un mensaje de prueba manual para confirmar que la plantilla renderiza bien las 4 variables.

- **6 de Agosto de 2026** *(continuación — recordatorios corridos por el propio proceso, no por cron externo)*:
  - **Pedido del usuario**: no quería depender de configurar un cron externo — pidió que los recordatorios se disparen solos "a la misma hora del día (puede ser las 10am) de cada usuario", configurado directamente en el código. También autorizó explícitamente mandar mensajes reales de prueba ("envíalos para probar, no hay problema").
  - **`reminder_scheduler.py`** (nuevo, [reminder_scheduler.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/reminder_scheduler.py) [CREATE], wireado en [`app/__init__.py`](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY])**: `BackgroundScheduler` de APScheduler (nueva dependencia, `requirements.txt` [MODIFY]) que arranca dentro del propio proceso de Flask y llama a `CloserFollowUpService.send_due_reminders()` cada 15 minutos — ya no depende de que el usuario configure nada fuera del repo. Se guarda contra arrancar donde no corresponde: comandos de Flask CLI (`flask db migrate/upgrade/shell`, detectados por `sys.argv[1]`) y el proceso "watcher" del reloader de debug de Werkzeug (`python run.py` en local sin `WERKZEUG_RUN_MAIN`) — sin esta guarda, `flask db migrate` habría arrancado un scheduler innecesario, y el reloader local lo habría duplicado.
  - **`send_due_reminders` ahora respeta la hora local de CADA closer, no la del servidor** ([closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY]): nuevo parámetro `reminder_hour` (default `10`) — antes de que sean las 10am en la zona horaria del propio closer (`User.timezone`, mismo patrón ya usado por `_user_day_bounds_utc`), sus seguimientos quedan gateados (`gated_before_hour` en la respuesta) sin tocar la base ni llamar a Whatchimp. Como el scheduler tickea cada 15 minutos y la protección contra reenvío (`followup_reminder_sent_at`) ya existía de la pasada anterior, cada closer recibe su aviso una sola vez, apenas pasan las 10am de **su propio** huso horario — no todos a la vez a la hora del servidor.
  - **Verificado en caliente contra el deploy local, con la API key real** (autorizado explícitamente por el usuario): al arrancar el proceso con el scheduler recién wireado, tickeó de inmediato y **envió 21 recordatorios reales** por WhatsApp a los 3 closers activos (Jean Carlo, Sebastián, Marlon) — uno por cada seguimiento pendiente real que tenían para hoy. Confirmado que una segunda corrida el mismo día no reenvía nada (`already_sent_today: 21`, `0` llamadas nuevas a Whatchimp). `flask db migrate`/`flask db current` confirmados sin arrancar el scheduler.
  - **Nota para el usuario**: el envío real de esta verificación no fue un solo mensaje de prueba controlado — al ser parte de arrancar el proceso normalmente, salieron los 21 avisos reales pendientes de una sola vez a los 3 closers. Quedó documentado acá por transparencia; si algún closer pregunta por qué recibió varios WhatsApp seguidos hoy, es por esto. El endpoint de cron externo (`GET /followups/cron/send-reminders?token=...`) de la entrada anterior se dejó tal cual, sin usarse — puede servir como disparador manual/de respaldo si hiciera falta.

- **6 de Agosto de 2026** *(continuación — el número de WhatsApp del closer no tenía dónde configurarse en producción)*:
  - **Pregunta del usuario**: antes de desplegar a Railway, preguntó si hacía falta configurar los números de WhatsApp de los closers en producción, sospechando (correctamente) que solo se habían cargado en la base local.
  - **Confirmado**: sí. Los 3 números (`User.two_chat_number`) se cargaron con un script one-off directo contra la base **local** (ver entrada de hoy sobre Whatchimp) — no es una migración, no viaja con el deploy. Producción tiene su propia base de datos, así que ese campo va a estar vacío ahí hasta que se configure aparte.
  - **Hallazgo adicional al revisar cómo configurarlo**: `User.two_chat_number` ya era parte del `formData` del modal de editar/crear miembro en `TeamManagementPage.jsx` (el panel real de "Gestión de Equipo" que se usa hoy) y el backend (`admin.py`) ya lo aceptaba y guardaba — pero el modal **nunca renderizaba ningún input para ese campo**. La única página que sí lo mostraba (`pages/legacy/UsersPage.jsx`, con la etiqueta "Número 2Chat (WhatsApp Emisor)") no está registrada en ninguna ruta de `App.jsx` — inalcanzable. En la práctica, no había ninguna forma de configurar este número desde la UI en absoluto, ni en local ni en producción.
  - **Corrección**: se agregó el input faltante al modal real de Gestión de Equipo (`TeamManagementPage.jsx` [MODIFY]) — mismo campo `two_chat_number`, con label "Número de WhatsApp (Recordatorios de Seguimiento)" y una nota aclarando que si se deja vacío ese closer no recibe avisos automáticos.
  - **Verificado en navegador real**: abierto "Editar" sobre Jean Carlo desde Gestión de Equipo — el nuevo campo aparece y precarga correctamente `525620873819` (el valor cargado por script en la base local). `npm run build` compila sin errores.
  - **Pendiente para el usuario, antes/después de desplegar a Railway**:
    1. Agregar `WHATCHIMP_API_KEY` como variable de entorno en Railway (está en el `.env` local, que no viaja con el deploy porque está en `.gitignore`) — sin esto, el scheduler corre pero cada envío falla silenciosamente (`failed` en el resultado, sin tumbar el proceso).
    2. Una vez desplegado, entrar a Gestión de Equipo en producción y cargar el número de WhatsApp de cada closer activo (Jean Carlo `525620873819`, Sebastián `584148109164`, Marlon `584141205686`) usando el campo nuevo — el closer que no tenga número configurado simplemente no recibe recordatorios (no rompe nada).

- **6 de Agosto de 2026** *(continuación — segunda franja de recordatorios a las 4pm)*:
  - **Pedido del usuario**: además del aviso de las 10am, quería un segundo recordatorio a las 4pm (hora local de cada closer) por los seguimientos que todavía sigan pendientes.
  - **`CloserFollowUpService.send_due_reminders`** ([closer_followup_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY]): en vez de un `reminder_hour` fijo, ahora recibe `slot` (`'am'` o `'pm'`, diccionario `REMINDER_SLOTS` con su hora y su propio campo de "ya enviado hoy") — cada franja es completamente independiente de la otra, así que marcar el aviso de las 10am no bloquea el de las 4pm del mismo día. Nueva columna `Appointment.followup_reminder_sent_pm_at` (migración `772f47fdf24d`) junto a la ya existente `followup_reminder_sent_at` (que pasa a ser específicamente la de la franja `'am'`).
  - **`reminder_scheduler.py`** ([MODIFY]) y el endpoint de respaldo `GET /followups/cron/send-reminders` ([closer_followups.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/closer_followups.py) [MODIFY], nuevo parámetro `slot`, default `'am'`): cada tick del scheduler interno (cada 15 min) ahora prueba **ambas** franjas, cada una gateada independientemente por la hora local del closer — un closer puede recibir hasta dos avisos por día (10am y 4pm) mientras le sigan quedando seguimientos sin resolver.
  - **Verificado en caliente, con envío real** (autorizado por el usuario): al momento de la verificación, los 3 closers activos ya tenían pasadas las 4pm de su propia zona horaria (Jean Carlo 18:59 México, Sebastián y Marlon 20:59 — Caracas/La Paz), así que el scheduler de fondo disparó la franja `'pm'` real apenas arrancó el proceso durante la prueba — **21 recordatorios reales más**, uno por cada seguimiento pendiente, independientes de los 21 de la franja `'am'` de la entrada anterior de hoy (confirmado en la base: `followup_reminder_sent_at` y `followup_reminder_sent_pm_at` marcados por separado para los mismos 21 registros). Confirmado que corridas posteriores el mismo día no reenvían nada en ninguna franja (`already_sent_today: 21` en ambas). `flask db migrate`/`current` siguen sin arrancar el scheduler.
