# Bitácora - Septiembre 2026

- **1 de Septiembre de 2026** *(sistema de reporte de bugs y feedback: widget flotante global + captura reactiva de errores, [bug_report.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/bug_report.py) [NEW], [bug_reports.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/bug_reports.py) [NEW], [b7c8d9e0f1a2_add_bug_report_model.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/b7c8d9e0f1a2_add_bug_report_model.py) [NEW], [__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY], [models/__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/__init__.py) [MODIFY], [BugReportWidget.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportWidget.jsx) [NEW], [BugReportChat.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportChat.jsx) [NEW], [bugReportBus.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/utils/bugReportBus.js) [NEW], [MainLayout.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/MainLayout.jsx) [MODIFY], [ErrorBoundary.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/ErrorBoundary.jsx) [MODIFY], [api.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/services/api.js) [MODIFY])*:
  - **Modelo y endpoint (`BugReport`, `app/api/bug_reports.py`)**: tabla `bug_reports` con `user_id`/`user_role` (snapshot del rol al momento del reporte, no el actual), `description`, `urgency` (`muy_urgente`/`urgente`/`neutro`/`sin_urgencia`), `route`, `user_agent`, `technical_context` (JSON serializado con el error técnico si el disparo fue reactivo) y `screenshot` (data URL base64 en `Text`, sin límite de tamaño porque el proyecto no define `MAX_CONTENT_LENGTH`). `POST /api/bug-reports` requiere sesión (`@login_required`) pero cualquier rol puede crear un reporte; `GET /api/bug-reports`, `GET /api/bug-reports/<id>` (con screenshot incluido) y `PATCH /api/bug-reports/<id>/status` quedan restringidos a `admin` para una futura pantalla de gestión, que no se construyó en este alcance (no estaba pedida).
  - **Migración escrita a mano**: el autogenerate de Alembic sigue fallando en este repo por el problema preexistente de `ads.ad_set_id` → `ad_groups` (ya documentado el 29/08). La migración `b7c8d9e0f1a2` se escribió siguiendo el patrón de `286308c75915` y se verificó aplicándola contra `instance/local.db` (`flask db upgrade` corrió limpio, tabla confirmada por inspección directa de `sqlite_master`).
  - **Disparador reactivo sin tocar los ~15 archivos con `toast.error(...)` existentes**: en vez de instrumentar cada call site, el interceptor de respuesta de [api.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/services/api.js) dispara un evento global (`bugReportBus.js` → `CustomEvent`) en cualquier error 5xx o de red (`!error.response || status >= 500`). `BugReportWidget` escucha ese evento y muestra un mini-prompt tipo toast ("¿Quieres reportarlo?") que expira solo a los 10s si no se toca — abrir el chat completo automáticamente en cada fallo de fondo hubiera sido intrusivo. La petición POST del propio reporte se marca con `{ skipBugReport: true }` en el config de axios para no reentrar en el loop si el envío del reporte mismo falla.
  - **Disparador desde ErrorBoundary**: ahí sí hay una acción explícita del usuario sobre un error visible en pantalla, así que el botón "Reportar error" dispara el mismo evento con `autoOpen: true` y abre el chat directo (con `error.message` y `errorInfo.componentStack` precargados como contexto técnico), sin pasar por el prompt intermedio.
  - **Captura de pantalla**: `html2canvas` (dependencia nueva) sobre `document.body`, con `scale: 0.5` y compresión JPEG (calidad 0.6) para no inflar el payload; los elementos del propio widget (`data-bug-report-ignore="true"`) se excluyen de la captura vía `ignoreElements` para no fotografiar el chat sobre sí mismo.
  - **Posicionamiento del botón flotante**: `bottom-8 right-24` en vez de `right-8` porque esa esquina ya la usa el botón de tutorial de `OnboardingTour.jsx` (mismo `bottom-8 right-8`, condicional a que haya un tutorial disponible) — con ambos en `right-8` se hubieran superpuesto cuando coincidieran.
  - **Bug de icono detectado en verificación de navegador**: `lucide-react` en esta versión del proyecto no exporta `MessageCircleWarning` (sí `Bug`/`BugIcon`) — el build de Vite tiraba `SyntaxError` en consola y dejaba la pantalla en negro. Corregido a `Bug` en `BugReportWidget.jsx` y `ErrorBoundary.jsx`; hubo que borrar `frontend/node_modules/.vite` para que el caché de dependencias pre-bundleadas de Vite dejara de servir la versión rota.
  - **Verificación**: backend probado end-to-end con el test client de Flask (POST/GET/GET-by-id/PATCH, sesión simulada con `login.session_protection = None` porque `'strong'` invalidaba la sesión simulada entre requests). Flujo completo también verificado en el navegador ya autenticado (usuario `Kerwin`, rol `operator`): abrir el widget, completar los dos pasos, POST real devolviendo `201`, fila confirmada en `instance/local.db` con un screenshot real de ~9KB en base64, y limpieza del registro de prueba al terminar.

- **1 de Septiembre de 2026 (2)** *(pregunta "¿Cuál es el problema?" para reportes no reactivos + pestaña de gestión para operator/admin con respuesta al usuario, [bug_report.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/bug_report.py) [MODIFY], [bug_reports.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/bug_reports.py) [MODIFY], [c9d1e2f3a4b5_add_bug_report_response_fields.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/c9d1e2f3a4b5_add_bug_report_response_fields.py) [NEW], [BugReportChat.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportChat.jsx) [MODIFY], [BugReportWidget.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportWidget.jsx) [MODIFY], [BugReportHistory.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportHistory.jsx) [NEW], [BugReportsPanel.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/BugReportsPanel.jsx) [NEW], [OperationsSettingsPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/operations/settings/OperationsSettingsPage.jsx) [MODIFY])*:
  - **Pregunta condicional "¿Cuál es el problema?"**: solo se pregunta cuando el reporte NO viene de un error detectado (`!technicalContext`) — si viene de un error reactivo, esa pregunta ya está respondida por el error mismo y `BugReportChat` arranca directo en el paso "qué intentabas hacer". Backend: `problem` es `nullable=True` en el modelo, pero el endpoint `POST /bug-reports` exige `problem` **o** `technical_context` (al menos uno de los dos tiene que explicar el problema — nunca ninguno).
  - **Gestión para operator/admin**: se amplió `check_admin` a `check_manager` (`MANAGER_ROLES = ('admin', 'operator')`) en los cuatro endpoints de gestión (`GET` lista, `GET` detalle, `PATCH status`, `POST respond` nuevo) — el pedido especificaba "operador", y el resto del panel de control técnico (`OperationsSettingsPage.jsx`, donde vive esta pestaña nueva "Reportes de Bugs") ya usa `roles={['operator','admin']}` en `App.jsx`, así que se siguió el mismo criterio de acceso en vez de dejarlo admin-only como alerts.py.
  - **Responder al usuario**: `POST /bug-reports/<id>/respond` guarda `admin_response`, `responded_at`, `responded_by_id` y pone `is_read_by_user=False`; si el estado seguía en `open` pasa a `reviewed` automáticamente (pero NO pisa un estado ya `resolved` — probado explícitamente: responder después de marcar resuelto no regresa a `reviewed`). `BugReportsPanel.jsx` (nueva pestaña) lista todos los reportes con filtro por estado, selector de estado inline, input de respuesta rápida y un lightbox que pide el detalle completo (`GET /bug-reports/<id>`, con el screenshot) solo al abrirlo — la lista general nunca trae los screenshots completos para no inflar el payload.
  - **El usuario ve la respuesta (`BugReportHistory.jsx`, nuevo)**: pestaña "Mis reportes" accesible desde un segundo botón flotante (icono de reloj) junto al botón de reportar. `GET /bug-reports/mine` es de cualquier usuario autenticado y solo devuelve sus propios reportes (aislado por `user_id`, verificado explícitamente en la simulación). Badge de "no leído" en el botón flotante: el fetch pasivo que arma el contador (al montar el widget) **no** manda `?mark_read=true`, así el aviso no desaparece antes de que el usuario efectivamente abra "Mis reportes" — solo abrir el historial de verdad marca `is_read_by_user=True` en el backend.
  - **Bug de icono #2 en verificación de navegador**: mismo problema que con `MessageCircleWarning` el 1/09 — `lucide-react` tampoco exporta `MessageSquareReply` en esta versión. Corregido a `Reply` en `BugReportHistory.jsx`, verificado con un chequeo rápido de `Object.keys(require('lucide-react'))` antes de dar por buenos los demás íconos nuevos (`ImageIcon`, `History`, `Reply`, etc.) para no repetir el mismo ciclo de error-en-navegador otra vez.
  - **Simulación multi-rol (pedido explícito del usuario, `scratch/simulate_bug_reports.py`, no comiteado — `scratch/` está en `.gitignore`)**: se simuló closer, setter, triage, operator y admin con sesiones de Flask test client independientes, cubriendo: reporte no-reactivo exige `problem` (400 si falta), reporte reactivo no lo exige, urgencia inválida rechazada, closer/setter/triage reciben 403 en los endpoints de gestión (lista completa, detalle, responder), operator ve la lista completa y responde, admin cambia estado a `resolved` y luego responde sin que el estado retroceda, el reportante ve su propia respuesta vía `/mine` (con la distinción pasivo vs `mark_read=true` del punto anterior), y el aislamiento por usuario en `/mine` (un setter no ve reportes de otros). Las 10 verificaciones pasaron.
  - **Bug real encontrado en el harness de simulación, no en la app**: la primera versión del script fallaba con confusión de identidad entre usuarios simulados (el reporte del setter terminaba atribuido al closer). Causa: el script envolvía TODAS las llamadas HTTP dentro de un único `with app.app_context():` externo — Flask-Login cachea el usuario resuelto en `flask.g`, que vive atado al app context; como Flask reusa el app context ya activo en vez de empujar uno nuevo por request cuando detecta uno igual ya en el stack, el segundo usuario logueado heredaba el `flask.g` del primero. Fix: las llamadas de `test_client()` se sacaron de cualquier `app_context()` manual (cada request de Flask empuja el suyo propio si no hay uno activo); el `app_context()` solo se usa para leer/limpiar la base de datos antes y después. Además se detectó que el usuario de triage por defecto (`Romer`) tiene `is_active=False` en la base local — `UserMixin.is_authenticated` de Flask-Login devuelve `self.is_active`, así que una cuenta desactivada nunca se considera autenticada aunque la sesión tenga su `_user_id` — el script se ajustó para tomar usuarios activos (`Dari` en vez de `Romer`).
  - **Verificación en navegador**: además de la simulación de backend, se probó el flujo real en el navegador (usuario `Kerwin`/operator) — envío de un reporte no-reactivo con la pregunta "¿Cuál es el problema?" visible, aparición inmediata en la pestaña "Reportes de Bugs" con ambas respuestas mostradas, apertura del lightbox de captura de pantalla (confirmó una imagen real de la vista), envío de una respuesta desde el panel (transición automática de estado a "en revisión" + toast de confirmación) y verificación de que "Mis reportes" del widget muestra esa misma respuesta. Registro de prueba eliminado al terminar.

- **1 de Septiembre de 2026 (3)** *(fix: closers y otros roles no veían el widget — `CloserWorkflowPage` corre fuera de MainLayout, [App.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/App.jsx) [MODIFY], [MainLayout.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/MainLayout.jsx) [MODIFY], [BugReportWidget.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportWidget.jsx) [MODIFY])*:
  - **El bug real**: `/closer/deck` (`CloserWorkflowPage`, donde los closers pasan literalmente todo su turno tras el login) es la ÚNICA ruta protegida de todo `App.jsx` que NO envuelve su contenido en `<MainLayout>` — el propio archivo ya tenía un comentario explicando por qué ("para que los modales fixed funcionen standalone") y duplica su propio listener de atajos de teclado por el mismo motivo. Como `BugReportWidget` se montaba dentro de `MainLayout`, los closers jamás veían el botón flotante — el usuario lo reportó directamente ("no veo como pueden los closers y otros usuarios reportar bugs") y encontrar la causa exacta requirió leer `App.jsx` ruta por ruta, no solo mirar el componente del widget.
  - **Fix**: se movió `<BugReportWidget />` de dentro de `MainLayout.jsx` a `App.jsx`, como hijo directo de `<Router>` junto a `<Toaster />` — así se monta una sola vez para TODA la app sin importar si la vista activa usa `MainLayout` o no, y como vive fuera del árbol DOM de cualquier página no lo afecta un `transform` de framer-motion en alguna vista que le rompiera el `position: fixed` (un problema real de CSS que sí habría aparecido si se hubiera insertado dentro de cada página en vez de a nivel raíz). El propio componente ahora se autogatea con `useAuth()` (`if (!user) return null` después de los hooks) en vez de depender de que su padre ya filtre por sesión.
  - **Verificación**: no se pudo iniciar sesión como closer real (sin credenciales), así que se usó la función de "Acceso Simulado" que ya tiene la app — impersonar al closer `Jean Carlo` vía `POST /api/auth/impersonate` (llamado directamente desde la consola del navegador, mismo flujo que usa `OperatorControls.jsx`) y navegar a `/closer/deck?step=confirmations`. El botón de bug aparece ahí, el chat abre con "¿Cuál es el problema?" y el header muestra "JEAN CARLO · CLOSER" correctamente. Se revirtió la simulación (`POST /api/auth/revert`) al terminar para dejar la sesión del operador como estaba.

- **1 de Septiembre de 2026 (4)** *(hilo de mensajes real: el operador solo podía enviar una respuesta que se pisaba a sí misma y el usuario no podía contestar, [bug_report.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/bug_report.py) [MODIFY], [bug_reports.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/bug_reports.py) [MODIFY], [d3e4f5a6b7c8_add_bug_report_messages.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/d3e4f5a6b7c8_add_bug_report_messages.py) [NEW], [BugReportThread.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportThread.jsx) [NEW], [BugReportHistory.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportHistory.jsx) [MODIFY], [BugReportWidget.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportWidget.jsx) [MODIFY], [BugReportsPanel.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/BugReportsPanel.jsx) [MODIFY], [BugReportChat.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportChat.jsx) [MODIFY])*:
  - **El pedido del usuario**: *"respondi como operador y envie 2 mensajes a un reporte y el usuario solo puede ver la ultima respuesta que le envié y no me puede responder"*. El diseño anterior (campos `admin_response`/`responded_at`/`responded_by_id` en `BugReport`) era literalmente una sola respuesta por reporte, sin capacidad de conversación — no era un bug puntual, era una limitación de diseño del alcance anterior.
  - **Modelo nuevo (`BugReportMessage`)**: tabla `bug_report_messages` (FK a `bug_reports` con `ondelete='CASCADE'`) reemplaza esos cuatro campos. En su lugar, `BugReport` guarda `user_last_read_at`/`manager_last_read_at` — un mensaje cuenta como no leído para un lado si es de "el otro lado" (comparando `sender_id` contra `report.user_id`) y llegó después de su último `*_last_read_at`. Esto generaliza sin necesitar chequear roles explícitamente: si el mismo operador reportó y respondió su propio bug (como en la verificación del 1/09 anterior), `sender_id == report.user_id` siempre, así que nunca se marca a sí mismo como no leído.
  - **Migración con paso de datos**: antes de borrar `admin_response` y compañía, la migración `d3e4f5a6b7c8` copia cualquier respuesta existente a un `BugReportMessage` (usando SQL crudo vía `conn.execute` porque en el momento de la migración el modelo ORM viejo ya no coincide con el código) para no perder historial de reportes que ya tenían una respuesta guardada.
  - **Bug real encontrado en desarrollo, no en producción**: `to_dict()` calculaba `last_message` con `self.messages.order_by(BugReportMessage.created_at.desc()).first()` sobre la relación `lazy='dynamic'`, pero como esa relación ya se declaró con `order_by='BugReportMessage.created_at'` (ascendente) en su definición, el `.order_by(...desc())` encadenado no reemplazaba el orden sino que se sumaba, y devolvía el mensaje más ANTIGUO en vez del más reciente. Se detectó en la simulación (`scratch/simulate_bug_thread.py`, no comiteado), no fue obvio hasta comparar el resultado esperado contra el real. Fix: consultar `BugReportMessage.query.filter_by(...)` directo en vez de a través de la relación dinámica, así el `.order_by()` sí controla el orden desde cero.
  - **Endpoints**: `GET/POST /bug-reports/<id>/messages` reemplazan a `POST .../respond`. Accesibles para managers (cualquier reporte) o el propio autor (solo el suyo) — un tercer usuario (ej. un setter viendo el reporte de un closer) recibe 403 en ambos métodos, verificado explícitamente. Abrir el hilo (`GET`) marca la lectura del lado que lo abre automáticamente, sin necesitar el parámetro `?mark_read=true` de la versión anterior (ya no hace falta distinguir "fetch pasivo" de "abrir de verdad": la lista (`/mine`, `GET /bug-reports`) nunca abre el hilo, así que nunca marca lectura por accidente).
  - **`BugReportThread.jsx` (nuevo, compartido)**: burbujas de chat alineadas según `sender_id === user.id`, reutilizado tanto en `BugReportsPanel.jsx` (dentro de cada tarjeta, para el operador) como en `BugReportHistory.jsx` (lista de reportes propios → click abre la conversación con botón de volver). El campo de urgencia/estado/captura de pantalla se sigue mostrando por fuera del hilo (no duplicado dentro del componente compartido).
  - **Badge de no leído bidireccional**: `unread_for_user` (para el badge del widget flotante y el punto rojo en la lista de "Mis reportes") y `unread_for_manager` (punto rojo en `BugReportsPanel` cuando el usuario respondió y el operador todavía no lo vio) — antes solo existía la dirección operador→usuario.
  - **Verificación de extremo a extremo en navegador real** (no solo backend): impersonar al closer `Jean Carlo`, crear un reporte desde `/closer/deck` con la UI real del widget; volver al operador y enviar DOS mensajes desde `BugReportsPanel` (confirmando "CONVERSACIÓN (2)", no una respuesta pisada); volver a impersonar al closer y confirmar que "Mis reportes" muestra el badge de no leído y AMBOS mensajes al abrir la conversación; responder como closer desde ahí; volver al operador y confirmar que ve los 3 mensajes y el punto de "el usuario respondió". Registros de prueba eliminados al terminar.
  - **Nota operativa**: el backend real (puerto 5000, proceso del usuario, fuera del sandbox de las herramientas de Claude) tiró 500 en todos los endpoints — incluido `/api/auth/csrf-token`, que no toca `BugReport` — porque seguía corriendo el código de antes de este cambio de modelo contra una base de datos ya migrada al nuevo esquema. Ninguna herramienta (Bash, PowerShell) tenía visibilidad de ese proceso ni del puerto 5000 para reiniciarlo — se le pidió al usuario que lo reiniciara manualmente, y tras eso los 500 desaparecieron.
  - **Fix de estilo (pedido aparte del usuario en el mismo turno): fondo transparente del chat**. `glass-effect` — usado en `BugReportChat.jsx` y `BugReportHistory.jsx`, copiado del patrón de `GlobalSettingsModal.jsx`/`OperatorControls.jsx` — no está definida en ningún `.css` del proyecto (confirmado con grep en todo `frontend/src`, incluyendo `tailwind.config.cjs`: no genera esa clase). El panel quedaba completamente transparente en vez del look "glass" traslúcido que aparentaba el nombre. Cambiado a `bg-surface` (clase real, `background: var(--bg-card)`, ya usada por el resto de tarjetas de la app) en ambos componentes. `GlobalSettingsModal.jsx` y `OperatorControls.jsx` tienen el mismo `glass-effect` sin definir — quedó fuera de este alcance porque no fue lo que se pidió arreglar, pero es la misma causa raíz si en algún momento se reporta que esos modales también se ven "vacíos". *(Nota: se abrió un task en paralelo para auditar y corregir `glass-effect` en el resto de la app — renombrada/reemplazada en esa sesión, ver commit `b2bf72ca fix(ui): actualiza referencias a la clase glass-effect renombrada` en `main`.)*

- **2 de Septiembre de 2026** *(incidente de producción: closer bloqueado al declarar un Parcial — cruce falso de identidad por email compartido con una venta huérfana, sin cambios de código, solo dato corregido en Postgres de producción)*:
  - **Síntoma reportado por el closer** (vía log de Railway pegado por el usuario): al declarar un pago Parcial de $250 para **Kervin Calderón** (`client_id=8523`) en Ace Learner, `SheetsService.post_to_sheets` lo rechazaba 8 veces seguidas con *"Este cliente ya pagó el programa por completo."* — el bloqueo de `SalesConsistencyService.validate_next_payment_type` en [sales_consistency_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/sales_consistency_service.py) para `tipo='parcial'` cuando `state['has_full_payment']` es `True`.
  - **Diagnóstico** (consulta de solo lectura contra `DATABASE_PRODUCTION`, con permiso explícito del usuario — no hay forma de reproducir esto en la base local de desarrollo porque el cliente no existe ahí): el matching por email/instagram/teléfono de `get_client_payment_state` (agregado deliberadamente para unir historial de un mismo cliente disperso entre client_ids, por `client_id` casi siempre `NULL` en datos históricos) encontró la venta `id=57637` (`AL - completo`, $1000, 27-ago-2026) por coincidencia de email (`mail_cliente = 'juancsanchez.jcsr@gmail.com'`, igual al de Kervin). Esa venta ya estaba huérfana de por sí *antes* de tocar a Kervin: su `client_id` apunta a **Emilia Collantes** (Ecuador, IG `dolcezza00_`), pero sus propios campos denormalizados dicen `nombre_cliente="juan Camilo Sanchez Ramirez"`, `telefono="57 316 8287492"` (Colombia) — ninguno de los tres (Emilia, Juan Camilo, Kervin) coincide con los otros dos salvo por ese único email compartido. Es basura de una importación/backfill anterior, no evidencia real de que Kervin ya hubiera pagado.
  - **Corrección aplicada**: `UPDATE financial_sales SET mail_cliente = NULL WHERE id = 57637` directo en producción (1 fila afectada, verificado antes/después) — se limpió solo el campo que causaba el cruce falso, sin tocar `monto`, `tipo_pago`, `client_id` ni el resto de la fila, para no alterar el historial real de esa venta (que sigue siendo válida para quien sea que realmente la hizo). Verificado con la misma consulta de matching que usa el código: ya no aparece ninguna venta cruzada con el email/instagram/teléfono/client_id de Kervin. El closer ya puede reintentar el Parcial de $250.
  - **Riesgo no resuelto, fuera de este alcance**: el matching sigue basándose en un solo campo (email O instagram O teléfono) sin corroborar con el nombre — cualquier otro email/instagram/teléfono compartido por coincidencia entre dos clientes reales seguiría produciendo el mismo falso positivo. No se tocó la lógica porque debilitarla a ciegas rompería la detección real de duplicados que motivó agregarla (casos documentados: Jonathan Aparicio, Marcos Melo). Requeriría una señal adicional (ej. exigir 2 de 3 coincidencias, o similitud de nombre) diseñada con cuidado, no un parche apurado sobre un incidente puntual.

- **2 de Septiembre de 2026 (2)** *(fix del riesgo señalado arriba: matching de identidad más robusto en `get_client_payment_state`, [sales_consistency_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/sales_consistency_service.py) [MODIFY])*:
  - **El cambio**: el filtro SQL por email/instagram/teléfono se mantiene igual de amplio (sigue trayendo cualquier fila con una sola señal en común, para no perder recall), pero ahora es solo el paso de **candidatos** — la decisión de si una `FinancialSale` realmente pertenece al cliente se mueve a Python con un criterio más estricto: un match directo por `client_id` sigue bastando por sí solo (es la señal más confiable, viene de un FK real, no de texto libre importado); si no hay `client_id` directo, hacen falta **al menos 2 de las 3 señales** (email + instagram, email + teléfono, o instagram + teléfono) coincidiendo, **o**, si solo coincide una, que el nombre del cliente (`Client.full_name`) y el nombre denormalizado de la venta (`FinancialSale.nombre_cliente`) se parezcan (`_names_corroborate`: comparten un token de ≥3 letras normalizado sin acentos, o `SequenceMatcher.ratio() >= 0.72`).
  - **Por qué esta forma y no otra**: el incidente de Kervin Calderón (entrada anterior de hoy) tenía exactamente 1 señal (email) y cero parecido de nombre — el nuevo criterio lo hubiera excluido automáticamente sin necesidad del parche manual en producción. Los casos reales que motivaron el cruce laxo original (Jonathan Aparicio, Marcos Melo — datos históricos con `client_id` NULL) no quedan documentados con el detalle exacto de qué campos coincidían en cada uno, así que no se pudo replicar 1:1; en su lugar se construyeron casos representativos del mismo patrón (venta histórica sin `client_id`, con 2 señales coincidentes, y con 1 señal + nombre reconocible) para confirmar que ese tipo de fusión real sigue funcionando.
  - **Verificación (`scratch/simulate_sales_matching_fix.py`, no comiteado)**: contra `instance/local.db`, 4 casos con Client/FinancialSale creados y borrados en la misma corrida — (A) réplica del incidente real (venta con `client_id` de un cliente distinto, `mail_cliente` compartido con el cliente real pero `nombre_cliente`/`telefono` de un tercero sin relación): `has_full_payment` queda `False` y `validate_next_payment_type(..., 'parcial')` permite el pago, donde antes del fix se habría bloqueado; (B) venta histórica sin `client_id`, coincide email + teléfono (2 señales): se fusiona (`has_installments=True`); (C) venta histórica sin `client_id`, coincide solo instagram (1 señal) pero el nombre es reconocible ("Marcos Melo" vs "Marcos Melo Gonzalez"): se fusiona (`has_deposit=True`); (D) control negativo, 1 sola señal (teléfono) y nombre sin ningún parecido: no se fusiona (`has_full_payment=False`). Las 4 verificaciones pasaron.

- **2 de Septiembre de 2026 (3)** *(pedido explícito del usuario: dejar de bloquear al closer por secuencia de pago inconsistente — caso real Emilia Collantes, solo se le dejaba declarar Renovación/Upsell — y darle la herramienta para corregir el historial en vez de solo negarle el registro, [sheets_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/sheets_service.py) [MODIFY], [DeclararVentaWizard.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/modals/DeclararVentaWizard.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])*:
  - **El problema**: `SalesConsistencyService.validate_next_payment_type` seguía siendo un bloqueo DURO en `SheetsService.post_to_sheets` (el único lugar donde se llama) — si el historial de un cliente estaba mal cargado (ej. una venta vieja con el tipo de pago equivocado, mismo tipo de dato huérfano que ya causó el incidente de Kervin Calderón el mismo día), el closer quedaba sin ninguna forma de declarar la venta REAL que estaba cerrando: la única corrección disponible (Operaciones editando la base) no la podía hacer él mismo en el momento, y el modal de venta (`DeclararVentaWizard`) directamente deshabilitaba el botón de la opción "inconsistente" (`Option disabled`), sin decirle qué hacer al respecto.
  - **El cambio de fondo**: la secuencia de negocio (Seña → Parcial/Completo → Cuota → Renovación/Upsell) sigue evaluándose igual — no se tocó `SalesConsistencyService` — pero ahora es solo un AVISO, nunca un bloqueo. `post_to_sheets` guarda la venta igual y agrega `warning: <razón>` a la respuesta cuando la secuencia no calza; el endpoint `/sheets/push` ya devolvía el dict completo (`jsonify(result)`), así que no hizo falta tocar esa capa. Todos los caminos que declaran una venta pasan por `post_to_sheets` (`DeclararVentaWizard`, `CuotaRow.markAsPaid` en `ClientHistoryModal`, `NewSalePage`, el endpoint público `/public/financial-sales/new`), así que el desbloqueo alcanza a todos por igual.
  - **Frontend — avisar sin bloquear**: en `DeclararVentaWizard`, el paso "¿Cómo paga?" ya no deshabilita ninguna tarjeta de tipo de pago (`Option` perdió su prop `disabled`, ahora tiene `warn`: borde/ícono ámbar en vez de opacidad + `cursor-not-allowed`) — cualquier tipo se puede elegir y clickear siempre. Si el tipo elegido tiene un `allowed_types[...].ok === false`, se muestra el motivo con un botón nuevo **"Ver / corregir historial del cliente"** que abre `ClientHistoryModal` (ya existía, con `VentaRow`/`PagoRow` editables — no se construyó nada nuevo para eso) sin cerrar el wizard; al cerrar el historial se refresca `saleClientState` (`fetchSaleClientState` extraída del `useEffect` a una función reusable) para que el aviso desaparezca solo si de verdad se corrigió el dato. También se sacó la lógica que saltaba automáticamente al primer tipo de pago "permitido" apenas cargaba el estado del cliente — hacía exactamente lo mismo que bloquear (le cambiaba la selección al closer sin que él lo pidiera).
  - **Aviso post-registro**: si la venta se guarda con `warning` en la respuesta, `handleRegisterSale` (en `CloserWorkflowPage`) muestra un `toast` aparte con el motivo, para que el closer se entere aunque no haya visto el aviso en el paso del wizard (ej. venía preseleccionado desde antes).
  - **Verificación (`scratch/simulate_closer_no_block.py`, no comiteado)**: contra `instance/local.db`, con `requests.post` mockeado para no escribir en la hoja de Google Sheets real — se replicó el patrón de Emilia (cliente con un `AL - completo` ya registrado) y se llamó `SheetsService.post_to_sheets` con un `AL - parcial` nuevo. Confirmado: `SalesConsistencyService.validate_next_payment_type` sigue devolviendo `ok=False` (la regla de negocio no se debilitó), pero `post_to_sheets` devuelve `status: success` con el `warning` incluido, y la venta Parcial queda de verdad guardada en `FinancialSale` — ya no hay bloqueo duro.

- **3 de Septiembre de 2026** *(panel de Postulaciones: nuevo voto "Reserva" entre Preseleccionar y Descartar, con su propio filtro/badge — pedido explícito del usuario a partir de un mockup HTML de referencia ("Panel de postulaciones (standalone).html"), [job_application.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/job_application.py) [MODIFY], [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesInbox.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesInbox.jsx) [MODIFY], [PostulacionDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionDetailModal.jsx) [MODIFY], [PostulacionVoteToast.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionVoteToast.jsx) [MODIFY])*:
  - **El pedido**: hasta ahora la ficha de un candidato solo tenía "Preseleccionar" / "Descartar". El usuario pidió un tercer botón "Reserva" para los candidatos que hoy no encajan pero podrían servir más adelante, con su propio lugar donde verlos agrupados — replicando el diseño de un HTML de referencia que compartió (mockup standalone con paleta de marca Learnation, no conectado a datos reales).
  - **Lógica de veredicto, copiada tal cual del mockup (no inventada)**: `JobApplication.veredicto()` (antes solo distinguía pre/des) ahora resuelve 3 valores por revisor con una jerarquía específica que el mockup ya definía en su función `veredictoDe`: si un revisor preselecciona y el otro descarta → `decidir` (conflicto real); si uno preselecciona y el otro solo reserva → gana `preseleccionada` directo, **sin pedir decisión** (preseleccionar es una señal más fuerte, no hace falta arbitrar); si uno reserva y el otro descarta → `decidir` también (ahí sí son señales opuestas); un solo voto `res` sin conflicto → `en_reserva`. Se replicó esta jerarquía exacta en vez de inventar una propia porque el mockup la definía explícitamente con ese orden de prioridad.
  - **Backend**: `VOTE_VALUES` pasa a `('pre', 'res', 'des')` (la columna `vote` ya era `String(10)`, sin migración necesaria). `FILTROS_VALIDOS` y `_aplica_filtro` en `job_applications.py` suman `en_reserva`.
  - **Frontend — filtro y tabla**: `PostulacionesInbox.jsx` suma el chip "En reserva" (entre "Preseleccionadas" y "Decidir") y el badge correspondiente en la tabla, en ámbar (mismo tono que "Decidir": el mockup usa el mismo color semántico `--warning` para ambos estados, son visualmente parecidos pero las etiquetas de texto los distinguen).
  - **Frontend — botones de voto con color permanente, no solo al seleccionar**: la primera versión dejaba los 3 botones con borde blanco neutro hasta que se votaban, y el usuario pidió explícitamente imitar más el mockup ("los botones tienen colores"). Se corrigió `PostulacionDetailModal.jsx` para que cada botón tenga su tinte semántico SIEMPRE visible (fondo/borde/texto translúcidos: esmeralda para Preseleccionar, ámbar para Reserva, rosa/rojo para Descartar) y pase a color sólido (fondo lleno + texto claro u oscuro según contraste) solo cuando ese es el voto del revisor actual — mismo patrón `bg` translúcido / `bg` sólido que usa `btnPre`/`btnRes`/`btnDes` en el mockup. Los tres botones se reorganizaron de un `flex justify-end` de 2 botones a un `grid grid-cols-3` de igual ancho para los 3.
  - **`PostulacionVoteToast.jsx`**: el toast de confirmación al votar solo distinguía Preseleccionado (verde) vs. Descartado (rojo, aplicado por defecto a cualquier otro valor) — con el voto nuevo, reservar hubiera mostrado el toast rojo de "Descartado" por error. Se generalizó a una tabla `VOTO_TOAST` con los 3 estados (label, color, ícono) en vez de la lógica binaria `esPre ? ... : ...`.
  - **Verificación en navegador** (simulación como `Santiago`, admin, vía "Simular" del panel de operaciones — luego revertida con `/api/auth/revert`): se sembraron 3 postulaciones de prueba (`scratch/seed_test_postulantes.py`, no comiteado) para tener datos con qué probar, porque la base local no tenía ninguna postulación completa cargada. Se verificó: votar "Reserva" en la ficha dispara `POST /job-applications/<id>/vote` con `valor: "res"` y devuelve `veredicto: "en_reserva"`; el chip "En reserva" y el badge de tabla reflejan el conteo correcto; el botón votado pasa a color sólido y los otros dos quedan con su tinte translúcido; el autoavance a "siguiente pendiente" sigue funcionando igual que con pre/des. Registros de prueba eliminados al terminar (`scratch/cleanup_test_postulantes.py`, no comiteado).
  - **Pendiente, no pedido en este alcance**: el usuario mencionó que "en un futuro podrían funcionar" los candidatos en reserva — no se agregó ningún recordatorio ni vencimiento para revisarlos de nuevo, el filtro "En reserva" es puramente una vista manual por ahora.

- **3 de Septiembre de 2026 (2)** *(script `actualizar_db.py` no traía Postulaciones desde producción, [actualizar_db.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/scripts/actualizar_db.py) [MODIFY])*:
  - **Contexto**: para testear el panel de Postulaciones con casos reales (no solo datos inventados) el usuario pidió actualizar la base local con lo que hay en producción. Un primer intento con un script propio conectando directo a `DATABASE_PRODUCTION` fue bloqueado por el clasificador de seguridad del entorno; el usuario aclaró que ya existe `scripts/actualizar_db.py` para esto exactamente, así que se usó ese en vez de insistir con un script nuevo.
  - **El gap real**: `JobApplication`, `JobApplicationVote` y `ClarityWeight` (agregados el 1-2/09) nunca se sumaron a la lista `modelos` de `actualizar_db.py` — el script sincroniza sin errores pero simplemente nunca tocaba esas 3 tablas. Se agregaron en el orden de dependencia correcto (`JobApplication`/`ClarityWeight` junto a los modelos independientes, `JobApplicationVote` en "Dependencia Nivel 1" por su FK a `JobApplication`/`User`).
  - **Ejecutado** (`python scripts/actualizar_db.py --target local`, permiso explícito del usuario): reemplazó TODA la base local (no solo Postulaciones — es el comportamiento ya existente del script, limpia y reimporta todos los modelos de `modelos`) con el estado real de producción al momento de correrlo: 215 postulaciones y 47 votos importados, entre otras ~60 tablas. La sesión de simulación de admin activa en el navegador se perdió (el `is_impersonating` no sobrevive al restart del backend que dispara el propio guardado de código durante la sesión, no la sincronización en sí) — hubo que volver a "Simular" desde Gestión de Equipo.

- **3 de Septiembre de 2026 (3)** *(pestaña propia para "En reserva" + rediseño completo de la pestaña "Incompletas" con contacto/avance/materiales, pedido explícito del usuario tras ver el panel con datos reales, [job_application.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/job_application.py) [MODIFY], [PostulacionesInbox.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesInbox.jsx) [MODIFY])*:
  - **"En reserva" deja de ser un chip de filtro y pasa a ser pestaña propia**: el usuario prefirió esto en vez de que compitiera como un chip más entre "Mis pendientes/Todas/Preseleccionadas/Decidir/Descartadas". `PostulacionesInbox` ahora tiene un selector de 3 vistas (`Revisión` / `En reserva` / `Incompletas`) por encima de esos chips; "Incompletas" también se sacó del arreglo `FILTROS` para vivir como la tercera vista (el usuario confirmó que su ubicación ya estaba bien, pero conceptualmente es la misma idea: no compite con los chips de la revisión normal). "Revisión" agrupa lo que antes eran todos los chips (mis pendientes/todas/preseleccionadas/decidir/descartadas); "En reserva" reutiliza la misma tabla de candidatos (Formación/Inglés/Cierre/Progreso/Score/Estado) filtrada a `en_reserva`, sin sub-filtros propios.
  - **Backend, `to_dict()`**: `whatsapp`, `pais`, `edad`, `video` y `llamada` pasan al bloque de campos "siempre visibles" (antes solo estaban bajo `include_respuestas=True`, que el listado nunca pide) — la nueva tabla de Incompletas los necesita en la respuesta liviana de `/job-applications?filtro=incompletas`.
  - **Investigación previa del "no se está midiendo bien" que reportó el usuario**: antes de tocar el diseño se auditó `respondidas()`/`CAMPOS_FORMULARIO` contra las 143 postulaciones incompletas reales recién importadas — sin mismatches entre el conteo del método y un conteo manual campo por campo, sin duplicados por `dedupe_key`, sin ninguna completa (`completo=True`) con porcentaje bajo. Lo que sí aparece, y probablemente es lo que motivó el comentario: 21 candidatos quedan en 91-96% (les falta solo `video`/`llamada`, las dos últimas preguntas del formulario) — matemáticamente correcto, no un bug, pero el número solo (sin desglose) sugiere "casi completo" sin explicar por qué se frenaron ahí. La columna nueva de Materiales resuelve exactamente esa ambigüedad. (Aparte, algunos nombres con tildes se veían con `�` al imprimirlos por `print()` en la consola de Windows — se confirmó con los bytes UTF-8 crudos que es solo cp1252 de la consola, el dato en la base está bien guardado y el navegador lo renderiza correcto.)
  - **Tabla de Incompletas, según el detalle que pidió el usuario**: Candidato (nombre + "país · edad" si hay al menos uno de los dos, mockup-style); Contacto (WhatsApp como link a `wa.me/<solo dígitos>` con ícono si hay número, texto "Sin WhatsApp" en itálica si no; correo como `mailto:`, "Sin correo" si viene vacío — el modelo permite `email=''` para quien abandonó en la primera pregunta); Avance (fracción + porcentaje + barra de progreso con gradiente azul→magenta); Materiales (pill verde "Ver video"/"Ver llamada" con link si están, pill gris "Sin video"/"Sin llamada" si no — para que se vea de un vistazo quién subió qué). Los links de WhatsApp/correo/video/llamada llevan `stopPropagation` para no disparar el modal de ficha completa al clickearlos.
  - **Verificación en navegador** (simulación admin, datos reales post-sincronización): pestaña "Incompletas (143)" mostrando los 3 casos límite (WhatsApp+correo completos, solo correo, ninguno de los dos) con el placeholder correcto en cada uno; pestaña "En reserva (0)" vacía por defecto (todavía no había votos `res` en los datos reales); votado un candidato real desde "Revisión" con "Reserva" → aparece en "En reserva (1)" → voto de prueba eliminado al terminar.

- **3 de Septiembre de 2026 (4)** *(pestaña Estadísticas: segmento "Todos" vs. "Seleccionados", KPIs arriba, embudo con % de caída entre etapas y exportar CSV — pedido explícito del usuario ("las estadísticas ahora no son fáciles de leer... que se puedan hacer mucho más intuitivas"), [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesStatsTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesStatsTab.jsx) [MODIFY])*:
  - **El pedido**: una vista de estadísticas para TODOS los postulantes (la que ya existía) y otra separada solo para los "seleccionados" (preseleccionados) — para poder comparar cómo viene ese grupo puntual contra el pool general. Además, mejorar la legibilidad general: KPIs arriba (Completaron, Score medio, Score 85+, Con video y llamada — como en el mockup de referencia de la sesión anterior), y un botón para exportar la información.
  - **Backend, `/job-applications/stats`**: nuevo query param `?segmento=todos|seleccionados` (default `todos`). Con `seleccionados`, el pool sobre el que se calcula TODO (score medio, tramos, histograma, distribuciones de país/inglés/herramientas/etc.) se recorta a `veredicto() == 'preseleccionada'` antes de los cálculos — se reutiliza el mismo cuerpo de la función, solo cambia qué lista de `JobApplication` entra. El `embudo` (Abrieron → Disclaimer → Completaron → Material → Score 85+) no tiene sentido recortado a seleccionados (sería un solo escalón), así que se devuelve `None`/se omite en ese segmento — el frontend oculta el panel entero cuando no viene. Se agregaron campos nuevos al JSON (`segmento`, `total_completas`, `abrieron_formulario`, `con_material`, `score_85`) que antes se calculaban pero no se exponían sueltos (solo empotrados dentro de los pasos del embudo), porque ahora las tarjetas KPI los necesitan como número independiente.
  - **Frontend — KPIs arriba**: 4 tarjetas (mismo estilo que las de `PostulacionesInbox`) con contenido que cambia según el segmento — en "Todos" la primera dice "Completaron X de Y que abrieron"; en "Seleccionados" dice "Seleccionados X de Y completas" (para ver de entrada qué fracción del pool terminó aceptada, un dato que no existía antes en ningún lado del panel).
  - **Embudo más intuitivo**: cada escalón ahora muestra "↓ N% no siguió" respecto al escalón anterior (en rojo si la caída es ≥50%), en vez de solo el ancho relativo de la barra — así se ve de un vistazo EN QUÉ PASO se pierde más gente, que es literalmente lo que el usuario describió ("vienen de acá, luego acá, luego acá").
  - **Exportar CSV**: botón que arma un CSV client-side (nombre/email/veredicto/score/conocimiento/inglés/cierre/avance/fecha) a partir del mismo listado que ya usa el inbox (`/job-applications?filtro=todas` o `=preseleccionadas` según el segmento activo) y lo descarga vía Blob URL — no se tocó el backend para esto, no hizo falta un endpoint de export dedicado.
  - **Animación de entrada sutil**: cada panel y KPI hace un fade+translateY corto al montar (keyframe `ln-stats-up`, con un pequeño delay escalonado por índice) — el pedido de "un poco más de motion graphics" del mockup de referencia, sin reescribir los gráficos existentes (siguen siendo barras/donut simples con transiciones CSS, no se metió una librería de charts nueva).
  - **Verificación en navegador** (simulación admin, datos reales): segmento "Todos" → 72 completas, score medio 59, embudo mostrando la caída real (65% no sigue entre "pasaron el disclaimer" y "completaron", el bloque de abandono más grande del proceso). Segmento "Seleccionados" → 8 preseleccionados, score medio 72 (bien por encima del pool general, como se esperaría), embudo ausente, panel de "Herramientas"/"País"/etc. recalculado solo sobre esos 8. Exportar CSV disparó la petición correcta (`filtro=preseleccionadas`) sin errores de consola nuevos.

- **3 de Septiembre de 2026 (5)** *(fix: "Histograma de scores" y "Postulaciones por día" no dibujaban barras proporcionales, el usuario reportó "se ven mal", [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesStatsTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesStatsTab.jsx) [MODIFY])*:
  - **El bug real (CSS, no de datos)**: las barras usaban `style={{ height: '${pct}%' }}` dentro de una columna `flex flex-col` cuyo padre (`flex h-40 items-end`) alinea cada columna con `align-items: flex-end` — eso saca a la columna del stretch por defecto, así que su altura queda en `auto` (se ajusta a su contenido) en vez de heredar los 160px del contenedor. Un `height: %` sobre un hijo cuyo padre no tiene una altura EXPLÍCITA se resuelve como si fuera `auto` (spec de CSS2.1 §10.5) — así que el `%` nunca se aplicaba, y todas las barras terminaban en su `minHeight` en píxeles (4px), prácticamente parejas sin importar el valor real. Este bug ya estaba en la versión anterior de este archivo (antes de esta sesión) — se heredó al reescribir el componente el mismo día, no se detectó porque la verificación previa no miró estos dos paneles con atención al detalle de altura.
  - **El fix**: se envolvió cada barra en una "pista" (`div` con altura fija en Tailwind, `h-28`/`h-24`) que sí tiene altura explícita — ahora el `%` de la barra interior se resuelve contra ESA pista, no contra la columna completa (que sigue siendo `auto`, conteniendo el label de arriba + la pista + el label de abajo). Aplicado igual en Histograma y en Postulaciones por día. De paso, el histograma gana el sufijo "s" en las decenas (`0s`, `10s`...) para que se lea como rango, no como score exacto.
  - **Segundo problema, solo de "Postulaciones por día" (datos, no CSS)**: el backend devolvía únicamente los días CON postulaciones (`Counter` sin ceros) — con los datos reales recién sincronizados, sólo 2 de los últimos 14 días tenían algo, así que el gráfico mostraba 1-2 barras gigantas sueltas sin ningún eje temporal continuo. `stats_job_applications` ahora arma los 14 días completos (`desde_fecha` fijo, un `for i in range(14)`) y rellena con 0 los días sin postulaciones, para que el gráfico sea una serie continua de verdad en vez de puntos sueltos. La condición de "sin postulaciones recientes" pasó de `length === 0` (nunca se cumple ahora, siempre hay 14 filas) a `every(d => d.cantidad === 0)`.
  - **Verificación en navegador** (simulación admin, datos reales, viewport angosto para ver los paneles en columna única y a mayor tamaño): Histograma muestra una curva real con forma de campana (4, 8, 9, 11, 21, 13, 6 por decena, subiendo y bajando de forma visible) en vez de barras parejas. Postulaciones por día muestra los 14 días completos (08-21 a 09-03) con la mayoría en cero y un pico real y proporcional el 09-02 (12) y 09-03 (60) — coincide con `created_at` real de los datos de producción recién importados, no es un artefacto de la sesión de testing.

- **3 de Septiembre de 2026 (6)** *(Estadísticas: segmentos Preseleccionados/En reserva/Descartados/Incompletos (antes solo Todos/Seleccionados) + barras de distribución más gruesas con el número anclado adentro, pedido explícito del usuario tras revisar el panel en vivo, [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesStatsTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesStatsTab.jsx) [MODIFY])*:
  - **El pedido, aclarado por el usuario en el mismo mensaje**: la pestaña de Postulaciones (con sus tabs Revisión/En reserva/Incompletas y los filtros Preseleccionadas/Decidir/Descartadas) ya está bien como está — lo que faltaba era que **Estadísticas** tuviera el mismo desglose (Todos / Preseleccionados / En reserva / Descartados / Incompletos) para poder comparar cómo viene cada grupo, no solo "Todos" vs. "Seleccionados" (que ya existía de la sesión anterior).
  - **Backend**: `SEGMENTOS_VALIDOS` pasa de 2 a 5 valores. Se generalizó el `if segmento == 'seleccionados'` de la sesión anterior a un diccionario `VEREDICTO_DE_SEGMENTO` (`preseleccionados`→`preseleccionada`, `en_reserva`→`en_reserva`, `descartados`→`descartado`) que recorta `completas`; `incompletos` es el único caso especial porque sale de `todas_las_filas` completo (nunca tiene `completo=True`) en vez de filtrar `completas`. El resto del cálculo (score medio, tramos, histograma, distribuciones) es exactamente el mismo código de antes, solo cambia qué lista de `JobApplication` entra — mismo patrón que ya se había armado para "seleccionados".
  - **Frontend — KPI principal por segmento**: se generalizó el `if/else` binario a un diccionario `KPI_PRINCIPAL` con una función por segmento, porque cada uno necesita una etiqueta y un denominador distintos (ej. "Preseleccionados X de Y completas" vs. "Incompletos X de Y que abrieron" — no tiene sentido comparar incompletos contra el total de completas).
  - **Barras de distribución más gruesas (`Conocimiento como closer`, `Nivel de inglés`, `Lugar de residencia`, `Edad`, `Herramientas`, `Disclaimer`)**: el usuario señaló específicamente que las barras eran "muy delgaditas" y que una más gruesa "hace que puedas poner el texto ahí" — se rediseñó `BarraDistribucion`: el label de la categoría pasa a una línea propia arriba (antes compartía fila con el número), y la barra en sí crece de `h-1.5` (6px) a `h-5`/`h-7`/`h-9` (20-36px) según `grosorBarra(items.length)` — más grosor cuando hay pocos elementos (ej. "Disclaimer de frustración" con un solo ítem, que antes se veía como una rayita perdida), más fino cuando hay muchos (ej. "Lugar de residencia" con 11 países) para no disparar la altura del panel. El número ahora vive DENTRO de la pista, anclado a la derecha (no al final del relleno) — así siempre es legible aunque el valor sea chico y el relleno casi no se note, que era exactamente el caso que el usuario señaló como "se ve raro".
  - **Verificación**: la sesión del navegador se cayó de nuevo al guardar el cambio de backend (mismo problema ya documentado hoy: el reinicio del reloader de Flask invalida la sesión activa). En vez de volver a pedir login, se verificaron los 5 segmentos directo contra el test client de Flask (`scratch/verify_stats_segments.py`, no comiteado, con `login.session_protection = None` — mismo workaround documentado el 1/09 para simular sesión entre requests): `todos`=72 (con embudo), `preseleccionados`=8 (score medio 72), `en_reserva`=0 (nadie votado `res` todavía en datos reales), `descartados`=38 (score medio 53, más bajo que el general como se esperaría), `incompletos`=143 (score medio 32). Los conteos de descartados/incompletos coinciden exactamente con los ya vistos en la pestaña de Postulaciones. El rediseño visual de las barras SÍ se alcanzó a verificar en el navegador (antes de que se cayera la sesión) con las 6 tablas de distribución reales, incluyendo el caso límite de un solo ítem (Disclaimer) y el de muchos (Herramientas, 10 ítems).

- **3 de Septiembre de 2026 (7)** *(nueva pestaña "Revisores": cuánto le falta a cada revisor y dónde votaron distinto, pedido explícito del usuario a partir de la pestaña "revisores" del mockup HTML de referencia, [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesRevisoresTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesRevisoresTab.jsx) [NEW], [PostulacionesDashboardPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/PostulacionesDashboardPage.jsx) [MODIFY])*:
  - **El pedido**: Mario y Marlon (los revisores) necesitan saber su propio estado de revisión — cuántos le faltan a cada uno, no solo "los míos" — y en qué candidatos votaron distinto entre sí (para saber a quién le toca decidir). Del mockup de referencia se tomó la pestaña `revisores` completa (`revisoresLista`, `revDesacuerdos`, con su "Ver más"), pero se dejó afuera deliberadamente el mini-heatmap de celdas por día de cada tarjeta de revisor — el usuario señaló explícitamente sobre ese elemento "esto de aca realmente no se que es, no se entiende".
  - **Quién es "revisor"**: no es una lista fija de 2 personas como en el mockup (`REVISORES = [['mario','Mario'], ['marlon','Marlon']]`) — acá cualquier `role == 'admin'` puede votar (mismo criterio de `check_admin`), así que el endpoint nuevo (`GET /job-applications/revisores`) trae TODOS los usuarios con `role='admin'` (`User.query.filter_by(role='admin')`), no solo a quienes ya votaron algo — así un admin nuevo que todavía no calificó nada aparece igual con "le faltan: total".
  - **Backend**: por cada admin, `hechas` = cuántas de las `completas` tienen un voto suyo, `faltan` = el resto, `pct` = porcentaje. `desacuerdos` = las `completas` con `veredicto() == 'decidir'`, con el voto de cada revisor adjunto (nombre + tipo de voto) para mostrar el badge de cada uno. `mis_pendientes` = las `completas` donde el usuario actual (`current_user.id`) todavía no votó — mismo criterio que el filtro `mis_pendientes` ya existente, pero como lista con detalle en vez de solo un conteo.
  - **Frontend**: 3 secciones — tarjetas de revisor (nombre, badge "Al día"/"Pendiente", el número grande de "le faltan" en rosa/verde, "revisadas", barra de progreso); "Votaron distinto" y "Te falta ver · {usuario}" como listas clickeables (abren `PostulacionDetailModal`, reusado tal cual) con un botón "Ver N más" que expande de a 5 en 5 — mismo patrón `hayMasDes`/`verMasDes` del mockup pero implementado como toggle local (`useState`) en vez de paginación server-side, porque el volumen (72 completas) no lo justifica.
  - **Verificación con datos reales**: `Mario Administra` (id real de producción) aparece con 46 revisadas / 26 pendientes / 64% completo — coincide con que ya viene usando el sistema de votos activamente; el resto de los admins en 0. 0 desacuerdos (todavía no hay conflicto porque solo un revisor activo votó hasta ahora). La sesión del navegador volvió a caerse durante esta verificación (mismo problema del reinicio del backend) y, al re-simular por segunda vez, se detectó la causa raíz real: `AuthContext.jsx` inicializa `user` desde `localStorage.getItem('user')`, no desde una consulta fresca al backend — así que un `fetch('/api/auth/impersonate')` hecho por fuera del botón real de la UI cambia la cookie de sesión en el servidor pero la app sigue mostrando (y aplicando `ProtectedRoute`) con el usuario viejo cacheado. No es un bug del producto (cualquier usuario real que clickea "Simular" en la UI sí actualiza `localStorage` correctamente, confirmado); fue una limitación del atajo de testing usado en sesiones anteriores de hoy. Con el click real de "Simular" (vía DOM, ubicando la tarjeta exacta por texto para no simular al usuario equivocado) las 3 tarjetas y ambas listas se vieron correctas en el navegador con los números reales de arriba.
  - **Fuera de alcance, aclarado por el usuario en el mismo mensaje**: pidió además cambios a las preguntas del formulario de postulación (validar que el link de video no sea texto random tipo "no quiero", exigir permisos de Google Drive, agregar todos los países con buscador, agregar una pregunta sobre "por qué sos la mejor opción") — pero el propio usuario aclaró que esas preguntas viven en la landing page del proyecto **institute-site**, un proyecto separado de NeurOPS. No se tocó nada de eso en esta sesión; queda pendiente para cuando se trabaje en ese otro repo.

- **3 de Septiembre de 2026 (8)** *(cambios en el formulario público de postulación — repo `institute-site`, con soporte cruzado en NeurOPS para la pregunta nueva — cierra el pendiente de la entrada anterior, [job_application.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/job_application.py) [MODIFY], [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/public/job_applications.py) [MODIFY], [f1a2b3c4d5e6_add_porque_mejor_opcion.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/f1a2b3c4d5e6_add_porque_mejor_opcion.py) [NEW], [PostulacionDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionDetailModal.jsx) [MODIFY] — y en `institute-site` (repo separado): `formulario/index.html` [MODIFY])*:
  - **`institute-site/formulario/index.html`** (commit `2c983bc`, repo separado, mismo working tree local): país de residencia pasa de una lista corta de ~19 países con un cajón "Otro" (que el usuario reportó como la opción más elegida) a la lista completa de `CODIGOS_PAIS` (ya usada por el selector de código de WhatsApp, ~195 países) con buscador — nuevo tipo de pregunta `'buscar'` en vez de `'radio'`, reutilizando el mismo patrón de input+lista filtrable del popover de WhatsApp pero como paso de formulario propio en vez de popover anclado. Video/llamada: instrucciones más explícitas (pegar solo el link, permisos "Cualquier persona con el enlace" en Drive, sin video no se revisa la postulación) y una validación real nueva (`q.validar`, regex `^https?:\/\/`) — antes cualquier texto no vacío pasaba, confirmado con un caso real reportado por el usuario ("no quiero" en vez de un link). Nueva pregunta de texto libre "¿Por qué eres la mejor opción para este puesto?" entre "objetivos" y "porque", para leer la motivación sin depender del video.
  - **Por qué también se tocó NeurOPS**: una pregunta nueva sin una columna donde guardarla se pierde en silencio — el endpoint público solo guarda los campos que reconoce por nombre. Columna `porque_mejor_opcion` (Text, nullable) + migración `f1a2b3c4d5e6`, sumada a `CAMPOS_FORMULARIO` (pasa de 23 a 24 preguntas totales — la etiqueta de Clarity "Video y llamada (22 y 23)" se corrigió a "(23 y 24)" por el corrimiento) y al endpoint público de alta. Se muestra en la ficha del candidato junto a habilidades/obstáculo/objetivos. No se la sumó como criterio ponderado de Clarity (no se tocó el score automático) — el pedido era poder *leerla*, no que alimente el cálculo, y convertir una respuesta libre en un criterio puntuable es una decisión de diseño aparte que no se pidió.
  - **Verificación**: `institute-site` corrido localmente (`npx serve`, agregada una entrada temporal a `.claude/launch.json` de NeurOPS para poder previsualizarlo con la herramienta de navegador — revertida al terminar, no comiteada) completando el formulario real paso a paso hasta el final: el buscador de país filtra en vivo (probado con "Venez" → "Venezuela"), la pregunta nueva aparece con el texto correcto en su lugar, y la validación del video rechaza "no quiero" con el mensaje de error nuevo y acepta un link real. No se completó el envío final a propósito: `SUBMIT_URL` apunta a producción (`work.thelearnation.com`) y el origen local no está en la whitelist de CORS de NeurOPS, así que cada guardado progresivo de prueba falló en el navegador por CORS (confirmado en consola) — ningún dato de prueba llegó a producción. El lado NeurOPS (columna nueva, migración, endpoint) se verificó aparte con el test client de Flask contra la base local: POST con el campo nuevo → persiste en la columna → aparece en el GET de admin → `respondidas/total_preguntas` correcto (21/24). Registro de prueba eliminado.

- **3 de Septiembre de 2026 (9)** *(fix: el dock flotante global tapaba los botones de votar del modal de ficha de candidato, reportado por el usuario con captura de pantalla de su propia sesion, [PostulacionDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionDetailModal.jsx) [MODIFY], [PostulacionVoteToast.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionVoteToast.jsx) [MODIFY])*:
  - **El bug**: `Dock.jsx` (`frontend/src/components/shared/Dock.jsx`), montado globalmente en `MainLayout.jsx` en todas las paginas autenticadas, es `fixed bottom-10 ... z-50`. El backdrop de `PostulacionDetailModal` estaba en `z-20` — el unico modal de toda la app con un z-index tan bajo (se relevaron ~50 modales en `frontend/src/**`, la inmensa mayoria usa `z-[100]` o mas, varios especificamente para quedar por encima de este mismo dock). Con esa diferencia, el dock (que vive `bottom-10`, justo en la franja donde cae el footer de 3 botones de voto) se pintaba encima del modal, cortando/tapando el boton "Preseleccionar" segun el ancho de ventana.
  - **El fix**: backdrop del modal `z-20` -> `z-[100]` (mismo valor que usa la mayoria de los otros modales de la app, incluido `AdDetailModal.jsx` que ya usaba `z-[60]` por el mismo motivo). De paso, `PostulacionVoteToast.jsx` (el toast de confirmacion al votar) paso de `z-30` a `z-[110]` — mismo problema potencial, no reportado explicitamente pero mismo origen.
  - **Verificacion en navegador** (simulacion admin, viewport 1366x768 para reproducir la proporcion de la captura del usuario): con el fix, los 3 botones (`getBoundingClientRect`) quedan visibles y parejos en y=650-700px, y el z-index computado confirma modal=100 > dock=50 — el dock queda completamente detras del velo semitransparente del modal, invisible, en vez de pisarlo.
  - **Nota aparte, no un bug de este cambio**: durante la verificacion se confirmo de nuevo la fragilidad ya documentada de `AuthContext` (localStorage como fuente de verdad) — revertir la impersonacion con un `fetch('/api/auth/revert')` crudo (en vez del flujo real de la UI) dejo el `localStorage` diciendo `Santiago/admin` mientras el servidor ya decia `Kerwin/operator`. Se corrigio sincronizando `localStorage['user']` a mano con la respuesta real de `/api/auth/me`. Aparte, se noto que la ruta `/` de la app siempre muestra el login sin importar la sesion (no es un redirect-aware root) — hay que navegar a una ruta concreta como `/ops/dashboard`; no es un bug de esta sesion, es como esta armado el router.

- **4 de Septiembre de 2026** *(rediseño del rol `director_comercial` (ventas/closing/setting) + rol nuevo `director_marketing` (workshops + anuncios/Instagram) — pedido explícito del usuario: lo que hoy tenía `director_comercial` (acceso a Workshop Intelligence) estaba mal ubicado, es contenido de Marketing, [user.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/user.py) [MODIFY], [decorators.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/decorators.py) [MODIFY], [App.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/App.jsx) [MODIFY], [useDockNavigation.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/hooks/useDockNavigation.js) [MODIFY], [LoginPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/auth/LoginPage.jsx) [MODIFY], [TeamManagementPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/team/TeamManagementPage.jsx) [MODIFY])*:
  - **El pedido**: el rol `director_comercial` (creado antes de esta sesión) solo daba acceso a `/admin/workshops` — eso estaba conceptualmente mal, porque "director comercial" debería ser ventas: closing y setting. El usuario pidió mover ese acceso a workshops (más talleres y anuncios de Instagram — "todo lo referente a Marketing") a un rol nuevo, `director_marketing`, y redefinir `director_comercial` para que apunte a la parte comercial/ventas. Explícitamente: "la idea es que desde Admin se vea todo" (el admin ya tenía y conserva acceso completo a ambas zonas, sin cambios ahí).
  - **Mapeo a páginas ya existentes, no se construyó nada nuevo**: se reutilizó infraestructura que ya vivía en el panel de admin — `director_comercial` → `/admin/ventas` (`AdminSalesHubPage`, con tabs Closers/Setters/Workshops, ya usada por admin); `director_marketing` → `/admin/workshops` (`WorkshopDashboardPage`, el embudo de talleres) como home tras login, más `/admin/marketing` (`AdminMarketingHubPage`: Gestión de Anuncios/Instagram, Generador UTM, Tráfico de Landings) sumado al Dock.
  - **Backend**: constante nueva `ROLE_DIRECTOR_MARKETING = 'director_marketing'` en `user.py` (se dejó `ROLE_DIRECTOR_COMERCIAL` intacta, solo cambia a qué apunta desde el frontend). El decorator `workshop_required` (único lugar del backend que mencionaba `director_comercial`) pasó a exigir `ROLE_DIRECTOR_MARKETING` en vez de `ROLE_DIRECTOR_COMERCIAL`. No hizo falta tocar nada más en la API: los endpoints de ventas (`closer.py`/`setter.py`) y de marketing/ads (`marketing.py`) solo exigen `@login_required` sin chequeo de rol adicional (el control real ya vivía en el ruteo del frontend); tampoco hay whitelist de roles válidos al crear/editar un usuario (`POST/PUT /admin/users` acepta cualquier string), así que no hizo falta migración ni cambio de esquema — `role` sigue siendo texto libre.
  - **Frontend**: en `App.jsx`, `/admin/ventas` sumó `director_comercial` a sus roles permitidos y `/admin/workshops`/`/admin/marketing` sumaron `director_marketing` (reemplazando a `director_comercial` en el primero); el redirect de `ProtectedRoute` y el de `LoginPage.jsx` para `director_comercial` pasaron de `/admin/workshops` a `/admin/ventas`, con una rama nueva para `director_marketing` → `/admin/workshops`. En `useDockNavigation.js`, el bloque de Dock que antes era de `director_comercial` (un solo destino, Workshops) se reasignó a `director_marketing` con dos destinos (Workshops + Marketing, reutilizando el ícono `Activity` que ya estaba importado y sin uso); `director_comercial` gana su propio bloque con un solo destino, Ventas. `TeamManagementPage.jsx` suma la opción "Director de Marketing" al selector de rol y a la lista de filtros, y el redirect de impersonación (`handleImpersonate`) sigue el mismo split que `LoginPage`.
  - **Verificación en navegador** (no solo lectura de código): se creó un admin temporal (`claude_test_admin`, vía script directo contra `instance/local.db`, porque no había credenciales de admin a mano) para loguearse, y desde "Gestión de Equipo" (atajo de teclado `C` para abrir el panel — el botón de settings del Dock no dispara el modal directamente, según `HotkeysManager.jsx`) se confirmó que el selector de rol ya lista "Director Comercial" y "Director de Marketing". Se crearon dos usuarios de prueba reales, uno por rol, y se logueó con cada uno por separado: `director_marketing` aterriza en `/admin/workshops` tras login, con el Dock mostrando Workshops + Marketing (el segundo click navegó de verdad a `/admin/marketing`, confirmado por `location.pathname`), y un intento directo a `/admin/ventas` rebotó a `/admin/workshops`. `director_comercial` aterriza en `/admin/ventas` con el Dock mostrando solo Ventas, y un intento directo a `/admin/marketing` rebotó a `/admin/ventas`. Los 3 usuarios de prueba (`claude_test_admin`, `claude_test_marketing`, `claude_test_comercial`) se eliminaron de la base al terminar.

- **4 de Septiembre de 2026 (2)** *(reskin visual completo de Workshop Intelligence — pedido explícito del usuario a partir de un proyecto de referencia externo que compartió (`learnation-workshop-analytics`, Next.js con un `README.md`/`app/globals.css` que documentan un design system "command center nocturno" navy/rosa/azul) — "necesito que sea tal cual como lo que te acabo de pasar"; alcance ampliado en el mismo turno a pedido del usuario para que también se vea así en la sesión de `admin`, [workshop-intel.css](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/workshop/workshop-intel.css) [NEW], [index.html](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/index.html) [MODIFY], [index.css](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/index.css) [MODIFY], y los 13 archivos de `frontend/src/pages/admin/workshop/` [MODIFY])*:
  - **Solo capa visual, cero cambios de datos/lógica**: se confirmó explorando el código que `funnelMath.js` ya documentaba que el modelo de embudo de NeurOPS ("una tasa por paso, techo 95%") está calcado de este mismo proyecto de referencia — los 8 stages y los nombres de campo de `WorkshopEvent` (`leads`, `whatsapp_leads`, `show_up`, `pitch_leads`, `pitch_final_leads`, `aplicaciones_form`, `agendas_exitosas`, `show_up_sales_call`, `sales`, `cash_collected`) calzan casi 1:1 con el `Workshop` type de la referencia. Por eso el trabajo fue reescribir el JSX/clases de los 13 archivos (`WorkshopDashboardPage.jsx` + 12 componentes en `components/`) sin tocar ningún `useState`/`useEffect`/llamada a `api.*` existente — mismos props, mismos handlers, mismos endpoints.
  - **Como `/admin/workshops` es la MISMA `WorkshopDashboardPage` para `admin` y `director_marketing`** (`App.jsx`, ver entrada anterior de hoy), reskinear este componente cerró automáticamente el pedido ampliado del usuario sin tocar nada aparte.
  - **Precedente ya existente en el repo, reutilizado a propósito**: `CloserWorkflowPage.jsx` ya había hecho este mismo tipo de "clonar una referencia visual externa" (28/ago/2026) con una clase envoltorio `.bg-v6` scopeada en `index.css`. Se copió el mismo mecanismo con una clase nueva `.ws-shell`: `index.html` (el `<style>` inline que carga antes que cualquier CSS de Vite/Tailwind) e `index.css` tienen resets globales `!important` de `font-family`/`font-weight` con una exclusión `:not(.bg-v6):not(.bg-v6 *)` tallada para el Workspace de Closers — la bitácora del 28/ago ya documentó que OLVIDAR la copia en `index.html` (no solo en `index.css`) produce networking sutil ("las letras se ven más finas que el mockup"), así que se extendió la MISMA cadena de exclusión en ambos archivos a `:not(.ws-shell):not(.ws-shell *)` en vez de solo agregar una regla nueva.
  - **`workshop-intel.css` (nuevo, ~430 líneas)**: todo el vocabulario de clases de la referencia (`.kpi-grid/.kpi-card`, `.panel/.lever-row/.track/.status`, `.event-grid/.event-card`, `.funnel-panel/.funnel-stage/.stage-bar/.rate-pill`, `.sim-grid/.slider-row`, `.actions-grid/.plan-item`, `.wizard-layout/.field-control`, `.modal-overlay/.action-modal`, etc.) portado con los mismos valores (colores/radios/spacing/breakpoints) pero con CADA selector prefijado `.ws-shell` — nombres genéricos como `.panel`/`.status`/`.track`/`.legend` no debían filtrarse al resto del admin. Fuente Montserrat: ya estaba cargada en `index.html` (28/ago) en pesos `400;600;700;900`; se sumó `800` (usado por varias reglas de la referencia).
  - **Adaptaciones deliberadas, no literales, donde la estructura de NeurOPS difiere de la referencia** (la referencia es una app multi-página con rutas; NeurOPS usa pestañas dentro de una sola página): la navegación de pestañas (Historial/Embudo/Simulador/Acciones/Landing) se restyleó como una cápsula tipo `.main-nav` de la referencia pero con botones en vez de links de ruta; el wizard de registrar/editar taller (`WorkshopFormModal.jsx`) se mantuvo como MODAL (no se convirtió a página completa, porque está enganchado al flujo de crear/editar desde las tarjetas y al prefill por fecha) con `.wizard-steps`/`.progress-circle` compactados adentro; la pestaña "Landing de la Grabación" (`WorkshopLandingView.jsx`) no tiene equivalente en la referencia (es un embudo propio de NeurOPS, la landing de la grabación `/replay/`) — se restyleó con el mismo vocabulario de tokens/clases para que se vea consistente con el resto igual.
  - **Bug real encontrado en verificación de navegador, no en lectura de código**: el modal "Comparar talleres" (`WorkshopCompareModal.jsx`) armaba la tabla con `.comparison-scroll`/`<table>` pero sin el div envolvente `.comparison-panel` — como casi todas las reglas de tabla en `workshop-intel.css` están scopeadas `.ws-shell .comparison-panel th/td/...`, la tabla se veía "casi bien" (heredaba tipografía/color general de `.ws-shell`) pero sin el padding/alineación/borde de celda correctos, y el header de cada columna (fecha + nombre del evento) se pegaba en una sola línea en vez de partirse en dos como en la referencia. Se detectó comparando el screenshot real contra el resultado esperado, no fue obvio de la lectura del JSX. Fix: envolver `.comparison-scroll` en un `.comparison-panel` (con padding/fondo/sombra pisados a cero inline, porque esos ya los pone el modal que lo contiene).
  - **Verificación en navegador, no solo lectura de código**: reutilizando el mismo admin temporal de la entrada anterior, se recorrieron las 5 pestañas (Historial en modo tarjetas y tabla, Embudo con selector de evento y desglose vivo/grabación, Simulador con sliders y proyección, Acciones con formulario inline de creación, Landing con KPIs/embudo/tabla de sesiones) y los 3 modales (wizard de Registrar Taller completo — se navegaron los 3 pasos con datos reales, confirmando que el prefill automático seguía funcionando, sin llegar a enviar el formulario — Comparar con 2 y 3 talleres seleccionados, y Editar Metas), sin errores de consola reales (un error de HMR que apareció en la consola del navegador resultó ser un mensaje repetido/cacheado de un estado intermedio ya resuelto, confirmado comparando contra los logs frescos del servidor de Vite, que no mostraban ningún error nuevo). Se probó además que otra página del admin ajena a este cambio (`/admin/ventas`, con su propio tema indigo/rosa "v6/v7") se sigue viendo exactamente igual que antes, confirmando que la exclusión `:not(.ws-shell)` no rompió nada fuera de Workshop Intelligence. Usuario de prueba eliminado al terminar.

- **4 de Septiembre de 2026 (3)** *(pedido explícito del usuario: desactivar los avisos de seguimiento por WhatsApp que le llegaban a los closers — "ya no es necesario" — sin cambios de código, solo variable de entorno en producción)*:
  - **Nada que tocar en el código**: el aviso (`CloserFollowUpService.send_due_reminders`, [closer_followup_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py)) ya tenía un freno de emergencia global de fábrica, `reminders_enabled()`, gobernado por la variable de entorno `FOLLOWUP_REMINDERS_ENABLED` (default `true`) — pensado a propósito para cubrir los dos caminos que pueden disparar el envío: el scheduler interno ([reminder_scheduler.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/reminder_scheduler.py), un `BackgroundScheduler` que tickea cada 5 min) y el endpoint de cron externo (`GET /api/closer/followups/cron/send-reminders`, [closer_followups.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer_followups.py)) que algún cron externo llama con un token.
  - **Detalle que casi lleva a un diagnóstico equivocado**: producción ya tenía `DISABLE_REMINDER_SCHEDULER=true` seteada (para otra cosa, evita que el scheduler EN PROCESO arranque) — pero esa variable no cubre el endpoint de cron externo, que sigue funcionando igual porque su propio corte real vive adentro de `send_due_reminders` (`reminders_enabled()`), no en si el scheduler interno llegó a arrancar. Como `FOLLOWUP_REMINDERS_ENABLED` no estaba seteada (default `true`), lo más probable es que los avisos siguieran saliendo por ese segundo camino pese a la otra variable ya apagada.
  - **La corrección real**: `railway variables --set "FOLLOWUP_REMINDERS_ENABLED=false"` sobre el servicio `learnation work` (producción) — dispara un redeploy automático de Railway, verificado con `railway status` hasta volver a `● Online`, y confirmado además leyendo la variable en vivo del propio proceso desplegado (`railway run ... os.environ.get('FOLLOWUP_REMINDERS_ENABLED')` → `'false'`). Espejada también en el `.env` local para que no diverja de producción.

- **5 de Septiembre de 2026** *(sistema de bugs: el usuario puede reabrir su propio reporte ya "resuelto" con un botón "Sigue roto" en vez de tener que crear uno nuevo — pedido explícito del usuario, [bug_reports.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/bug_reports.py) [MODIFY], [BugReportThread.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportThread.jsx) [MODIFY])*:
  - **Sin tocar el modelo**: no hizo falta ningún estado nuevo ni columna — reabrir es simplemente volver `status` a `'open'` (mismo valor que un reporte recién creado, así que reaparece igual en "pendiente(s) de revisar" del panel del operador) más un `BugReportMessage` automático (`sender_id`/`sender_role` del propio reportante) para que el operador tenga contexto de que fue una reapertura y no un mensaje cualquiera — se apoya en el mismo mecanismo de `unread_for_manager` que ya existía, no hizo falta ningún flag nuevo.
  - **Endpoint nuevo, `POST /bug-reports/<id>/reopen`**: solo el dueño del reporte puede llamarlo (`report.user_id != current_user.id` → 403; los managers ya tienen su propio control de estado vía el `<select>` de `BugReportsPanel`, así que no se les dio este atajo) y solo si `status == 'resolved'` (400 en cualquier otro estado — no tiene sentido "reabrir" un reporte que ya está pendiente o en revisión).
  - **Frontend**: `BugReportThread.jsx` (compartido entre `BugReportHistory` del usuario y `BugReportsPanel` del operador) ahora guarda el `report` completo en estado local y muestra un banner ámbar con el botón "Sigue roto" solo cuando `report.status === 'resolved' && report.user_id === user.id` — así el banner nunca aparece del lado del operador viendo el reporte de otra persona, aunque reutilicen el mismo componente.
  - **Verificación en navegador** (sesión real del operador Kerwin, sin impersonar — probar como dueño del reporte era justo lo que hacía falta cubrir): reporte de prueba creado con el widget flotante, marcado "Resuelto" desde `BugReportsPanel`, abierto desde "Mis reportes" → apareció el banner "Sigue roto", clickeado → `POST /bug-reports/3/reopen` devolvió 200, el banner desapareció y quedó el mensaje automático "El problema sigue presente, no quedó resuelto." en la conversación; recargando `BugReportsPanel` el reporte volvió a "Pendiente" y el contador de pendientes subió de 1 a 2. Registro de prueba eliminado de `instance/local.db` al terminar (`env/Scripts/python.exe -c "..."` borrando el `BugReport` y su `BugReportMessage` directo, ya que no existe endpoint de borrado).

- **5 de Septiembre de 2026 (2)** *(nuevo módulo: videos de documentación con quiz de comprensión — pedido explícito del usuario para que los usuarios vean videos de onboarding/novedades y confirmen que los entendieron, con gestión desde el panel de operador, [training_video.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/training_video.py) [NEW], [training_videos.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/training_videos.py) [NEW], [ad569b0263be_add_training_videos.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/ad569b0263be_add_training_videos.py) [NEW], [__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY], [models/__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/__init__.py) [MODIFY], [TrainingVideoGate.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/training/TrainingVideoGate.jsx) [NEW], [TrainingVideosPanel.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/TrainingVideosPanel.jsx) [NEW], [TrainingVideoFormModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/TrainingVideoFormModal.jsx) [NEW], [App.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/App.jsx) [MODIFY], [OperationsSettingsPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/operations/settings/OperationsSettingsPage.jsx) [MODIFY])*:
  - **Modelo (4 tablas nuevas)**: `TrainingVideo` (título, descripción, `loom_link`, `target_roles` JSON — lista de roles a quienes se les muestra, vacía/nula = todos los roles —, `is_active`, `created_by_id`), `TrainingVideoQuestion` y `TrainingVideoOption` (con `is_correct` por opción, soporta más de una opción correcta por pregunta — el pedido decía "marcar cuáles son las correctas" en plural), y `TrainingVideoCompletion` (par único `video_id`+`user_id`, con `UniqueConstraint`) que registra quién ya lo completó. Las respuestas del usuario nunca se guardan, solo el hecho de haber acertado todo.
  - **Editar preguntas reemplaza el set completo, no las diffea**: `_apply_questions` (en `training_videos.py`) borra y recrea todas las preguntas/opciones de un video en cada `PUT` — el volumen por video es chico (un puñado de preguntas) y el formulario del operador siempre manda el set completo, así que era más simple que diffear pregunta por pregunta. Si se edita el quiz de un video ya completado por alguien, sus `TrainingVideoCompletion` se borran a propósito: si la info de comprobación cambió, quien lo "aprobó" con las preguntas viejas debería volver a verlo.
  - **Corrección en el backend, nunca en el cliente**: `POST /training-videos/<id>/submit` recibe `{answers: {question_id: [option_id, ...]}}` y compara el set de opciones marcadas contra el set de opciones `is_correct` de cada pregunta — deben coincidir exactamente (ni de más ni de menos) para que esa pregunta cuente como correcta. El frontend nunca recibe qué opción es la correcta hasta que el video ya se completó (`to_dict(include_correct=False)` en `GET /training-videos/pending`), solo si acertó o no cada pregunta tras enviar.
  - **`GET /training-videos/pending`**: un video aparece para el usuario actual solo si está `is_active`, tiene al menos 1 pregunta (uno sin preguntas nunca podría "completarse", así que no tendría sentido mostrarlo), su `target_roles` está vacío o incluye el rol del usuario, y no existe ya un `TrainingVideoCompletion` suyo para ese video.
  - **Frontend, gate global (`TrainingVideoGate.jsx`)**: montado en `App.jsx` como hermano de `BugReportWidget` (NO dentro de `MainLayout`) por el mismo motivo ya documentado el 01/09 para ese widget — `CloserWorkflowPage` ("/closer/deck", donde los closers pasan todo su turno) corre standalone sin `MainLayout`, y los closers son justamente uno de los públicos objetivo de este feature. Se autogatea con `useAuth()`. Al montar, pide `pending` y si hay alguno muestra un modal de pantalla completa (portal a `document.body`) con el Loom embebido (mismo `getLoomEmbedUrl` que `LoomModal` de `BugReportsPanel.jsx`, duplicado a propósito en vez de extraído a un util compartido, siguiendo el mismo criterio ya usado en ese archivo) y el quiz debajo — checkboxes en vez de radios para soportar preguntas con más de una respuesta correcta. Si falla alguna pregunta, el modal NO se cierra y muestra qué preguntas quedaron mal (ícono verde/rojo) para reintentar; si acierta todo, se marca como completo en el backend y desaparece de la cola (pasando al siguiente video pendiente, si hay más de uno).
  - **Gestión desde el panel de operador**: pestaña nueva "Videos de Documentación" en `OperationsSettingsPage.jsx` (mismo criterio de acceso que "Reportes de Bugs", `operator`/`admin`) con `TrainingVideosPanel.jsx` (listar/activar-desactivar/editar/eliminar) y `TrainingVideoFormModal.jsx` (crear/editar: título, descripción, link de Loom, selector de roles objetivo — botones tipo chip, reutilizando el mismo `ROLE_OPTIONS` en ambos archivos —, y un builder de preguntas/opciones dinámico donde cada opción se marca correcta con un botón check independiente). Justo lo que pidió el usuario: "simplemente pego el loom y configuro las preguntas y sus respuestas".
  - **Migración**: el ID de revisión generado inicialmente (`a1b2c3d4e5f6`) chocaba con uno ya existente de otra migración vieja (`a1b2c3d4e5f6_add_is_active_to_programs.py`) — `flask db upgrade` lo detectó como "Cycle is detected in revisions" antes de aplicar nada. Se regeneró un ID aleatorio nuevo (`ad569b0263be`) sin colisión y se renombró el archivo; aplicada limpio contra `instance/local.db` después.
  - **Verificación end-to-end (`scratch/simulate_training_videos.py`, no comiteado)**: mismo patrón ya documentado el 01/09 para evitar el bug de identidad cruzada entre usuarios simulados (las llamadas de `test_client()` van FUERA de cualquier `app_context()` manual). Cubrió: un operador crea un video con 2 preguntas dirigido solo a `closer`; un setter (rol no targeteado) NO lo ve en `/pending`; un closer sí lo ve, y las opciones no traen `is_correct`; responder mal una pregunta no marca el video completo y sigue apareciendo en pendientes; responder bien todo lo marca completo, crea el `TrainingVideoCompletion` y lo saca de pendientes; un rol sin permisos de manager recibe 403 en los endpoints de gestión; editar el video (quitar una pregunta) borra la completion existente. Las 8 verificaciones pasaron.
  - **Verificación real en navegador** (con el usuario ya logueado como operador `Kerwin`): creado un video real desde el formulario del panel dirigido a `closer` con una pregunta de 2 opciones; impersonado el closer `Marlon Closer` desde "Gestión de Equipo" → el modal de quiz apareció automáticamente al entrar al Closer Workspace (`/closer/deck`, confirmando que el gate SÍ funciona fuera de `MainLayout`); respondida la opción incorrecta → el modal no cerró y marcó la pregunta con ícono rojo; respondida la correcta → el modal se cerró solo; revertida la impersonación (`POST /api/auth/revert`) y confirmado en el panel del operador que "0 completados" pasó a "1 completado(s)". Video de prueba eliminado al terminar, base local queda en cero filas en las 4 tablas nuevas.

- **5 de Septiembre de 2026 (3)** *(incidente de producción: director_comercial no podía cargar "Dashboard de Performance" (403) + acotar su alcance a closing/setting quitándole la pestaña Workshops — reportado por el usuario con logs de Railway, [closer_dashboard.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer_dashboard.py) [MODIFY], [AdminSalesHubPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/reports/AdminSalesHubPage.jsx) [MODIFY])*:
  - **El síntoma**: log de proxy de Railway mostrando `GET /api/closer/performance-dashboard` → `403` para una sesión real en `work.thelearnation.com`. `director_comercial` aterriza en `/admin/ventas` (`AdminSalesHubPage`) tras el login, cuya pestaña por defecto es "Closers" (`PublicCloserStatsPage`) con su sub-pestaña por defecto "Dashboard" (`CloserDashboard.jsx`) — ese componente pide `/closer/performance-dashboard` apenas monta, sin que el usuario haga nada más.
  - **La causa real**: los 4 endpoints de `closer_dashboard.py` (agregado el 19-20/ago, antes de que `director_comercial` reapuntara a `/admin/ventas` el 04/09) tenían el chequeo de rol hardcodeado a `if current_user.role not in ['closer', 'admin']` — nunca se actualizó al mover `director_comercial` a esta zona, así que cualquier director comercial real caía directo en el 403 al entrar a su propio dashboard. No relacionado con el warning `[AGENDA SIN EMAIL]` del mismo log pegado por el usuario (ese es un aviso preexistente y deliberado de `financial_agendas.py` cuando n8n manda una agenda sin `mail`/`email` en el payload — confirmado leyendo el código, es un problema de datos de esa agenda puntual de Calendly/n8n, no toca el rol ni el dashboard).
  - **El fix**: `performance-dashboard` y `pending-summary` (los dos que de verdad usa la UI de `director_comercial`, ambos de solo lectura) suman `ROLE_DIRECTOR_COMERCIAL` a la whitelist mediante una constante compartida `ROLES_CON_VISTA_AGREGADA = ['admin', ROLE_DIRECTOR_COMERCIAL]` — se comporta igual que `admin` (ve todos los closers o uno puntual por `closer_id`, nunca queda forzado a `current_user.id` porque no es closer real). `slots-pendientes`/`slots` (declarar los propios cupos del día) quedaron sin tocar a propósito: son estrictamente de un closer sobre sí mismo (`current_user.id`) y el frontend ya los gatea con `user?.role === 'closer'` (`CloserDashboard.jsx:169`), así que `director_comercial` nunca los llama.
  - **Acotar el rol, pedido explícito del usuario en el mismo mensaje**: "él debería ver closing y setting, el de workshop no lo debería revisar, eso lo vamos a poner aparte, en marketing" — `AdminSalesHubPage` (compartida entre `admin` y `director_comercial` desde el 04/09) tenía sus 3 pestañas (Closers/Setters/Workshops) visibles para ambos roles por igual. Se agregó `puedeVerWorkshops = user?.role === 'admin'` (vía `useAuth()`) que oculta el botón de la pestaña y el render de `WorkshopDashboardPage` cuando no es admin — `director_comercial` ahora solo puede ver Closers/Setters ahí; admin conserva las 3 sin cambios, consistente con "desde Admin se vea todo" ya establecido el 04/09. Workshop-como-Marketing ya vive aparte en `/admin/workshops` para `director_marketing` (sin tocar en esta sesión).
  - **Verificación**: contra `instance/local.db` con el test client de Flask (`scratch/verify_director_comercial_dashboard.py`, no comiteado, sesión simulada por usuario real `Kerwin Ventas` (`director_comercial`) — mismo patrón de `login.session_protection = None` ya documentado en sesiones anteriores): `GET /closer/performance-dashboard` y `GET /closer/pending-summary` pasan de `403` a `200`; un `setter` de control sigue en `403` (el fix no abrió la puerta de más); `admin` sigue en `200` sin cambios. El JSX de `AdminSalesHubPage.jsx` se confirmó vía el propio dev server de Vite (`GET /src/pages/admin/reports/AdminSalesHubPage.jsx` transformado sin errores).
  - **Verificación en navegador no completada, motivo externo**: no se pudo hacer el recorrido real logueado (crear un admin temporal + impersonar, patrón ya usado en sesiones anteriores) porque, a mitad de esta sesión, otra sesión de Claude Code del mismo usuario trabajando en paralelo sobre `develop` (aviso explícito del usuario: "hay más sesiones tuyas trabajando sobre la rama develop") tenía en curso el borrado del módulo de Videos de Documentación (`app/models/training_video.py`, `app/api/training_videos.py`, sus componentes de frontend) — el backend compartido del puerto 5000 quedó momentáneamente sin arrancar por el import roto a mitad de ese guardado (`ModuleNotFoundError: app.api.training_videos`), ajeno a este cambio. Se optó por no tocar esos archivos (no es esta tarea) y apoyarse en la verificación por test client, que corre en proceso propio contra la misma base de datos sin depender de que el server de la otra sesión esté arriba.

- **5 de Septiembre de 2026 (4)** *(Workshop Intelligence: pestañas siempre visibles arriba + "Resumen" separado de las pestañas de datos, y sincronización en vivo automática de agendas/ventas — pedido explícito del usuario a partir de `learnation-workshop-analytics-react-python` (referencia externa ya usada el 04/09 para el reskin visual) y de una queja de uso real ("no se actualizan rápidamente las agendas, las ventas... hay que recargar"), [workshop.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/workshop.py) [MODIFY], [c2d3e4f5a6b7_add_workshop_event_synced_at.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/c2d3e4f5a6b7_add_workshop_event_synced_at.py) [NEW], [workshop_live_sync.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/workshop_live_sync.py) [NEW], [__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY], [workshop.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/workshop.py) [MODIFY], [WorkshopDashboardPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/workshop/WorkshopDashboardPage.jsx) [MODIFY], [WorkshopFunnelView.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/workshop/components/WorkshopFunnelView.jsx) [MODIFY], [workshop-intel.css](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/workshop/workshop-intel.css) [MODIFY])*:
  - **El pedido, en dos partes**: (1) las pestañas (Historial/Embudo/Simulador/Acciones/Landing) vivían DEBAJO del hero + las 6 tarjetas KPI + el panel de Diagnóstico — cambiar de sección obligaba a scrollear esa franja entera cada vez, a diferencia de la referencia externa donde el `.topbar`/`.main-nav` está arriba de todo y siempre visible; (2) las métricas de un workshop (agendas, ventas) quedaban congeladas en el snapshot con el que se guardó el evento — reflejar una agenda o venta nueva requería que alguien entrara al panel y apretara "Resync sistema" a mano.
  - **Navegación — `ws-topbar` pegado arriba (`position: sticky; top: 0`)**: título compacto + las 6 pestañas (se sumó una nueva, "Resumen") ahora viven en un `<header>` sticky contra `#app-main-scroll` (el contenedor con scroll real de `MainLayout.jsx`, confirmado que no hay otro elemento sticky por encima compitiendo por ese `top: 0`). El hero grande ("Rendimiento & Workshop Intelligence.", copy, período analizado), las 6 `WorkshopKpiCards` y `WorkshopDiagnostico` — antes SIEMPRE visibles arriba de cualquier pestaña — se movieron adentro de la pestaña nueva "Resumen" (ícono `LayoutDashboard`, calcado de la vista "General" de la referencia). El resto de pestañas (Historial/Embudo/Simulador/Acciones) ahora arrancan justo debajo del topbar sticky, sin ese bloque de por medio — que es literalmente el pedido de la sesión ("separa el resumen de los datos de los eventos, para no tener que hacer tanto scroll para analizar"). El toggle Tarjetas/Tabla (antes en el header global) se movió adentro de la pestaña "Historial", que es la única que lo usa.
  - **Sincronización en vivo — por qué NO recalcular todo en cada carga**: se investigó primero (agente de exploración) si `calcular_prefill` (el cálculo real detrás de "Resync sistema") era barato — no lo es: `financial_agendas.registro` y `.created_at` no tienen índice, hay patrones N+1 en `_estado_post_call`/`_ventas_de`, y recorre clientes/agendas/ventas con varios `LIKE`. Recalcular TODOS los `WorkshopEvent` cargados en cada visita al dashboard hubiera multiplicado ese costo por cada evento, en cada carga de cualquier usuario — justo el motivo por el que el código ya evitaba pedirlo para la lista completa (comentario preexistente en `WorkshopDashboardPage.jsx`).
  - **El hook (`workshop_live_sync.py`)**: un listener `before_commit` sobre `db.session` (no por cada uno de los ~7 sitios donde se crean `FinancialAgenda`/`FinancialSale` — vive a nivel de sesión y los intercepta a todos por igual) que, si el commit que está por pasar creó o modificó una de esas dos, recalcula (vía `calcular_prefill`, la MISMA función del botón manual) solo los `WorkshopEvent` ya registrados cuya ventana vivo+grabación (`_ventana_landing`, hasta `DIAS_VENTANA_LANDING` días) cae sobre esa fecha — en la práctica, como mucho 1 o 2 eventos, nunca todos. Se probó `after_commit` primero y falló (`InvalidRequestError: session is in 'committed' state`) porque ahí la sesión ya no admite más SQL — la doc de SQLAlchemy señala justo `before_commit` para este caso: los cambios quedan DENTRO del mismo commit que los disparó (atómico con la agenda/venta que los originó), y `calcular_prefill` ya ve la fila nueva por el autoflush que dispara su propia query. Si `calcular_prefill` lanza una excepción para un evento puntual, se loguea y se sigue con el resto — nunca aborta el commit original (el closer que está declarando una venta no tiene por qué quedar bloqueado porque falle el recálculo del dashboard).
  - **Columna nueva `synced_at`**: se estampa tanto desde el hook como desde el resync manual (`PUT /workshop/events/<id>` cuando el payload trae las 4 métricas automáticas) — mismo criterio para las dos vías, así "Sincronizado hace X" (nuevo, junto al botón "Resync sistema" en la pestaña Embudo) no distingue si fue automático o manual.
  - **Frontend — recoger el cambio sin que el usuario haga nada**: `WorkshopDashboardPage.jsx` pasa a pollear `workshop/events` cada 20s en modo silencioso (mismo patrón ya usado en `CommentsSection.jsx`/`AlertsHubPage.jsx`, sin spinner ni toast de error por tick fallido) y, mientras la pestaña Embudo está abierta, también re-pide el desglose vivo/grabación (`workshop/prefill`) cada 30s. De paso se corrigió un bug latente: `selectedEventForFunnel` era una copia congelada del evento al momento de seleccionarlo — un refresh manual (`fetchEvents()`) actualizaba la lista pero NO ese objeto, así que la pestaña Embudo podía mostrar números viejos aun después de recargar a mano. Ahora `fetchEvents` siempre resincroniza `selectedEventForFunnel` por id contra la lista fresca.
  - **Verificación del hook (`scratch/test_workshop_live_sync.py`, no comiteado)**: contra `instance/local.db`, un `WorkshopEvent` de prueba con métricas en 0 → crear una `FinancialAgenda` que matchea su fuente y fecha → commit → sin llamar a `calcular_prefill` ni al endpoint de resync a mano, el evento ya tenía `agendas_exitosas=1` y `synced_at` seteado; crear además una `FinancialSale` válida → segundo commit → `sales=1`, `cash_collected=500.0`, `synced_at` refrescado de nuevo. Las 2 verificaciones pasaron. `app.py` arrancó limpio con el hook registrado (sin errores de servidor).
  - **Verificación en navegador NO completada, mismo motivo externo que la entrada anterior**: al momento de verificar, otra sesión de Claude Code en paralelo sobre este mismo `develop` seguía a mitad del reemplazo de Videos de Documentación por un módulo "Playbook" — `frontend/src/App.jsx` importaba `./components/training/TrainingVideoGate`, un archivo ya borrado por esa sesión pero con el import todavía sin actualizar, así que CUALQUIER pantalla de la SPA (no solo Workshop) tiraba `Failed to resolve import` en el dev server compartido. Verificado en cambio: los dos archivos JSX tocados pasan un chequeo de sintaxis aislado con `esbuild.buildSync` (sin depender de que el resto de la app resuelva), y se revisaron a mano las 6 pestañas/props contra los componentes hijos existentes. Pendiente repetir el recorrido visual real (sticky nav, pestaña Resumen, badge "En vivo", "Sincronizado hace X") en cuanto el import roto de la otra sesión se resuelva.
  - **Nota de colisión, misma raíz que la entrada anterior**: el `down_revision` de la migración nueva (`c2d3e4f5a6b7`) apuntaba a `ad569b0263be` (la migración de training videos) cuando se escribió; esa sesión paralela la borró minutos después al reemplazarla por `playbook`, pero su propia migración nueva ya se había generado con `down_revision='c2d3e4f5a6b7'` (la cadena vio primero esta migración), y el archivo de esta sesión terminó re-apuntado solo a `b4c5d6e7f8a9` — la cadena de Alembic quedó consistente sin que hiciera falta tocar nada manualmente, pero se documenta como recordatorio de que dos sesiones escribiendo migraciones en paralelo sobre el mismo working directory (no worktrees separados) es frágil.

- **5 de Septiembre de 2026 (5)** *(reemplazo completo del módulo de Videos de Documentación (sesión de hoy más temprano) por "Playbook": jerarquía Roadmap → Módulo → Lección con quiz de comprensión — pedido explícito del usuario a partir de un mockup de referencia externo (`Closer Workspace - Etapas, Formulario y Bugs.dc.html`, con su design system `learnation-design-system-.../README.md`) que compartió para rediseñar el Closer Workspace, [playbook.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/playbook.py) [NEW], [playbook.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/playbook.py) [NEW], [4c64ef2c0f18_replace_training_videos_with_playbook.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/4c64ef2c0f18_replace_training_videos_with_playbook.py) [NEW], [training_video.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/training_video.py) [DELETE], [training_videos.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/training_videos.py) [DELETE], [__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY], [models/__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/__init__.py) [MODIFY], [PlaybookContext.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/contexts/PlaybookContext.jsx) [NEW], [PlaybookOverlay.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/playbook/PlaybookOverlay.jsx) [NEW], [PlaybookLessonView.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/playbook/PlaybookLessonView.jsx) [NEW], [PlaybookNotification.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/playbook/PlaybookNotification.jsx) [NEW], [PlaybookAdminPanel.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/PlaybookAdminPanel.jsx) [NEW], [PlaybookLessonFormModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/PlaybookLessonFormModal.jsx) [NEW], [App.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/App.jsx) [MODIFY], [Dock.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/shared/Dock.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [OperationsSettingsPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/operations/settings/OperationsSettingsPage.jsx) [MODIFY], [training_videos.dc.html mockup revisado, no comiteado])*:
  - **Por qué reemplazar en vez de extender lo de esta mañana**: el mockup pedía una jerarquía real (Skills/Platform/Learnation como roadmaps, cada uno con módulos y lecciones), tres tipos de pregunta (única/múltiple/verdadero-falso) con feedback inmediato por pregunta (no recién al final), y un asistente "Learnito" de búsqueda por transcripción — mucho más que el modelo plano de un video con un quiz. Como el feature de esta mañana nunca llegó a producción ni tenía datos reales, se lo borró por completo (modelo, API, migración, componentes de frontend) en vez de migrarlo, y se escribió Playbook desde cero. El usuario aclaró el alcance real antes de arrancar: Learnito queda como placeholder "Próximamente" (sin buscador de verdad todavía) y la parte visual se ajusta cuando mande las bases de diseño completas del sistema — esta sesión se concentró en la estructura y el funcionamiento, con el lenguaje visual ya existente del proyecto (Tailwind + tokens propios), no en clonar pixel a pixel el mockup.
  - **Modelo (7 tablas)**: `PlaybookRoadmap` (nombre, acento de color) → `PlaybookModule` → `PlaybookLesson` (título, descripción, `loom_link`, `duration_minutes`, `transcript` opcional ya guardado para cuando exista Learnito, `target_roles`, `is_active`) → `PlaybookQuestion` (`question_type`: `single`/`multiple`/`true_false`) → `PlaybookOption`. Progreso de un usuario se separa en dos tablas: `PlaybookLessonProgress` (solo `video_watched_at`, no implica aprobado) y `PlaybookCompletion` (aprobó el quiz completo, o no había quiz y ya vio el video) — la distinción es la que permite calcular el estado real de una lección (`nuevo` / `en_progreso` / `pendiente` / `completado`) sin la cual no se podían replicar los badges del mockup.
  - **Corrección server-side por pregunta, no al final**: a diferencia del feedback "todo o nada" de esta mañana, acá `POST /playbook/lessons/<id>/answer` corrige UNA pregunta a la vez (mismo criterio de nunca exponer la respuesta correcta hasta responder) — el frontend puede pintar esa pregunta puntual en verde/rojo apenas el usuario la contesta, como en el mockup, en vez de esperar a enviar todo el formulario. `POST .../complete` igual vuelve a validar TODAS las respuestas contra la base antes de aprobar — nunca confía en que el frontend ya mostró todo en verde.
  - **`question_type='single'` validado server-side**: si alguien manda 2+ opciones marcadas correctas en una pregunta de selección única, `_apply_questions` la rechaza con 400 antes de guardar nada — evita un quiz roto donde nunca se puede acertar exactamente el set esperado.
  - **Frontend, misma decisión de ubicación que el feature de esta mañana**: `PlaybookOverlay`/`PlaybookNotification` se montan una sola vez en `App.jsx` (vía `PlaybookContext`, con `pendingCount`/`openPlaybook` compartidos) para que abran igual desde cualquier rol. El botón que los dispara SÍ cambia según la página: pill con gradiente en el header propio de `CloserWorkflowPage` (calcado del mockup, con badge de pendientes) para closers, y un ícono nuevo en `Dock.jsx` (compartido por el resto de roles vía `MainLayout`) para todos los demás — un solo overlay, dos puntos de entrada.
  - **Decisión explícita del usuario sobre el botón de bugs**: aclaró que el ícono de bug del header del mockup NO debía tocar el botón flotante ya existente (`BugReportWidget`, esquina inferior derecha) — se dejó completamente intacto, el mockup solo aportó ideas para Playbook/Confirmaciones.
  - **Bug real encontrado en verificación de navegador**: entrar a una lección directo desde "Pendientes" (sin pasar antes por Roadmaps→Módulos) dejaba `moduleId`/`roadmapId` en `null` en `PlaybookOverlay`; al aprobar la única lección del módulo (sin siguiente lección) el `onBack()` intentaba mostrar la vista "lecciones" con esos ids vacíos y quedaba en blanco (el `useEffect` que pide `/playbook/modules/<id>/lessons` nunca corría). Fix: `PlaybookLessonView` avisa a su padre (`onModuleResolved`) qué módulo/roadmap resultó ser el de la lección apenas la carga, sin importar por dónde se entró — así "volver" siempre tiene a dónde ir.
  - **Migración, mismo tipo de colisión que la de esta mañana**: mientras se armaba esta migración, otra sesión en paralelo ya había agregado la suya (`c2d3e4f5a6b7`, `synced_at` de Workshop) encadenada sobre la de training videos (única head en ese momento) — al borrar esa migración vieja, se tuvo que reapuntar el `down_revision` de `c2d3e4f5a6b7` a `b4c5d6e7f8a9` para no romper la cadena de Alembic (ver nota de esa sesión más arriba). Aplicada limpio después contra `instance/local.db`.
  - **Fricción real causada a las otras dos sesiones en paralelo (documentada también desde su lado, entradas de arriba)**: borrar `training_video.py`/`training_videos.py` y recién después actualizar `app/__init__.py`/`App.jsx` dejó una ventana corta con el backend compartido del puerto 5000 tirando `ModuleNotFoundError` y el frontend compartido tirando `Failed to resolve import` para CUALQUIER pantalla — no solo Playbook. Ambas sesiones (director_comercial 403, Workshop live sync) tuvieron que verificar por otros medios (test client aislado, chequeo de sintaxis con esbuild) en vez de en el navegador real mientras tanto. Para la próxima vez: al borrar un archivo importado desde otro compartido (`app/__init__.py`, `App.jsx`), actualizar esa referencia en el mismo paso, no en ediciones separadas.
  - **Verificación end-to-end (`scratch/simulate_playbook.py`, no comiteado)**: 15 verificaciones con el mismo patrón de test client fuera de `app_context()` manual — un setter no puede crear roadmaps (403); operador crea roadmap+módulo+lección con una pregunta `single` y una `multiple`; una pregunta `single` con 2 opciones correctas es rechazada (400); un setter no ve el roadmap (0 lecciones visibles para su rol); un closer lo ve con 0% de avance y el módulo en "no_iniciado"; la lección aparece "nueva" sin exponer respuestas correctas; marcar el video visto pasa el estado a "en_progreso"; feedback inmediato por pregunta (mal=false, bien=true, la de tipo `multiple` exige el set exacto); completar con una respuesta mal da 400 sin crear `PlaybookCompletion`; completar bien la aprueba y devuelve `next_lesson_id`; ya no aparece en pendientes y el roadmap pasa a 100%; el overview de admin refleja el cumplimiento por rol; editar el quiz resetea la completion existente. Las 15 pasaron.
  - **Verificación real en navegador** (operador `Kerwin`, sesión propia en un puerto de Vite separado del resto de sesiones por el conflicto de puertos ya documentado): creado un roadmap/módulo/lección real desde el wizard del panel de administración, con una pregunta verdadero/falso (confirmando que el tipo genera las dos opciones fijas "Verdadero"/"Falso" sin campos de texto editables); impersonado el closer `Jean Carlo` → notificación de "Playbook nuevo" apareció sola al entrar, con autor/duración/preguntas/audiencia; "Verlo ahora" abrió el overlay en Pendientes; el video (falso, sin Loom real) disparó igual `onLoad` y habilitó "Ir a las preguntas"; respondida "Falso" → se pintó en rojo sin cerrar; respondida "Verdadero" → verde, botón "Aprobar" habilitado → lección aprobada y overlay cerrado; confirmado en el panel del operador que "0/5 closers" pasó a reflejar el completado y que el roadmap del closer subió a 100%. Se navegó además Roadmaps→Módulo (estado "Completo")→Lección completada (badge verde en la lista) para confirmar el fix de navegación de arriba. Datos de prueba eliminados al terminar por API (`DELETE /playbook/roadmaps/<id>`, cascada completa), base local en cero filas en las 7 tablas nuevas.

- **5 de Septiembre de 2026 (6)** *(Kanban de Confirmaciones: de 3 a 2 columnas + stepper del modal con círculos numerados — retoque liviano pedido explícito del usuario a partir del mismo mockup de referencia de la entrada anterior ("ahora solo se divide en dos... el modal de confirmación cambió... no lo quiero cambiar tanto, pero toma lo mejor de ese nuevo modal"), [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [index.css](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/index.css) [MODIFY])*:
  - **Alcance elegido por el usuario entre dos opciones planteadas**: se ofreció (a) un retoque liviano — colapsar el Kanban sin tocar `confirm_status`/lógica, o (b) rehacer el modelo completo a las 5 etapas del mockup (Por contactar/Contactado/Horario/VideoAsk/Testimonio) + "dolores" + notas. El usuario eligió (a) explícitamente — ni el modelo de datos (`confirm_status`: `por_confirmar`/`conversando`/`Confirmado`) ni `saveConfirmReport`, `confirmationsPipeline`, ni ningún endpoint del backend se tocaron en esta entrada.
  - **Kanban, 3→2 columnas**: la columna "Conversando" desaparece como columna propia y sus tarjetas pasan a vivir DENTRO de "Por confirmar", como un subgrupo con su propio encabezado ("Sin contactar" / "Conversando", cada uno con el punto de color que antes tenía su columna — ámbar/azul). Cada tarjeta sigue recibiendo su `phase` real (`'por_confirmar'` o `'conversando'`) al armarse, así que el botón de acción ("Contactar y registrar" vs. "Confirmar asistencia") y el resto de `renderKanbanCard` no cambiaron una sola línea. CSS nueva: `.kb-v6.kb2-v6` (modificador de 2 columnas) y `.ksub-v6` (encabezado de subgrupo) — se agregaron como clases APARTE en vez de editar `.kb-v6` directo, porque esa clase también arma el Kanban de 3 columnas de "Llamadas" (atrasadas/hoy/reportadas) unas pestañas más allá, que debía quedar intacto.
  - **Modal — círculos numerados en el stepper existente**: el `.pipe`/`.pstep` de la banda de 3 pasos (Por confirmar → Conversando → Confirmado) ya tenía una identidad visual bastante lograda (pastilla segmentada con degradado en el paso actual) — en vez de reconstruirlo como el stepper de círculos-con-línea del mockup, se le sumó un círculo numerado (`①②③`, o `✓` si el paso ya se completó) al costado de cada etiqueta, reemplazando el tick flotante en la esquina que tenía antes. Es la pieza más reconocible del stepper nuevo del mockup, incorporada sin tocar la estructura de 3 pasos, los textos, ni la lógica de `currentIdx`/`currentKey` que ya existía.
  - **Verificación en navegador** (closer `Jean Carlo`, impersonado desde el panel de operador y revertido al terminar): el Kanban de Confirmaciones muestra "POR CONFIRMAR (2)" con el subgrupo "SIN CONTACTAR" y "CONFIRMADO (0)" — 2 columnas reales, no un placeholder; abierta la ficha de un lead sin contactar, el stepper muestra los 3 pasos con círculo "①" relleno en gradiente sobre el paso actual y círculos "②"/"③" en contorno sobre los pasos futuros. Se confirmó además que el Kanban de "Llamadas" (pestaña 02, Reportar) sigue mostrando sus 3 columnas originales sin cambios — la clase `.kb2-v6` no filtró a ningún otro tablero. No se guardó ningún cambio real sobre el lead de prueba (se cerró el modal sin tocar "Guardar Reporte"), así que no hizo falta limpiar nada en la base al terminar.

- **5 de Septiembre de 2026 (7)** *(causa raíz del contador de agendas: el webhook de n8n/Calendly guardaba la hora de la cita en horario de La Paz dentro de una columna que todo el sistema lee como UTC — reportado por el usuario mirando el Kanban del Closer Workspace contra el calendario de los closers ("decían que fueron hace dos, hace una hora, y en realidad falta una hora para esa agenda"), [agenda_time_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/agenda_time_service.py) [NEW], [financial_agendas.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/public/financial_agendas.py) [MODIFY])*:
  - **Dónde estaba el error, exactamente**: en `receive_financial_agendas` (`app/api/public/financial_agendas.py`), la fecha de la cita que manda n8n se normalizaba con `agenda_date.astimezone(la_paz_tz).replace(tzinfo=None)` — es decir, se guardaba **hora de pared de America/La_Paz** en `FinancialAgenda.date`. De ahí `BookingService.sync_financial_agenda_to_appointment` la copia tal cual a `Appointment.start_time` (`appt.start_time = agenda.date`). Pero absolutamente todo lo que consume esa columna la lee como **UTC**: `user_time_service.limites_dia_utc` arma las ventanas del día pasando de la zona del usuario a UTC, la API la publica con `.isoformat()` sin sufijo, el frontend la parsea con `parseUtcIso()` (que le antepone la `Z` a propósito) y `google_service.py:114` directamente hace `start_time.isoformat() + 'Z' # Assuming it's UTC`. O sea: el dato entraba en una convención y salía leído en otra.
  - **El error NO viene de Calendly**: el instante que manda Calendly es correcto (ISO 8601 con zona). Se pierde en la ingesta nuestra. Reproducido antes de tocar nada con los cuatro formatos que puede mandar n8n (`scratch/repro_tz.py`, no comiteado — `scratch/` está en `.gitignore`): los cuatro terminaban guardados como `15:00` naive para una cita real de las 16:00 de Curitiba, y leídos como UTC se veían a las 12:00 → **4 horas de corrimiento hacia atrás, exactamente el offset de La Paz**. Por eso una agenda a la que le faltaba una hora aparecía como "Hace 3 horas" en el contador del Kanban; el número que veía el usuario no era un redondeo mal hecho, era el instante equivocado.
  - **Por qué el calendario de agendas y el Kanban decían cosas distintas**: `FinancialAgendasPage.jsx` (panel de agendas) formatea con `new Date(str)` sobre un string sin zona, que JS interpreta como **hora local del navegador** — o sea, mostraba los `15:00` guardados como "15:00" y parecía correcto. El Kanban del closer usa `parseUtcIso()` y mostraba "12:00" del mismo registro. Las dos vistas leían bien su propio código y mal el dato: la diferencia entre ambas es literalmente el offset del navegador de quien mira.
  - **La regla nueva, en un solo lugar (`app/services/agenda_time_service.py`)**: en la base **todo es UTC naive**. Si el string trae offset (`...Z`, `-04:00`) se convierte a UTC y punto; si viene sin offset se asume `AGENDAS_SOURCE_TZ` (variable de entorno, por defecto `America/La_Paz`, que es como n8n formatea cuando pierde la zona) y recién ahí se pasa a UTC. Se eligió variable de entorno en vez de constante para poder corregir el supuesto sin deploy si la auditoría muestra que n8n manda otra zona.
  - **Aviso explícito en los logs**: cuando llega una fecha **sin** offset, el webhook loguea `[AGENDA SIN ZONA]` con el string crudo y la zona asumida. Un string sin zona es un dato ambiguo por definición — el sistema tiene que adivinar. El aviso queda en Railway para poder pedirle a n8n que mande ISO con zona y dejar de adivinar.
  - **Deduplicación de agendas ajustada al cambio de convención**: el bloque que busca "la misma agenda del mismo día para el mismo lead" cortaba el día con `agenda_date.replace(hour=0...)`. Con las fechas ya en UTC eso pasa a ser el día UTC, y una cita de las 21:00 local cae en el día UTC siguiente — el deduplicador dejaría de reconocer una reprogramación y crearía un registro duplicado. Se reemplazó por `limites_dia_origen()`, que calcula el día calendario en la zona de origen y lo devuelve como rango UTC (mismo patrón que `user_time_service.limites_dia_utc`, que ya hacía esto para las ventanas de día del usuario).
  - **Edición manual del panel (`PUT /public/financial-agendas/<id>`)**: escribía la misma columna con `parse_date_robustly` sin normalizar zona, así que reintroducía el corrimiento a mano cada vez que alguien corregía una fecha desde el panel. Ahora pasa por `parse_a_utc`. `created_at` hacía lo mismo con La Paz y quedó también en UTC; el campo de texto `registro` (que es lo que ordena y muestra el panel) se mantiene en hora local de la fuente a propósito, para no mover los listados que ordenan por él.
  - **Verificación** (`scratch/test_ingesta_utc.py`, no comiteado): se levantó la app real con SQLite temporal y se le mandaron al webhook los cuatro formatos posibles para la misma cita (`...Z`, `-04:00`, `-03:00`, y sin zona). Los cuatro quedan ahora en `2026-09-05 19:00 UTC` tanto en `FinancialAgenda.date` como en el `Appointment.start_time` sincronizado, y se ven a las 16:00 en Curitiba — el instante real. Antes los cuatro quedaban en `15:00`.
  - **Pendiente en esta entrada, resuelto en la siguiente**: las agendas que ya están en la base siguen guardadas con la convención vieja. El fix arregla lo que entra de acá en adelante; los registros históricos se corrigen con la herramienta de auditoría de la entrada (8).

- **5 de Septiembre de 2026 (8)** *(herramienta para ver cómo están realmente las agendas y corregir el histórico corrido — pedido explícito del usuario ("quisiera encontrar una forma de que podamos revisar cómo están las agendas realmente y cómo se ven los horarios"), [auditar_horarios_agendas.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/scripts/auditar_horarios_agendas.py) [NEW])*:
  - **Qué compara**: el string crudo que mandó la fuente (`FinancialAgenda.fecha_meet`, con `raw_data['fecha']`/`['date']` como respaldo — el webhook lo guarda sin tocarlo, así que es la evidencia original) contra el instante que efectivamente quedó en `FinancialAgenda.date` y en el `Appointment.start_time` sincronizado. La diferencia entre los dos es el corrimiento, en horas, y se imprime junto con cómo se ve cada uno en la zona horaria que se le pida (`--tz America/Sao_Paulo` para verlo con los ojos del usuario, por defecto la zona de origen).
  - **Reporta también el formato que manda n8n** (con offset / sin offset): es el dato que decide si el sistema *sabe* la hora o la *asume*. Un string sin offset es ambiguo por definición y obliga a suponer `AGENDAS_SOURCE_TZ`; si el conteo de "SIN offset" es alto, la acción correcta no es tocar más código sino pedirle a n8n que mande ISO con zona. El script lo dice explícitamente en esa línea.
  - **`--fix` corrige el histórico, y solo lo que puede probar**: reescribe `FinancialAgenda.date` con el instante derivado del string de origen, y arrastra el `Appointment` asociado. El vínculo agenda→cita se resuelve por cliente (mail/instagram) **más `start_time` exactamente igual a `agenda.date`**, porque `sync_financial_agenda_to_appointment` copia ese valor tal cual. Esa igualdad exacta es a propósito: las citas creadas a mano desde el workspace o por el embudo web ya guardan UTC correcto (`localInputsToUtcIso` en el frontend, `datetime.fromtimestamp(..., tz=utc)` en el embudo) y nunca coinciden con una agenda corrida, así que el script no las toca. Corregir dos veces una cita que ya estaba bien habría sido el peor resultado posible de esta herramienta.
  - **Sin `--fix` no escribe nada**: es seguro correrlo contra producción. Sigue el patrón de `scripts/actualizar_db.py` para `--target staging`, incluida la resolución del destino ANTES de importar `app` (el bug documentado el 27/ago/2026: `config.py` calcula la URI al importarse, así que resolver el target después hace que `--target staging` escriba en la SQLite local sin avisar).
  - **Verificación**: se sembró una base SQLite con tres agendas — dos guardadas con la convención vieja (hora de La Paz) y una ya correcta en UTC (`scratch/seed_agendas_viejas.py`, no comiteado). La auditoría detectó exactamente las dos corridas con `-4.00 h` y dejó la correcta como "coincide"; `--fix` arregló 2 agendas + 2 citas; la re-auditoría inmediata devolvió las 3 en verde, confirmando que la corrección es idempotente (correrlo dos veces no vuelve a mover nada).
