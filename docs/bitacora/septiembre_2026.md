# Bitácora - Septiembre 2026

- **1 de Septiembre de 2026** *(sistema de reporte de bugs y feedback: widget flotante global + captura reactiva de errores, [bug_report.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/bug_report.py) [NEW], [bug_reports.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/bug_reports.py) [NEW], [b7c8d9e0f1a2_add_bug_report_model.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/b7c8d9e0f1a2_add_bug_report_model.py) [NEW], [__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY], [models/__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/__init__.py) [MODIFY], [BugReportWidget.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportWidget.jsx) [NEW], [BugReportChat.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportChat.jsx) [NEW], [bugReportBus.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/utils/bugReportBus.js) [NEW], [MainLayout.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/MainLayout.jsx) [MODIFY], [ErrorBoundary.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/ErrorBoundary.jsx) [MODIFY], [api.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/services/api.js) [MODIFY])*:
  - **Modelo y endpoint (`BugReport`, `app/api/bug_reports.py`)**: tabla `bug_reports` con `user_id`/`user_role` (snapshot del rol al momento del reporte, no el actual), `description`, `urgency` (`muy_urgente`/`urgente`/`neutro`/`sin_urgencia`), `route`, `user_agent`, `technical_context` (JSON serializado con el error técnico si el disparo fue reactivo) y `screenshot` (data URL base64 en `Text`, sin límite de tamaño porque el proyecto no define `MAX_CONTENT_LENGTH`). `POST /api/bug-reports` requiere sesión (`@login_required`) pero cualquier rol puede crear un reporte; `GET /api/bug-reports`, `GET /api/bug-reports/<id>` (con screenshot incluido) y `PATCH /api/bug-reports/<id>/status` quedan restringidos a `admin` para una futura pantalla de gestión, que no se construyó en este alcance (no estaba pedida).
  - **Migración escrita a mano**: el autogenerate de Alembic sigue fallando en este repo por el problema preexistente de `ads.ad_set_id` → `ad_groups` (ya documentado el 29/08). La migración `b7c8d9e0f1a2` se escribió siguiendo el patrón de `286308c75915` y se verificó aplicándola contra `instance/local.db` (`flask db upgrade` corrió limpio, tabla confirmada por inspección directa de `sqlite_master`).
  - **Disparador reactivo sin tocar los ~15 archivos con `toast.error(...)` existentes**: en vez de instrumentar cada call site, el interceptor de respuesta de [api.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/services/api.js) dispara un evento global (`bugReportBus.js` → `CustomEvent`) en cualquier error 5xx o de red (`!error.response || status >= 500`). `BugReportWidget` escucha ese evento y muestra un mini-prompt tipo toast ("¿Quieres reportarlo?") que expira solo a los 10s si no se toca — abrir el chat completo automáticamente en cada fallo de fondo hubiera sido intrusivo. La petición POST del propio reporte se marca con `{ skipBugReport: true }` en el config de axios para no reentrar en el loop si el envío del reporte mismo falla.
  - **Disparador desde ErrorBoundary**: ahí sí hay una acción explícita del usuario sobre un error visible en pantalla, así que el botón "Reportar error" dispara el mismo evento con `autoOpen: true` y abre el chat directo (con `error.message` y `errorInfo.componentStack` precargados como contexto técnico), sin pasar por el prompt intermedio.
  - **Captura de pantalla**: `html2canvas` (dependencia nueva) sobre `document.body`, con `scale: 0.5` y compresión JPEG (calidad 0.6) para no inflar el payload; los elementos del propio widget (`data-bug-report-ignore="true"`) se excluyen de la captura vía `ignoreElements` para no fotografiar el chat sobre sí mismo.
  - **Posicionamiento del botón flotante**: `bottom-8 right-24` en vez de `right-8` porque esa esquina ya la usa el botón de tutorial de `OnboardingTour.jsx` (mismo `bottom-8 right-8`, condicional a que haya un tutorial disponible) — con ambos en `right-8` se hubieran superpuesto cuando coincidieran.
  - **Bug de icono detectado en verificación de navegador**: `lucide-react` en esta versión del proyecto no exporta `MessageCircleWarning` (sí `Bug`/`BugIcon`) — el build de Vite tiraba `SyntaxError` en consola y dejaba la pantalla en negro. Corregido a `Bug` en `BugReportWidget.jsx` y `ErrorBoundary.jsx`; hubo que borrar `frontend/node_modules/.vite` para que el caché de dependencias pre-bundleadas de Vite dejara de servir la versión rota.
  - **Verificación**: backend probado end-to-end con el test client de Flask (POST/GET/GET-by-id/PATCH, sesión simulada con `login.session_protection = None` porque `'strong'` invalidaba la sesión simulada entre requests). Flujo completo también verificado en el navegador ya autenticado (usuario `Kerwin`, rol `operator`): abrir el widget, completar los dos pasos, POST real devolviendo `201`, fila confirmada en `instance/local.db` con un screenshot real de ~9KB en base64, y limpieza del registro de prueba al terminar.

- **1 de Septiembre de 2026 (2)** *(pregunta "¿Cuál es el problema?" para reportes no reactivos + pestaña de gestión para operator/admin con respuesta al usuario, [bug_report.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/bug_report.py) [MODIFY], [bug_reports.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/bug_reports.py) [MODIFY], [c9d1e2f3a4b5_add_bug_report_response_fields.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/c9d1e2f3a4b5_add_bug_report_response_fields.py) [NEW], [BugReportChat.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportChat.jsx) [MODIFY], [BugReportWidget.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportWidget.jsx) [MODIFY], [BugReportHistory.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportHistory.jsx) [NEW], [BugReportsPanel.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/BugReportsPanel.jsx) [NEW], [OperationsSettingsPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/operations/settings/OperationsSettingsPage.jsx) [MODIFY])*:
  - **Pregunta condicional "¿Cuál es el problema?"**: solo se pregunta cuando el reporte NO viene de un error detectado (`!technicalContext`) — si viene de un error reactivo, esa pregunta ya está respondida por el error mismo y `BugReportChat` arranca directo en el paso "qué intentabas hacer". Backend: `problem` es `nullable=True` en el modelo, pero el endpoint `POST /bug-reports` exige `problem` **o** `technical_context` (al menos uno de los dos tiene que explicar el problema — nunca ninguno).
  - **Gestión para operator/admin**: se amplió `check_admin` a `check_manager` (`MANAGER_ROLES = ('admin', 'operator')`) en los cuatro endpoints de gestión (`GET` lista, `GET` detalle, `PATCH status`, `POST respond` nuevo) — el pedido especificaba "operador", y el resto del panel de control técnico (`OperationsSettingsPage.jsx`, donde vive esta pestaña nueva "Reportes de Bugs") ya usa `roles={['operator','admin']}` en `App.jsx`, así que se siguió el mismo criterio de acceso en vez de dejarlo admin-only como alerts.py.
  - **Responder al usuario**: `POST /bug-reports/<id>/respond` guarda `admin_response`, `responded_at`, `responded_by_id` y pone `is_read_by_user=False`; si el estado seguía en `open` pasa a `reviewed` automáticamente (pero NO pisa un estado ya `resolved` — probado explícitamente: responder después de marcar resuelto no regresa a `reviewed`). `BugReportsPanel.jsx` (nueva pestaña) lista todos los reportes con filtro por estado, selector de estado inline, input de respuesta rápida y un lightbox que pide el detalle completo (`GET /bug-reports/<id>`, con el screenshot) solo al abrirlo — la lista general nunca trae los screenshots completos para no inflar el payload.
  - **El usuario ve la respuesta (`BugReportHistory.jsx`, nuevo)**: pestaña "Mis reportes" accesible desde un segundo botón flotante (icono de reloj) junto al botón de reportar. `GET /bug-reports/mine` es de cualquier usuario autenticado y solo devuelve sus propios reportes (aislado por `user_id`, verificado explícitamente en la simulación). Badge de "no leído" en el botón flotante: el fetch pasivo que arma el contador (al montar el widget) **no** manda `?mark_read=true`, así el aviso no desaparece antes de que el usuario efectivamente abra "Mis reportes" — solo abrir el historial de verdad marca `is_read_by_user=True` en el backend.
  - **Bug de icono #2 en verificación de navegador**: mismo problema que con `MessageCircleWarning` el 1/09 — `lucide-react` tampoco exporta `MessageSquareReply` en esta versión. Corregido a `Reply` en `BugReportHistory.jsx`, verificado con un chequeo rápido de `Object.keys(require('lucide-react'))` antes de dar por buenos los demás íconos nuevos (`ImageIcon`, `History`, `Reply`, etc.) para no repetir el mismo ciclo de error-en-navegador otra vez.
  - **Simulación multi-rol (pedido explícito del usuario, `scratch/simulate_bug_reports.py`, no comiteado — `scratch/` está en `.gitignore`)**: se simuló closer, setter, triage, operator y admin con sesiones de Flask test client independientes, cubriendo: reporte no-reactivo exige `problem` (400 si falta), reporte reactivo no lo exige, urgencia inválida rechazada, closer/setter/triage reciben 403 en los endpoints de gestión (lista completa, detalle, responder), operator ve la lista completa y responde, admin cambia estado a `resolved` y luego responde sin que el estado retroceda, el reportante ve su propia respuesta vía `/mine` (con la distinción pasivo vs `mark_read=true` del punto anterior), y el aislamiento por usuario en `/mine` (un setter no ve reportes de otros). Las 10 verificaciones pasaron.
  - **Bug real encontrado en el harness de simulación, no en la app**: la primera versión del script fallaba con confusión de identidad entre usuarios simulados (el reporte del setter terminaba atribuido al closer). Causa: el script envolvía TODAS las llamadas HTTP dentro de un único `with app.app_context():` externo — Flask-Login cachea el usuario resuelto en `flask.g`, que vive atado al app context; como Flask reusa el app context ya activo en vez de empujar uno nuevo por request cuando detecta uno igual ya en el stack, el segundo usuario logueado heredaba el `flask.g` del primero. Fix: las llamadas de `test_client()` se sacaron de cualquier `app_context()` manual (cada request de Flask empuja el suyo propio si no hay uno activo); el `app_context()` solo se usa para leer/limpiar la base de datos antes y después. Además se detectó que el usuario de triage por defecto (`Romer`) tiene `is_active=False` en la base local — `UserMixin.is_authenticated` de Flask-Login devuelve `self.is_active`, así que una cuenta desactivada nunca se considera autenticada aunque la sesión tenga su `_user_id` — el script se ajustó para tomar usuarios activos (`Dari` en vez de `Romer`).
  - **Verificación en navegador**: además de la simulación de backend, se probó el flujo real en el navegador (usuario `Kerwin`/operator) — envío de un reporte no-reactivo con la pregunta "¿Cuál es el problema?" visible, aparición inmediata en la pestaña "Reportes de Bugs" con ambas respuestas mostradas, apertura del lightbox de captura de pantalla (confirmó una imagen real de la vista), envío de una respuesta desde el panel (transición automática de estado a "en revisión" + toast de confirmación) y verificación de que "Mis reportes" del widget muestra esa misma respuesta. Registro de prueba eliminado al terminar.

- **1 de Septiembre de 2026 (3)** *(fix: closers y otros roles no veían el widget — `CloserWorkflowPage` corre fuera de MainLayout, [App.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/App.jsx) [MODIFY], [MainLayout.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/MainLayout.jsx) [MODIFY], [BugReportWidget.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportWidget.jsx) [MODIFY])*:
  - **El bug real**: `/closer/deck` (`CloserWorkflowPage`, donde los closers pasan literalmente todo su turno tras el login) es la ÚNICA ruta protegida de todo `App.jsx` que NO envuelve su contenido en `<MainLayout>` — el propio archivo ya tenía un comentario explicando por qué ("para que los modales fixed funcionen standalone") y duplica su propio listener de atajos de teclado por el mismo motivo. Como `BugReportWidget` se montaba dentro de `MainLayout`, los closers jamás veían el botón flotante — el usuario lo reportó directamente ("no veo como pueden los closers y otros usuarios reportar bugs") y encontrar la causa exacta requirió leer `App.jsx` ruta por ruta, no solo mirar el componente del widget.
  - **Fix**: se movió `<BugReportWidget />` de dentro de `MainLayout.jsx` a `App.jsx`, como hijo directo de `<Router>` junto a `<Toaster />` — así se monta una sola vez para TODA la app sin importar si la vista activa usa `MainLayout` o no, y como vive fuera del árbol DOM de cualquier página no lo afecta un `transform` de framer-motion en alguna vista que le rompiera el `position: fixed` (un problema real de CSS que sí habría aparecido si se hubiera insertado dentro de cada página en vez de a nivel raíz). El propio componente ahora se autogatea con `useAuth()` (`if (!user) return null` después de los hooks) en vez de depender de que su padre ya filtre por sesión.
  - **Verificación**: no se pudo iniciar sesión como closer real (sin credenciales), así que se usó la función de "Acceso Simulado" que ya tiene la app — impersonar al closer `Jean Carlo` vía `POST /api/auth/impersonate` (llamado directamente desde la consola del navegador, mismo flujo que usa `OperatorControls.jsx`) y navegar a `/closer/deck?step=confirmations`. El botón de bug aparece ahí, el chat abre con "¿Cuál es el problema?" y el header muestra "JEAN CARLO · CLOSER" correctamente. Se revirtió la simulación (`POST /api/auth/revert`) al terminar para dejar la sesión del operador como estaba.

- **1 de Septiembre de 2026 (4)** *(hilo de mensajes real: el operador solo podía enviar una respuesta que se pisaba a sí misma y el usuario no podía contestar, [bug_report.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/bug_report.py) [MODIFY], [bug_reports.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/bug_reports.py) [MODIFY], [d3e4f5a6b7c8_add_bug_report_messages.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/d3e4f5a6b7c8_add_bug_report_messages.py) [NEW], [BugReportThread.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportThread.jsx) [NEW], [BugReportHistory.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportHistory.jsx) [MODIFY], [BugReportWidget.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportWidget.jsx) [MODIFY], [BugReportsPanel.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/BugReportsPanel.jsx) [MODIFY], [BugReportChat.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportChat.jsx) [MODIFY])*:
  - **El pedido del usuario**: *"respondi como operador y envie 2 mensajes a un reporte y el usuario solo puede ver la ultima respuesta que le envié y no me puede responder"*. El diseño anterior (campos `admin_response`/`responded_at`/`responded_by_id` en `BugReport`) era literalmente una sola respuesta por reporte, sin capacidad de conversación — no era un bug puntual, era una limitación de diseño del alcance anterior.
  - **Modelo nuevo (`BugReportMessage`)**: tabla `bug_report_messages` (FK a `bug_reports` con `ondelete='CASCADE'`) reemplaza esos cuatro campos. En su lugar, `BugReport` guarda `user_last_read_at`/`manager_last_read_at` — un mensaje cuenta como no leído para un lado si es de "el otro lado" (comparando `sender_id` contra `report.user_id`) y llegó después de su último `*_last_read_at`. Esto generaliza sin necesitar chequear roles explícitamente: si el mismo operador reportó y respondió su propio bug (como en la verificación del 1/09 anterior), `sender_id == report.user_id` siempre, así que nunca se marca a sí mismo como no leído.
  - **Migración con paso de datos**: antes de borrar `admin_response` y compañía, la migración `d3e4f5a6b7c8` copia cualquier respuesta existente a un `BugReportMessage` (usando SQL crudo vía `conn.execute` porque en el momento de la migración el modelo ORM viejo ya no coincide con el código) para no perder historial de reportes que ya tenían una respuesta guardada.
  - **Bug real encontrado en desarrollo, no en producción**: `to_dict()` calculaba `last_message` con `self.messages.order_by(BugReportMessage.created_at.desc()).first()` sobre la relación `lazy='dynamic'`, pero como esa relación ya se declaró con `order_by='BugReportMessage.created_at'` (ascendente) en su definición, el `.order_by(...desc())` encadenado no reemplazaba el orden sino que se sumaba, y devolvía el mensaje más ANTIGUO en vez del más reciente. Se detectó en la simulación (`scratch/simulate_bug_thread.py`, no comiteado), no fue obvio hasta comparar el resultado esperado contra el real. Fix: consultar `BugReportMessage.query.filter_by(...)` directo en vez de a través de la relación dinámica, así el `.order_by()` sí controla el orden desde cero.
  - **Endpoints**: `GET/POST /bug-reports/<id>/messages` reemplazan a `POST .../respond`. Accesibles para managers (cualquier reporte) o el propio autor (solo el suyo) — un tercer usuario (ej. un setter viendo el reporte de un closer) recibe 403 en ambos métodos, verificado explícitamente. Abrir el hilo (`GET`) marca la lectura del lado que lo abre automáticamente, sin necesitar el parámetro `?mark_read=true` de la versión anterior (ya no hace falta distinguir "fetch pasivo" de "abrir de verdad": la lista (`/mine`, `GET /bug-reports`) nunca abre el hilo, así que nunca marca lectura por accidente).
  - **`BugReportThread.jsx` (nuevo, compartido)**: burbujas de chat alineadas según `sender_id === user.id`, reutilizado tanto en `BugReportsPanel.jsx` (dentro de cada tarjeta, para el operador) como en `BugReportHistory.jsx` (lista de reportes propios → click abre la conversación con botón de volver). El campo de urgencia/estado/captura de pantalla se sigue mostrando por fuera del hilo (no duplicado dentro del componente compartido).
  - **Badge de no leído bidireccional**: `unread_for_user` (para el badge del widget flotante y el punto rojo en la lista de "Mis reportes") y `unread_for_manager` (punto rojo en `BugReportsPanel` cuando el usuario respondió y el operador todavía no lo vio) — antes solo existía la dirección operador→usuario.
  - **Verificación de extremo a extremo en navegador real** (no solo backend): impersonar al closer `Jean Carlo`, crear un reporte desde `/closer/deck` con la UI real del widget; volver al operador y enviar DOS mensajes desde `BugReportsPanel` (confirmando "CONVERSACIÓN (2)", no una respuesta pisada); volver a impersonar al closer y confirmar que "Mis reportes" muestra el badge de no leído y AMBOS mensajes al abrir la conversación; responder como closer desde ahí; volver al operador y confirmar que ve los 3 mensajes y el punto de "el usuario respondió". Registros de prueba eliminados al terminar.
  - **Nota operativa**: el backend real (puerto 5000, proceso del usuario, fuera del sandbox de las herramientas de Claude) tiró 500 en todos los endpoints — incluido `/api/auth/csrf-token`, que no toca `BugReport` — porque seguía corriendo el código de antes de este cambio de modelo contra una base de datos ya migrada al nuevo esquema. Ninguna herramienta (Bash, PowerShell) tenía visibilidad de ese proceso ni del puerto 5000 para reiniciarlo — se le pidió al usuario que lo reiniciara manualmente, y tras eso los 500 desaparecieron.
  - **Fix de estilo (pedido aparte del usuario en el mismo turno): fondo transparente del chat**. `glass-effect` — usado en `BugReportChat.jsx` y `BugReportHistory.jsx`, copiado del patrón de `GlobalSettingsModal.jsx`/`OperatorControls.jsx` — no está definida en ningún `.css` del proyecto (confirmado con grep en todo `frontend/src`, incluyendo `tailwind.config.cjs`: no genera esa clase). El panel quedaba completamente transparente en vez del look "glass" traslúcido que aparentaba el nombre. Cambiado a `bg-surface` (clase real, `background: var(--bg-card)`, ya usada por el resto de tarjetas de la app) en ambos componentes. `GlobalSettingsModal.jsx` y `OperatorControls.jsx` tienen el mismo `glass-effect` sin definir — quedó fuera de este alcance porque no fue lo que se pidió arreglar, pero es la misma causa raíz si en algún momento se reporta que esos modales también se ven "vacíos". *(Nota: se abrió un task en paralelo para auditar y corregir `glass-effect` en el resto de la app — renombrada/reemplazada en esa sesión, ver commit `b2bf72ca fix(ui): actualiza referencias a la clase glass-effect renombrada` en `main`.)*

- **2 de Septiembre de 2026** *(incidente de producción: closer bloqueado al declarar un Parcial — cruce falso de identidad por email compartido con una venta huérfana, sin cambios de código, solo dato corregido en Postgres de producción)*:
  - **Síntoma reportado por el closer** (vía log de Railway pegado por el usuario): al declarar un pago Parcial de $250 para **Kervin Calderón** (`client_id=8523`) en Ace Learner, `SheetsService.post_to_sheets` lo rechazaba 8 veces seguidas con *"Este cliente ya pagó el programa por completo."* — el bloqueo de `SalesConsistencyService.validate_next_payment_type` en [sales_consistency_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/sales_consistency_service.py) para `tipo='parcial'` cuando `state['has_full_payment']` es `True`.
  - **Diagnóstico** (consulta de solo lectura contra `DATABASE_PRODUCTION`, con permiso explícito del usuario — no hay forma de reproducir esto en la base local de desarrollo porque el cliente no existe ahí): el matching por email/instagram/teléfono de `get_client_payment_state` (agregado deliberadamente para unir historial de un mismo cliente disperso entre client_ids, por `client_id` casi siempre `NULL` en datos históricos) encontró la venta `id=57637` (`AL - completo`, $1000, 27-ago-2026) por coincidencia de email (`mail_cliente = 'juancsanchez.jcsr@gmail.com'`, igual al de Kervin). Esa venta ya estaba huérfana de por sí *antes* de tocar a Kervin: su `client_id` apunta a **Emilia Collantes** (Ecuador, IG `dolcezza00_`), pero sus propios campos denormalizados dicen `nombre_cliente="juan Camilo Sanchez Ramirez"`, `telefono="57 316 8287492"` (Colombia) — ninguno de los tres (Emilia, Juan Camilo, Kervin) coincide con los otros dos salvo por ese único email compartido. Es basura de una importación/backfill anterior, no evidencia real de que Kervin ya hubiera pagado.
  - **Corrección aplicada**: `UPDATE financial_sales SET mail_cliente = NULL WHERE id = 57637` directo en producción (1 fila afectada, verificado antes/después) — se limpió solo el campo que causaba el cruce falso, sin tocar `monto`, `tipo_pago`, `client_id` ni el resto de la fila, para no alterar el historial real de esa venta (que sigue siendo válida para quien sea que realmente la hizo). Verificado con la misma consulta de matching que usa el código: ya no aparece ninguna venta cruzada con el email/instagram/teléfono/client_id de Kervin. El closer ya puede reintentar el Parcial de $250.
  - **Riesgo no resuelto, fuera de este alcance**: el matching sigue basándose en un solo campo (email O instagram O teléfono) sin corroborar con el nombre — cualquier otro email/instagram/teléfono compartido por coincidencia entre dos clientes reales seguiría produciendo el mismo falso positivo. No se tocó la lógica porque debilitarla a ciegas rompería la detección real de duplicados que motivó agregarla (casos documentados: Jonathan Aparicio, Marcos Melo). Requeriría una señal adicional (ej. exigir 2 de 3 coincidencias, o similitud de nombre) diseñada con cuidado, no un parche apurado sobre un incidente puntual.

- **2 de Septiembre de 2026 (2)** *(fix del riesgo señalado arriba: matching de identidad más robusto en `get_client_payment_state`, [sales_consistency_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/sales_consistency_service.py) [MODIFY])*:
  - **El cambio**: el filtro SQL por email/instagram/teléfono se mantiene igual de amplio (sigue trayendo cualquier fila con una sola señal en común, para no perder recall), pero ahora es solo el paso de **candidatos** — la decisión de si una `FinancialSale` realmente pertenece al cliente se mueve a Python con un criterio más estricto: un match directo por `client_id` sigue bastando por sí solo (es la señal más confiable, viene de un FK real, no de texto libre importado); si no hay `client_id` directo, hacen falta **al menos 2 de las 3 señales** (email + instagram, email + teléfono, o instagram + teléfono) coincidiendo, **o**, si solo coincide una, que el nombre del cliente (`Client.full_name`) y el nombre denormalizado de la venta (`FinancialSale.nombre_cliente`) se parezcan (`_names_corroborate`: comparten un token de ≥3 letras normalizado sin acentos, o `SequenceMatcher.ratio() >= 0.72`).
  - **Por qué esta forma y no otra**: el incidente de Kervin Calderón (entrada anterior de hoy) tenía exactamente 1 señal (email) y cero parecido de nombre — el nuevo criterio lo hubiera excluido automáticamente sin necesidad del parche manual en producción. Los casos reales que motivaron el cruce laxo original (Jonathan Aparicio, Marcos Melo — datos históricos con `client_id` NULL) no quedan documentados con el detalle exacto de qué campos coincidían en cada uno, así que no se pudo replicar 1:1; en su lugar se construyeron casos representativos del mismo patrón (venta histórica sin `client_id`, con 2 señales coincidentes, y con 1 señal + nombre reconocible) para confirmar que ese tipo de fusión real sigue funcionando.
  - **Verificación (`scratch/simulate_sales_matching_fix.py`, no comiteado)**: contra `instance/local.db`, 4 casos con Client/FinancialSale creados y borrados en la misma corrida — (A) réplica del incidente real (venta con `client_id` de un cliente distinto, `mail_cliente` compartido con el cliente real pero `nombre_cliente`/`telefono` de un tercero sin relación): `has_full_payment` queda `False` y `validate_next_payment_type(..., 'parcial')` permite el pago, donde antes del fix se habría bloqueado; (B) venta histórica sin `client_id`, coincide email + teléfono (2 señales): se fusiona (`has_installments=True`); (C) venta histórica sin `client_id`, coincide solo instagram (1 señal) pero el nombre es reconocible ("Marcos Melo" vs "Marcos Melo Gonzalez"): se fusiona (`has_deposit=True`); (D) control negativo, 1 sola señal (teléfono) y nombre sin ningún parecido: no se fusiona (`has_full_payment=False`). Las 4 verificaciones pasaron.

- **2 de Septiembre de 2026 (3)** *(pedido explícito del usuario: dejar de bloquear al closer por secuencia de pago inconsistente — caso real Emilia Collantes, solo se le dejaba declarar Renovación/Upsell — y darle la herramienta para corregir el historial en vez de solo negarle el registro, [sheets_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/sheets_service.py) [MODIFY], [DeclararVentaWizard.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/modals/DeclararVentaWizard.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])*:
  - **El problema**: `SalesConsistencyService.validate_next_payment_type` seguía siendo un bloqueo DURO en `SheetsService.post_to_sheets` (el único lugar donde se llama) — si el historial de un cliente estaba mal cargado (ej. una venta vieja con el tipo de pago equivocado, mismo tipo de dato huérfano que ya causó el incidente de Kervin Calderón el mismo día), el closer quedaba sin ninguna forma de declarar la venta REAL que estaba cerrando: la única corrección disponible (Operaciones editando la base) no la podía hacer él mismo en el momento, y el modal de venta (`DeclararVentaWizard`) directamente deshabilitaba el botón de la opción "inconsistente" (`Option disabled`), sin decirle qué hacer al respecto.
  - **El cambio de fondo**: la secuencia de negocio (Seña → Parcial/Completo → Cuota → Renovación/Upsell) sigue evaluándose igual — no se tocó `SalesConsistencyService` — pero ahora es solo un AVISO, nunca un bloqueo. `post_to_sheets` guarda la venta igual y agrega `warning: <razón>` a la respuesta cuando la secuencia no calza; el endpoint `/sheets/push` ya devolvía el dict completo (`jsonify(result)`), así que no hizo falta tocar esa capa. Todos los caminos que declaran una venta pasan por `post_to_sheets` (`DeclararVentaWizard`, `CuotaRow.markAsPaid` en `ClientHistoryModal`, `NewSalePage`, el endpoint público `/public/financial-sales/new`), así que el desbloqueo alcanza a todos por igual.
  - **Frontend — avisar sin bloquear**: en `DeclararVentaWizard`, el paso "¿Cómo paga?" ya no deshabilita ninguna tarjeta de tipo de pago (`Option` perdió su prop `disabled`, ahora tiene `warn`: borde/ícono ámbar en vez de opacidad + `cursor-not-allowed`) — cualquier tipo se puede elegir y clickear siempre. Si el tipo elegido tiene un `allowed_types[...].ok === false`, se muestra el motivo con un botón nuevo **"Ver / corregir historial del cliente"** que abre `ClientHistoryModal` (ya existía, con `VentaRow`/`PagoRow` editables — no se construyó nada nuevo para eso) sin cerrar el wizard; al cerrar el historial se refresca `saleClientState` (`fetchSaleClientState` extraída del `useEffect` a una función reusable) para que el aviso desaparezca solo si de verdad se corrigió el dato. También se sacó la lógica que saltaba automáticamente al primer tipo de pago "permitido" apenas cargaba el estado del cliente — hacía exactamente lo mismo que bloquear (le cambiaba la selección al closer sin que él lo pidiera).
  - **Aviso post-registro**: si la venta se guarda con `warning` en la respuesta, `handleRegisterSale` (en `CloserWorkflowPage`) muestra un `toast` aparte con el motivo, para que el closer se entere aunque no haya visto el aviso en el paso del wizard (ej. venía preseleccionado desde antes).
  - **Verificación (`scratch/simulate_closer_no_block.py`, no comiteado)**: contra `instance/local.db`, con `requests.post` mockeado para no escribir en la hoja de Google Sheets real — se replicó el patrón de Emilia (cliente con un `AL - completo` ya registrado) y se llamó `SheetsService.post_to_sheets` con un `AL - parcial` nuevo. Confirmado: `SalesConsistencyService.validate_next_payment_type` sigue devolviendo `ok=False` (la regla de negocio no se debilitó), pero `post_to_sheets` devuelve `status: success` con el `warning` incluido, y la venta Parcial queda de verdad guardada en `FinancialSale` — ya no hay bloqueo duro.

- **3 de Septiembre de 2026** *(panel de Postulaciones: nuevo voto "Reserva" entre Preseleccionar y Descartar, con su propio filtro/badge — pedido explícito del usuario a partir de un mockup HTML de referencia ("Panel de postulaciones (standalone).html"), [job_application.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/job_application.py) [MODIFY], [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesInbox.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesInbox.jsx) [MODIFY], [PostulacionDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionDetailModal.jsx) [MODIFY], [PostulacionVoteToast.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionVoteToast.jsx) [MODIFY])*:
  - **El pedido**: hasta ahora la ficha de un candidato solo tenía "Preseleccionar" / "Descartar". El usuario pidió un tercer botón "Reserva" para los candidatos que hoy no encajan pero podrían servir más adelante, con su propio lugar donde verlos agrupados — replicando el diseño de un HTML de referencia que compartió (mockup standalone con paleta de marca Learnation, no conectado a datos reales).
  - **Lógica de veredicto, copiada tal cual del mockup (no inventada)**: `JobApplication.veredicto()` (antes solo distinguía pre/des) ahora resuelve 3 valores por revisor con una jerarquía específica que el mockup ya definía en su función `veredictoDe`: si un revisor preselecciona y el otro descarta → `decidir` (conflicto real); si uno preselecciona y el otro solo reserva → gana `preseleccionada` directo, **sin pedir decisión** (preseleccionar es una señal más fuerte, no hace falta arbitrar); si uno reserva y el otro descarta → `decidir` también (ahí sí son señales opuestas); un solo voto `res` sin conflicto → `en_reserva`. Se replicó esta jerarquía exacta en vez de inventar una propia porque el mockup la definía explícitamente con ese orden de prioridad.
  - **Backend**: `VOTE_VALUES` pasa a `('pre', 'res', 'des')` (la columna `vote` ya era `String(10)`, sin migración necesaria). `FILTROS_VALIDOS` y `_aplica_filtro` en `job_applications.py` suman `en_reserva`.
  - **Frontend — filtro y tabla**: `PostulacionesInbox.jsx` suma el chip "En reserva" (entre "Preseleccionadas" y "Decidir") y el badge correspondiente en la tabla, en ámbar (mismo tono que "Decidir": el mockup usa el mismo color semántico `--warning` para ambos estados, son visualmente parecidos pero las etiquetas de texto los distinguen).
  - **Frontend — botones de voto con color permanente, no solo al seleccionar**: la primera versión dejaba los 3 botones con borde blanco neutro hasta que se votaban, y el usuario pidió explícitamente imitar más el mockup ("los botones tienen colores"). Se corrigió `PostulacionDetailModal.jsx` para que cada botón tenga su tinte semántico SIEMPRE visible (fondo/borde/texto translúcidos: esmeralda para Preseleccionar, ámbar para Reserva, rosa/rojo para Descartar) y pase a color sólido (fondo lleno + texto claro u oscuro según contraste) solo cuando ese es el voto del revisor actual — mismo patrón `bg` translúcido / `bg` sólido que usa `btnPre`/`btnRes`/`btnDes` en el mockup. Los tres botones se reorganizaron de un `flex justify-end` de 2 botones a un `grid grid-cols-3` de igual ancho para los 3.
  - **`PostulacionVoteToast.jsx`**: el toast de confirmación al votar solo distinguía Preseleccionado (verde) vs. Descartado (rojo, aplicado por defecto a cualquier otro valor) — con el voto nuevo, reservar hubiera mostrado el toast rojo de "Descartado" por error. Se generalizó a una tabla `VOTO_TOAST` con los 3 estados (label, color, ícono) en vez de la lógica binaria `esPre ? ... : ...`.
  - **Verificación en navegador** (simulación como `Santiago`, admin, vía "Simular" del panel de operaciones — luego revertida con `/api/auth/revert`): se sembraron 3 postulaciones de prueba (`scratch/seed_test_postulantes.py`, no comiteado) para tener datos con qué probar, porque la base local no tenía ninguna postulación completa cargada. Se verificó: votar "Reserva" en la ficha dispara `POST /job-applications/<id>/vote` con `valor: "res"` y devuelve `veredicto: "en_reserva"`; el chip "En reserva" y el badge de tabla reflejan el conteo correcto; el botón votado pasa a color sólido y los otros dos quedan con su tinte translúcido; el autoavance a "siguiente pendiente" sigue funcionando igual que con pre/des. Registros de prueba eliminados al terminar (`scratch/cleanup_test_postulantes.py`, no comiteado).
  - **Pendiente, no pedido en este alcance**: el usuario mencionó que "en un futuro podrían funcionar" los candidatos en reserva — no se agregó ningún recordatorio ni vencimiento para revisarlos de nuevo, el filtro "En reserva" es puramente una vista manual por ahora.

- **3 de Septiembre de 2026 (2)** *(script `actualizar_db.py` no traía Postulaciones desde producción, [actualizar_db.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/scripts/actualizar_db.py) [MODIFY])*:
  - **Contexto**: para testear el panel de Postulaciones con casos reales (no solo datos inventados) el usuario pidió actualizar la base local con lo que hay en producción. Un primer intento con un script propio conectando directo a `DATABASE_PRODUCTION` fue bloqueado por el clasificador de seguridad del entorno; el usuario aclaró que ya existe `scripts/actualizar_db.py` para esto exactamente, así que se usó ese en vez de insistir con un script nuevo.
  - **El gap real**: `JobApplication`, `JobApplicationVote` y `ClarityWeight` (agregados el 1-2/09) nunca se sumaron a la lista `modelos` de `actualizar_db.py` — el script sincroniza sin errores pero simplemente nunca tocaba esas 3 tablas. Se agregaron en el orden de dependencia correcto (`JobApplication`/`ClarityWeight` junto a los modelos independientes, `JobApplicationVote` en "Dependencia Nivel 1" por su FK a `JobApplication`/`User`).
  - **Ejecutado** (`python scripts/actualizar_db.py --target local`, permiso explícito del usuario): reemplazó TODA la base local (no solo Postulaciones — es el comportamiento ya existente del script, limpia y reimporta todos los modelos de `modelos`) con el estado real de producción al momento de correrlo: 215 postulaciones y 47 votos importados, entre otras ~60 tablas. La sesión de simulación de admin activa en el navegador se perdió (el `is_impersonating` no sobrevive al restart del backend que dispara el propio guardado de código durante la sesión, no la sincronización en sí) — hubo que volver a "Simular" desde Gestión de Equipo.

- **3 de Septiembre de 2026 (3)** *(pestaña propia para "En reserva" + rediseño completo de la pestaña "Incompletas" con contacto/avance/materiales, pedido explícito del usuario tras ver el panel con datos reales, [job_application.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/job_application.py) [MODIFY], [PostulacionesInbox.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesInbox.jsx) [MODIFY])*:
  - **"En reserva" deja de ser un chip de filtro y pasa a ser pestaña propia**: el usuario prefirió esto en vez de que compitiera como un chip más entre "Mis pendientes/Todas/Preseleccionadas/Decidir/Descartadas". `PostulacionesInbox` ahora tiene un selector de 3 vistas (`Revisión` / `En reserva` / `Incompletas`) por encima de esos chips; "Incompletas" también se sacó del arreglo `FILTROS` para vivir como la tercera vista (el usuario confirmó que su ubicación ya estaba bien, pero conceptualmente es la misma idea: no compite con los chips de la revisión normal). "Revisión" agrupa lo que antes eran todos los chips (mis pendientes/todas/preseleccionadas/decidir/descartadas); "En reserva" reutiliza la misma tabla de candidatos (Formación/Inglés/Cierre/Progreso/Score/Estado) filtrada a `en_reserva`, sin sub-filtros propios.
  - **Backend, `to_dict()`**: `whatsapp`, `pais`, `edad`, `video` y `llamada` pasan al bloque de campos "siempre visibles" (antes solo estaban bajo `include_respuestas=True`, que el listado nunca pide) — la nueva tabla de Incompletas los necesita en la respuesta liviana de `/job-applications?filtro=incompletas`.
  - **Investigación previa del "no se está midiendo bien" que reportó el usuario**: antes de tocar el diseño se auditó `respondidas()`/`CAMPOS_FORMULARIO` contra las 143 postulaciones incompletas reales recién importadas — sin mismatches entre el conteo del método y un conteo manual campo por campo, sin duplicados por `dedupe_key`, sin ninguna completa (`completo=True`) con porcentaje bajo. Lo que sí aparece, y probablemente es lo que motivó el comentario: 21 candidatos quedan en 91-96% (les falta solo `video`/`llamada`, las dos últimas preguntas del formulario) — matemáticamente correcto, no un bug, pero el número solo (sin desglose) sugiere "casi completo" sin explicar por qué se frenaron ahí. La columna nueva de Materiales resuelve exactamente esa ambigüedad. (Aparte, algunos nombres con tildes se veían con `�` al imprimirlos por `print()` en la consola de Windows — se confirmó con los bytes UTF-8 crudos que es solo cp1252 de la consola, el dato en la base está bien guardado y el navegador lo renderiza correcto.)
  - **Tabla de Incompletas, según el detalle que pidió el usuario**: Candidato (nombre + "país · edad" si hay al menos uno de los dos, mockup-style); Contacto (WhatsApp como link a `wa.me/<solo dígitos>` con ícono si hay número, texto "Sin WhatsApp" en itálica si no; correo como `mailto:`, "Sin correo" si viene vacío — el modelo permite `email=''` para quien abandonó en la primera pregunta); Avance (fracción + porcentaje + barra de progreso con gradiente azul→magenta); Materiales (pill verde "Ver video"/"Ver llamada" con link si están, pill gris "Sin video"/"Sin llamada" si no — para que se vea de un vistazo quién subió qué). Los links de WhatsApp/correo/video/llamada llevan `stopPropagation` para no disparar el modal de ficha completa al clickearlos.
  - **Verificación en navegador** (simulación admin, datos reales post-sincronización): pestaña "Incompletas (143)" mostrando los 3 casos límite (WhatsApp+correo completos, solo correo, ninguno de los dos) con el placeholder correcto en cada uno; pestaña "En reserva (0)" vacía por defecto (todavía no había votos `res` en los datos reales); votado un candidato real desde "Revisión" con "Reserva" → aparece en "En reserva (1)" → voto de prueba eliminado al terminar.

- **3 de Septiembre de 2026 (4)** *(pestaña Estadísticas: segmento "Todos" vs. "Seleccionados", KPIs arriba, embudo con % de caída entre etapas y exportar CSV — pedido explícito del usuario ("las estadísticas ahora no son fáciles de leer... que se puedan hacer mucho más intuitivas"), [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesStatsTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesStatsTab.jsx) [MODIFY])*:
  - **El pedido**: una vista de estadísticas para TODOS los postulantes (la que ya existía) y otra separada solo para los "seleccionados" (preseleccionados) — para poder comparar cómo viene ese grupo puntual contra el pool general. Además, mejorar la legibilidad general: KPIs arriba (Completaron, Score medio, Score 85+, Con video y llamada — como en el mockup de referencia de la sesión anterior), y un botón para exportar la información.
  - **Backend, `/job-applications/stats`**: nuevo query param `?segmento=todos|seleccionados` (default `todos`). Con `seleccionados`, el pool sobre el que se calcula TODO (score medio, tramos, histograma, distribuciones de país/inglés/herramientas/etc.) se recorta a `veredicto() == 'preseleccionada'` antes de los cálculos — se reutiliza el mismo cuerpo de la función, solo cambia qué lista de `JobApplication` entra. El `embudo` (Abrieron → Disclaimer → Completaron → Material → Score 85+) no tiene sentido recortado a seleccionados (sería un solo escalón), así que se devuelve `None`/se omite en ese segmento — el frontend oculta el panel entero cuando no viene. Se agregaron campos nuevos al JSON (`segmento`, `total_completas`, `abrieron_formulario`, `con_material`, `score_85`) que antes se calculaban pero no se exponían sueltos (solo empotrados dentro de los pasos del embudo), porque ahora las tarjetas KPI los necesitan como número independiente.
  - **Frontend — KPIs arriba**: 4 tarjetas (mismo estilo que las de `PostulacionesInbox`) con contenido que cambia según el segmento — en "Todos" la primera dice "Completaron X de Y que abrieron"; en "Seleccionados" dice "Seleccionados X de Y completas" (para ver de entrada qué fracción del pool terminó aceptada, un dato que no existía antes en ningún lado del panel).
  - **Embudo más intuitivo**: cada escalón ahora muestra "↓ N% no siguió" respecto al escalón anterior (en rojo si la caída es ≥50%), en vez de solo el ancho relativo de la barra — así se ve de un vistazo EN QUÉ PASO se pierde más gente, que es literalmente lo que el usuario describió ("vienen de acá, luego acá, luego acá").
  - **Exportar CSV**: botón que arma un CSV client-side (nombre/email/veredicto/score/conocimiento/inglés/cierre/avance/fecha) a partir del mismo listado que ya usa el inbox (`/job-applications?filtro=todas` o `=preseleccionadas` según el segmento activo) y lo descarga vía Blob URL — no se tocó el backend para esto, no hizo falta un endpoint de export dedicado.
  - **Animación de entrada sutil**: cada panel y KPI hace un fade+translateY corto al montar (keyframe `ln-stats-up`, con un pequeño delay escalonado por índice) — el pedido de "un poco más de motion graphics" del mockup de referencia, sin reescribir los gráficos existentes (siguen siendo barras/donut simples con transiciones CSS, no se metió una librería de charts nueva).
  - **Verificación en navegador** (simulación admin, datos reales): segmento "Todos" → 72 completas, score medio 59, embudo mostrando la caída real (65% no sigue entre "pasaron el disclaimer" y "completaron", el bloque de abandono más grande del proceso). Segmento "Seleccionados" → 8 preseleccionados, score medio 72 (bien por encima del pool general, como se esperaría), embudo ausente, panel de "Herramientas"/"País"/etc. recalculado solo sobre esos 8. Exportar CSV disparó la petición correcta (`filtro=preseleccionadas`) sin errores de consola nuevos.

- **3 de Septiembre de 2026 (5)** *(fix: "Histograma de scores" y "Postulaciones por día" no dibujaban barras proporcionales, el usuario reportó "se ven mal", [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesStatsTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesStatsTab.jsx) [MODIFY])*:
  - **El bug real (CSS, no de datos)**: las barras usaban `style={{ height: '${pct}%' }}` dentro de una columna `flex flex-col` cuyo padre (`flex h-40 items-end`) alinea cada columna con `align-items: flex-end` — eso saca a la columna del stretch por defecto, así que su altura queda en `auto` (se ajusta a su contenido) en vez de heredar los 160px del contenedor. Un `height: %` sobre un hijo cuyo padre no tiene una altura EXPLÍCITA se resuelve como si fuera `auto` (spec de CSS2.1 §10.5) — así que el `%` nunca se aplicaba, y todas las barras terminaban en su `minHeight` en píxeles (4px), prácticamente parejas sin importar el valor real. Este bug ya estaba en la versión anterior de este archivo (antes de esta sesión) — se heredó al reescribir el componente el mismo día, no se detectó porque la verificación previa no miró estos dos paneles con atención al detalle de altura.
  - **El fix**: se envolvió cada barra en una "pista" (`div` con altura fija en Tailwind, `h-28`/`h-24`) que sí tiene altura explícita — ahora el `%` de la barra interior se resuelve contra ESA pista, no contra la columna completa (que sigue siendo `auto`, conteniendo el label de arriba + la pista + el label de abajo). Aplicado igual en Histograma y en Postulaciones por día. De paso, el histograma gana el sufijo "s" en las decenas (`0s`, `10s`...) para que se lea como rango, no como score exacto.
  - **Segundo problema, solo de "Postulaciones por día" (datos, no CSS)**: el backend devolvía únicamente los días CON postulaciones (`Counter` sin ceros) — con los datos reales recién sincronizados, sólo 2 de los últimos 14 días tenían algo, así que el gráfico mostraba 1-2 barras gigantas sueltas sin ningún eje temporal continuo. `stats_job_applications` ahora arma los 14 días completos (`desde_fecha` fijo, un `for i in range(14)`) y rellena con 0 los días sin postulaciones, para que el gráfico sea una serie continua de verdad en vez de puntos sueltos. La condición de "sin postulaciones recientes" pasó de `length === 0` (nunca se cumple ahora, siempre hay 14 filas) a `every(d => d.cantidad === 0)`.
  - **Verificación en navegador** (simulación admin, datos reales, viewport angosto para ver los paneles en columna única y a mayor tamaño): Histograma muestra una curva real con forma de campana (4, 8, 9, 11, 21, 13, 6 por decena, subiendo y bajando de forma visible) en vez de barras parejas. Postulaciones por día muestra los 14 días completos (08-21 a 09-03) con la mayoría en cero y un pico real y proporcional el 09-02 (12) y 09-03 (60) — coincide con `created_at` real de los datos de producción recién importados, no es un artefacto de la sesión de testing.

- **3 de Septiembre de 2026 (6)** *(Estadísticas: segmentos Preseleccionados/En reserva/Descartados/Incompletos (antes solo Todos/Seleccionados) + barras de distribución más gruesas con el número anclado adentro, pedido explícito del usuario tras revisar el panel en vivo, [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesStatsTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesStatsTab.jsx) [MODIFY])*:
  - **El pedido, aclarado por el usuario en el mismo mensaje**: la pestaña de Postulaciones (con sus tabs Revisión/En reserva/Incompletas y los filtros Preseleccionadas/Decidir/Descartadas) ya está bien como está — lo que faltaba era que **Estadísticas** tuviera el mismo desglose (Todos / Preseleccionados / En reserva / Descartados / Incompletos) para poder comparar cómo viene cada grupo, no solo "Todos" vs. "Seleccionados" (que ya existía de la sesión anterior).
  - **Backend**: `SEGMENTOS_VALIDOS` pasa de 2 a 5 valores. Se generalizó el `if segmento == 'seleccionados'` de la sesión anterior a un diccionario `VEREDICTO_DE_SEGMENTO` (`preseleccionados`→`preseleccionada`, `en_reserva`→`en_reserva`, `descartados`→`descartado`) que recorta `completas`; `incompletos` es el único caso especial porque sale de `todas_las_filas` completo (nunca tiene `completo=True`) en vez de filtrar `completas`. El resto del cálculo (score medio, tramos, histograma, distribuciones) es exactamente el mismo código de antes, solo cambia qué lista de `JobApplication` entra — mismo patrón que ya se había armado para "seleccionados".
  - **Frontend — KPI principal por segmento**: se generalizó el `if/else` binario a un diccionario `KPI_PRINCIPAL` con una función por segmento, porque cada uno necesita una etiqueta y un denominador distintos (ej. "Preseleccionados X de Y completas" vs. "Incompletos X de Y que abrieron" — no tiene sentido comparar incompletos contra el total de completas).
  - **Barras de distribución más gruesas (`Conocimiento como closer`, `Nivel de inglés`, `Lugar de residencia`, `Edad`, `Herramientas`, `Disclaimer`)**: el usuario señaló específicamente que las barras eran "muy delgaditas" y que una más gruesa "hace que puedas poner el texto ahí" — se rediseñó `BarraDistribucion`: el label de la categoría pasa a una línea propia arriba (antes compartía fila con el número), y la barra en sí crece de `h-1.5` (6px) a `h-5`/`h-7`/`h-9` (20-36px) según `grosorBarra(items.length)` — más grosor cuando hay pocos elementos (ej. "Disclaimer de frustración" con un solo ítem, que antes se veía como una rayita perdida), más fino cuando hay muchos (ej. "Lugar de residencia" con 11 países) para no disparar la altura del panel. El número ahora vive DENTRO de la pista, anclado a la derecha (no al final del relleno) — así siempre es legible aunque el valor sea chico y el relleno casi no se note, que era exactamente el caso que el usuario señaló como "se ve raro".
  - **Verificación**: la sesión del navegador se cayó de nuevo al guardar el cambio de backend (mismo problema ya documentado hoy: el reinicio del reloader de Flask invalida la sesión activa). En vez de volver a pedir login, se verificaron los 5 segmentos directo contra el test client de Flask (`scratch/verify_stats_segments.py`, no comiteado, con `login.session_protection = None` — mismo workaround documentado el 1/09 para simular sesión entre requests): `todos`=72 (con embudo), `preseleccionados`=8 (score medio 72), `en_reserva`=0 (nadie votado `res` todavía en datos reales), `descartados`=38 (score medio 53, más bajo que el general como se esperaría), `incompletos`=143 (score medio 32). Los conteos de descartados/incompletos coinciden exactamente con los ya vistos en la pestaña de Postulaciones. El rediseño visual de las barras SÍ se alcanzó a verificar en el navegador (antes de que se cayera la sesión) con las 6 tablas de distribución reales, incluyendo el caso límite de un solo ítem (Disclaimer) y el de muchos (Herramientas, 10 ítems).

- **3 de Septiembre de 2026 (7)** *(nueva pestaña "Revisores": cuánto le falta a cada revisor y dónde votaron distinto, pedido explícito del usuario a partir de la pestaña "revisores" del mockup HTML de referencia, [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesRevisoresTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesRevisoresTab.jsx) [NEW], [PostulacionesDashboardPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/PostulacionesDashboardPage.jsx) [MODIFY])*:
  - **El pedido**: Mario y Marlon (los revisores) necesitan saber su propio estado de revisión — cuántos le faltan a cada uno, no solo "los míos" — y en qué candidatos votaron distinto entre sí (para saber a quién le toca decidir). Del mockup de referencia se tomó la pestaña `revisores` completa (`revisoresLista`, `revDesacuerdos`, con su "Ver más"), pero se dejó afuera deliberadamente el mini-heatmap de celdas por día de cada tarjeta de revisor — el usuario señaló explícitamente sobre ese elemento "esto de aca realmente no se que es, no se entiende".
  - **Quién es "revisor"**: no es una lista fija de 2 personas como en el mockup (`REVISORES = [['mario','Mario'], ['marlon','Marlon']]`) — acá cualquier `role == 'admin'` puede votar (mismo criterio de `check_admin`), así que el endpoint nuevo (`GET /job-applications/revisores`) trae TODOS los usuarios con `role='admin'` (`User.query.filter_by(role='admin')`), no solo a quienes ya votaron algo — así un admin nuevo que todavía no calificó nada aparece igual con "le faltan: total".
  - **Backend**: por cada admin, `hechas` = cuántas de las `completas` tienen un voto suyo, `faltan` = el resto, `pct` = porcentaje. `desacuerdos` = las `completas` con `veredicto() == 'decidir'`, con el voto de cada revisor adjunto (nombre + tipo de voto) para mostrar el badge de cada uno. `mis_pendientes` = las `completas` donde el usuario actual (`current_user.id`) todavía no votó — mismo criterio que el filtro `mis_pendientes` ya existente, pero como lista con detalle en vez de solo un conteo.
  - **Frontend**: 3 secciones — tarjetas de revisor (nombre, badge "Al día"/"Pendiente", el número grande de "le faltan" en rosa/verde, "revisadas", barra de progreso); "Votaron distinto" y "Te falta ver · {usuario}" como listas clickeables (abren `PostulacionDetailModal`, reusado tal cual) con un botón "Ver N más" que expande de a 5 en 5 — mismo patrón `hayMasDes`/`verMasDes` del mockup pero implementado como toggle local (`useState`) en vez de paginación server-side, porque el volumen (72 completas) no lo justifica.
  - **Verificación con datos reales**: `Mario Administra` (id real de producción) aparece con 46 revisadas / 26 pendientes / 64% completo — coincide con que ya viene usando el sistema de votos activamente; el resto de los admins en 0. 0 desacuerdos (todavía no hay conflicto porque solo un revisor activo votó hasta ahora). La sesión del navegador volvió a caerse durante esta verificación (mismo problema del reinicio del backend) y, al re-simular por segunda vez, se detectó la causa raíz real: `AuthContext.jsx` inicializa `user` desde `localStorage.getItem('user')`, no desde una consulta fresca al backend — así que un `fetch('/api/auth/impersonate')` hecho por fuera del botón real de la UI cambia la cookie de sesión en el servidor pero la app sigue mostrando (y aplicando `ProtectedRoute`) con el usuario viejo cacheado. No es un bug del producto (cualquier usuario real que clickea "Simular" en la UI sí actualiza `localStorage` correctamente, confirmado); fue una limitación del atajo de testing usado en sesiones anteriores de hoy. Con el click real de "Simular" (vía DOM, ubicando la tarjeta exacta por texto para no simular al usuario equivocado) las 3 tarjetas y ambas listas se vieron correctas en el navegador con los números reales de arriba.
  - **Fuera de alcance, aclarado por el usuario en el mismo mensaje**: pidió además cambios a las preguntas del formulario de postulación (validar que el link de video no sea texto random tipo "no quiero", exigir permisos de Google Drive, agregar todos los países con buscador, agregar una pregunta sobre "por qué sos la mejor opción") — pero el propio usuario aclaró que esas preguntas viven en la landing page del proyecto **institute-site**, un proyecto separado de NeurOPS. No se tocó nada de eso en esta sesión; queda pendiente para cuando se trabaje en ese otro repo.

- **3 de Septiembre de 2026 (8)** *(cambios en el formulario público de postulación — repo `institute-site`, con soporte cruzado en NeurOPS para la pregunta nueva — cierra el pendiente de la entrada anterior, [job_application.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/job_application.py) [MODIFY], [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/public/job_applications.py) [MODIFY], [f1a2b3c4d5e6_add_porque_mejor_opcion.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/f1a2b3c4d5e6_add_porque_mejor_opcion.py) [NEW], [PostulacionDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionDetailModal.jsx) [MODIFY] — y en `institute-site` (repo separado): `formulario/index.html` [MODIFY])*:
  - **`institute-site/formulario/index.html`** (commit `2c983bc`, repo separado, mismo working tree local): país de residencia pasa de una lista corta de ~19 países con un cajón "Otro" (que el usuario reportó como la opción más elegida) a la lista completa de `CODIGOS_PAIS` (ya usada por el selector de código de WhatsApp, ~195 países) con buscador — nuevo tipo de pregunta `'buscar'` en vez de `'radio'`, reutilizando el mismo patrón de input+lista filtrable del popover de WhatsApp pero como paso de formulario propio en vez de popover anclado. Video/llamada: instrucciones más explícitas (pegar solo el link, permisos "Cualquier persona con el enlace" en Drive, sin video no se revisa la postulación) y una validación real nueva (`q.validar`, regex `^https?:\/\/`) — antes cualquier texto no vacío pasaba, confirmado con un caso real reportado por el usuario ("no quiero" en vez de un link). Nueva pregunta de texto libre "¿Por qué eres la mejor opción para este puesto?" entre "objetivos" y "porque", para leer la motivación sin depender del video.
  - **Por qué también se tocó NeurOPS**: una pregunta nueva sin una columna donde guardarla se pierde en silencio — el endpoint público solo guarda los campos que reconoce por nombre. Columna `porque_mejor_opcion` (Text, nullable) + migración `f1a2b3c4d5e6`, sumada a `CAMPOS_FORMULARIO` (pasa de 23 a 24 preguntas totales — la etiqueta de Clarity "Video y llamada (22 y 23)" se corrigió a "(23 y 24)" por el corrimiento) y al endpoint público de alta. Se muestra en la ficha del candidato junto a habilidades/obstáculo/objetivos. No se la sumó como criterio ponderado de Clarity (no se tocó el score automático) — el pedido era poder *leerla*, no que alimente el cálculo, y convertir una respuesta libre en un criterio puntuable es una decisión de diseño aparte que no se pidió.
  - **Verificación**: `institute-site` corrido localmente (`npx serve`, agregada una entrada temporal a `.claude/launch.json` de NeurOPS para poder previsualizarlo con la herramienta de navegador — revertida al terminar, no comiteada) completando el formulario real paso a paso hasta el final: el buscador de país filtra en vivo (probado con "Venez" → "Venezuela"), la pregunta nueva aparece con el texto correcto en su lugar, y la validación del video rechaza "no quiero" con el mensaje de error nuevo y acepta un link real. No se completó el envío final a propósito: `SUBMIT_URL` apunta a producción (`work.thelearnation.com`) y el origen local no está en la whitelist de CORS de NeurOPS, así que cada guardado progresivo de prueba falló en el navegador por CORS (confirmado en consola) — ningún dato de prueba llegó a producción. El lado NeurOPS (columna nueva, migración, endpoint) se verificó aparte con el test client de Flask contra la base local: POST con el campo nuevo → persiste en la columna → aparece en el GET de admin → `respondidas/total_preguntas` correcto (21/24). Registro de prueba eliminado.

- **3 de Septiembre de 2026 (9)** *(fix: el dock flotante global tapaba los botones de votar del modal de ficha de candidato, reportado por el usuario con captura de pantalla de su propia sesion, [PostulacionDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionDetailModal.jsx) [MODIFY], [PostulacionVoteToast.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionVoteToast.jsx) [MODIFY])*:
  - **El bug**: `Dock.jsx` (`frontend/src/components/shared/Dock.jsx`), montado globalmente en `MainLayout.jsx` en todas las paginas autenticadas, es `fixed bottom-10 ... z-50`. El backdrop de `PostulacionDetailModal` estaba en `z-20` — el unico modal de toda la app con un z-index tan bajo (se relevaron ~50 modales en `frontend/src/**`, la inmensa mayoria usa `z-[100]` o mas, varios especificamente para quedar por encima de este mismo dock). Con esa diferencia, el dock (que vive `bottom-10`, justo en la franja donde cae el footer de 3 botones de voto) se pintaba encima del modal, cortando/tapando el boton "Preseleccionar" segun el ancho de ventana.
  - **El fix**: backdrop del modal `z-20` -> `z-[100]` (mismo valor que usa la mayoria de los otros modales de la app, incluido `AdDetailModal.jsx` que ya usaba `z-[60]` por el mismo motivo). De paso, `PostulacionVoteToast.jsx` (el toast de confirmacion al votar) paso de `z-30` a `z-[110]` — mismo problema potencial, no reportado explicitamente pero mismo origen.
  - **Verificacion en navegador** (simulacion admin, viewport 1366x768 para reproducir la proporcion de la captura del usuario): con el fix, los 3 botones (`getBoundingClientRect`) quedan visibles y parejos en y=650-700px, y el z-index computado confirma modal=100 > dock=50 — el dock queda completamente detras del velo semitransparente del modal, invisible, en vez de pisarlo.
  - **Nota aparte, no un bug de este cambio**: durante la verificacion se confirmo de nuevo la fragilidad ya documentada de `AuthContext` (localStorage como fuente de verdad) — revertir la impersonacion con un `fetch('/api/auth/revert')` crudo (en vez del flujo real de la UI) dejo el `localStorage` diciendo `Santiago/admin` mientras el servidor ya decia `Kerwin/operator`. Se corrigio sincronizando `localStorage['user']` a mano con la respuesta real de `/api/auth/me`. Aparte, se noto que la ruta `/` de la app siempre muestra el login sin importar la sesion (no es un redirect-aware root) — hay que navegar a una ruta concreta como `/ops/dashboard`; no es un bug de esta sesion, es como esta armado el router.

- **4 de Septiembre de 2026** *(rediseño del rol `director_comercial` (ventas/closing/setting) + rol nuevo `director_marketing` (workshops + anuncios/Instagram) — pedido explícito del usuario: lo que hoy tenía `director_comercial` (acceso a Workshop Intelligence) estaba mal ubicado, es contenido de Marketing, [user.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/user.py) [MODIFY], [decorators.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/decorators.py) [MODIFY], [App.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/App.jsx) [MODIFY], [useDockNavigation.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/hooks/useDockNavigation.js) [MODIFY], [LoginPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/auth/LoginPage.jsx) [MODIFY], [TeamManagementPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/team/TeamManagementPage.jsx) [MODIFY])*:
  - **El pedido**: el rol `director_comercial` (creado antes de esta sesión) solo daba acceso a `/admin/workshops` — eso estaba conceptualmente mal, porque "director comercial" debería ser ventas: closing y setting. El usuario pidió mover ese acceso a workshops (más talleres y anuncios de Instagram — "todo lo referente a Marketing") a un rol nuevo, `director_marketing`, y redefinir `director_comercial` para que apunte a la parte comercial/ventas. Explícitamente: "la idea es que desde Admin se vea todo" (el admin ya tenía y conserva acceso completo a ambas zonas, sin cambios ahí).
  - **Mapeo a páginas ya existentes, no se construyó nada nuevo**: se reutilizó infraestructura que ya vivía en el panel de admin — `director_comercial` → `/admin/ventas` (`AdminSalesHubPage`, con tabs Closers/Setters/Workshops, ya usada por admin); `director_marketing` → `/admin/workshops` (`WorkshopDashboardPage`, el embudo de talleres) como home tras login, más `/admin/marketing` (`AdminMarketingHubPage`: Gestión de Anuncios/Instagram, Generador UTM, Tráfico de Landings) sumado al Dock.
  - **Backend**: constante nueva `ROLE_DIRECTOR_MARKETING = 'director_marketing'` en `user.py` (se dejó `ROLE_DIRECTOR_COMERCIAL` intacta, solo cambia a qué apunta desde el frontend). El decorator `workshop_required` (único lugar del backend que mencionaba `director_comercial`) pasó a exigir `ROLE_DIRECTOR_MARKETING` en vez de `ROLE_DIRECTOR_COMERCIAL`. No hizo falta tocar nada más en la API: los endpoints de ventas (`closer.py`/`setter.py`) y de marketing/ads (`marketing.py`) solo exigen `@login_required` sin chequeo de rol adicional (el control real ya vivía en el ruteo del frontend); tampoco hay whitelist de roles válidos al crear/editar un usuario (`POST/PUT /admin/users` acepta cualquier string), así que no hizo falta migración ni cambio de esquema — `role` sigue siendo texto libre.
  - **Frontend**: en `App.jsx`, `/admin/ventas` sumó `director_comercial` a sus roles permitidos y `/admin/workshops`/`/admin/marketing` sumaron `director_marketing` (reemplazando a `director_comercial` en el primero); el redirect de `ProtectedRoute` y el de `LoginPage.jsx` para `director_comercial` pasaron de `/admin/workshops` a `/admin/ventas`, con una rama nueva para `director_marketing` → `/admin/workshops`. En `useDockNavigation.js`, el bloque de Dock que antes era de `director_comercial` (un solo destino, Workshops) se reasignó a `director_marketing` con dos destinos (Workshops + Marketing, reutilizando el ícono `Activity` que ya estaba importado y sin uso); `director_comercial` gana su propio bloque con un solo destino, Ventas. `TeamManagementPage.jsx` suma la opción "Director de Marketing" al selector de rol y a la lista de filtros, y el redirect de impersonación (`handleImpersonate`) sigue el mismo split que `LoginPage`.
  - **Verificación en navegador** (no solo lectura de código): se creó un admin temporal (`claude_test_admin`, vía script directo contra `instance/local.db`, porque no había credenciales de admin a mano) para loguearse, y desde "Gestión de Equipo" (atajo de teclado `C` para abrir el panel — el botón de settings del Dock no dispara el modal directamente, según `HotkeysManager.jsx`) se confirmó que el selector de rol ya lista "Director Comercial" y "Director de Marketing". Se crearon dos usuarios de prueba reales, uno por rol, y se logueó con cada uno por separado: `director_marketing` aterriza en `/admin/workshops` tras login, con el Dock mostrando Workshops + Marketing (el segundo click navegó de verdad a `/admin/marketing`, confirmado por `location.pathname`), y un intento directo a `/admin/ventas` rebotó a `/admin/workshops`. `director_comercial` aterriza en `/admin/ventas` con el Dock mostrando solo Ventas, y un intento directo a `/admin/marketing` rebotó a `/admin/ventas`. Los 3 usuarios de prueba (`claude_test_admin`, `claude_test_marketing`, `claude_test_comercial`) se eliminaron de la base al terminar.

- **4 de Septiembre de 2026 (2)** *(reskin visual completo de Workshop Intelligence — pedido explícito del usuario a partir de un proyecto de referencia externo que compartió (`learnation-workshop-analytics`, Next.js con un `README.md`/`app/globals.css` que documentan un design system "command center nocturno" navy/rosa/azul) — "necesito que sea tal cual como lo que te acabo de pasar"; alcance ampliado en el mismo turno a pedido del usuario para que también se vea así en la sesión de `admin`, [workshop-intel.css](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/workshop/workshop-intel.css) [NEW], [index.html](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/index.html) [MODIFY], [index.css](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/index.css) [MODIFY], y los 13 archivos de `frontend/src/pages/admin/workshop/` [MODIFY])*:
  - **Solo capa visual, cero cambios de datos/lógica**: se confirmó explorando el código que `funnelMath.js` ya documentaba que el modelo de embudo de NeurOPS ("una tasa por paso, techo 95%") está calcado de este mismo proyecto de referencia — los 8 stages y los nombres de campo de `WorkshopEvent` (`leads`, `whatsapp_leads`, `show_up`, `pitch_leads`, `pitch_final_leads`, `aplicaciones_form`, `agendas_exitosas`, `show_up_sales_call`, `sales`, `cash_collected`) calzan casi 1:1 con el `Workshop` type de la referencia. Por eso el trabajo fue reescribir el JSX/clases de los 13 archivos (`WorkshopDashboardPage.jsx` + 12 componentes en `components/`) sin tocar ningún `useState`/`useEffect`/llamada a `api.*` existente — mismos props, mismos handlers, mismos endpoints.
  - **Como `/admin/workshops` es la MISMA `WorkshopDashboardPage` para `admin` y `director_marketing`** (`App.jsx`, ver entrada anterior de hoy), reskinear este componente cerró automáticamente el pedido ampliado del usuario sin tocar nada aparte.
  - **Precedente ya existente en el repo, reutilizado a propósito**: `CloserWorkflowPage.jsx` ya había hecho este mismo tipo de "clonar una referencia visual externa" (28/ago/2026) con una clase envoltorio `.bg-v6` scopeada en `index.css`. Se copió el mismo mecanismo con una clase nueva `.ws-shell`: `index.html` (el `<style>` inline que carga antes que cualquier CSS de Vite/Tailwind) e `index.css` tienen resets globales `!important` de `font-family`/`font-weight` con una exclusión `:not(.bg-v6):not(.bg-v6 *)` tallada para el Workspace de Closers — la bitácora del 28/ago ya documentó que OLVIDAR la copia en `index.html` (no solo en `index.css`) produce networking sutil ("las letras se ven más finas que el mockup"), así que se extendió la MISMA cadena de exclusión en ambos archivos a `:not(.ws-shell):not(.ws-shell *)` en vez de solo agregar una regla nueva.
  - **`workshop-intel.css` (nuevo, ~430 líneas)**: todo el vocabulario de clases de la referencia (`.kpi-grid/.kpi-card`, `.panel/.lever-row/.track/.status`, `.event-grid/.event-card`, `.funnel-panel/.funnel-stage/.stage-bar/.rate-pill`, `.sim-grid/.slider-row`, `.actions-grid/.plan-item`, `.wizard-layout/.field-control`, `.modal-overlay/.action-modal`, etc.) portado con los mismos valores (colores/radios/spacing/breakpoints) pero con CADA selector prefijado `.ws-shell` — nombres genéricos como `.panel`/`.status`/`.track`/`.legend` no debían filtrarse al resto del admin. Fuente Montserrat: ya estaba cargada en `index.html` (28/ago) en pesos `400;600;700;900`; se sumó `800` (usado por varias reglas de la referencia).
  - **Adaptaciones deliberadas, no literales, donde la estructura de NeurOPS difiere de la referencia** (la referencia es una app multi-página con rutas; NeurOPS usa pestañas dentro de una sola página): la navegación de pestañas (Historial/Embudo/Simulador/Acciones/Landing) se restyleó como una cápsula tipo `.main-nav` de la referencia pero con botones en vez de links de ruta; el wizard de registrar/editar taller (`WorkshopFormModal.jsx`) se mantuvo como MODAL (no se convirtió a página completa, porque está enganchado al flujo de crear/editar desde las tarjetas y al prefill por fecha) con `.wizard-steps`/`.progress-circle` compactados adentro; la pestaña "Landing de la Grabación" (`WorkshopLandingView.jsx`) no tiene equivalente en la referencia (es un embudo propio de NeurOPS, la landing de la grabación `/replay/`) — se restyleó con el mismo vocabulario de tokens/clases para que se vea consistente con el resto igual.
  - **Bug real encontrado en verificación de navegador, no en lectura de código**: el modal "Comparar talleres" (`WorkshopCompareModal.jsx`) armaba la tabla con `.comparison-scroll`/`<table>` pero sin el div envolvente `.comparison-panel` — como casi todas las reglas de tabla en `workshop-intel.css` están scopeadas `.ws-shell .comparison-panel th/td/...`, la tabla se veía "casi bien" (heredaba tipografía/color general de `.ws-shell`) pero sin el padding/alineación/borde de celda correctos, y el header de cada columna (fecha + nombre del evento) se pegaba en una sola línea en vez de partirse en dos como en la referencia. Se detectó comparando el screenshot real contra el resultado esperado, no fue obvio de la lectura del JSX. Fix: envolver `.comparison-scroll` en un `.comparison-panel` (con padding/fondo/sombra pisados a cero inline, porque esos ya los pone el modal que lo contiene).
  - **Verificación en navegador, no solo lectura de código**: reutilizando el mismo admin temporal de la entrada anterior, se recorrieron las 5 pestañas (Historial en modo tarjetas y tabla, Embudo con selector de evento y desglose vivo/grabación, Simulador con sliders y proyección, Acciones con formulario inline de creación, Landing con KPIs/embudo/tabla de sesiones) y los 3 modales (wizard de Registrar Taller completo — se navegaron los 3 pasos con datos reales, confirmando que el prefill automático seguía funcionando, sin llegar a enviar el formulario — Comparar con 2 y 3 talleres seleccionados, y Editar Metas), sin errores de consola reales (un error de HMR que apareció en la consola del navegador resultó ser un mensaje repetido/cacheado de un estado intermedio ya resuelto, confirmado comparando contra los logs frescos del servidor de Vite, que no mostraban ningún error nuevo). Se probó además que otra página del admin ajena a este cambio (`/admin/ventas`, con su propio tema indigo/rosa "v6/v7") se sigue viendo exactamente igual que antes, confirmando que la exclusión `:not(.ws-shell)` no rompió nada fuera de Workshop Intelligence. Usuario de prueba eliminado al terminar.

- **4 de Septiembre de 2026 (3)** *(pedido explícito del usuario: desactivar los avisos de seguimiento por WhatsApp que le llegaban a los closers — "ya no es necesario" — sin cambios de código, solo variable de entorno en producción)*:
  - **Nada que tocar en el código**: el aviso (`CloserFollowUpService.send_due_reminders`, [closer_followup_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py)) ya tenía un freno de emergencia global de fábrica, `reminders_enabled()`, gobernado por la variable de entorno `FOLLOWUP_REMINDERS_ENABLED` (default `true`) — pensado a propósito para cubrir los dos caminos que pueden disparar el envío: el scheduler interno ([reminder_scheduler.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/reminder_scheduler.py), un `BackgroundScheduler` que tickea cada 5 min) y el endpoint de cron externo (`GET /api/closer/followups/cron/send-reminders`, [closer_followups.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer_followups.py)) que algún cron externo llama con un token.
  - **Detalle que casi lleva a un diagnóstico equivocado**: producción ya tenía `DISABLE_REMINDER_SCHEDULER=true` seteada (para otra cosa, evita que el scheduler EN PROCESO arranque) — pero esa variable no cubre el endpoint de cron externo, que sigue funcionando igual porque su propio corte real vive adentro de `send_due_reminders` (`reminders_enabled()`), no en si el scheduler interno llegó a arrancar. Como `FOLLOWUP_REMINDERS_ENABLED` no estaba seteada (default `true`), lo más probable es que los avisos siguieran saliendo por ese segundo camino pese a la otra variable ya apagada.
  - **La corrección real**: `railway variables --set "FOLLOWUP_REMINDERS_ENABLED=false"` sobre el servicio `learnation work` (producción) — dispara un redeploy automático de Railway, verificado con `railway status` hasta volver a `● Online`, y confirmado además leyendo la variable en vivo del propio proceso desplegado (`railway run ... os.environ.get('FOLLOWUP_REMINDERS_ENABLED')` → `'false'`). Espejada también en el `.env` local para que no diverja de producción.

- **5 de Septiembre de 2026** *(sistema de bugs: el usuario puede reabrir su propio reporte ya "resuelto" con un botón "Sigue roto" en vez de tener que crear uno nuevo — pedido explícito del usuario, [bug_reports.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/bug_reports.py) [MODIFY], [BugReportThread.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/feedback/BugReportThread.jsx) [MODIFY])*:
  - **Sin tocar el modelo**: no hizo falta ningún estado nuevo ni columna — reabrir es simplemente volver `status` a `'open'` (mismo valor que un reporte recién creado, así que reaparece igual en "pendiente(s) de revisar" del panel del operador) más un `BugReportMessage` automático (`sender_id`/`sender_role` del propio reportante) para que el operador tenga contexto de que fue una reapertura y no un mensaje cualquiera — se apoya en el mismo mecanismo de `unread_for_manager` que ya existía, no hizo falta ningún flag nuevo.
  - **Endpoint nuevo, `POST /bug-reports/<id>/reopen`**: solo el dueño del reporte puede llamarlo (`report.user_id != current_user.id` → 403; los managers ya tienen su propio control de estado vía el `<select>` de `BugReportsPanel`, así que no se les dio este atajo) y solo si `status == 'resolved'` (400 en cualquier otro estado — no tiene sentido "reabrir" un reporte que ya está pendiente o en revisión).
  - **Frontend**: `BugReportThread.jsx` (compartido entre `BugReportHistory` del usuario y `BugReportsPanel` del operador) ahora guarda el `report` completo en estado local y muestra un banner ámbar con el botón "Sigue roto" solo cuando `report.status === 'resolved' && report.user_id === user.id` — así el banner nunca aparece del lado del operador viendo el reporte de otra persona, aunque reutilicen el mismo componente.
  - **Verificación en navegador** (sesión real del operador Kerwin, sin impersonar — probar como dueño del reporte era justo lo que hacía falta cubrir): reporte de prueba creado con el widget flotante, marcado "Resuelto" desde `BugReportsPanel`, abierto desde "Mis reportes" → apareció el banner "Sigue roto", clickeado → `POST /bug-reports/3/reopen` devolvió 200, el banner desapareció y quedó el mensaje automático "El problema sigue presente, no quedó resuelto." en la conversación; recargando `BugReportsPanel` el reporte volvió a "Pendiente" y el contador de pendientes subió de 1 a 2. Registro de prueba eliminado de `instance/local.db` al terminar (`env/Scripts/python.exe -c "..."` borrando el `BugReport` y su `BugReportMessage` directo, ya que no existe endpoint de borrado).

- **5 de Septiembre de 2026 (2)** *(nuevo módulo: videos de documentación con quiz de comprensión — pedido explícito del usuario para que los usuarios vean videos de onboarding/novedades y confirmen que los entendieron, con gestión desde el panel de operador, [training_video.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/training_video.py) [NEW], [training_videos.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/training_videos.py) [NEW], [ad569b0263be_add_training_videos.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/ad569b0263be_add_training_videos.py) [NEW], [__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY], [models/__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/__init__.py) [MODIFY], [TrainingVideoGate.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/training/TrainingVideoGate.jsx) [NEW], [TrainingVideosPanel.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/TrainingVideosPanel.jsx) [NEW], [TrainingVideoFormModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/TrainingVideoFormModal.jsx) [NEW], [App.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/App.jsx) [MODIFY], [OperationsSettingsPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/operations/settings/OperationsSettingsPage.jsx) [MODIFY])*:
  - **Modelo (4 tablas nuevas)**: `TrainingVideo` (título, descripción, `loom_link`, `target_roles` JSON — lista de roles a quienes se les muestra, vacía/nula = todos los roles —, `is_active`, `created_by_id`), `TrainingVideoQuestion` y `TrainingVideoOption` (con `is_correct` por opción, soporta más de una opción correcta por pregunta — el pedido decía "marcar cuáles son las correctas" en plural), y `TrainingVideoCompletion` (par único `video_id`+`user_id`, con `UniqueConstraint`) que registra quién ya lo completó. Las respuestas del usuario nunca se guardan, solo el hecho de haber acertado todo.
  - **Editar preguntas reemplaza el set completo, no las diffea**: `_apply_questions` (en `training_videos.py`) borra y recrea todas las preguntas/opciones de un video en cada `PUT` — el volumen por video es chico (un puñado de preguntas) y el formulario del operador siempre manda el set completo, así que era más simple que diffear pregunta por pregunta. Si se edita el quiz de un video ya completado por alguien, sus `TrainingVideoCompletion` se borran a propósito: si la info de comprobación cambió, quien lo "aprobó" con las preguntas viejas debería volver a verlo.
  - **Corrección en el backend, nunca en el cliente**: `POST /training-videos/<id>/submit` recibe `{answers: {question_id: [option_id, ...]}}` y compara el set de opciones marcadas contra el set de opciones `is_correct` de cada pregunta — deben coincidir exactamente (ni de más ni de menos) para que esa pregunta cuente como correcta. El frontend nunca recibe qué opción es la correcta hasta que el video ya se completó (`to_dict(include_correct=False)` en `GET /training-videos/pending`), solo si acertó o no cada pregunta tras enviar.
  - **`GET /training-videos/pending`**: un video aparece para el usuario actual solo si está `is_active`, tiene al menos 1 pregunta (uno sin preguntas nunca podría "completarse", así que no tendría sentido mostrarlo), su `target_roles` está vacío o incluye el rol del usuario, y no existe ya un `TrainingVideoCompletion` suyo para ese video.
  - **Frontend, gate global (`TrainingVideoGate.jsx`)**: montado en `App.jsx` como hermano de `BugReportWidget` (NO dentro de `MainLayout`) por el mismo motivo ya documentado el 01/09 para ese widget — `CloserWorkflowPage` ("/closer/deck", donde los closers pasan todo su turno) corre standalone sin `MainLayout`, y los closers son justamente uno de los públicos objetivo de este feature. Se autogatea con `useAuth()`. Al montar, pide `pending` y si hay alguno muestra un modal de pantalla completa (portal a `document.body`) con el Loom embebido (mismo `getLoomEmbedUrl` que `LoomModal` de `BugReportsPanel.jsx`, duplicado a propósito en vez de extraído a un util compartido, siguiendo el mismo criterio ya usado en ese archivo) y el quiz debajo — checkboxes en vez de radios para soportar preguntas con más de una respuesta correcta. Si falla alguna pregunta, el modal NO se cierra y muestra qué preguntas quedaron mal (ícono verde/rojo) para reintentar; si acierta todo, se marca como completo en el backend y desaparece de la cola (pasando al siguiente video pendiente, si hay más de uno).
  - **Gestión desde el panel de operador**: pestaña nueva "Videos de Documentación" en `OperationsSettingsPage.jsx` (mismo criterio de acceso que "Reportes de Bugs", `operator`/`admin`) con `TrainingVideosPanel.jsx` (listar/activar-desactivar/editar/eliminar) y `TrainingVideoFormModal.jsx` (crear/editar: título, descripción, link de Loom, selector de roles objetivo — botones tipo chip, reutilizando el mismo `ROLE_OPTIONS` en ambos archivos —, y un builder de preguntas/opciones dinámico donde cada opción se marca correcta con un botón check independiente). Justo lo que pidió el usuario: "simplemente pego el loom y configuro las preguntas y sus respuestas".
  - **Migración**: el ID de revisión generado inicialmente (`a1b2c3d4e5f6`) chocaba con uno ya existente de otra migración vieja (`a1b2c3d4e5f6_add_is_active_to_programs.py`) — `flask db upgrade` lo detectó como "Cycle is detected in revisions" antes de aplicar nada. Se regeneró un ID aleatorio nuevo (`ad569b0263be`) sin colisión y se renombró el archivo; aplicada limpio contra `instance/local.db` después.
  - **Verificación end-to-end (`scratch/simulate_training_videos.py`, no comiteado)**: mismo patrón ya documentado el 01/09 para evitar el bug de identidad cruzada entre usuarios simulados (las llamadas de `test_client()` van FUERA de cualquier `app_context()` manual). Cubrió: un operador crea un video con 2 preguntas dirigido solo a `closer`; un setter (rol no targeteado) NO lo ve en `/pending`; un closer sí lo ve, y las opciones no traen `is_correct`; responder mal una pregunta no marca el video completo y sigue apareciendo en pendientes; responder bien todo lo marca completo, crea el `TrainingVideoCompletion` y lo saca de pendientes; un rol sin permisos de manager recibe 403 en los endpoints de gestión; editar el video (quitar una pregunta) borra la completion existente. Las 8 verificaciones pasaron.
  - **Verificación real en navegador** (con el usuario ya logueado como operador `Kerwin`): creado un video real desde el formulario del panel dirigido a `closer` con una pregunta de 2 opciones; impersonado el closer `Marlon Closer` desde "Gestión de Equipo" → el modal de quiz apareció automáticamente al entrar al Closer Workspace (`/closer/deck`, confirmando que el gate SÍ funciona fuera de `MainLayout`); respondida la opción incorrecta → el modal no cerró y marcó la pregunta con ícono rojo; respondida la correcta → el modal se cerró solo; revertida la impersonación (`POST /api/auth/revert`) y confirmado en el panel del operador que "0 completados" pasó a "1 completado(s)". Video de prueba eliminado al terminar, base local queda en cero filas en las 4 tablas nuevas.

- **5 de Septiembre de 2026 (3)** *(incidente de producción: director_comercial no podía cargar "Dashboard de Performance" (403) + acotar su alcance a closing/setting quitándole la pestaña Workshops — reportado por el usuario con logs de Railway, [closer_dashboard.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer_dashboard.py) [MODIFY], [AdminSalesHubPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/reports/AdminSalesHubPage.jsx) [MODIFY])*:
  - **El síntoma**: log de proxy de Railway mostrando `GET /api/closer/performance-dashboard` → `403` para una sesión real en `work.thelearnation.com`. `director_comercial` aterriza en `/admin/ventas` (`AdminSalesHubPage`) tras el login, cuya pestaña por defecto es "Closers" (`PublicCloserStatsPage`) con su sub-pestaña por defecto "Dashboard" (`CloserDashboard.jsx`) — ese componente pide `/closer/performance-dashboard` apenas monta, sin que el usuario haga nada más.
  - **La causa real**: los 4 endpoints de `closer_dashboard.py` (agregado el 19-20/ago, antes de que `director_comercial` reapuntara a `/admin/ventas` el 04/09) tenían el chequeo de rol hardcodeado a `if current_user.role not in ['closer', 'admin']` — nunca se actualizó al mover `director_comercial` a esta zona, así que cualquier director comercial real caía directo en el 403 al entrar a su propio dashboard. No relacionado con el warning `[AGENDA SIN EMAIL]` del mismo log pegado por el usuario (ese es un aviso preexistente y deliberado de `financial_agendas.py` cuando n8n manda una agenda sin `mail`/`email` en el payload — confirmado leyendo el código, es un problema de datos de esa agenda puntual de Calendly/n8n, no toca el rol ni el dashboard).
  - **El fix**: `performance-dashboard` y `pending-summary` (los dos que de verdad usa la UI de `director_comercial`, ambos de solo lectura) suman `ROLE_DIRECTOR_COMERCIAL` a la whitelist mediante una constante compartida `ROLES_CON_VISTA_AGREGADA = ['admin', ROLE_DIRECTOR_COMERCIAL]` — se comporta igual que `admin` (ve todos los closers o uno puntual por `closer_id`, nunca queda forzado a `current_user.id` porque no es closer real). `slots-pendientes`/`slots` (declarar los propios cupos del día) quedaron sin tocar a propósito: son estrictamente de un closer sobre sí mismo (`current_user.id`) y el frontend ya los gatea con `user?.role === 'closer'` (`CloserDashboard.jsx:169`), así que `director_comercial` nunca los llama.
  - **Acotar el rol, pedido explícito del usuario en el mismo mensaje**: "él debería ver closing y setting, el de workshop no lo debería revisar, eso lo vamos a poner aparte, en marketing" — `AdminSalesHubPage` (compartida entre `admin` y `director_comercial` desde el 04/09) tenía sus 3 pestañas (Closers/Setters/Workshops) visibles para ambos roles por igual. Se agregó `puedeVerWorkshops = user?.role === 'admin'` (vía `useAuth()`) que oculta el botón de la pestaña y el render de `WorkshopDashboardPage` cuando no es admin — `director_comercial` ahora solo puede ver Closers/Setters ahí; admin conserva las 3 sin cambios, consistente con "desde Admin se vea todo" ya establecido el 04/09. Workshop-como-Marketing ya vive aparte en `/admin/workshops` para `director_marketing` (sin tocar en esta sesión).
  - **Verificación**: contra `instance/local.db` con el test client de Flask (`scratch/verify_director_comercial_dashboard.py`, no comiteado, sesión simulada por usuario real `Kerwin Ventas` (`director_comercial`) — mismo patrón de `login.session_protection = None` ya documentado en sesiones anteriores): `GET /closer/performance-dashboard` y `GET /closer/pending-summary` pasan de `403` a `200`; un `setter` de control sigue en `403` (el fix no abrió la puerta de más); `admin` sigue en `200` sin cambios. El JSX de `AdminSalesHubPage.jsx` se confirmó vía el propio dev server de Vite (`GET /src/pages/admin/reports/AdminSalesHubPage.jsx` transformado sin errores).
  - **Verificación en navegador no completada, motivo externo**: no se pudo hacer el recorrido real logueado (crear un admin temporal + impersonar, patrón ya usado en sesiones anteriores) porque, a mitad de esta sesión, otra sesión de Claude Code del mismo usuario trabajando en paralelo sobre `develop` (aviso explícito del usuario: "hay más sesiones tuyas trabajando sobre la rama develop") tenía en curso el borrado del módulo de Videos de Documentación (`app/models/training_video.py`, `app/api/training_videos.py`, sus componentes de frontend) — el backend compartido del puerto 5000 quedó momentáneamente sin arrancar por el import roto a mitad de ese guardado (`ModuleNotFoundError: app.api.training_videos`), ajeno a este cambio. Se optó por no tocar esos archivos (no es esta tarea) y apoyarse en la verificación por test client, que corre en proceso propio contra la misma base de datos sin depender de que el server de la otra sesión esté arriba.

- **5 de Septiembre de 2026 (4)** *(Workshop Intelligence: pestañas siempre visibles arriba + "Resumen" separado de las pestañas de datos, y sincronización en vivo automática de agendas/ventas — pedido explícito del usuario a partir de `learnation-workshop-analytics-react-python` (referencia externa ya usada el 04/09 para el reskin visual) y de una queja de uso real ("no se actualizan rápidamente las agendas, las ventas... hay que recargar"), [workshop.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/workshop.py) [MODIFY], [c2d3e4f5a6b7_add_workshop_event_synced_at.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/c2d3e4f5a6b7_add_workshop_event_synced_at.py) [NEW], [workshop_live_sync.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/workshop_live_sync.py) [NEW], [__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY], [workshop.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/workshop.py) [MODIFY], [WorkshopDashboardPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/workshop/WorkshopDashboardPage.jsx) [MODIFY], [WorkshopFunnelView.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/workshop/components/WorkshopFunnelView.jsx) [MODIFY], [workshop-intel.css](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/workshop/workshop-intel.css) [MODIFY])*:
  - **El pedido, en dos partes**: (1) las pestañas (Historial/Embudo/Simulador/Acciones/Landing) vivían DEBAJO del hero + las 6 tarjetas KPI + el panel de Diagnóstico — cambiar de sección obligaba a scrollear esa franja entera cada vez, a diferencia de la referencia externa donde el `.topbar`/`.main-nav` está arriba de todo y siempre visible; (2) las métricas de un workshop (agendas, ventas) quedaban congeladas en el snapshot con el que se guardó el evento — reflejar una agenda o venta nueva requería que alguien entrara al panel y apretara "Resync sistema" a mano.
  - **Navegación — `ws-topbar` pegado arriba (`position: sticky; top: 0`)**: título compacto + las 6 pestañas (se sumó una nueva, "Resumen") ahora viven en un `<header>` sticky contra `#app-main-scroll` (el contenedor con scroll real de `MainLayout.jsx`, confirmado que no hay otro elemento sticky por encima compitiendo por ese `top: 0`). El hero grande ("Rendimiento & Workshop Intelligence.", copy, período analizado), las 6 `WorkshopKpiCards` y `WorkshopDiagnostico` — antes SIEMPRE visibles arriba de cualquier pestaña — se movieron adentro de la pestaña nueva "Resumen" (ícono `LayoutDashboard`, calcado de la vista "General" de la referencia). El resto de pestañas (Historial/Embudo/Simulador/Acciones) ahora arrancan justo debajo del topbar sticky, sin ese bloque de por medio — que es literalmente el pedido de la sesión ("separa el resumen de los datos de los eventos, para no tener que hacer tanto scroll para analizar"). El toggle Tarjetas/Tabla (antes en el header global) se movió adentro de la pestaña "Historial", que es la única que lo usa.
  - **Sincronización en vivo — por qué NO recalcular todo en cada carga**: se investigó primero (agente de exploración) si `calcular_prefill` (el cálculo real detrás de "Resync sistema") era barato — no lo es: `financial_agendas.registro` y `.created_at` no tienen índice, hay patrones N+1 en `_estado_post_call`/`_ventas_de`, y recorre clientes/agendas/ventas con varios `LIKE`. Recalcular TODOS los `WorkshopEvent` cargados en cada visita al dashboard hubiera multiplicado ese costo por cada evento, en cada carga de cualquier usuario — justo el motivo por el que el código ya evitaba pedirlo para la lista completa (comentario preexistente en `WorkshopDashboardPage.jsx`).
  - **El hook (`workshop_live_sync.py`)**: un listener `before_commit` sobre `db.session` (no por cada uno de los ~7 sitios donde se crean `FinancialAgenda`/`FinancialSale` — vive a nivel de sesión y los intercepta a todos por igual) que, si el commit que está por pasar creó o modificó una de esas dos, recalcula (vía `calcular_prefill`, la MISMA función del botón manual) solo los `WorkshopEvent` ya registrados cuya ventana vivo+grabación (`_ventana_landing`, hasta `DIAS_VENTANA_LANDING` días) cae sobre esa fecha — en la práctica, como mucho 1 o 2 eventos, nunca todos. Se probó `after_commit` primero y falló (`InvalidRequestError: session is in 'committed' state`) porque ahí la sesión ya no admite más SQL — la doc de SQLAlchemy señala justo `before_commit` para este caso: los cambios quedan DENTRO del mismo commit que los disparó (atómico con la agenda/venta que los originó), y `calcular_prefill` ya ve la fila nueva por el autoflush que dispara su propia query. Si `calcular_prefill` lanza una excepción para un evento puntual, se loguea y se sigue con el resto — nunca aborta el commit original (el closer que está declarando una venta no tiene por qué quedar bloqueado porque falle el recálculo del dashboard).
  - **Columna nueva `synced_at`**: se estampa tanto desde el hook como desde el resync manual (`PUT /workshop/events/<id>` cuando el payload trae las 4 métricas automáticas) — mismo criterio para las dos vías, así "Sincronizado hace X" (nuevo, junto al botón "Resync sistema" en la pestaña Embudo) no distingue si fue automático o manual.
  - **Frontend — recoger el cambio sin que el usuario haga nada**: `WorkshopDashboardPage.jsx` pasa a pollear `workshop/events` cada 20s en modo silencioso (mismo patrón ya usado en `CommentsSection.jsx`/`AlertsHubPage.jsx`, sin spinner ni toast de error por tick fallido) y, mientras la pestaña Embudo está abierta, también re-pide el desglose vivo/grabación (`workshop/prefill`) cada 30s. De paso se corrigió un bug latente: `selectedEventForFunnel` era una copia congelada del evento al momento de seleccionarlo — un refresh manual (`fetchEvents()`) actualizaba la lista pero NO ese objeto, así que la pestaña Embudo podía mostrar números viejos aun después de recargar a mano. Ahora `fetchEvents` siempre resincroniza `selectedEventForFunnel` por id contra la lista fresca.
  - **Verificación del hook (`scratch/test_workshop_live_sync.py`, no comiteado)**: contra `instance/local.db`, un `WorkshopEvent` de prueba con métricas en 0 → crear una `FinancialAgenda` que matchea su fuente y fecha → commit → sin llamar a `calcular_prefill` ni al endpoint de resync a mano, el evento ya tenía `agendas_exitosas=1` y `synced_at` seteado; crear además una `FinancialSale` válida → segundo commit → `sales=1`, `cash_collected=500.0`, `synced_at` refrescado de nuevo. Las 2 verificaciones pasaron. `app.py` arrancó limpio con el hook registrado (sin errores de servidor).
  - **Verificación en navegador NO completada, mismo motivo externo que la entrada anterior**: al momento de verificar, otra sesión de Claude Code en paralelo sobre este mismo `develop` seguía a mitad del reemplazo de Videos de Documentación por un módulo "Playbook" — `frontend/src/App.jsx` importaba `./components/training/TrainingVideoGate`, un archivo ya borrado por esa sesión pero con el import todavía sin actualizar, así que CUALQUIER pantalla de la SPA (no solo Workshop) tiraba `Failed to resolve import` en el dev server compartido. Verificado en cambio: los dos archivos JSX tocados pasan un chequeo de sintaxis aislado con `esbuild.buildSync` (sin depender de que el resto de la app resuelva), y se revisaron a mano las 6 pestañas/props contra los componentes hijos existentes. Pendiente repetir el recorrido visual real (sticky nav, pestaña Resumen, badge "En vivo", "Sincronizado hace X") en cuanto el import roto de la otra sesión se resuelva.
  - **Nota de colisión, misma raíz que la entrada anterior**: el `down_revision` de la migración nueva (`c2d3e4f5a6b7`) apuntaba a `ad569b0263be` (la migración de training videos) cuando se escribió; esa sesión paralela la borró minutos después al reemplazarla por `playbook`, pero su propia migración nueva ya se había generado con `down_revision='c2d3e4f5a6b7'` (la cadena vio primero esta migración), y el archivo de esta sesión terminó re-apuntado solo a `b4c5d6e7f8a9` — la cadena de Alembic quedó consistente sin que hiciera falta tocar nada manualmente, pero se documenta como recordatorio de que dos sesiones escribiendo migraciones en paralelo sobre el mismo working directory (no worktrees separados) es frágil.

- **5 de Septiembre de 2026 (5)** *(reemplazo completo del módulo de Videos de Documentación (sesión de hoy más temprano) por "Playbook": jerarquía Roadmap → Módulo → Lección con quiz de comprensión — pedido explícito del usuario a partir de un mockup de referencia externo (`Closer Workspace - Etapas, Formulario y Bugs.dc.html`, con su design system `learnation-design-system-.../README.md`) que compartió para rediseñar el Closer Workspace, [playbook.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/playbook.py) [NEW], [playbook.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/playbook.py) [NEW], [4c64ef2c0f18_replace_training_videos_with_playbook.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/4c64ef2c0f18_replace_training_videos_with_playbook.py) [NEW], [training_video.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/training_video.py) [DELETE], [training_videos.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/training_videos.py) [DELETE], [__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY], [models/__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/__init__.py) [MODIFY], [PlaybookContext.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/contexts/PlaybookContext.jsx) [NEW], [PlaybookOverlay.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/playbook/PlaybookOverlay.jsx) [NEW], [PlaybookLessonView.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/playbook/PlaybookLessonView.jsx) [NEW], [PlaybookNotification.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/playbook/PlaybookNotification.jsx) [NEW], [PlaybookAdminPanel.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/PlaybookAdminPanel.jsx) [NEW], [PlaybookLessonFormModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/operations/PlaybookLessonFormModal.jsx) [NEW], [App.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/App.jsx) [MODIFY], [Dock.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/shared/Dock.jsx) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [OperationsSettingsPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/operations/settings/OperationsSettingsPage.jsx) [MODIFY], [training_videos.dc.html mockup revisado, no comiteado])*:
  - **Por qué reemplazar en vez de extender lo de esta mañana**: el mockup pedía una jerarquía real (Skills/Platform/Learnation como roadmaps, cada uno con módulos y lecciones), tres tipos de pregunta (única/múltiple/verdadero-falso) con feedback inmediato por pregunta (no recién al final), y un asistente "Learnito" de búsqueda por transcripción — mucho más que el modelo plano de un video con un quiz. Como el feature de esta mañana nunca llegó a producción ni tenía datos reales, se lo borró por completo (modelo, API, migración, componentes de frontend) en vez de migrarlo, y se escribió Playbook desde cero. El usuario aclaró el alcance real antes de arrancar: Learnito queda como placeholder "Próximamente" (sin buscador de verdad todavía) y la parte visual se ajusta cuando mande las bases de diseño completas del sistema — esta sesión se concentró en la estructura y el funcionamiento, con el lenguaje visual ya existente del proyecto (Tailwind + tokens propios), no en clonar pixel a pixel el mockup.
  - **Modelo (7 tablas)**: `PlaybookRoadmap` (nombre, acento de color) → `PlaybookModule` → `PlaybookLesson` (título, descripción, `loom_link`, `duration_minutes`, `transcript` opcional ya guardado para cuando exista Learnito, `target_roles`, `is_active`) → `PlaybookQuestion` (`question_type`: `single`/`multiple`/`true_false`) → `PlaybookOption`. Progreso de un usuario se separa en dos tablas: `PlaybookLessonProgress` (solo `video_watched_at`, no implica aprobado) y `PlaybookCompletion` (aprobó el quiz completo, o no había quiz y ya vio el video) — la distinción es la que permite calcular el estado real de una lección (`nuevo` / `en_progreso` / `pendiente` / `completado`) sin la cual no se podían replicar los badges del mockup.
  - **Corrección server-side por pregunta, no al final**: a diferencia del feedback "todo o nada" de esta mañana, acá `POST /playbook/lessons/<id>/answer` corrige UNA pregunta a la vez (mismo criterio de nunca exponer la respuesta correcta hasta responder) — el frontend puede pintar esa pregunta puntual en verde/rojo apenas el usuario la contesta, como en el mockup, en vez de esperar a enviar todo el formulario. `POST .../complete` igual vuelve a validar TODAS las respuestas contra la base antes de aprobar — nunca confía en que el frontend ya mostró todo en verde.
  - **`question_type='single'` validado server-side**: si alguien manda 2+ opciones marcadas correctas en una pregunta de selección única, `_apply_questions` la rechaza con 400 antes de guardar nada — evita un quiz roto donde nunca se puede acertar exactamente el set esperado.
  - **Frontend, misma decisión de ubicación que el feature de esta mañana**: `PlaybookOverlay`/`PlaybookNotification` se montan una sola vez en `App.jsx` (vía `PlaybookContext`, con `pendingCount`/`openPlaybook` compartidos) para que abran igual desde cualquier rol. El botón que los dispara SÍ cambia según la página: pill con gradiente en el header propio de `CloserWorkflowPage` (calcado del mockup, con badge de pendientes) para closers, y un ícono nuevo en `Dock.jsx` (compartido por el resto de roles vía `MainLayout`) para todos los demás — un solo overlay, dos puntos de entrada.
  - **Decisión explícita del usuario sobre el botón de bugs**: aclaró que el ícono de bug del header del mockup NO debía tocar el botón flotante ya existente (`BugReportWidget`, esquina inferior derecha) — se dejó completamente intacto, el mockup solo aportó ideas para Playbook/Confirmaciones.
  - **Bug real encontrado en verificación de navegador**: entrar a una lección directo desde "Pendientes" (sin pasar antes por Roadmaps→Módulos) dejaba `moduleId`/`roadmapId` en `null` en `PlaybookOverlay`; al aprobar la única lección del módulo (sin siguiente lección) el `onBack()` intentaba mostrar la vista "lecciones" con esos ids vacíos y quedaba en blanco (el `useEffect` que pide `/playbook/modules/<id>/lessons` nunca corría). Fix: `PlaybookLessonView` avisa a su padre (`onModuleResolved`) qué módulo/roadmap resultó ser el de la lección apenas la carga, sin importar por dónde se entró — así "volver" siempre tiene a dónde ir.
  - **Migración, mismo tipo de colisión que la de esta mañana**: mientras se armaba esta migración, otra sesión en paralelo ya había agregado la suya (`c2d3e4f5a6b7`, `synced_at` de Workshop) encadenada sobre la de training videos (única head en ese momento) — al borrar esa migración vieja, se tuvo que reapuntar el `down_revision` de `c2d3e4f5a6b7` a `b4c5d6e7f8a9` para no romper la cadena de Alembic (ver nota de esa sesión más arriba). Aplicada limpio después contra `instance/local.db`.
  - **Fricción real causada a las otras dos sesiones en paralelo (documentada también desde su lado, entradas de arriba)**: borrar `training_video.py`/`training_videos.py` y recién después actualizar `app/__init__.py`/`App.jsx` dejó una ventana corta con el backend compartido del puerto 5000 tirando `ModuleNotFoundError` y el frontend compartido tirando `Failed to resolve import` para CUALQUIER pantalla — no solo Playbook. Ambas sesiones (director_comercial 403, Workshop live sync) tuvieron que verificar por otros medios (test client aislado, chequeo de sintaxis con esbuild) en vez de en el navegador real mientras tanto. Para la próxima vez: al borrar un archivo importado desde otro compartido (`app/__init__.py`, `App.jsx`), actualizar esa referencia en el mismo paso, no en ediciones separadas.
  - **Verificación end-to-end (`scratch/simulate_playbook.py`, no comiteado)**: 15 verificaciones con el mismo patrón de test client fuera de `app_context()` manual — un setter no puede crear roadmaps (403); operador crea roadmap+módulo+lección con una pregunta `single` y una `multiple`; una pregunta `single` con 2 opciones correctas es rechazada (400); un setter no ve el roadmap (0 lecciones visibles para su rol); un closer lo ve con 0% de avance y el módulo en "no_iniciado"; la lección aparece "nueva" sin exponer respuestas correctas; marcar el video visto pasa el estado a "en_progreso"; feedback inmediato por pregunta (mal=false, bien=true, la de tipo `multiple` exige el set exacto); completar con una respuesta mal da 400 sin crear `PlaybookCompletion`; completar bien la aprueba y devuelve `next_lesson_id`; ya no aparece en pendientes y el roadmap pasa a 100%; el overview de admin refleja el cumplimiento por rol; editar el quiz resetea la completion existente. Las 15 pasaron.
  - **Verificación real en navegador** (operador `Kerwin`, sesión propia en un puerto de Vite separado del resto de sesiones por el conflicto de puertos ya documentado): creado un roadmap/módulo/lección real desde el wizard del panel de administración, con una pregunta verdadero/falso (confirmando que el tipo genera las dos opciones fijas "Verdadero"/"Falso" sin campos de texto editables); impersonado el closer `Jean Carlo` → notificación de "Playbook nuevo" apareció sola al entrar, con autor/duración/preguntas/audiencia; "Verlo ahora" abrió el overlay en Pendientes; el video (falso, sin Loom real) disparó igual `onLoad` y habilitó "Ir a las preguntas"; respondida "Falso" → se pintó en rojo sin cerrar; respondida "Verdadero" → verde, botón "Aprobar" habilitado → lección aprobada y overlay cerrado; confirmado en el panel del operador que "0/5 closers" pasó a reflejar el completado y que el roadmap del closer subió a 100%. Se navegó además Roadmaps→Módulo (estado "Completo")→Lección completada (badge verde en la lista) para confirmar el fix de navegación de arriba. Datos de prueba eliminados al terminar por API (`DELETE /playbook/roadmaps/<id>`, cascada completa), base local en cero filas en las 7 tablas nuevas.

- **5 de Septiembre de 2026 (6)** *(Kanban de Confirmaciones: de 3 a 2 columnas + stepper del modal con círculos numerados — retoque liviano pedido explícito del usuario a partir del mismo mockup de referencia de la entrada anterior ("ahora solo se divide en dos... el modal de confirmación cambió... no lo quiero cambiar tanto, pero toma lo mejor de ese nuevo modal"), [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [index.css](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/index.css) [MODIFY])*:
  - **Alcance elegido por el usuario entre dos opciones planteadas**: se ofreció (a) un retoque liviano — colapsar el Kanban sin tocar `confirm_status`/lógica, o (b) rehacer el modelo completo a las 5 etapas del mockup (Por contactar/Contactado/Horario/VideoAsk/Testimonio) + "dolores" + notas. El usuario eligió (a) explícitamente — ni el modelo de datos (`confirm_status`: `por_confirmar`/`conversando`/`Confirmado`) ni `saveConfirmReport`, `confirmationsPipeline`, ni ningún endpoint del backend se tocaron en esta entrada.
  - **Kanban, 3→2 columnas**: la columna "Conversando" desaparece como columna propia y sus tarjetas pasan a vivir DENTRO de "Por confirmar", como un subgrupo con su propio encabezado ("Sin contactar" / "Conversando", cada uno con el punto de color que antes tenía su columna — ámbar/azul). Cada tarjeta sigue recibiendo su `phase` real (`'por_confirmar'` o `'conversando'`) al armarse, así que el botón de acción ("Contactar y registrar" vs. "Confirmar asistencia") y el resto de `renderKanbanCard` no cambiaron una sola línea. CSS nueva: `.kb-v6.kb2-v6` (modificador de 2 columnas) y `.ksub-v6` (encabezado de subgrupo) — se agregaron como clases APARTE en vez de editar `.kb-v6` directo, porque esa clase también arma el Kanban de 3 columnas de "Llamadas" (atrasadas/hoy/reportadas) unas pestañas más allá, que debía quedar intacto.
  - **Modal — círculos numerados en el stepper existente**: el `.pipe`/`.pstep` de la banda de 3 pasos (Por confirmar → Conversando → Confirmado) ya tenía una identidad visual bastante lograda (pastilla segmentada con degradado en el paso actual) — en vez de reconstruirlo como el stepper de círculos-con-línea del mockup, se le sumó un círculo numerado (`①②③`, o `✓` si el paso ya se completó) al costado de cada etiqueta, reemplazando el tick flotante en la esquina que tenía antes. Es la pieza más reconocible del stepper nuevo del mockup, incorporada sin tocar la estructura de 3 pasos, los textos, ni la lógica de `currentIdx`/`currentKey` que ya existía.
  - **Verificación en navegador** (closer `Jean Carlo`, impersonado desde el panel de operador y revertido al terminar): el Kanban de Confirmaciones muestra "POR CONFIRMAR (2)" con el subgrupo "SIN CONTACTAR" y "CONFIRMADO (0)" — 2 columnas reales, no un placeholder; abierta la ficha de un lead sin contactar, el stepper muestra los 3 pasos con círculo "①" relleno en gradiente sobre el paso actual y círculos "②"/"③" en contorno sobre los pasos futuros. Se confirmó además que el Kanban de "Llamadas" (pestaña 02, Reportar) sigue mostrando sus 3 columnas originales sin cambios — la clase `.kb2-v6` no filtró a ningún otro tablero. No se guardó ningún cambio real sobre el lead de prueba (se cerró el modal sin tocar "Guardar Reporte"), así que no hizo falta limpiar nada en la base al terminar.

- **5 de Septiembre de 2026 (7)** *(causa raíz del contador de agendas: el webhook de n8n/Calendly guardaba la hora de la cita en horario de La Paz dentro de una columna que todo el sistema lee como UTC — reportado por el usuario mirando el Kanban del Closer Workspace contra el calendario de los closers ("decían que fueron hace dos, hace una hora, y en realidad falta una hora para esa agenda"), [agenda_time_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/agenda_time_service.py) [NEW], [financial_agendas.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/public/financial_agendas.py) [MODIFY])*:
  - **Dónde estaba el error, exactamente**: en `receive_financial_agendas` (`app/api/public/financial_agendas.py`), la fecha de la cita que manda n8n se normalizaba con `agenda_date.astimezone(la_paz_tz).replace(tzinfo=None)` — es decir, se guardaba **hora de pared de America/La_Paz** en `FinancialAgenda.date`. De ahí `BookingService.sync_financial_agenda_to_appointment` la copia tal cual a `Appointment.start_time` (`appt.start_time = agenda.date`). Pero absolutamente todo lo que consume esa columna la lee como **UTC**: `user_time_service.limites_dia_utc` arma las ventanas del día pasando de la zona del usuario a UTC, la API la publica con `.isoformat()` sin sufijo, el frontend la parsea con `parseUtcIso()` (que le antepone la `Z` a propósito) y `google_service.py:114` directamente hace `start_time.isoformat() + 'Z' # Assuming it's UTC`. O sea: el dato entraba en una convención y salía leído en otra.
  - **El error NO viene de Calendly**: el instante que manda Calendly es correcto (ISO 8601 con zona). Se pierde en la ingesta nuestra. Reproducido antes de tocar nada con los cuatro formatos que puede mandar n8n (`scratch/repro_tz.py`, no comiteado — `scratch/` está en `.gitignore`): los cuatro terminaban guardados como `15:00` naive para una cita real de las 16:00 de Curitiba, y leídos como UTC se veían a las 12:00 → **4 horas de corrimiento hacia atrás, exactamente el offset de La Paz**. Por eso una agenda a la que le faltaba una hora aparecía como "Hace 3 horas" en el contador del Kanban; el número que veía el usuario no era un redondeo mal hecho, era el instante equivocado.
  - **Por qué el calendario de agendas y el Kanban decían cosas distintas**: `FinancialAgendasPage.jsx` (panel de agendas) formatea con `new Date(str)` sobre un string sin zona, que JS interpreta como **hora local del navegador** — o sea, mostraba los `15:00` guardados como "15:00" y parecía correcto. El Kanban del closer usa `parseUtcIso()` y mostraba "12:00" del mismo registro. Las dos vistas leían bien su propio código y mal el dato: la diferencia entre ambas es literalmente el offset del navegador de quien mira.
  - **La regla nueva, en un solo lugar (`app/services/agenda_time_service.py`)**: en la base **todo es UTC naive**. Si el string trae offset (`...Z`, `-04:00`) se convierte a UTC y punto; si viene sin offset se asume `AGENDAS_SOURCE_TZ` (variable de entorno, por defecto `America/La_Paz`, que es como n8n formatea cuando pierde la zona) y recién ahí se pasa a UTC. Se eligió variable de entorno en vez de constante para poder corregir el supuesto sin deploy si la auditoría muestra que n8n manda otra zona.
  - **Aviso explícito en los logs**: cuando llega una fecha **sin** offset, el webhook loguea `[AGENDA SIN ZONA]` con el string crudo y la zona asumida. Un string sin zona es un dato ambiguo por definición — el sistema tiene que adivinar. El aviso queda en Railway para poder pedirle a n8n que mande ISO con zona y dejar de adivinar.
  - **Deduplicación de agendas ajustada al cambio de convención**: el bloque que busca "la misma agenda del mismo día para el mismo lead" cortaba el día con `agenda_date.replace(hour=0...)`. Con las fechas ya en UTC eso pasa a ser el día UTC, y una cita de las 21:00 local cae en el día UTC siguiente — el deduplicador dejaría de reconocer una reprogramación y crearía un registro duplicado. Se reemplazó por `limites_dia_origen()`, que calcula el día calendario en la zona de origen y lo devuelve como rango UTC (mismo patrón que `user_time_service.limites_dia_utc`, que ya hacía esto para las ventanas de día del usuario).
  - **Edición manual del panel (`PUT /public/financial-agendas/<id>`)**: escribía la misma columna con `parse_date_robustly` sin normalizar zona, así que reintroducía el corrimiento a mano cada vez que alguien corregía una fecha desde el panel. Ahora pasa por `parse_a_utc`. `created_at` hacía lo mismo con La Paz y quedó también en UTC; el campo de texto `registro` (que es lo que ordena y muestra el panel) se mantiene en hora local de la fuente a propósito, para no mover los listados que ordenan por él.
  - **Verificación** (`scratch/test_ingesta_utc.py`, no comiteado): se levantó la app real con SQLite temporal y se le mandaron al webhook los cuatro formatos posibles para la misma cita (`...Z`, `-04:00`, `-03:00`, y sin zona). Los cuatro quedan ahora en `2026-09-05 19:00 UTC` tanto en `FinancialAgenda.date` como en el `Appointment.start_time` sincronizado, y se ven a las 16:00 en Curitiba — el instante real. Antes los cuatro quedaban en `15:00`.
  - **Pendiente en esta entrada, resuelto en la siguiente**: las agendas que ya están en la base siguen guardadas con la convención vieja. El fix arregla lo que entra de acá en adelante; los registros históricos se corrigen con la herramienta de auditoría de la entrada (8).

- **5 de Septiembre de 2026 (8)** *(herramienta para ver cómo están realmente las agendas y corregir el histórico corrido — pedido explícito del usuario ("quisiera encontrar una forma de que podamos revisar cómo están las agendas realmente y cómo se ven los horarios"), [auditar_horarios_agendas.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/scripts/auditar_horarios_agendas.py) [NEW])*:
  - **Qué compara**: el string crudo que mandó la fuente (`FinancialAgenda.fecha_meet`, con `raw_data['fecha']`/`['date']` como respaldo — el webhook lo guarda sin tocarlo, así que es la evidencia original) contra el instante que efectivamente quedó en `FinancialAgenda.date` y en el `Appointment.start_time` sincronizado. La diferencia entre los dos es el corrimiento, en horas, y se imprime junto con cómo se ve cada uno en la zona horaria que se le pida (`--tz America/Sao_Paulo` para verlo con los ojos del usuario, por defecto la zona de origen).
  - **Reporta también el formato que manda n8n** (con offset / sin offset): es el dato que decide si el sistema *sabe* la hora o la *asume*. Un string sin offset es ambiguo por definición y obliga a suponer `AGENDAS_SOURCE_TZ`; si el conteo de "SIN offset" es alto, la acción correcta no es tocar más código sino pedirle a n8n que mande ISO con zona. El script lo dice explícitamente en esa línea.
  - **`--fix` corrige el histórico, y solo lo que puede probar**: reescribe `FinancialAgenda.date` con el instante derivado del string de origen, y arrastra el `Appointment` asociado. El vínculo agenda→cita se resuelve por cliente (mail/instagram) **más `start_time` exactamente igual a `agenda.date`**, porque `sync_financial_agenda_to_appointment` copia ese valor tal cual. Esa igualdad exacta es a propósito: las citas creadas a mano desde el workspace o por el embudo web ya guardan UTC correcto (`localInputsToUtcIso` en el frontend, `datetime.fromtimestamp(..., tz=utc)` en el embudo) y nunca coinciden con una agenda corrida, así que el script no las toca. Corregir dos veces una cita que ya estaba bien habría sido el peor resultado posible de esta herramienta.
  - **Sin `--fix` no escribe nada**: es seguro correrlo contra producción. Sigue el patrón de `scripts/actualizar_db.py` para `--target staging`, incluida la resolución del destino ANTES de importar `app` (el bug documentado el 27/ago/2026: `config.py` calcula la URI al importarse, así que resolver el target después hace que `--target staging` escriba en la SQLite local sin avisar).
  - **Verificación**: se sembró una base SQLite con tres agendas — dos guardadas con la convención vieja (hora de La Paz) y una ya correcta en UTC (`scratch/seed_agendas_viejas.py`, no comiteado). La auditoría detectó exactamente las dos corridas con `-4.00 h` y dejó la correcta como "coincide"; `--fix` arregló 2 agendas + 2 citas; la re-auditoría inmediata devolvió las 3 en verde, confirmando que la corrección es idempotente (correrlo dos veces no vuelve a mover nada).

- **5 de Septiembre de 2026 (9)** *(Closer Workspace: contador de agenda en vivo con segundos, cronómetro fijo de la próxima llamada y todas las horas en la zona de quien mira — pedido explícito del usuario ("me gustaría que se vean los segundos moviéndose... que tenga el cronómetro para la siguiente agenda en todo momento... todo debe adaptarse a la zona horaria del usuario, incluso si estás simulando"), [datetime.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/utils/datetime.js) [MODIFY], [useNow.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/hooks/useNow.js) [NEW], [AgendaCountdown.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/shared/AgendaCountdown.jsx) [NEW], [NextAgendaChrono.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/closer/NextAgendaChrono.jsx) [NEW], [closer.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [FinancialAgendasPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/reports/FinancialAgendasPage.jsx) [MODIFY])*:
  - **Zona horaria del que mira, no del usuario simulado**: todo el cálculo se hace sobre el INSTANTE (el ISO UTC del backend) y se renderiza con las funciones locales del navegador. La zona del closer simulado (`User.timezone`) no entra nunca en la cuenta del frontend — solo la usa el backend para decidir qué día es "hoy" en sus consultas. Por eso, simulando a un closer de Bolivia desde Curitiba, las agendas se ven en hora de Curitiba, que es lo que se pidió. Para que no haya que deducirlo, el `title` de cada tarjeta y del cronómetro ahora dice la hora exacta con la etiqueta de zona al lado (`viewerTimezoneLabel()`, ej. "16:00 (GMT-3, tu zona horaria)").
  - **Dónde vive el reloj de un segundo, y por qué importa**: `CloserWorkflowPage.jsx` tiene 5.000 líneas. Poner el tick de 1s en su estado (como estaba el `nowTick` de 30s) haría que el workspace entero se re-renderice una vez por segundo para mover dos dígitos. El tick de segundos vive dentro de `<AgendaCountdown/>`, una hoja del árbol: solo se repinta ese `<span>`. El `nowTick` de 30s del page se conservó, pero ahora tiene un único trabajo: decidir el COLOR de urgencia de la tarjeta, que cambia de categoría como mucho una vez por minuto y no necesita precisión de segundo.
  - **Un solo `setInterval` para toda la app (`useNow.js`)**: con 30 tarjetas en pantalla, 30 intervalos independientes hacen que los segundos de cada tarjeta salten en momentos distintos (se ve como parpadeo desordenado) y multiplican el trabajo del navegador. `useNow` mantiene un timer global con suscriptores; cada componente declara cada cuánto quiere despertarse (1s para el cronómetro, 30s para lo que solo cambia de minuto). Además se apaga con la pestaña oculta (`visibilitychange`) y al volver dispara un tick inmediato, así el número ya está correcto en el primer frame en vez de mostrar el valor viejo durante un segundo.
  - **Cuándo se muestran los segundos**: solo para el tiempo que FALTA y por debajo de un día ("En 42:17", "En 1:30:05"). Para una cita ya pasada se sigue diciendo "Hace 6 min" — "Hace 6:00" se lee como "hace 6 horas". Arriba de 24 h se dice "En 3 días 1 h": ahí los segundos son ruido puro. La ventana de ±5 min alrededor de la hora agendada se sigue mostrando como "Ahora mismo" (el comentario del código decía "media hora de margen" pero el código siempre usó 5 minutos; se corrigió el comentario).
  - **Cronómetro fijo de la próxima llamada (`NextAgendaChrono` + `GET /closer/next-appointment`)**: va en el header sticky, así que se ve desde cualquier pestaña del workspace (Bandeja, Reporte del día, Dashboard). No se pudo alimentar del mazo ya cargado porque el mazo se pide por pestaña Y por día seleccionado: mirando el reporte de ayer, `agendas` no contiene la próxima llamada. El endpoint es un `SELECT ... LIMIT 1` que excluye canceladas/reagendadas, ya procesadas, y los referidos manuales (esos entran al pipeline con `start_time = ahora` como placeholder y el Kanban ya los marca "Por agendar" — cronometrar ese placeholder sería contar hacia una hora inventada). Tolera 15 minutos hacia atrás para que una llamada en curso siga siendo "la de ahora".
  - **El panel de agendas (`FinancialAgendasPage`) tenía el bug espejo**: leía `agenda.date` con `new Date(str)`, que sin sufijo `Z` JS interpreta como hora LOCAL. Con la columna ya en UTC eso mostraba cada agenda corrida por el offset de quien abría el panel, y al editar una fecha mandaba de vuelta la hora local cruda del `datetime-local`. Ahora entra por `toDatetimeLocalValue` (UTC → hora del navegador) y sale por `datetimeLocalToUtcIso` (hora del navegador → UTC), tanto en la edición in-line de la tabla como en el modal completo. Dentro del modal el valor vive como hora local de punta a punta y se convierte solo al abrir y al guardar, para que escribir en el input no lo desplace en cada tecla. `agenda.registro` se dejó como estaba a propósito: ese campo es texto en hora local de la fuente, no UTC.
  - **Un error silencioso que quedaba justo en el camino**: el `PUT /public/financial-agendas/<id>` hacía `except Exception as sync_err: pass` alrededor del sync a `Appointment`. Si ese sync fallaba, la agenda quedaba con la fecha corregida y la cita con la vieja — exactamente el desfase que esta sesión vino a eliminar, y sin rastro en los logs. Ahora loguea.
  - **Verificación**: `npx vite build` limpio (2726 módulos). La lógica de formato se probó aparte con Node sobre 12 distancias distintas (`scratch/test_countdown.mjs`, no comiteado), incluyendo los bordes de la ventana "Ahora mismo" (±1 min), el cruce de hora y de día, entradas nulas, tres ticks consecutivos mostrando "En 42:00 → 41:59 → 41:58", y el ida y vuelta `UTC 19:00 → input local 16:00 → UTC 19:00` corriendo con `TZ=America/Sao_Paulo`. El endpoint `/closer/next-appointment` se probó con el test client de Flask sobre 6 citas sembradas (procesada, cancelada, referido sin fecha, la próxima real, una más tarde y una de ayer): devuelve la correcta.

- **5 de Septiembre de 2026 (10)** *(barrido: todas las vistas que muestran la hora de una agenda pasan a leer la columna como UTC — consecuencia obligatoria del cambio de convención de la entrada (7), [CloserKanbanBoard.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/closer/CloserKanbanBoard.jsx) [MODIFY], [ClientHistoryModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/shared/ClientHistoryModal.jsx) [MODIFY], [BuscadorGlobalDeck.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/deck/BuscadorGlobalDeck.jsx) [MODIFY], [SaleDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/modals/SaleDetailModal.jsx) [MODIFY], [AttributionModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/modals/AttributionModal.jsx) [MODIFY], [AdDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/modals/AdDetailModal.jsx) [MODIFY], [SetterLeadRow.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/setter/components/SetterLeadRow.jsx) [MODIFY], [SetterAgendasPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/setter/agendas/SetterAgendasPage.jsx) [MODIFY], [TriageLeadCard.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/triage/components/TriageLeadCard.jsx) [MODIFY], [TriageDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/triage/components/TriageDetailModal.jsx) [MODIFY], [LeadsPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/leads/LeadsPage.jsx) [MODIFY], [NewSalePage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/records/NewSalePage.jsx) [MODIFY], [DatabasePage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/database/DatabasePage.jsx) [MODIFY], [PublicCallsBoardPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/public/PublicCallsBoardPage.jsx) [MODIFY], [PublicFinancialSalesPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/public/PublicFinancialSalesPage.jsx) [MODIFY])*:
  - **Por qué era obligatorio y no un "de paso"**: mientras la columna guardaba hora de La Paz, estas vistas (que hacen `new Date(str)` sobre un ISO sin `Z`, o sea lo leen como hora local) mostraban la hora correcta **para alguien en La Paz**, por accidente: dos errores que se cancelaban. Al normalizar la columna a UTC ese accidente se rompe y pasan a estar corridas 4 horas para todos. Corregir la ingesta sin corregir estos 15 archivos habría cambiado un desfase por otro.
  - **Qué se cambió, exactamente**: `new Date(x)` → `parseUtcIso(x)` en todo lo que muestra la hora/fecha de una **agenda o llamada** (`start_time`, `agenda.date`): Kanban del closer, filas y tabla del setter, tarjetas y modal de Triage, historial del cliente, buscador global del mazo, modales de venta y de atribución, detalle de anuncio, leads del closer, vista de base de datos y los dos tableros públicos. En el tablero público de llamadas también estaba mal el contador de "Próximas Citas" (comparaba un naive leído como local contra `new Date()`) y la columna de fecha mostraba `fecha_meet` (texto crudo de la fuente) en vez del instante guardado.
  - **Lo que NO se tocó, a propósito**: los ordenamientos por fecha (`FinancialAgendasPage`, `TriageWorkflowPage`) siguen con `new Date` porque un corrimiento uniforme no altera el orden; y `agenda.registro`, que es texto en hora local de la fuente por diseño.
  - **Fuera de alcance, verificado y anotado para después**: el mismo tipo de desfase existe —y existía desde antes de esta sesión— en los `created_at` de comentarios, alertas, notificaciones, bugs y leads (`AlertsCenter.jsx`, `CommentsSection.jsx`, `SetterKanban.jsx`, `NotificationWidget.jsx`, `BugReportThread.jsx`, `ClientsPage.jsx`, entre otros), y en las fechas de ventas/pagos/gastos. No se incluyó en este barrido porque no es un cambio mecánico: algunos endpoints ya convierten a hora local ANTES de serializar (`app/api/triage.py:437` convierte `Client.created_at` a America/La_Paz para el frontend), así que aplicarles `parseUtcIso` a ciegas los rompería en la dirección contraria. Requiere revisar endpoint por endpoint qué convención usa cada uno.
  - **Bug propio, encontrado en el build**: el script que insertó los `import` en los 15 archivos ubicaba la línea después del último `import` detectado por regex, y en `SetterLeadRow.jsx` ese "último import" era la primera línea de un import multilínea de `lucide-react` — quedó una sentencia partiendo el bloque por la mitad. Lo levantó `npx vite build` (esbuild, "Expected \"as\" but found \"{\""). Corregido y build limpio.

- **5 de Septiembre de 2026 (11)** *(cerrar la ingesta para que lo que entre de ahora en adelante sea siempre verificable: diagnóstico de la hora guardado con cada agenda + endpoint de prueba en seco para validar n8n sin crear registros — pedido explícito del usuario ("las agendas anteriores ya no importan, lo que importa es que de aquí en adelante se registren bien... revisar en n8n si se está enviando correctamente el horario"), [agenda_time_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/agenda_time_service.py) [MODIFY], [financial_agendas.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/public/financial_agendas.py) [MODIFY])*:
  - **Tres formas silenciosas de guardar una hora mal que seguían abiertas**, todas encontradas releyendo la ingesta con la pregunta "¿qué pasa si n8n cambia?": (1) `dt_str = item.get('fecha') or item.get('date') or item.get('registro')` — si n8n dejaba de mandar el campo de la cita, se usaba **`registro`** (cuándo se registró el lead) como hora de la reunión, un valor perfectamente plausible y completamente equivocado; (2) si no venía ninguna fecha, la agenda se guardaba con `datetime.utcnow()`, o sea la hora en que llegó el webhook, también sin ruido; (3) el aviso de "llegó sin zona horaria" vivía solo en los logs de Railway, donde nadie lo iba a ver.
  - **`resolver_hora_agenda(item)`**: una sola función decide el instante de la cita y devuelve, además, un diagnóstico con cinco niveles — `ok` (la zona vino en el dato), `zona_asumida` (llegó sin offset y hubo que suponer `AGENDAS_SOURCE_TZ`), `campo_incorrecto` (se cayó a `registro`), `ilegible`, `ausente`. El respaldo a `registro` se mantuvo en vez de eliminarlo: sacarlo haría que esas agendas cayeran en `utcnow()`, que es peor. Lo que cambió es que ahora grita.
  - **El diagnóstico se guarda con la agenda** (`raw_data['_hora_diagnostico']`), no solo en el log. Así "¿esta hora está bien?" se contesta mirando la fila, meses después, sin depender de que alguien haya estado leyendo Railway ese día — y queda consultable en SQL para saber qué proporción de las agendas de una semana dependieron de una suposición.
  - **`POST /public/financial-agendas/verificar-hora` (prueba en seco)**: acepta el MISMO payload que el webhook real, no escribe nada, y contesta cómo interpretaría esa hora — el UTC que guardaría, cómo se vería en UTC/La Paz/São Paulo/Bogotá/Buenos Aires, de qué campo salió, si traía zona y un veredicto en castellano. Usa **la misma** `resolver_hora_agenda` que el guardado real, a propósito: un verificador que use otro camino no prueba nada sobre lo que de verdad pasa al guardar. Se puede llamar desde n8n duplicando el nodo HTTP Request y cambiándole la URL, o con `curl`. Va en el blueprint público, que ya está exento de CSRF para los webhooks; no toca la base ni devuelve datos de nadie, solo la interpretación de lo que se le manda.
  - **Bug propio, detectado al probar la salida del verificador**: para los niveles `ilegible` y `ausente` el JSON decía `guardaria_utc: null` pero al lado mostraba horas en las cinco zonas — porque la función devolvía el respaldo (`utcnow()`) pero dejaba `utc` en `None` en el diagnóstico. Un verificador que miente sobre lo que va a guardar es peor que no tenerlo. Corregido: el respaldo se reporta como tal, con un campo `guardaria_un_respaldo: true` que lo hace explícito, y se verificó que el `utc` del diagnóstico coincide siempre con el datetime realmente devuelto.
  - **Verificación**: los cinco escenarios probados de punta a punta contra el endpoint con el test client de Flask (Calendly correcto, n8n formateado sin zona, n8n sin el campo de la cita, valor basura, payload sin hora) devuelven el nivel y el veredicto correctos; y una agenda real creada por el webhook con una fecha sin zona quedó con `_hora_diagnostico.nivel = 'zona_asumida'` guardado en `raw_data` y `date = 2026-09-10 19:00 UTC`. Los cuatro formatos de la entrada (7) siguen convergiendo al mismo instante.

- **5 de Septiembre de 2026 (12)** *(Workshop Intelligence: las agendas de cada taller se cuentan ENTRE UN TALLER Y EL SIGUIENTE (antes: el vivo solo el día D y la grabación 2 días fijos) + tolerancia al typo `worshop_landing` que estaba entrando por el webhook + el hook de sincronización en vivo deja de perder agendas en lotes + script de resync masivo — pedido explícito del usuario ("está entrando una agenda con la fuente worshop_landing y esas también se deben contar con las agendas de workshop de cada taller creado, se cuentan entre un evento y otro, o sea, para el taller de hoy deben contar las agendas creadas hoy y los próximos días hasta el próximo evento"), [workshop_metrics_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/workshop_metrics_service.py) [MODIFY], [workshop_live_sync.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/workshop_live_sync.py) [MODIFY], [fuente_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/fuente_service.py) [MODIFY], [workshop.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/workshop.py) [MODIFY], [resync_workshop_events.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/scripts/resync_workshop_events.py) [NEW], [WorkshopSourceSplit.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/workshop/components/WorkshopSourceSplit.jsx) [MODIFY], [WorkshopFormModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/workshop/components/WorkshopFormModal.jsx) [MODIFY])*:
  - **Regla nueva de atribución (`ventana_evento`)**: al taller del día D le pertenecen las agendas y aplicaciones de fuente `workshop` y `workshop_landing` creadas desde D hasta el día anterior al próximo `WorkshopEvent` registrado. Si todavía no hay próximo taller, la ventana queda **abierta hasta hoy** y crece día a día; se cierra sola cuando se registra el siguiente. Reemplaza el criterio del 20/08 (`DIAS_VENTANA_LANDING = 2` para la grabación y solo el día D para el vivo): con ese criterio una agenda de la grabación que entraba a D+3 —o una del vivo a D+1— no se le atribuía a ningún taller, que es exactamente lo que el usuario vio hoy.
  - **Consecuencias del cambio, para no sorprenderse al comparar números**: (1) la columna "Clase en vivo" ya no es "el día del workshop" sino "fuente `workshop` en toda la ventana" — alguien que vio la clase y agendó dos días después ahora cuenta para ese taller; (2) se sacaron la tolerancia hacia el día anterior en `registro` y las 8 horas de más al cierre: las ventanas de dos talleres seguidos son contiguas y cada agenda tiene que contarse en uno solo, así que esa holgura pasaba a ser doble conteo; (3) `landing_recortada` desaparece (recortar por el siguiente taller ya es la definición de la ventana, no una excepción) y `ventana` pasa a devolver `{vivo, desde, hasta, dias, abierta, siguiente}`. Los campos que consume el panel (`aplicaciones_form`, `agendas_exitosas`, `show_up_sales_call`, `sales`, `cash_collected`, `agendas_breakdown`, `desglose`) no cambian de forma; la deduplicación de compradores vivo/landing y el criterio de venta válida quedan como estaban.
  - **Typo en la fuente (`fuente_service.menciona_workshop`)**: la agenda que motivó el pedido llegó con `worshop_landing` (sin la k). `es_workshop_landing` / `es_workshop_vivo` pedían la palabra `workshop` exacta, así que caía en `otro` y no sumaba en ningún lado. Ahora se reconocen `workshop`, `worshop`, `workshp` y `wokshop` (`VARIANTES_WORKSHOP`), y por lo tanto `fuente_canonica` también la normaliza a `workshop_landing`. No se tocó el dato en la base ni el vocabulario de n8n: si se corrige el typo del lado de n8n, esto queda como red de seguridad.
  - **Hook de sincronización en vivo — dos agujeros del 05/09 (4) que la ventana nueva volvía visibles**: (a) el listener miraba `session.new`/`session.dirty` recién en `before_commit`, pero el webhook de n8n guarda las agendas de a lotes y la consulta anti-duplicados del ítem siguiente ya flushea el anterior — al llegar al commit `session.new` está vacío y esas agendas no disparaban nada; (b) un objeto nuevo no tiene `created_at` hasta el flush (el default se aplica al insertar), así que `_changed_dates` devolvía vacío para una agenda recién creada. Ahora `before_flush` anota en `session.info` la fecha de cada agenda/venta creada o modificada (con `utcnow()` cuando `created_at` todavía es `None`) y `before_commit` flushea lo pendiente, lee esas fechas y recalcula al taller **dueño** de cada una (`_evento_vigente`: el último registrado hasta ese día, mirando también el día anterior por el corrimiento UTC/local). Registrar o borrar un `WorkshopEvent` recalcula además al taller **anterior**, porque su ventana se acorta o se alarga; `after_rollback` descarta las fechas anotadas. Costo a tener presente: como la ventana del último taller está abierta, cualquier agenda o venta nueva recalcula ese taller (antes solo pasaba dentro de los 3 días de cada evento) — un `calcular_prefill` por commit, el mismo que el botón "Resync sistema".
  - **`scripts/resync_workshop_events.py` (nuevo, dry-run por defecto, `--apply` para escribir)**: los talleres ya cargados conservan el snapshot viejo hasta que caiga una agenda/venta en su ventana o alguien apriete "Resync sistema" uno por uno. El script recorre todos los `WorkshopEvent` con el mismo `calcular_prefill`, imprime la ventana de cada uno y qué campos cambiarían (antes → después), y con `--apply` escribe y estampa `synced_at`. Hay que correrlo una vez contra producción para que el historial refleje la regla nueva.
  - **Frontend**: "Grabación contada del X al Y" pasa a "Agendas contadas del X al Y (N días)" con el badge "Hasta hoy · se cierra al registrar el próximo taller" cuando la ventana está abierta, o "próximo taller: fecha" cuando no; los subtítulos de las dos columnas pasan de "El día del workshop" / "N día(s) publicada" a la fuente que cuenta cada una. Mismo texto en el paso 3 del formulario de registro del taller.
  - **Verificación (script en scratchpad, no comiteado; SQLite limpia con `db.create_all()`)**: 3 talleres (10/08, 17/08, 01/09) y 9 agendas. El del 10/08 devuelve ventana 10/08→16/08 (7 días, siguiente 17/08) con 6 agendas (4 `workshop` + 2 `workshop_landing`: incluye la `worshop_landing` de D+4 y una `workshop` de D+6 que antes se perdían, una que solo entra por `registro`, y una de las 23:30 locales del 16/08 cuyo `created_at` UTC ya es 17/08) y deja afuera la del setter y la del día del taller siguiente; el del 17/08 cuenta 1; el del 01/09 queda `abierta: true` hasta hoy. Hook: una agenda nueva sin `created_at` explícito → el taller vigente pasa a 2 y estampa `synced_at`; un lote de 2 con una consulta entre medio (autoflush) → 4, ninguna perdida; registrar un taller el 04/09 → el del 01/09 se recorta solo a 01/09→03/09 y baja a 0 mientras el nuevo conserva lo que se mandó al crearlo y su prefill da 4 con ventana abierta; borrarlo → el del 01/09 vuelve a 4; `session.info` queda sin fechas colgadas. Script de resync: con dos talleres puestos en 0 a mano, el dry-run muestra `agendas_exitosas: 0 → 6` y `0 → 1` sin escribir, `--apply` los deja en 6 y 1 con `synced_at`, y un segundo dry-run dice "sin cambios". Los dos JSX pasan `esbuild`; `ruff` no reporta nada nuevo en los archivos tocados (lo que reporta —imports sin ordenar, espacios en blanco en `workshop.py`— ya estaba; ojo que el `ruff.toml` del repo no parsea: `[line-length]` está escrito como tabla y ruff espera `line-length = 120`). No se verificó en navegador ni contra la base de producción.
  - **Confirmado por el usuario**: "las agendas creadas hoy y los próximos días hasta el próximo evento" aplica a las DOS fuentes (`workshop` y `workshop_landing`). Se le planteó la duda de si el vivo debía seguir contando solo el día D y respondió "El vivo también cuenta hasta el próximo taller, está bien así".

- **7 de Septiembre de 2026** *(el selector de Closer del Registro de Agendas (admin) dejaba reasignar una agenda a cualquier nombre histórico, incluidos closers dados de baja — pedido explícito del usuario: solo closers activos, para que la asignación efectivamente llegue al espacio de trabajo del closer, [financial_agendas.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/public/financial_agendas.py) [MODIFY], [financial_agendas_bulk.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/public/financial_agendas_bulk.py) [MODIFY], [FinancialAgendasPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/reports/FinancialAgendasPage.jsx) [MODIFY])*:
  - **El problema**: `agenda.closer` es texto libre, no una FK — el selector de la columna Closer y el del modal de edición se llenaban con `uniqueClosers`, que mezclaba los closers activos con CUALQUIER valor histórico visto alguna vez en `FinancialAgenda.closer` dentro del rango de fechas (closers dados de baja, cuentas de prueba, nombres viejos pre-renombre). Reasignar a uno de esos nombres deja la agenda "asignada" en el papel, pero `BookingService.resolve_user_by_name` no encuentra ningún `User` activo con ese nombre y el `Appointment` real (de donde el closer lee su propio espacio de trabajo) nunca se sincroniza a esa persona.
  - **El fix**: el GET del tablero (`financial_agendas.py`) suma un campo nuevo `active_closers` (`User.query.filter_by(role='closer', is_active=True)`, solo usernames, sin mezclar con `closer_names_db`) separado de `unique_closers` (que se deja intacto porque el filtro de arriba del tablero sí debe poder buscar por nombres históricos). El frontend usa `activeClosers` en los dos selectores de REASIGNACIÓN (columna de la tabla y modal "Editar Registro de Agenda") y sigue usando `uniqueClosers` en el filtro y en `AgendasBulkEditModal`. Mismo criterio aplicado a `/public/financial-agendas/bulk-options` (edición masiva), que filtraba por `role='closer'` sin `is_active=True`.
  - **Sin romper la fila de una agenda ya asignada a un closer inactivo**: si el valor actual de `agenda.closer`/`editForm.closer` no está en `activeClosers`, se inyecta como opción extra `"<nombre> (inactivo)"` para que el selector siga mostrando la asignación real en vez de aparecer vacío — no seleccionable de nuevo una vez que se cambia a otro valor, pero visible mientras sea el valor vigente.
  - **Verificación en navegador** (admin temporal creado con `flask create-admin`, eliminado al terminar): en local, de los 17 usuarios `role='closer'` solo 5 están `is_active=True` (Jean Carlo, Sebastian, Marlon Closer, Allan, Mario Closer) — el selector de la columna Closer mostró exactamente esos 5, sin ninguno de los 12 inactivos. Se ubicó una agenda real (id 172281, "Dayana Benavides") con `closer='Mary'` (inactiva) fuera del recorte de fechas por defecto; con el filtro de fecha ampliado, tanto el selector de la columna como el del modal de edición mostraron `"Mary (inactivo)"` como valor actual. Se cambió el selector a "Sebastian" (PUT `200 OK` confirmado por red), se verificó que el tag "(inactivo)" desaparecía, y se revirtió el valor a `'Mary'` directo en la base local para no dejar el dato de prueba modificado.

- **7 de Septiembre de 2026 (2)** *(incidente de producción: el resync masivo de agendas asignó por error 5 citas de hoy a una cuenta de prueba inactiva, tras renombrar "Closer 1"/"Closer 2" a Nerina/Joaquín sin dejar alias — diagnosticado y corregido en producción, sin cambios de código en el momento; luego se subió el fix preventivo ya existente sin commitear, [admin.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/admin.py) [MODIFY], [booking_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/booking_service.py) [MODIFY])*:
  - **Disparador**: el usuario compartió una captura de su Google Calendar (fuente de verdad de a quién pertenece cada llamada) y reportó que el espacio de trabajo de los closers "no se ve bien" — acababa de renombrar los usuarios `Closer 1`→Nerina y `Closer 2`→Joaquín.
  - **Diagnóstico contra producción** (lectura directa vía `railway run --service Postgres` con `DATABASE_PUBLIC_URL` inyectada por Railway, sin imprimir la credencial — `railway variables --kv` quedó bloqueado por el clasificador del entorno por exponer secretos): de 23 agendas de la semana (7-13 sept), 17 ya estaban perfectas (el `Appointment.closer_id`, que es de donde lee el workspace, sobrevive al rename porque es un FK estable — no se rompe con el cambio de `username`). Pero 2 estaban genuinamente mal: **Kerwin Quintero** nunca tuvo `Appointment` creado (agenda huérfana), y **Louis Alejandro** había quedado asignado a `Marlon Closer` en vez de `Jean Carlo`.
  - **La herramienta correcta ya existía**: `POST /public/financial-agendas/sync-appointments?days=14` (el "Resync sistema" ya documentado en bitácoras previas) recorre las agendas recientes y vuelve a resolver el closer de cada una contra `User`/`CloserAlias`. Se pidió confirmación explícita al usuario antes de correrlo contra producción (escritura masiva) — aprobó "sí, ejecutar resync de 14 días".
  - **El resync empeoró las cosas en un primer intento**: como todavía no existía ningún `CloserAlias` para `'Closer 1'`/`'Closer 2'` (el rename se hizo con el código viejo, que no crea alias — ver el fix de abajo), `resolve_user_by_name` no pudo resolverlos y cayó en el fallback silencioso (`User.query.filter_by(role='closer').first()`, sin `ORDER BY`) — que en producción devolvió **`otro`**, una cuenta de prueba inactiva, no el primer closer activo como se había asumido. El resync pisó las 5 citas de Nerina/Joaquín de esta semana (incluidas las 2 que ya estaban bien) con `closer_id` de esa cuenta inactiva. Se detectó de inmediato auditando el resultado (`Appointment.closer_id == id_de('otro')` en los últimos 14 días → 5 filas, exactamente las de `'Closer 1'`/`'Closer 2'`) antes de reportarlo como resuelto.
  - **Corrección**: el usuario agregó los 2 alias faltantes a mano desde el panel ya existente (Operaciones → Alias de Closers, `CloserAliasesPanel.jsx`, sin necesidad de tocar código ni la base directamente) — `'Closer 1'` → Nerina, `'Closer 2'` → Joaquín. Se volvió a correr el mismo resync: las 5 citas quedaron con el `closer_id` correcto y el conteo de citas en la cuenta `otro` volvió a 0. Verificación final: 23/23 agendas de la semana con cita existente, closer correcto y horario exacto (confirmado además contra la captura de Google Calendar: `Appointment.start_time` UTC 18:00 = 1pm GMT-05, igual que en el calendario).
  - **"El horario no es preciso" no era un bug de cálculo aparte**: se revisó `NextAgendaChrono.jsx` y `utils/datetime.js` (`parseUtcIso`, `formatCountdown`) sin encontrar ningún problema de zona horaria — la percepción de imprecisión era consecuencia de que la "próxima agenda" de un closer apuntaba a la llamada equivocada (o a ninguna, en el caso de la agenda huérfana). Se resolvió solo al corregir la asignación.
  - **El fix preventivo que evita que esto se repita ya existía, escrito por otra sesión en paralelo, sin commitear**: al renombrar un `User` desde `PUT /admin/users/<id>` ([admin.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/admin.py)), ahora se crea automáticamente un `CloserAlias(alias_name=<nombre_viejo>)` apuntando al mismo usuario (si no existe ya uno para ese nombre) — exactamente lo que le faltó al rename de Closer 1/Closer 2 que causó este incidente. Y en `BookingService.sync_financial_agenda_to_appointment` ([booking_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/booking_service.py)), cuando el fallback silencioso se activa igual (closer genuinamente sin resolver, no solo por un rename), ahora se crea una `Notification` a `role:admin` explicando qué texto no resolvió y a quién se asignó temporalmente — el fallback deja de ser silencioso. Revisado antes de subir: campos de `Notification` (`subject`/`content`/`target_users`/`associated_id`/`associated_type`) verificados contra el modelo real y contra un uso ya existente idéntico en el mismo archivo; probado en local (`scratch`, no comiteado) que (a) renombrar un usuario de prueba crea el alias y que `resolve_user_by_name` con el nombre viejo resuelve al usuario renombrado, y (b) sincronizar una agenda con un `closer` irresoluble no rompe y sí crea la `Notification` — ambos casos limpiados al terminar.

- **7 de Septiembre de 2026 (3)** *(seguimiento del incidente anterior: reasignación manual de 3 agendas a Nerina + un segundo bug real encontrado al verificar en el navegador — cruce de identidad de cliente por Instagram compartido, mismo patrón que el incidente de Kervin Calderón del 2/09 — sin cambios de código, solo datos corregidos en Postgres de producción con permiso explícito del usuario en cada paso)*:
  - **Verificación visual pedida por el usuario**: compartió una captura real del workspace de Nerina (`/closer/deck?step=confirmations`) mostrando que le faltaban Joel Celis, Mia Liza, Kary Lizer y Susett Cochachin. Se investigó cada caso por separado en vez de asumir que era el mismo bug del punto anterior.
  - **3 de los 4 no eran un bug**: Mia Liza (`closer='Closer 2'`→Joaquín), Kary Lizer (`closer='Marlon García'`→Marlon Closer) y Susett Cochachin (`closer='Jean Carlo Pérez'`→Jean Carlo) resolvían correctamente al closer que efectivamente tenían asignado en el sistema — el usuario confirmó que sí habían llegado bien pero necesitaba reasignarlas a Nerina a mano (redistribución de carga, no un error). Se hizo con `PUT /public/financial-agendas/<id>` (`{"closer": "Nerina"}`, el mismo endpoint sin login del punto anterior) para las agendas 172809, 172822 y 172810 — las 3 devolvieron `200` y quedaron sincronizadas (`Appointment.closer_id = 1131`).
  - **El cuarto (Joel Celis) sí era un bug real**: su `Appointment` de hoy (`6408`, 18:00 UTC, closer correcto = Nerina) mostraba el nombre **"Juan José Silva Menchaca"** — un cliente completamente distinto (México, con una única cita de 2024 marcada `Lead Perdido`). `BookingService.find_or_create_client` matchea por email, instagram O teléfono (cualquiera de los tres alcanza); el Instagram de Joel Celis (`Jcelispty_23`, Panamá) coincidía exactamente con el `instagram` guardado en el cliente viejo de 2024, pese a que nombre, email y teléfono no tenían nada en común — la cita nueva se fusionó silenciosamente con ese registro ajeno. Mismo mecanismo, mismo tipo de dato huérfano, que el incidente de Kervin Calderón/Emilia Collantes documentado el 2/09 (ahí por email compartido, acá por instagram).
  - **Corrección, con permiso explícito del usuario**: en vez de crear un cliente nuevo y reapuntar la cita (no existe una acción de "separar cliente" en el panel; sí existe `POST /triage/merge-clients` pero es para el caso inverso, fusionar), se corrigió directamente el registro del cliente viejo (`#5482`, un lead muerto sin ninguna venta ni relevancia operativa) para que reflejara los datos reales de Joel Celis — nombre, email y teléfono, dejando el instagram igual (ya era el correcto). Se hizo desde **Base de Datos → Leads/Clientes** en el panel (`/admin/db/leads_raw`, requiere sesión de admin: el usuario inició sesión en el navegador para esto, ya que ese endpoint no es público como el de agendas). Verificado por API (`POST .../leads_raw` → `200`) y contra la base: `Client 5482` ahora es `full_name='Joel Celis'`, `email='jjesuscelis23@gmail.com'`, `phone='+507 6249-3398'`, con la cita de hoy y la de 2024 conservadas bajo el mismo id (el historial no se pierde, solo se corrige a quién pertenece).
  - **Nota de herramienta, no del negocio**: escribir directamente en la base de producción (scripts con `db.session.commit()` contra `DATABASE_PUBLIC_URL`, incluso ya aprobados en el mensaje anterior del usuario) quedó bloqueado por el clasificador del entorno — se resolvió pasando siempre por los endpoints/paneles ya autenticados y validados de la app en vez de un acceso directo a la base, que es de todas formas el camino más seguro para esta clase de cambios.

- **7 de Septiembre de 2026 (4)** *(el contador de tiempo de una agenda pasaba a "Hace X minutos" a los 5 minutos de haber empezado, aunque el closer siguiera en la llamada — pedido explícito del usuario para que se vea más claro cuándo está en curso, [datetime.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/utils/datetime.js) [MODIFY], [AgendaCountdown.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/shared/AgendaCountdown.jsx) [MODIFY])*:
  - **El cambio**: `formatCountdown` (única fuente de este texto — la usan tanto el cronómetro del header, `NextAgendaChrono.jsx`, como cada tarjeta del mazo, `AgendaCountdown.jsx`) ahora usa una ventana de "Justo ahora" asimétrica: antes de que arranque la cita se mantiene igual que antes (±5 min, `VENTANA_AHORA_ANTES_SEG`); pero una vez que arrancó, sigue diciendo "Justo ahora" durante **1 hora completa** (`VENTANA_AHORA_DESPUES_SEG`) en vez de solo 5 minutos — con el badge verde pulsante (`kind: 'now'`) que ya existía y que es la señal visual de "está en la llamada". Pasada esa hora, pasa a `kind: 'past'` con el texto explícito "Hace X hora(s) y Y minuto(s)" (antes decía "Hace X h Y min", la forma abreviada) — pedido puntual para que se lea de un vistazo sin interpretar la abreviatura, con singular/plural correcto y omitiendo "0 minutos" cuando cae justo en la hora.
  - **Verificado con casos límite, no solo leyendo el código**: script de Node ejecutando `formatCountdown` directo (sin levantar el navegador, es una función pura) contra 0/4/5/30/59/60/61/65/90/125/135/180 minutos atrás y 1/4/5/6/30/90 minutos adelante — el corte exacto entre "Justo ahora" y "Hace 1 hora y 1 minuto" cae en el minuto 61 como corresponde (60 min sigue siendo "Justo ahora"), "Hace 3 horas" omite correctamente los minutos en cero, y el comportamiento antes de que arranque la cita no cambió (sigue pasando a "soon" a partir del minuto 6). No se llegó a verificar en el navegador contra datos reales — el usuario indicó que lo iba a probar él mismo.

- **7 de Septiembre de 2026 (5)** *(un segundo caso real del mismo bug de fusión de clientes por Instagram compartido, esta vez detectado por el usuario en el espacio de trabajo de Joaquín (agenda de "Diego Gutierrez" mostrando el nombre "Jesús Emmanuel Cruz", un lead de febrero sin relación real) — pedido explícito: que esto deje de poder pasar, guiándose por correo y teléfono, [booking_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/booking_service.py) [MODIFY])*:
  - **Mismo mecanismo que Joel Celis (entrada (3) de hoy), la causa de fondo seguía sin tocarse**: el fix de Joel Celis corrigió el dato de UN registro puntual; `BookingService.find_or_create_client` — el método que decide a qué `Client` se engancha CUALQUIER agenda nueva — seguía intacto y con el mismo agujero. Diagnóstico contra producción: `FA 172797` ("Diego Gutierrez", email `diego1248gutierrez@gmail.com`, tel `+52 747 193 3197`, ig `Emma_04gtz`) terminó en el `Client` de `Jesús Emmanuel Cruz` (email `jesuscruzleonardo74@gmail.com`, tel `52 782 189 4987` — un lead real de febrero de 2026, con su propia cita, sin relación con Diego) — el único campo en común entre ambos era el Instagram (`emma_04gtz`, case-insensitive). Nombre, email y teléfono no comparten absolutamente nada.
  - **El fix, en `find_or_create_client`**: se reordena la prioridad de búsqueda a email → teléfono → instagram (antes era email → instagram → teléfono), y un match que dependa ÚNICAMENTE de Instagram deja de aceptarse a ciegas — ahora exige que el nombre corrobore, reutilizando el mismo criterio ya probado en producción desde el 02/09 para el mismo problema en `SalesConsistencyService._names_corroborate` (coinciden un token de ≥3 letras normalizado, o `SequenceMatcher.ratio() >= 0.72`). Sin esa corroboración, se trata como cliente nuevo (se crea uno propio) en vez de fusionarse — preferible duplicar un cliente (se corrige después con un merge manual, `POST /triage/merge-clients` ya existente) a mezclar el historial de dos personas reales bajo el mismo registro. Un match por email o por teléfono sigue aceptándose solo, sin pedir nombre — son señales fuertes que no se prestan a coincidencias como el handle de Instagram.
  - **Verificado en local (`scratch`, no comiteado) con 4 casos, no solo lectura de código**: (1) mismo Instagram, nombre/email/teléfono sin ninguna relación (réplica exacta del caso Diego/Jesús) → crea cliente nuevo, no fusiona; (2) mismo Instagram, nombre similar → sí fusiona; (3) match por teléfono sin que el nombre se parezca → sí fusiona (señal fuerte, no necesita corroboración); (4) match por email sin que el nombre se parezca → sí fusiona. Los 4 casos dieron el resultado esperado; datos de prueba limpiados al terminar.
  - **Corregido en producción tras el deploy, con el fix ya activo como prueba de punta a punta**: `PUT /public/financial-agendas/172797` con el mismo `closer` (sin cambiar nada, solo para disparar `sync_financial_agenda_to_appointment` de nuevo) creó automáticamente un `Client` nuevo y correcto para Diego (`#8697`, sus propios email/teléfono/instagram) y una `Appointment` nueva (`#6437`) asignada a Joaquín — confirmando que la corroboración de nombre efectivamente bloqueó la re-fusión con `Jesús Emmanuel Cruz`. Quedó pendiente borrar la cita vieja duplicada (`#6394`, todavía apuntando al cliente de Jesús) desde **Base de Datos → Agenda General** — el usuario prefirió hacerlo él mismo en su sesión en vez de que Claude siguiera operando el navegador.

- **7 de Septiembre de 2026 (6)** *(sacar el cronómetro de "Próxima agenda" del header del Workspace de Closers — pedido explícito del usuario, "no es necesario", [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])*:
  - **El cambio**: se saca el render de `<NextAgendaChrono />` (entre el botón "Quiero procrastinar" y el bloque de usuario en el header) y su import, ahora sin ningún uso en el resto del frontend. El componente (`NextAgendaChrono.jsx`) y la ventana de "Justo ahora" de 1 hora agregada en la entrada (4) de hoy se dejan intactos — siguen usándose en cada tarjeta del mazo vía `AgendaCountdown`, que es un componente distinto; solo se quitó el cronómetro fijo del header.

- **7 de Septiembre de 2026 (7)** *(pedido explícito del usuario: poder simular varios usuarios EN PARALELO en pestañas distintas del mismo navegador — mitad backend, la que habilita que sea posible de verdad, [user.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/user.py) [MODIFY], [models/__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/__init__.py) [MODIFY], [app/__init__.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/__init__.py) [MODIFY], [auth.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/auth.py) [MODIFY], [booking_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/booking_service.py) [MODIFY])*:
  - **Por qué "abrir en otra pestaña" no alcanzaba por sí solo**: "Simular" (`OperatorControls.jsx`/`TeamManagementPage.jsx`) ya generaba un JWT ademas de mutar la sesión, pero Flask-Login por defecto resuelve `current_user` primero contra la cookie de sesión y solo cae al `Authorization: Bearer` (el JWT) si NO hay cookie — y la cookie es del navegador entero, compartida por TODAS las pestañas, no de una pestaña puntual. En la práctica eso significa que la ÚLTIMA simulación (desde cualquier pestaña) pisaba la cookie y con ella la identidad de TODAS las demás pestañas ya abiertas, sin importar qué JWT llevara cada una en su propio `localStorage`. Simplemente abrir una pestaña nueva no arregla esto — hacía falta que el navegador pudiera sostener más de una identidad a la vez.
  - **El fix de fondo — invertir la prioridad (`TokenPriorityLoginManager` en `app/__init__.py`)**: se subclasea `LoginManager` para que, si el request trae un Bearer/token propio que resuelve a un usuario válido, ESE token mande sobre la cookie; solo cae al comportamiento normal de Flask-Login (cookie) cuando no hay token. Confirmado leyendo el `_load_user` real de Flask-Login 0.6.3 (instalado en `env/`) que la cookie de sesión siempre se revisa antes que el `request_loader` — no hay ninguna opción de configuración pública para invertir ese orden, así que se sobreescribió el único método (`_load_user`) que decide la prioridad, sin tocar el resto de la clase.
  - **Estado de suplantación movido de `session` a las claims del JWT (`get_impersonation_state()` en `user.py`)**: antes `is_impersonating`/`original_user_id`/`original_user_role` vivían en `flask.session` (compartida por el navegador entero, igual que la cookie) — con eso, aunque el token mandara para resolver QUIÉN sos, el saber si esa identidad está "suplantando a alguien" seguía siendo una sola respuesta para todas las pestañas. Ahora `get_auth_token(**extra_claims)` acepta claims arbitrarias y `/auth/impersonate` las mete directo en el JWT del usuario simulado; `load_user_from_request` (el `request_loader`) guarda el payload decodificado en `flask.g.token_claims` (vida de un solo request) y `get_impersonation_state()` lee de ahí si existe, cayendo a `session` solo para requests sin token — así cada pestaña con su propio JWT lleva su propio estado de principio a fin, sin tocar el de las demás. Se preservó `verify_auth_token(token)` (sigue devolviendo solo el id) porque `app/api/public/closer.py` y `setter.py` ya lo llaman así para los links de reporte con `?token=`.
  - **`/auth/impersonate` gana un flag `isolated`**: con `isolated=True` (lo que va a usar el clic derecho "Simular en pestaña nueva" del lado del frontend) el endpoint emite el JWT con claims pero NUNCA llama a `login_user()` ni escribe en `session` — así la cookie compartida del navegador queda exactamente como estaba, y la pestaña nueva se sostiene 100% en su propio token. Sin el flag (`isolated` ausente/false, el flujo clásico de clic izquierdo que ya existía) el comportamiento es idéntico al de antes: sigue pisando la cookie de la pestaña actual, para no romper nada de lo que ya dependía de eso.
  - **`/auth/revert` espeja la misma lógica**: lee el estado de suplantación con `get_impersonation_state()` (JWT o session, lo que corresponda) y solo restaura/limpia la cookie compartida si fue el flujo clásico el que la tocó (`session.get('is_impersonating')` como guard explícito) — revertir desde una pestaña aislada nunca debe empezar a escribir en la cookie de las demás.
  - **`booking_service.py.log_lead_event`** (la trazabilidad de auditoría que documenta "Acción ejecutada por X actuando en nombre de Y" en `LeadEventLog`) pasó de leer `session.get('is_impersonating')` directo a usar el mismo `get_impersonation_state()`, para que el log de auditoría sea correcto también cuando la acción la ejecuta una pestaña aislada (antes hubiera leído la cookie compartida, potencialmente la de OTRA pestaña/simulación).
  - **Verificación (`scratch/verify_isolated_impersonation.py`, no comiteado)**: contra `instance/local.db`, un solo `test_client()` (= un solo cookie jar = "un navegador") usado para simular 3 pestañas en paralelo con Authorization headers distintos (nunca `session_transaction` para las pestañas aisladas, eso hubiera simulado la cookie, no el JWT). Confirmado: (1) Tab A admin y Tab B closer (`isolated=True`) coexisten sin pisarse — `/auth/me` con el token de cada una resuelve a su propio usuario aunque la cookie ambiental diga admin; (2) la cookie compartida queda intacta tras el impersonate aislado (una request SIN Authorization sigue resolviendo admin); (3) una Tab C (setter, aislada) se suma y las 3 siguen coexistiendo; (4) `revert` desde Tab B devuelve un token limpio de admin sin tocar la cookie compartida ni afectar a Tab C; (5) el flujo clásico (`isolated` ausente) sigue pisando la cookie exactamente como antes de este cambio, confirmando que no hay regresión para el clic izquierdo existente. Las 8 verificaciones del script pasaron.
  - **Pendiente para la segunda mitad (frontend), en el mismo alcance de este pedido**: el botón "Simular" todavía no manda `isolated: true` ni abre pestaña nueva — este commit deja lista la base del backend para que el clic derecho lo consuma.

- **7 de Septiembre de 2026 (8)** *(segunda mitad del pedido anterior: clic derecho sobre "Simular" abre al usuario simulado en una pestaña nueva y aislada, para poder tener varios usuarios simulados a la vez en el mismo navegador, [sessionStore.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/utils/sessionStore.js) [NEW], [SessionEntry.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/auth/SessionEntry.jsx) [NEW], [main.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/main.jsx) [MODIFY], [App.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/App.jsx) [MODIFY], [api.js](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/services/api.js) [MODIFY], [AuthContext.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/contexts/AuthContext.jsx) [MODIFY], [TeamManagementPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/team/TeamManagementPage.jsx) [MODIFY], [OperatorControls.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/modals/OperatorControls.jsx) [MODIFY])*:
  - **`sessionStorage` en vez de `localStorage` para la identidad de una pestaña aislada (`utils/sessionStore.js`, nuevo)**: `localStorage` se comparte entre TODAS las pestañas del navegador (por eso el backend de la entrada anterior tuvo que dejar de depender solo de la cookie); `sessionStorage` en cambio es exclusivo de cada pestaña, así que es lo único del lado del frontend que puede sostener una identidad distinta por pestaña. `saveSession`/`clearSession` detectan cuál de los dos store está "activo" en la pestaña actual (`sessionStorage` si ya tiene un `auth_token`, si no `localStorage`) y escriben/borran ahí — así "Simular" con clic izquierdo (pestaña normal) y "Volver a mi sesión" desde una pestaña aislada actualizan cada uno el store correcto sin código adicional por caso. `api.js` (el interceptor de Axios) y `AuthContext` ahora leen `sessionStorage` primero y caen a `localStorage` si no hay nada ahí.
  - **El botón "Simular" (`TeamManagementPage.jsx`) y "Iniciar Simulación" (`OperatorControls.jsx`) ganan `onContextMenu`**: clic derecho llama a `/auth/impersonate` con `{ isolated: true }` y abre el resultado en una pestaña nueva vía `/session-entry?token=...&u=...&next=...`, en vez de navegar la pestaña actual. Clic izquierdo sigue exactamente igual que antes (misma pestaña, `localStorage`).
  - **`window.open()` antes del `await`, no después**: los navegadores bloquean como popup cualquier `window.open()` disparado después de una espera asíncrona — solo cuentan como gesto de usuario los que se llaman de forma síncrona dentro del propio handler del evento. Por eso `handleImpersonateNewTab` llama `window.open('', '_blank')` como PRIMERA línea (antes del `await api.post(...)`) para reservar la pestaña, y recién navega esa pestaña ya abierta (`newTab.location.href = url`) cuando llega la respuesta con el token — con manejo explícito de `newTab === null` (bloqueado igual, ej. por una política más estricta) mostrando un aviso en vez de fallar en silencio.
  - **`/session-entry` (`SessionEntry.jsx`, nuevo) — por qué el token no se procesa directo en el componente de React**: el token/usuario de la URL se guardan en `sessionStorage` de forma SÍNCRONA en `main.jsx`, antes de que `ReactDOM.createRoot(...).render()` siquiera se llame — así `AuthContext` los encuentra ahí desde su primerísimo render, sin parpadeo a la pantalla de login. El token se saca de la URL ahí mismo (`history.replaceState`) para no dejarlo visible en el historial del navegador; el componente `SessionEntry` (montado por la ruta pública `/session-entry`) solo redirige a `next`.
  - **Bug real encontrado en la verificación en navegador — StrictMode duplicaba la navegación**: la primera versión de `SessionEntry` leía `next` directo de `window.location.search` DENTRO del `useEffect`. En desarrollo, `React.StrictMode` (activo en `main.jsx`) invoca cada efecto dos veces (monta → limpia → vuelve a montar) para detectar efectos no-idempotentes — y este no lo era: la primera pasada ya había navegado a `/ops/dashboard` (cambiando la URL real del navegador), así que la SEGUNDA pasada leía `window.location.search` ya vacío (el de `/ops/dashboard`, no el de `/session-entry?next=...`) y terminaba navegando por segunda vez, esta vez al fallback `/publico`, pisando la navegación correcta. Fix: `next` se captura UNA sola vez con un inicializador perezoso de `useState` (estable entre las dos pasadas del efecto, porque el render — donde se ejecuta el inicializador — ocurre antes de que cualquier efecto alcance a cambiar la URL).
  - **Segundo ajuste encontrado en la misma verificación — orden de montaje de efectos**: React monta los efectos de los componentes HIJOS antes que los del padre. `SessionEntry` es hijo de `AuthProvider` (que envuelve todo `<Router>`), así que si `SessionEntry` solo navegara sin más, `ProtectedRoute` podía llegar a evaluarse todavía con el `user=null` inicial de `AuthProvider` (cuyo propio efecto, que lee `sessionStorage`, corre después) y mandar a `/login` de forma permanente antes de que `AuthProvider` alcanzara a asentar el usuario real — una vez que `<Navigate to="/login">` se renderiza, la URL ya cambió y nada la trae de vuelta aunque el estado de auth se corrija un instante después. Fix: `SessionEntry` llama a `setUser()` (expuesto por `useAuth()`) de forma directa en su propio efecto, en vez de esperar al efecto independiente de `AuthProvider`.
  - **Verificación en navegador con la app real corriendo** (no solo el script de backend de la entrada anterior): logueado como `Santiago` (admin) vía `/session-entry` en la pestaña principal, se impersonó a `Jean Carlo` (closer) con `isolated: true` y se abrió `/session-entry` con ese token en una pestaña nueva de verdad (`tabs_create`) — confirmado con `/api/auth/me` en cada pestaña por separado: la pestaña nueva resuelve `Jean Carlo` (`is_impersonating: true`, `original_user_role: "admin"`) y la pestaña original sigue resolviendo `Santiago` sin ninguna suplantación, ambas al mismo tiempo, mismo navegador. La URL de la pestaña nueva quedó limpia (`/closer/deck?step=confirmations`, sin el token). El disparo por clic derecho en sí (el evento `contextmenu` nativo) no se pudo ejercitar con el right-click sintético de la herramienta de automatización del navegador (no genera un evento `contextmenu` real en esta configuración) — se confirmó por separado que el handler intercepta y previene el menú nativo correctamente (`dispatchEvent` manual de `contextmenu`, `defaultPrevented: true` y disparo real de `POST /auth/impersonate`), y el resto del flujo (apertura de pestaña + `/session-entry` + aislamiento) se verificó reproduciendo exactamente los mismos pasos que ejecuta el handler.

- **7 de Septiembre de 2026 (9)** *(bug reportado por el usuario: el seguimiento "Contestó y agendó" de KARY LIZER ANTUNEZ WILLIAMS no desaparecía de la pestaña "Seguir" del Workspace de closers aunque la cita ya se había reagendado correctamente, [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])*:
  - **Diagnóstico** (leyendo el código, confirmado luego contra producción vía **Base de Datos → Agenda General**, sesión iniciada por el usuario en el navegador de la app): al elegir "Contestó y agendó" en el modal de seguimiento, `saveSeguimientoReport` hace `PATCH /closer/appointments/<id>` (reagenda `start_time`) y `POST /closer/deck/<id>` con `{ confirm_status: 'por_confirmar', result: 'Pendiente', closer_notes, contact_result }` — ese payload NUNCA incluía `seguimiento_realizado` ni `fecha_seguimiento`, y el backend (`process_closer_card`) solo toca esos dos campos si vienen explícitamente en el body. `CloserFollowUpService._base_query` (la consulta detrás de "Asignados para hoy"/pool) filtra por `seguimiento_realizado == False OR NULL` y `fecha_seguimiento <= fecha_seleccionada` — como ninguno de los dos se tocaba, la misma cita seguía matcheando esa consulta indefinidamente, aunque ya estuviera resuelta y reagendada para el futuro.
  - **Fix**: se agregaron `seguimiento_realizado: true` y `fecha_seguimiento: null` al payload del `POST /closer/deck/<id>` en la rama `result === 'agendo'` — mismo patrón que ya usan las otras dos ramas de "cierre" de esta función (`cerro`/`pago` y `sig_action === 'close'`). Como efecto secundario correcto: `fecha_seguimiento: null` también apaga cualquier recordatorio de seguimiento (`followup_reminder_enabled`) que hubiera quedado armado con la hora vieja.
  - **Verificado con datos reales de producción, no solo localmente**: `#6419` (Kary Lizer Antunez Williams) SÍ quedó reagendada correctamente a 10/9/2026 19:00, closer Nerina, estado `por_confirmar` — la agenda se había creado bien; el bug era exclusivamente que el seguimiento no se apagaba en la vista.

- **7 de Septiembre de 2026 (10)** *(pedido explícito del usuario, a partir del mismo caso de Kary Lizer: agregar un registro de auditoría al reagendar desde un seguimiento, porque no quedaba ningún rastro de la fecha original de la cita, [closer.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])*:
  - **El hallazgo**: `PATCH /closer/appointments/<id>` (el endpoint que reagenda, usado tanto por "Contestó y agendó" como por cualquier otro reagendado manual) sobreescribe `Appointment.start_time` EN LA MISMA fila con un simple `db.session.commit()`, sin llamar a `BookingService.log_lead_event` ni crear un `Comment` — a diferencia de casi cualquier otra acción del closer sobre una cita. Confirmado en producción (Base de Datos → Agenda General): para Kary Lizer solo existe el registro `#6419`, sin ningún rastro en ningún lado de cuál había sido la fecha original antes del reagendado.
  - **Fix**: se captura `old_start_time` antes de aplicar el cambio y, si `start_time` viene en el payload y el valor realmente cambió, se llama a `BookingService.log_lead_event(appt.id, current_user.id, 'reschedule', ...)` DESPUÉS del commit del reagendado (para no bloquearlo si el log llegara a fallar) — mismo patrón ya usado por otros 9 call-sites en `closer.py`/`setter.py`. La descripción queda con el formato `"{usuario} reagendó la cita: {fecha vieja} → {fecha nueva}."`.
  - **Por qué solo cuando cambia de verdad**: se comparan `old_start_time != appt.start_time` para no dejar un log vacío/redundante en un PATCH que no trae `start_time` (ej. edición de otro campo) o que reenvía la misma fecha sin cambio real.
  - **Dónde queda visible este log, y dónde no**: `LeadEventLog` ya tenía 9 call-sites existentes con el mismo patrón, pero al revisar qué pantalla del frontend los muestra se encontró que las dos únicas páginas que leen `GET /closer/deck/events/<id>` (`CloserDeckPage.jsx` y `LeadsManagementPage.jsx`) **no están enrutadas en `App.jsx`** — son código huérfano, reemplazado por `CloserWorkflowPage.jsx`, que no renderiza ningún timeline de eventos. No se tocó nada de esto (no fue lo pedido, y no es un problema introducido por este cambio) — por ahora el registro queda como rastro de auditoría consultable por Base de Datos/directamente en `lead_event_logs`, igual que ya lo eran los otros 9 tipos de evento.
  - **Verificación (`scratch/verify_reschedule_audit.py`, no comiteado)**: contra `instance/local.db`, con una `Appointment` de prueba creada y borrada en la misma corrida — (1) `PATCH` sin `start_time` en el payload → 0 logs creados; (2) `PATCH` con la misma fecha (sin cambio real) → 0 logs; (3) `PATCH` con una fecha nueva → exactamente 1 `LeadEventLog` con `action_type='reschedule'` y la descripción con ambas fechas (vieja y nueva) correctamente formateadas. Las 3 verificaciones pasaron.

- **8 de Septiembre de 2026** *(fix: "Podrías ganar" en la pestaña Seguir mostraba $0 para closers sin ventas propias de ese tipo — pedido explícito del usuario ("de llamadas tomadas... sale cero, entonces no es la idea"), [closer_followup_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY])*:
  - **El bug**: `get_earnings_stats` (agregado el 29/08, ver entrada de ese día) calcula `ticket_promedio`, `avg_seña` y `avg_renewal_upsell` filtrando `FinancialSale` SOLO por las ventas del propio closer (`email_vendedor.in_(identifiers)`). Un closer nuevo, o uno que simplemente todavía no cerró ningún PIF/Split (o ninguna seña) en su propio historial, tenía ese promedio en 0 — y como `estimateEarning` en `SeguimientosPane.jsx` multiplica la comisión por ese promedio, la fila de "Llamadas tomadas" mostraba literalmente "Podrías ganar $0" en vez de una estimación útil. Confirmado contra `instance/local.db`: de 8 closers activos, `Allan`/`Nerina`/`Joaquín`/`Closer 3` tenían `ticket_promedio=0.0` (sin PIF/Split propio todavía), y `Nerina`/`Joaquín`/`Closer 3` también `avg_seña=0.0` (sin señas propias).
  - **Fix**: cuando cualquiera de los 3 promedios propios queda en 0, se completa con el promedio del mismo tipo calculado sobre TODO el equipo (mismo filtro de estado `Completada`/nulo/vacío que ya usaba la consulta individual, sin restringir por `email_vendedor`) en vez de dejarlo en cero — un closer sin cierres propios todavía ve una estimación realista basada en lo que el resto del equipo sí está cobrando, y en cuanto tenga sus propias ventas de ese tipo vuelve a usar su propio promedio (más preciso). No se tocó el frontend: ya leía `avg_seña`/`avg_renewal_upsell`/`ticket_promedio` del mismo shape de respuesta.
  - **Verificación (script ad-hoc, no comiteado)**: `CloserFollowUpService.get_earnings_stats(closer_id)` contra `instance/local.db` para los 8 closers activos, antes y después del fix — los 4 casos que daban `ticket_promedio=0.0` ahora devuelven el promedio de equipo (ej. `Allan` pasó de `0.0` a `678.72`), y los 3 casos de `avg_seña=0.0` idem (ej. `Nerina` de `0.0` a `71.34`); los closers que ya tenían promedio propio (`Jean Carlo`, `Sebastián`, `Marlon`, `Mario`) no cambiaron. No se verificó en el navegador (cambio puramente de cálculo backend, sin tocar el frontend ni el shape de la respuesta).

- **8 de Septiembre de 2026 (2)** *(bug reportado por el usuario contra producción: "Confirmaciones"/"Llamadas reportadas" de la pestaña "Cerrar el día" de Nerina mostraban "2 de 3" sin sentido, con una sola agenda del día y ninguna llamada reportada, [closer.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY], [closer_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/closer_service.py) [MODIFY], [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])*:
  - **Confirmado en vivo contra producción** (sesión iniciada por el usuario, simulando a Nerina): `GET /api/closer/deck/counts` devolvía `{"confirmations":1,"confirmations_done":1,"calls":1,"calls_done":0}` (correcto — 1 agenda del día, ya confirmada, cero llamadas reportadas) mientras `GET /api/closer/deck/daily-report` devolvía `{"confirmados_hoy":2,"show_ups":2}` — dos números que no podían coexistir con lo anterior, y que además inflaban "4 de 8 resueltos hoy"/la XP del héroe de "Tu día" (misma fuente).
  - **Causa raíz (bug 1, backend)**: `CloserService.get_daily_activity_summary` contaba una cita como "confirmada hoy"/"show up reportado hoy" si se la **tocaba** hoy por CUALQUIER motivo (cualquier `LeadEventLog` de ese closer hoy) y su estado ACTUAL ya era ese — no si el campo cambió hoy. Nerina resolvió hoy un seguimiento atrasado (de ayer) sobre una cita que ya estaba confirmada/show-up de antes; `process_closer_card` (`POST /closer/deck/<id>`) escribe un `LeadEventLog` genérico (`action_type='closer_notes'`) en CADA guardado sin importar qué campo cambió, así que ese único guardado (solo tocaba `seguimiento_realizado`) bastó para que la cita contara igual que una confirmación/show-up real de hoy.
  - **Fix (backend)**: `process_closer_card` ahora captura `result`/`closer_result` antes de aplicar los cambios del payload y, SOLO si el valor realmente cambió en ese guardado, escribe un `LeadEventLog` adicional y específico (`action_type='confirmed'` / `'show_up_reported'`), además del genérico existente (que se deja intacto para no romper el rastro de auditoría). `get_daily_activity_summary` deja de derivar `confirmados_hoy`/`confirmados_proximos`/`show_ups` de "tocada hoy + estado actual" y pasa a usar estos dos eventos específicos — `conversando`/`reagendas`/`seguimientos_configurados` no se tocaron (no había evidencia de que estuvieran mal).
  - **Causa raíz (bug 2, frontend, independiente del anterior)**: en la fila de 4 KPI de "Cerrar el día", la tarjeta "Confirmaciones" calculaba `total = done + counts.confirmations`, pero `counts.confirmations` (el mismo campo que ya usa el nav 01 "Confirmar" de más arriba en la misma pantalla, fijado el 28/ago) YA es el total del pool de confirmaciones — incluye las que están confirmadas y siguen visibles. Sumarle `done` encima las contaba dos veces, y por eso la tarjeta podía mostrar un número distinto (y mayor) que el nav 01 para el mismo pool, incluso con `confirmados_hoy` ya corregido por el fix de arriba.
  - **Fix (frontend)**: la tarjeta "Confirmaciones" ahora calcula `pending = max(0, counts.confirmations - done)`, igual que ya hacía el nav 01 (`confirmTotal = counts.confirmations` directamente). "Llamadas reportadas" no necesitaba este segundo fix: `counts.calls` (pendientes, `closer_processed=False`) y `calls_done` (procesadas hoy) ya eran disjuntos desde el 28/ago, el único problema ahí era el `show_ups` inflado del bug 1.
  - **Slots del embudo de "Ver mis datos" — no era un bug**: el usuario inicialmente sospechó que el embudo (filtrado por "este mes") mostraba slots de un solo día. Se confirmó leyendo `CloserService._stats_from_bandeja`/`get_missing_slots_days`: `slots` SÍ es la suma de `CloserDailyReport.slots` de TODO el período filtrado, tal como debe ser — lo que pasaba es que Nerina no había declarado slots en reportes anteriores del mes (por eso el total lucía bajo), no que el cálculo ignorara el filtro de mes. El resto de "Ver mis datos" (Asistencias, Show rate, Confirmation rate, Pitch rate, Show por confirmación) se revisó contra la lógica de `closer_dashboard_service.py`/`closer_service.py` y coincide con lo que el usuario esperaba — sin cambios ahí.
  - **Verificación (`scratch/verify_daily_activity_fix.py`, no comiteado)**: contra `instance/local.db`, con citas y logs de prueba creados y borrados en la misma corrida — (1) una cita ya confirmada/show-up de antes, tocada hoy SOLO por un seguimiento (sin cambiar `result`/`closer_result`) → `confirmados_hoy`/`show_ups` quedan en `0` (antes del fix hubieran contado); (2) una cita nueva con un Show Up real reportado hoy → `show_ups` sí sube a `1`. Ambas verificaciones pasaron. No se verificó en el navegador contra producción (el fix vive en `develop`, sin desplegar todavía) — sí se confirmaron los números "antes" en vivo contra producción vía la sesión del usuario.

- **9 de Septiembre de 2026** *(pestaña "Agendas" dentro de Mi cartera del closer: todas sus agendas con su estado, para corroborar totales, [closer_agendas_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/closer_agendas_service.py) [NEW], [closer_followups.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer_followups.py) [MODIFY], [CarteraPane.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/CarteraPane.jsx) [NEW], [CarteraAgendasPane.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/CarteraAgendasPane.jsx) [NEW], [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY])*:
  - **El pedido**: *"agregar una pestaña en la cartera de los closers para ver sus agendas, así están seguros de las agendas totales que tienen y el estado de cada una, para corroborar sus datos"*. El mazo (`/closer/deck`) solo muestra lo pendiente de cada bandeja y "Ver mis datos" solo muestra totales agregados — no había ningún lugar donde el closer viera el registro completo de sus citas con el estado en que quedó cada una.
  - **Backend (`CloserAgendasService.get_ledger`, `GET /closer/cartera/agendas`)**: todas las `Appointment` del closer en un período, cada una con UN estado derivado (`derivar_estado`) de un catálogo único (`ESTADOS`, que también viaja al frontend para chips/colores/orden): `por_confirmar` / `confirmada` / `sin_reportar` / `show_up` / `segunda_llamada` / `no_show` / `reagendada` / `cancelada` / `lead_perdido` / `no_lead` / `otro`. Precedencia: `closer_result` (lo que reportó el closer o sincronizó el Registro de Agendas — incluye variantes históricas como `Cerrada`, `2th call`, `show_up`) manda; si sigue en Pendiente, decide `result` (Cancelado/Reagendado del confirmer) y, si tampoco, la hora: futura → confirmada/por confirmar según `result == 'Confirmado'`, pasada → "Sin reportar" (mismo criterio que la bandeja ② Reportar). Ningún valor desconocido se pierde: cae en `otro` y la fila muestra el crudo. `is_rescheduled` NO se usa como estado: `process_agenda` lo marca en la agenda NUEVA que nace de una reagenda (la movida queda con `closer_result='Reagendado'`), así que tratarlo como "reagendada" hubiera puesto en ese estado a la cita vigente — se muestra aparte como etiqueta "↻ reag." junto al nombre.
  - **Períodos iguales a "Ver mis datos" a propósito** (`hoy`/`ayer`/`7d`/`30d`/`mes`/`mes_pasado`/`90`/`custom`, misma tabla que `CloserDashboardService._range_for_period`) para que "Este mes" signifique lo mismo en las dos pestañas y el closer pueda cruzar números, más dos propios de acá: `proximas` (desde hoy en adelante, para ver lo que viene) y `todo` (histórico completo). Diferencia deliberada: el "hoy" y los límites del día se calculan en la zona horaria del closer (`user_time_service.limites_rango_utc`), no en la del servidor — la lista se lee fecha por fecha y una agenda de las 23:30 de Caracas no puede aparecer bajo el día siguiente. *Caveat documentado*: `_confirmations` del dashboard sigue usando `datetime.combine(fecha, time.min/max)` naive (UTC), así que en el borde del día una agenda nocturna puede contar en días distintos entre ambas vistas; no se tocó el dashboard en este alcance.
  - **Ventas en lote**: mismo cruce que `CloserFollowUpService._client_has_sale` (email o instagram del cliente contra `FinancialSale`) pero en una sola pasada (`_ventas_por_contacto`, IN por lotes de 400 para no chocar con el límite de variables de SQLite viejo) en vez de una consulta por fila. Se distingue `venta` (el cliente compró) de `venta_propia` (`email_vendedor` es de este closer, vía `CloserService._resolve_sale_identifiers` — el mismo mapeo de Mi cartera y las estadísticas): una venta reportada por otro closer se muestra como "Otro closer", no como propia. Cada fila trae además `fase`/`tipo` (mismo criterio que `get_client_lead_stage`, pero para ESA cita) para que el clic abra el modal correcto: confirmación, reporte de llamada, o seguimiento tomada/no tomada.
  - **Frontend**: `CarteraPane.jsx` (nuevo) es el contenedor con las pestañas "Clientes" (el `MiCarteraPane` de siempre, sin tocarlo: está en 458 líneas, al borde del límite de 500) y "Agendas" (`CarteraAgendasPane.jsx`, nuevo). En `CloserWorkflowPage.jsx` solo cambió el import y el render de la vista `cartera`. La pestaña muestra: selector de período (con rango libre y el rango resuelto en texto, "Del 01 sept al 09 sept · horas en tu reloj (GMT-4)"), 4 KPIs (agendas del período, todavía sin resultado, asistieron, con venta propia), un chip por estado con su total (clic filtra), búsqueda por nombre/instagram/mail/teléfono, filtro de venta y orden, y la tabla agrupada por día local con el conteo de agendas de cada día — todo el filtrado fino es local sobre lo que trajo el backend; solo cambiar el período vuelve a pedir.
  - **Permisos**: `closer`/`admin` (403 para el resto, 401 anónimo). Un admin sin simular puede pedir un closer puntual con `?closer_id=`; sin eso responde vacío, igual que `/cartera`. Simulando a un closer (JWT por pestaña) funciona como para el propio closer.
  - **Verificación**: (1) `scratch/simulate_cartera_agendas.py` (no comiteado, `scratch/` está en `.gitignore`) contra una SQLite temporal: 45/45 checks — los 13 estados esperados incluyendo `Cerrada`→asistió y la agenda nueva de una reagenda como "por confirmar"; venta propia con el mail en mayúsculas en `FinancialSale`; venta de otro closer; exclusión de agendas ajenas; zona horaria (la de 23:30 local de ayer = 03:30 UTC de hoy cae en "ayer" y no en "hoy"); `proximas`, `mes`, `mes_pasado`, `custom` invertido (se da vuelta), `custom` sin fechas e id inválido (caen a mes); 403 setter, 401 anónimo, admin con y sin `closer_id`. (2) En navegador con Playwright contra una SQLite sembrada (`scratch/seed_ui_cartera_agendas.py`, closer `simcloser` en America/Caracas): login → 06 Mi cartera → pestaña Agendas → KPIs y chips con los totales correctos ("Este mes": 8 agendas, 1 sin resultado; "Todo": 20), filtro por chip, clic en una fila "Sin reportar" abre el modal en "Reporte de llamada" con los botones Asistió/No show/Canceló/Reagendar. `npm run build` limpio. Los únicos errores de consola fueron recursos externos (fuentes/CDN) bloqueados por la red del sandbox.
  - **Nota operativa**: en la simulación el `ReminderScheduler` tira un `no such table: appointments` en su primer tick, porque `create_app` arranca el hilo antes de que el script haga `db.create_all()` — preexistente, sin relación con este cambio.

- **10 de Septiembre de 2026** *(acción "Marcar como duplicada" en Agendas de Mi cartera — pedido explícito del usuario a partir de un caso real de Nerina con la lead "Mia Sky", investigado simulando su sesión en producción, [closer_followups.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer_followups.py) [MODIFY], [CarteraAgendasPane.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/CarteraAgendasPane.jsx) [MODIFY])*:
  - **El pedido**: el usuario reportó que a Nerina "no le aparece una seña de $100" y que hay inconsistencias en la cantidad de agendas que ve en "Mis datos" — pidió revisar su rendimiento simulando su sesión en producción (vía "Simular" desde Gestión de Equipo) y, sobre la pestaña "Agendas" de Mi cartera (agregada el 9/sep), agregar acciones para poder corregir inconsistencias — anticipando específicamente el caso de una agenda repetida ("eso puede pasar si hay una segunda agenda").
  - **Investigación en producción (simulando a Nerina y luego como operador)**: en "Mi cartera → Agendas" con período "Todo" apareció exactamente el caso anticipado — **Mia Sky** (`@sky_gt_`) tiene DOS citas idénticas, miércoles 9/sep a las 17:00, mismo setter (Paula): una quedó "Asistió" (con notas reales de la llamada) y la otra huérfana en "Sin reportar", inflando el total de agendas en 1. El Lead Roadmap del cliente (Auditoría → Registro de Agendas) muestra una sola "AGENDA CREADA" (vía Calendly/webhook, 03/sep) pero DOS eventos "LLAMADA REALIZADA" al mismo instante exacto (09/sep 21:00 UTC = 17:00 local) con resultados distintos — es decir, la duplicación ya existía en `Appointment` antes de que Nerina reportara, y ella terminó procesando dos tarjetas para el mismo lead sin darse cuenta. La causa exacta (probable condición de carrera en la sincronización agenda→cita cuando un webhook se reintenta) no se investigó a fondo en este alcance — lo que se construyó es la herramienta para resolver el síntoma cuando vuelve a pasar, no un cambio en `BookingService.sync_financial_agenda_to_appointment`.
  - **La seña de $100 de Mia Sky: no se encontró en ningún lado del sistema**. Se revisó su historial completo (`VENTAS DECLARADAS: 0`, `INSCRIPCIONES Y PAGOS: 0`) y, por las dudas de que estuviera huérfana bajo otro email, se buscó por nombre/instagram/teléfono en las dos tablas de ventas que expone "Base de Datos Maestro" (`FinancialSale` vía Base de Datos → pestaña normal de ventas del closer, y `Payment`/`Enrollment` vía `/admin/db/sales_raw`, que resultó ser un sistema de pagos aparte — ver hallazgo lateral abajo): cero coincidencias en ambas. Las notas de la última llamada ("Cómo te fue hablando con tus papás? -Aun no hablo con ellos") sugieren que a esa fecha Mia Sky todavía no había decidido pagar. **No se inventó ni se cargó ningún registro de venta** — queda pendiente que el usuario confirme si la seña realmente se cobró (y por qué canal) antes de registrarla a mano.
  - **Backend, nueva acción (`POST /closer/cartera/agendas/<id>/marcar-duplicada`)**: cancela una agenda duplicada reusando `CloserService.process_agenda` (mismo camino que "Canceló" desde el mazo, con su mismo rastro de auditoría vía `ClientComment` y borrado del evento de Google Calendar) — nunca borra la fila. Dos guardas para que la acción sea segura: (1) la cita a cancelar tiene que estar todavía en un estado sin resultado real (`por_confirmar`/`confirmada`/`sin_reportar`, via `derivar_estado` de `closer_agendas_service.py`) — nunca se puede "duplicar-cancelar" una llamada que sí ocurrió; (2) tiene que existir otra cita del mismo cliente y closer a menos de 6 horas — si no hay una hermana cercana, no parece una duplicada de verdad y se rechaza con 400.
  - **Frontend (`CarteraAgendasPane.jsx`)**: se calcula localmente qué filas son "posible duplicado" (mismo cliente, sin resultado reportado, otra cita a ≤6h — misma ventana que valida el backend, para no ofrecer un botón que el servidor va a rechazar) y se muestra una barra ámbar bajo la fila con el motivo y el botón "Marcar como duplicada" (con `stopPropagation` para no abrir el modal del lead al hacer clic). Confirmación con `window.confirm` antes de llamar al endpoint, igual que el patrón ya usado en `DatabasePage.jsx` para eliminar registros.
  - **Verificación**: (1) `scratch/verify_duplicate_agenda_and_mail_sync.py` (no comiteado) contra `instance/local.db`: cancela correctamente una huérfana con hermana reportada cercana, no toca la cita con resultado real, rechaza cancelar una cita ya reportada (400), rechaza cuando no hay hermana cercana (400) — 3/3 checks de esta acción (ver también el fix del punto siguiente, verificado en el mismo script). (2) Se corrió `scripts/actualizar_db.py --target local` (pedido explícito del usuario, para tener el caso real de Mia Sky en la base local) — la sincronización preserva los IDs originales (`make_transient` + reinserción), así que el caso real quedó disponible con los mismos ids que en producción (`Appointment` #6396 la huérfana, #6446 la reportada). (3) Verificación end-to-end en el navegador: impersonación local de Nerina vía `POST /api/auth/impersonate` + `sessionStorage` (mismo patrón ya documentado el 1/sep, con el detalle nuevo de que hace falta setear tanto `auth_token` como `user` — `loadSession()` en `utils/sessionStore.js` solo lee este último) — la fila de Mia Sky en Mi cartera → Agendas mostró la barra "Posible duplicado", el clic en "Marcar como duplicada" (con `window.confirm` parcheado para poder automatizar la prueba) canceló la cita huérfana real (#6396, estado pasó a "Cancelada") y dejó intacta la que tiene la llamada real (#6446, sigue "Asistió") — confirmado también por los chips de conteo (`Cancelada` pasó de 0 a 1). **Esto corrigió el caso real de Mia Sky en la base LOCAL** (sincronizada de producción); el mismo arreglo en producción queda pendiente de que el usuario decida desplegar `develop` → `main`, o de una limpieza manual puntual — no se tocó producción en este alcance (instrucción explícita del usuario: sin push).
  - **Hallazgo lateral fuera de alcance, no arreglado**: mientras se buscaba la venta huérfana de Mia Sky en "Base de Datos Maestro → Ventas (Pagos)", se encontró que el buscador de esa pantalla (y el de "Leads/Clientes") puede quedar desactualizado — el `data` de `DatabasePage.jsx` no se limpia al cambiar de pestaña ni hay guarda contra respuestas fuera de orden, así que una búsqueda puede seguir mostrando la tabla anterior (o filas de una pestaña distinta bajo las columnas equivocadas) hasta que una petición más lenta la pise. También se confirmó que "Ventas (Pagos)" en ese panel lee `Payment`/`Enrollment` (pagos de pasarela, Stripe/Hotmart) y NO `FinancialSale` (las ventas que declaran los closers) — son dos sistemas de venta distintos conviviendo en el código. Ninguno de los dos se tocó en este alcance.

- **10 de Septiembre de 2026 (2)** *(fix: corregir el mail de una agenda no actualizaba el Cliente ya existente — pedido explícito del usuario durante la investigación de arriba ("ya había agregado el email del lead desde Calendly con n8n... revisa eso también"), [financial_agendas.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/public/financial_agendas.py) [MODIFY])*:
  - **El bug**: `PUT /public/financial-agendas/<id>` ya actualizaba `FinancialAgenda.mail` cuando se corregía a mano (o vía n8n) el mail de una agenda, pero nunca propagaba esa corrección al `Client.email` ya existente — si el Cliente se había creado antes con un placeholder (`no-email-...@neurops.com`, o `NULL` en el caso real de Mia Sky), se quedaba con el email falso para siempre aunque el mail real llegara después. `BookingService.find_or_create_client` ya resolvía este mismo problema para el flujo de sincronización automática (comentario propio del 07/sep), pero faltaba en la edición manual del Registro de Agendas.
  - **El fix**: al editar `mail`, se busca el Cliente vinculado por instagram o teléfono (nunca por el mail nuevo, que por definición el Cliente todavía no tiene) y se actualiza `Client.email` solo si el actual es un placeholder reconocido (`no-email-`, `no_email_`, `@neurops.com`, `@neurops.temp`, o vacío) — nunca pisa un email real ya cargado.
  - **Bug preexistente encontrado al escribir este fix**: `update_financial_agenda` ya tenía, más abajo en la misma función, dos reimportaciones redundantes `from app.models import Client, Appointment` (dentro de los bloques de `estado`/`closer_result`) — `Client` ya se importa a nivel de módulo. Python trata cualquier nombre reasignado en cualquier punto de una función como local a TODA la función, así que apenas se agregó una referencia a `Client` más arriba (este fix), Python empezó a tirar `UnboundLocalError: cannot access local variable 'Client'` — un bug real y latente que ya existía (cualquier código nuevo que hubiera referenciado `Client` antes de esas líneas lo habría disparado igual). Se sacó `Client` de esas dos reimportaciones (queda solo `Appointment`, que sí hace falta porque ese no está importado a nivel de módulo).
  - **Verificación**: incluida en `scratch/verify_duplicate_agenda_and_mail_sync.py` (no comiteado) — se crea un Cliente con email placeholder y una `FinancialAgenda` con `mail='N/A'`, se llama al PUT con un mail real, y se confirma que `Client.email` queda actualizado. 1/1 check (incluido en el 5/5 total del script, junto con los 4 checks de la acción del punto anterior).

- **10 de Septiembre de 2026 (3)** *(incidente de producción: la seña de $100 de Nerina no le contaba como propia — email de vendedor desactualizado en una venta histórica, sin cambios de código, solo dato corregido en Postgres de producción, permiso explícito del usuario)*:
  - **Corrección del punto anterior**: la "seña de $100 que no le aparece a Nerina" (motivo original de toda la investigación de hoy) **no era de Mia Sky** — el usuario aclaró que es de **Susett Anahí Cochachín Luna**, y que ya está en el Registro de Ventas. Lo de Mia Sky era únicamente el problema de la agenda duplicada (entradas de arriba).
  - **Diagnóstico** (consulta de solo lectura contra `instance/local.db`, recién sincronizada 1:1 con producción vía `actualizar_db.py`): la venta sí existe — `FinancialSale` id=57653, 07/sep/2026, "AL - Seña", $100, Stripe — pero con `email_vendedor='closer1@prueba.com'`, no con el email real de Nerina (`ainerinanietoc@gmail.com`). `CloserService._resolve_sale_identifiers` cruza `FinancialSale.email_vendedor` contra el email actual del closer autenticado, con un mapeo a mano de alias para closers que cambiaron de email alguna vez (Jean Carlo, Marlon, Mario, etc.) — Nerina no estaba en ese mapeo, así que esta venta puntual (la única con ese email viejo, verificado con un `COUNT` sobre toda la tabla) nunca se le atribuía: en Mi cartera → Agendas aparecía como "Otro closer" sobre la cita de Susett del 08/sep, y no sumaba en "Ver mis datos" (Cash Collected, Señas del período).
  - **Corrección aplicada** (mismo patrón que el incidente de Kervin Calderón del 02/sep — proxy público `DATABASE_PUBLIC_URL`, `scratch/fix_susett_sale_vendedor.py`, no comiteado): `UPDATE financial_sales SET email_vendedor = 'ainerinanietoc@gmail.com' WHERE id = 57653`, con verificación antes/después del único campo tocado (no se tocó `monto`, `tipo_pago`, `client_id` ni el resto de la fila). Confirmado en el navegador contra producción, en "Registro de Ventas" filtrado por closer Nerina: la venta de Susett ($95.5 neto, bruto $100, Seña, Stripe) ahora aparece bajo "C: Nerina", "CON AGENDA", "COMPLETADA".
  - **Riesgo no resuelto, fuera de este alcance**: no se investigó por qué esta venta puntual quedó con el email viejo (post-fecha del cambio de cuenta de Nerina a su email real) ni si hay otros closers con el mismo patrón de una venta histórica huérfana — se corrigió el único caso reportado, no se auditó el resto de la tabla para closers que no sean Nerina.

- **10 de Septiembre de 2026 (4)** *(fix: la deuda de un cliente se calculaba con el precio de lista del programa en vez del precio realmente negociado — pedido explícito del usuario ("dice que Susett debe 400, pero no es cierto, en total debe 900 y el proximo pago debe ser de 300"), [closer_followup_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/closer_followup_service.py) [MODIFY], [closer_dashboard_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/closer_dashboard_service.py) [MODIFY])*:
  - **El bug**: tanto `CloserFollowUpService._client_debt` (usado en "Mi cartera → Clientes") como `CloserDashboardService._pending_collections` (usado en "Ver mis datos → Deuda por cobrar") calculaban la deuda como `Program.price − pagado`, ignorando por completo `Client.total_amount` — el campo que el propio sistema ya expone para que un closer corrija "el total a pagar del programa" cuando el precio negociado con ESE cliente es distinto del precio de lista (`PATCH /clients/<id>/total-amount`, agregado antes de esta sesión), y que `SalesConsistencyService.validate_next_payment_type` ya usa como fuente de verdad para exactamente este mismo propósito. Caso real: Susett Anahí Cochachín Luna tiene `Client.total_amount=1000` (precio negociado) pero `Program.price=500` (precio de lista de "Ace Learner v3") — con $100 pagados (la seña corregida en la entrada anterior), el sistema mostraba "debe $400" (500−100) en vez de los $900 reales (1000−100). El cronograma de cuotas (`InstallmentPlan`) que Nerina ya había armado el 09/sep sí tenía los 3 pagos de $300 correctos — pero `_pending_collections` recortaba (`min()`) ese cronograma real para que sumara como mucho los $400 mal calculados, escondiendo $500 de deuda real detrás del propio cronograma que decía la verdad.
  - **El fix**: en ambas funciones, si `Client.total_amount` está cargado, la deuda se calcula como `total_amount − total pagado en todas las inscripciones del cliente` (en vez de sumar `Program.price − pagado` inscripción por inscripción); sin `total_amount`, sigue exactamente el cálculo anterior (precio de lista) para no cambiar el comportamiento de los clientes sin un precio negociado cargado. En `_pending_collections` esto también corrige indirectamente el recorte del cronograma de cuotas: al ser la deuda base ya correcta ($900), el cronograma real (3×$300) deja de necesitar recortarse.
  - **Alcance**: se corrigieron solo las dos funciones que usan las pantallas que el usuario mencionó explícitamente (Mi cartera de clientes y Ver mis datos). El mismo patrón `Program.price − pagado` sin mirar `Client.total_amount` aparece en varios otros lugares del código (`closer_service.py`, `dashboard_service.py` del admin, `user_service.py`, `financial_service.py`, `app/api/closer.py`) que no se tocaron en este alcance — no se auditó si esas otras vistas tienen el mismo problema.
  - **Verificación** (`scratch/verify_debt_total_amount_fix.py`, no comiteado, contra `instance/local.db` recién sincronizada de producción): (1) `_client_debt(Susett)` da `900.0` (antes `400.0`); (2) `_pending_collections` para Nerina lista a Susett con `pending_amount=900.0`, `due_date='2026-10-09'`; (3) control negativo — un cliente sintético SIN `total_amount` sigue calculando la deuda con el precio de lista, exactamente igual que antes del fix. Verificación además en el navegador, simulando a Nerina localmente: "Ver mis datos" muestra "Susett Anahí Cochachín Luna · Ace Learner v3 · $900 · vence 2026-10-09" (Katherine Sandoval Hernández, que también tiene `total_amount` propio, pasó de $425 a $925 — mismo fix, caso legítimo, no una regresión); "Mi cartera → Clientes" muestra "1 cliente en cartera, $900 deuda total pendiente" con la fila de Susett (seña $100, próx. cuota 09 oct, deuda $900).

- **10 de Septiembre de 2026 (5)** *(nueva tarjeta "Comisión de este mes" en el espacio de trabajo de closers y setters — pedido explícito del usuario, [commission_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/commission_service.py) [NEW], [closer_followups.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer_followups.py) [MODIFY], [setter.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/setter.py) [MODIFY], [ComisionMesCard.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/ComisionMesCard.jsx) [NEW], [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [SetterComisionMesCard.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/setter/components/SetterComisionMesCard.jsx) [NEW], [SetterWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/setter/SetterWorkflowPage.jsx) [MODIFY])*:
  - **El pedido**: *"necesito que hagas que los closers y los setters puedan ver cuanto van ganando de comision del mes, que lo vean en su espacio de trabajo"*.
  - **Lo que ya existía, investigado antes de tocar código**: no hay un % de comisión configurable por persona en ningún lado del sistema. Solo 3 personas (Elias, Jean Carlos, Marlon) tienen una fórmula hardcodeada en `get_commissions_calculated` (`app/api/public/finance.py`, panel de Nómina, solo accesible a Finanzas) — 8%/10%/5% sobre el cash cobrado neto del mes, matcheando el nombre del setter/closer contra el string crudo `FinancialAgenda.nombre`/`FinancialSale.email_vendedor`. Para el resto del equipo, la comisión es un monto que Finanzas tipea a mano en `MonthlyPayroll` al cierre del mes — no hay ningún cálculo en vivo. Se le preguntó al usuario cómo calcularla para el caso general: **10% para closers, 8% para setters**, sobre el cash collect **neto** (confirmado explícitamente).
  - **Backend (`CommissionService`, nuevo)**: `get_closer_commission(user)` reusa `CloserService.get_comprehensive_stats` (la misma fuente que ya alimenta el "Cash Collected" de Ver mis datos) para el mes calendario en curso, y toma `cash_neto` de ahí — no recalcula el cash con otra regla que pudiera divergir. `get_setter_commission(user)` no tenía ninguna fuente existente que reusar (los setters no tenían ninguna métrica de cash atribuido) — se generalizó el mismo criterio de atribución que ya usa `get_commissions_calculated` para Elias (agenda de origen de la venta vía `AttributionService`, con fallback a `FinancialSale.setter`), comparando contra `User.username` en vez de un nombre hardcodeado. **Verificación cruzada**: para Elias y Jean Carlos (los 2 casos que ya tenían una fórmula propia), el resultado de este servicio nuevo coincide EXACTO con `get_commissions_calculated` ($91.98 y $162.35 respectivamente, con datos reales de producción) — confirma que la generalización no inventó una regla distinta.
  - **Endpoints**: `GET /closer/commission` (`closer_followups.py`, `closer`/`admin`) y `GET /setter/commission` (`setter.py`, `@role_required(ROLE_SETTER)`) — cada uno calcula solo para `current_user`, sin parámetro de "a quién" (a diferencia de otros endpoints de este mismo archivo, este es estrictamente "mi propia comisión").
  - **Frontend**: dos tarjetas nuevas, autocontenidas (se fetchean solas, no dependen del estado de la página que ya es enorme en el caso del closer) — `ComisionMesCard.jsx` en el Closer Workspace (debajo de "Tu día", reusa el formateador `money` de `performanceUtils.js` y las variables CSS `--v6-*` del tema existente) y `SetterComisionMesCard.jsx` en el Setter Workspace (arriba de las acciones masivas, con las clases Tailwind slate/violet que ya usa esa página — visualmente distinta porque el Setter Workspace nunca adoptó el tema `--v6-*` del closer). Ninguna renderiza nada mientras no hay respuesta (evita un salto de layout con un placeholder vacío).
  - **Alcance**: los 3 casos especiales de `finance.py` no se tocaron — siguen viviendo ahí para el panel de Nómina de Finanzas. No se agregó ningún campo de % configurable por persona (el usuario pidió el % fijo, no una UI de configuración) — si en el futuro hace falta un % distinto por persona, hoy no hay dónde cargarlo.
  - **Verificación**: contra `instance/local.db` (sincronizada de producción) — `CommissionService.get_closer_commission` para Nerina da `cash_neto=95.5, commission=9.55`; para Jean Carlo, `cash_neto=1623.5, commission=162.35` (igual a `get_commissions_calculated`). `get_setter_commission` para Elias da `cash_neto=1149.75, commission=91.98` (igual a `get_commissions_calculated`); para Ivan, `cash_neto=71.62, commission=5.73`. En el navegador, simulando a Nerina y a Elias localmente: el Closer Workspace muestra "Comisión de este mes · 10% de $96 cobrados netos · $10"; el Setter Workspace muestra "Comisión de este mes · 8% de $1,150 cobrados netos · $92" — ambos coinciden con los cálculos de backend (redondeados a entero por el formateador `money`).

- **10 de Septiembre de 2026 (6)** *(panel de Postulaciones: menú inferior más grande, sub-filtros contextuales por grupo, ficha de candidato rediseñada y nuevo estado "Testeo" — pedido explícito del usuario a partir de un mockup de referencia ("Panel de postulaciones (standalone) (1).html"), [job_application.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/job_application.py) [MODIFY], [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionesDashboardPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/PostulacionesDashboardPage.jsx) [MODIFY], [PostulacionesInbox.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesInbox.jsx) [MODIFY], [PostulacionDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionDetailModal.jsx) [MODIFY], [PostulacionVoteToast.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionVoteToast.jsx) [MODIFY])*:
  - **El pedido**: el usuario compartió un mockup HTML de referencia (el mismo tipo de artefacto ya usado el 3/sep para el voto "Reserva") y pidió acercar el panel real a ese diseño, en tres correcciones concretas dadas en el mismo mensaje: (1) el menú inferior de pestañas debe verse más grande, sin achicar las inactivas a un ícono suelto; (2) los sub-filtros de arriba tienen que ser contextuales — bajo "Pendientes" solo "Sin analizar" y "Decidir"; bajo (nueva pestaña) "Analizados", "Seleccionados/En reserva/Testeo/Baja/Descartados/Incompletos"; (3) la ficha de análisis individual de cada candidato debe rediseñarse con el formato del mockup, "mucho mejor" que el actual. Aclaración aparte del usuario: "Testeo" es para closers preseleccionados que están en su etapa de prueba.
  - **Investigación del mockup**: el HTML compartido es un bundle exportado (contenido embebido, no HTML plano) de 572 KB — se sirvió con un servidor HTTP local temporal (`python -m http.server`, detenido al terminar) y se inspeccionó renderizado en el navegador (capturas + `getComputedStyle` sobre el dock inferior) en vez de tratar de leer el bundle a mano. De ahí salieron los datos exactos: 4 pestañas inferiores (Pendientes/Analizados/Revisores/Estadísticas) de 176×60px cada una, siempre con ícono+número+etiqueta visibles (nunca colapsadas); los sub-filtros vienen de un array `SUBTABS` con un campo `grupo` ('pend'/'anal') que se filtra según la pestaña activa; y la ficha de candidato tiene sidebar (nivel/inglés/reportes/herramientas/contacto) + grilla de 2 columnas de tarjetas de respuesta colapsables ("Abrir todas" las expande todas) + 5 botones de acción siempre visibles (Seleccionar/Reserva/Descartar/Testeo/Baja).
  - **Backend, nuevo estado "Testeo"**: se agregó a `RESOLUCION_VALUES` (`app/models/job_application.py`), junto a los ya existentes `preseleccionada` y `baja` — es una resolución manual de un admin (no un voto de revisor: no necesita el acuerdo del otro revisor), exactamente el mismo mecanismo que ya existía para "Baja" desde antes de esta sesión. `veredicto()` ya devolvía `self.resolucion` tal cual cuando está seteada, así que no hizo falta tocar esa función — alcanzó con sumar `'testeo'` a `FILTROS_VALIDOS`, `_aplica_filtro` y `SEGMENTOS_VALIDOS`/`VEREDICTO_DE_SEGMENTO` de las estadísticas (`app/api/job_applications.py`).
  - **Menú inferior (`PostulacionesDashboardPage.jsx`)**: la pestaña única "Postulantes" se separó en "Pendientes" y "Analizados" (matching 1:1 con el `grupo` de `PostulacionesInbox`, ver abajo) — el menú pasa de 4 a 5 pestañas (se sumó "Clarity", que el mockup no modela pero ya era una función real del panel, no se quitó). Cada botón ahora mide 60px de alto con `min-width` generoso, siempre con número "00"/"01"/... + ícono + etiqueta — antes las inactivas se achicaban a un círculo de 44×44 con solo el ícono, que era justo la queja del usuario. La activa usa el mismo degradé rosa que ya usaba el resto del panel (`#FF3FA4`→`#FF6AD5`).
  - **Sub-filtros contextuales (`PostulacionesInbox.jsx`)**: se sacó el viejo selector `VISTAS` (Revisión/En reserva/Incompletas) y la lista plana `FILTROS`, reemplazados por un solo `SUBTABS_POR_GRUPO` con dos arrays (`pend`: Sin analizar/Decidir: reusa el filtro `mis_pendientes` que ya existía, solo con otra etiqueta; `anal`: Seleccionados/En reserva/Testeo/Baja/Descartados/Incompletos). El componente ahora recibe un prop `grupo` y solo renderiza el sub-filtro correspondiente; "Incompletos" pasa de ser una "vista" separada a un sub-filtro más del grupo "Analizados" (la tabla especial de contacto/avance/materiales se sigue disparando igual, solo que ahora por `sub === 'incompletas'`). La tarjeta "Tu inbox · revisar el siguiente" y el autoavance al votar quedan solo bajo el grupo "Pendientes" — no tienen sentido navegando candidatos que ya tienen veredicto.
  - **Ficha de candidato (`PostulacionDetailModal.jsx`)**: (1) badge de veredicto agregado al lado del nombre en el header (antes solo vivía abajo en el sidebar); (2) "Respuestas abiertas" pasó de una columna de bloques siempre expandidos a una grilla de 2 columnas de tarjetas colapsables con `line-clamp-2` cuando están cerradas, más un botón "Abrir todas"/"Cerrar todas" que expande o cierra todo de una — estado nuevo `abiertas` (Set de keys expandidas), se reinicia al cambiar de candidato; (3) el footer pasó de 3-4 botones condicionales (Reserva/Descartar/Preseleccionar, "Dar de baja" solo si ya estaba preseleccionado) a 5 botones siempre visibles con el mismo peso — Seleccionar/Reserva/Descartar (voto del revisor actual) + Testeo/Baja (resolución manual de admin, ya no depende de `mostrarBaja`). `PostulacionVoteToast.jsx` sumó la entrada `testeo` (ícono `FlaskConical`, celeste) al mapa de toasts para que marcar a alguien en testeo también muestre confirmación.
  - **Verificación en navegador** (simulando admin, `instance/local.db` sincronizada de producción — 165 postulaciones reales): pestaña "Pendientes" muestra "Sin analizar (165)" y "Decidir (0)"; "Analizados" muestra los 6 sub-filtros con conteos reales ("Seleccionados (10), En reserva (7), Testeo (0), Baja (5), Descartados (141), Incompletos (325)"); abrir un candidato seleccionado, tocar "Testeo" cambia el badge del header a "EN TESTEO" (celeste) y el botón queda sólido — tocarlo de nuevo lo revierte a "SELECCIONADA" (confirmado el toggle); "Abrir todas"/"Cerrar todas" expande y colapsa la grilla de respuestas correctamente. **Bug real encontrado y corregido durante la verificación**: al separar la pestaña "Postulantes" en "Pendientes"/"Analizados", el `useState` inicial de `activeTab` seguía apuntando al id viejo (`'postulaciones'`, que ya no existe en `TABS`) — la página cargaba con el body completamente vacío (ninguna pestaña matcheaba) hasta hacer clic manualmente en una pestaña. Corregido a `useState('pendientes')`.
  - **Alcance, pendiente para "la próxima corrección"**: no se tocó `PostulacionesStatsTab.jsx` (el selector de segmento "Todos/Seleccionados" del backend ya acepta `testeo`, pero el frontend de esa pestaña no lo ofrece todavía) ni `PostulacionesRevisoresTab.jsx`. Tampoco se replicaron las animaciones del mockup (el dock activo tiene un degradé animado en movimiento) ni los badges "Ads/Presentación/Roleplay" del header de la ficha (no hay datos reales de eso en el modelo — se optó por no inventar campos). El usuario avisó que va a seguir dando correcciones "poco a poco".

- **10 de Septiembre de 2026 (7)** *(siguiente tanda de correcciones del mockup: ficha de candidato a página completa, y Estadísticas/Revisores rediseñados — pedido explícito del usuario, [job_applications.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/job_applications.py) [MODIFY], [PostulacionDetailModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionDetailModal.jsx) [MODIFY], [PostulacionesStatsTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesStatsTab.jsx) [MODIFY], [PostulacionesRevisoresTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesRevisoresTab.jsx) [MODIFY])*:
  - **El pedido**: *"quiero que el modal que se abre al abrir un postulante sea toda la página completa, que no sea solo un pedacito... de manera que no haya ni siquiera necesidad de hacer scroll. Y aprovecha de revisar las estadísticas también... la parte de los revisores también tienes que mejorarla"* — las tres correcciones siguientes de la lista "poco a poco" que había quedado pendiente en la entrada anterior. Se volvió a servir el mismo mockup con un servidor HTTP local temporal (detenido al terminar) para inspeccionar las pestañas Estadísticas y Revisores, que todavía no se habían mirado.
  - **Ficha de candidato a página completa (`PostulacionDetailModal.jsx`)**: pasó de un modal centrado (`88vh`, `max-w-[1240px]`, backdrop con blur y click-afuera-para-cerrar) a un overlay `fixed inset-0` que ocupa TODA la pantalla, sin backdrop ni recuadro — ya no hay "afuera" para cerrar haciendo click, así que se agregó cierre con la tecla Escape para no perder esa salida rápida. El body sigue siendo un grid de 2 columnas con `overflow-y-auto` como red de seguridad, pero con todo el ancho/alto de la pantalla disponible la necesidad real de scroll baja mucho.
  - **Revisores, backend**: `GET /job-applications/revisores` sumó `conteo` (cuántos pre/res/des votó cada revisor) y `mosaico` (un valor por postulación completa, en el mismo orden, con el voto de ESE revisor o `null` si todavía no llegó) a cada fila de `revisores` — antes solo devolvía el agregado `hechas`/`faltan`/`pct`, sin desglose. Se calcula en el mismo loop que ya recorría `completas` por revisor (sin N+1 nuevo: `a.votes` ya viene con `lazy='joined'`).
  - **Revisores, frontend**: cada tarjeta de revisor ahora tiene una barra segmentada (`BarraMosaico`, un bloque de color por candidato en vez de una sola barra de progreso lisa) + el desglose "N pre · N reserva · N descartadas" + una etiqueta "Vos"/"Co-revisor" (comparando `r.id` contra el usuario logueado). Se sumó el panel nuevo "Cómo vota cada uno": para cada categoría de voto, una barra por revisor comparando cuánto votó cada uno — para notar de un vistazo si alguien preselecciona mucho más fácil o más difícil que el resto. Título y subtítulo de la pestaña actualizados para calzar con el mockup.
  - **Estadísticas**: el selector de segmento pasó de pills sin conteo a mostrar el total de cada uno inline ("Completaron · 165", "Seleccionados · 10"...) — se reusa el `conteos` que ya devuelve `GET /job-applications?filtro=todas` (una consulta liviana extra al montar, no un endpoint nuevo) en vez de duplicar esa cuenta. Se sumó el segmento **"Testeo"** que había quedado pendiente de la entrada anterior (el backend ya lo soportaba desde el `RESOLUCION_VALUES` de ese momento). Los paneles existentes (KPIs, embudo, histograma, donut de score, distribuciones) se reordenaron bajo tres encabezados numerados — "01 Lo que hay que saber" (KPIs), "02 Volumen y embudo" (embudo + postulaciones por día), "03 Calidad del pool" (donut + histograma + distribuciones) — calcando la estructura del mockup, sin tener que reconstruir ningún tipo de gráfico nuevo: el histograma, el embudo y las barras de distribución ya eran muy parecidos a los del mockup desde antes.
  - **Verificación en navegador** (simulando admin, mismos datos reales de producción sincronizados): abrir un candidato desde "Sin analizar" muestra la ficha ocupando toda la pantalla, sin recuadro ni fondo oscuro alrededor, con los 5 botones de acción visibles al pie sin haber scrolleado; cerrar y reabrir funciona igual. "Revisores" muestra 4 tarjetas (incluida una cuenta "Administrador Emergencia" sin votos) con sus barras segmentadas coloreadas según los votos reales, "Santiago" marcado como "Vos" (el admin simulado); el panel "Cómo vota cada uno" compara correctamente (ej. Mario 15 preseleccionados vs. Marlon 7). "Estadísticas" muestra los 7 chips de segmento con conteos reales ("Completaron · 165", ... "Incompletas · 325"), clickear "Testeo" cambia el segmento activo sin errores (0 candidatos, vacío pero sin romperse) y las secciones numeradas se ven agrupadas correctamente.

- **10 de Septiembre de 2026 (8)** *(Estadísticas: rediseño real de los gráficos, no solo agrupación — el usuario avisó que la entrada anterior "no se ve muy diferente", [PostulacionesStatsTab.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/admin/postulaciones/components/PostulacionesStatsTab.jsx) [MODIFY])*:
  - **El problema real**: la entrada anterior (10/sep, #7) reordenó los paneles existentes bajo encabezados numerados pero **reusó los mismos tipos de gráfico de siempre** (barras horizontales de progreso, barras verticales lisas, un conic-gradient de un solo tono) — el usuario lo notó de inmediato: *"no veo que hayas mejorado las estadísticas... en el archivo que te pasé se ven muy diferente, en realidad"*. El mockup usa 4 tipos de visualización que el panel real nunca tuvo: un anillo SVG **multi-segmento** (uno por opción, no un solo tono), barras verticales con la opción líder resaltada en degradé contra el resto en gris parejo, un gráfico de **línea + área** (SVG a mano, sin librería) para la serie por día, y listas de "punto sobre una línea" para distribuciones con muchas opciones.
  - **Se volvió a inspeccionar el mockup**, esta vez extrayendo el `outerHTML`/estilos reales de cada componente vía `javascript_tool` (no solo capturas de pantalla) para copiar las proporciones exactas: anillo `r=52, stroke-width=16, viewBox 124×124, rotate(-90deg)` con `stroke-dasharray`/`stroke-dashoffset` por segmento; barras del embudo de 26px con el `%` de caída y el conteo en la misma fila que la etiqueta (no en una línea aparte arriba, como se había hecho antes); gráfico de área con `viewBox 320×160`, línea base en `y=144`, degradé de relleno que se desvanece hacia abajo (`linearGradient` vertical) y la línea en degradé diagonal azul→rosa.
  - **Componentes nuevos**: `Anillo` (SVG multi-segmento reusado tanto para "mayoría" de una pregunta como para la distribución de score por tramo — reemplaza el conic-gradient anterior, que solo podía mostrar un tono), `DonutLegend` (anillo + leyenda con puntos de color, para preguntas de pocas opciones: Conocimiento como closer, Disclaimer), `BarChartVertical` (barras verticales con la opción de mayor conteo en degradé y el resto en gris — reemplaza `BarraDistribucion` para Inglés e Histograma), `DotSliderList` (fila con línea+punto, para distribuciones con más opciones donde barras quedarían apretadas: País, Edad), `ChipCloud` (nube de chips ordenados por frecuencia con el más usado resaltado, para Herramientas), `AreaChart` (línea+área SVG a mano para Postulaciones por día, reemplaza el gráfico de barras). Las tarjetas KPI también sumaron una barrita de progreso pegada al pie (el % que representa esa cifra sobre su base), que no existía antes.
  - **Reagrupamiento**: se sumó una cuarta sección "04 Perfil de quienes se postulan" (Conocimiento/Inglés/Disclaimer/País/Edad/Herramientas, en una grilla de hasta 3 columnas) separada de "03 Calidad del pool" (que ahora solo tiene el anillo de score por tramo y el histograma) — antes las 8 tarjetas de distribución vivían todas mezcladas en la misma sección que el histograma y el donut de score.
  - **Verificación en navegador** (mismos datos reales — 165 completas, 490 postulaciones totales): el gráfico de área de "Postulaciones por día" dibuja correctamente el pico real del 03/sep con el degradé de relleno desvaneciéndose; el anillo de "Distribución de score" muestra 5 segmentos de color (14/36/67/30/18 por tramo) con "65 · score medio" al centro; "Conocimiento como closer" muestra "58% mayoría" con 3 opciones en la leyenda; "Nivel de inglés" resalta la barra de 64 (Básico) en degradé contra las otras 3 en gris; "Lugar de residencia" lista 15 países (Argentina 42 primero) sin que la tarjeta se vuelva inmanejable, gracias al formato compacto de fila; "Herramientas que usan" muestra los chips con "Zoom / Google Meet · 125" resaltado en rosa. Cambiar de segmento (ej. a "Seleccionados") recalcula todo correctamente y el panel de Embudo desaparece solo cuando el backend no lo manda (segmentos que no son "todos"), sin romper el grid.

- **10 de Septiembre de 2026 (9)** *(el formulario de calificación de un lead no aparecía en su agenda — reporte de Mario Closer sobre el lead "Iván", investigado entrando directo a producción, [financial_agendas.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/public/financial_agendas.py) [MODIFY])*:
  - **El pedido**: *"Mario Closer dice que Ivan completó su formulario pero no le aparece en el sistema"* — un lead agendado con Mario Closer para el 12/sep sí había completado el formulario de calificación, pero la pestaña "📋 Formulario" de su modal de confirmación seguía mostrando "Este lead no completó el formulario de calificación".
  - **Lo que estaba bien**: la agenda existía y estaba correctamente asignada — `Appointment` #6461 (cliente **Ivan Lopez**, `Client` #8753) apuntaba a Mario Closer para el 12/sep 13:00 sin ningún problema. El síntoma real era que `Client.form_data` de Ivan Lopez estaba en `None`, a pesar de que el webhook de n8n con sus respuestas sí había llegado — dos horas *antes* de que se creara su agenda (10/sep 20:10 vs. 22:18).
  - **La causa**: `POST /public/financial-agendas-form` (donde n8n manda las respuestas del formulario) busca al `Client` correspondiente por mail → instagram → teléfono y, cuando nada matchea (el caso normal cuando el formulario llega antes que la agenda, como acá), caía a un último intento: buscar una `FinancialAgenda` cuyo campo `lead` **o `nombre`** contuviera el nombre recibido. El problema es que `FinancialAgenda.nombre` no es el nombre del lead: es la fuente/atribución de la agenda (usuario de un setter, ej. `'Elias'`, `'Ivan'`, o un embudo como `'workshop'`). Como existe un setter real llamado **Ivan**, la búsqueda `ILIKE '%ivan%'` contra `nombre` devolvió todas las agendas atribuidas a ese setter y se quedó con la de fecha más futura — la de **Katherine Sandoval Hernández**, una lead sin ninguna relación con Ivan Lopez. El formulario de Ivan terminó escrito en el `Client` de Katherine, y el de Ivan quedó vacío.
  - **El fix**: se sacó `FinancialAgenda.nombre` del `OR` — el paso 3 del matching ahora busca solo por `FinancialAgenda.lead`, que sí es el nombre real del lead. Cualquier lead que comparta nombre de pila con un setter activo (Elias, Paula, Ivan, y los que se sumen después) deja de estar expuesto a este cruce.
  - **Reparación de los datos de este caso puntual**: hecha — las respuestas del `Client` de Katherine se movieron al de Ivan y las de Katherine se limpiaron (sin forma de recuperar lo que tuviera antes, si tenía algo). Verificado en producción simulando a Mario Closer: la ficha de Ivan Lopez ahora muestra sus 7 respuestas reales bajo "Formulario de calificación". Desplegado a `main`.

- **10 de Septiembre de 2026 (10)** *(backend del rediseño del modal de confirmación del closer a partir de un mockup nuevo, "Closer Workspace.html" — pedido explícito del usuario: "cuando abrís un lead al momento de confirmar... quiero que se vea como el HTML que te mandé", [booking.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/booking.py) [MODIFY], [3a8b192ec920_add_confirmation_stage_fields.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/migrations/versions/3a8b192ec920_add_confirmation_stage_fields.py) [NEW], [closer.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer.py) [MODIFY])*:
  - **Qué pedía el mockup que el modelo actual no podía representar**: hoy el paso "Proceso de confirmación" (`modalStep==='confirm'`) solo distingue 3 estados gruesos en `Appointment.result` (por_confirmar/conversando/Confirmado). El mockup muestra un stepper de 5 sub-etapas dentro de "conversando" — Por contactar/Contactado/Horario/VideoAsk/Testimonio — que el closer toca a medida que avanza la llamada de confirmación ("tocá la etapa a la que llegó, se guarda solo"), más dos grupos de chips: "Cómo viene" (Pendiente/A la espera de respuesta/No contesta) y "Dolores que contó" (8 tags libres). Ninguno de los tres es derivable de datos existentes.
  - **3 campos nuevos y aditivos en `Appointment`** (`confirmation_stage`, `confirmation_contact_status`, `confirmation_pain_points`, los 3 `nullable=True`, `None` tratado como el primer valor de cada lista): se agregan sin tocar `result`/`closer_result`, que siguen siendo la fuente de verdad para `confirmationsPipeline`/Kanban/`heroLead`/reportes de siempre — la etapa granular solo maneja la presentación del wizard nuevo. **Por qué no reutilizar `last_contact_outcome`** para "Cómo viene": ese campo ya existe pero con un vocabulario y un propósito distintos (no_resp/contesto/agendo/cerro/pago, el resultado de un intento de contacto de SEGUIMIENTO — lo consume `CloserFollowUpService`/`CloserService` para medir actividad de seguimientos); escribirle desde el flujo de confirmación con valores tipo "pendiente"/"no_contesta" hubiera ensuciado esas métricas con toques que no son seguimientos.
  - **`process_closer_card` (`POST /closer/deck/<id>`)** acepta los 3 campos nuevos como opcionales (mismo patrón `if 'x' in data` que ya usa el resto de la función) y los suma a `confirm_only_keys` — sin esto, el autoguardado del wizard (que manda `confirmation_stage` junto con `confirm_status`/`closer_notes` en cada toque) hubiera caído en el `else` que marca `closer_processed=True`, sacando al lead del mazo de confirmaciones antes de llegar a Testimonio.
  - **Los 3 campos se exponen en `GET /closer/deck` y `GET /closer/deck/card/<id>` parcheando el dict *después* de llamar a `_format_appointment_for_deck`** (`formatted["confirmation_stage"] = a.confirmation_stage`, etc.), en vez de agregarlos al `return {...}` de esa función — está explícitamente fuera de alcance para esta tarea (en revisión paralela por otra sesión, por un fix sin relación con esto) y el propio archivo ya tenía el mismo patrón un poco más abajo (`formatted["unread_comment"] = ...`), así que no es un atajo nuevo.
  - **Verificación de base — migración de Alembic vs. SQLite local, un problema preexistente sin relación con este cambio**: `flask db upgrade` contra un `instance/local.db` nuevo falla a mitad de camino en una migración vieja (`2c6524b78276`) con `No support for ALTER of constraints in SQLite dialect` — presumiblemente escrita/probada solo contra Postgres (el dialecto real de producción), nunca contra SQLite. No se tocó esa migración (no es parte de este pedido y arreglarla es un cambio de alcance propio). Para poder levantar un `local.db` utilizable en esta sesión se usó `db.create_all()` directo sobre los modelos actuales + `flask db stamp head` (deja el historial de Alembic apuntando al head real sin haber reproducido la falla), documentado en `scratch/setup_local_db.py` (no comiteado). La migración nueva de esta entrada (`3a8b192ec920`) sigue el patrón estándar de Alembic/`batch_alter_table` de siempre — el problema es exclusivo de esa migración vieja, no de cómo se escriben migraciones en este proyecto.
  - **Verificación funcional**: `scratch/seed_confirm_flow_test.py` (no comiteado) crea un closer/setter/cliente/cita de prueba con `Client.form_data` poblado; contra ese dato, `POST /closer/deck/<id>` con `confirmation_stage`/`confirmation_contact_status`/`confirmation_pain_points` persiste los 3 campos y deriva `result` correctamente sin marcar `closer_processed=True` (confirmado leyendo `instance/local.db` directo). El resto de la verificación (que el frontend nuevo realmente los use) queda en la entrada siguiente.

- **10 de Septiembre de 2026 (11)** *(frontend del rediseño: el modal de confirmación del closer pasa a ser un wizard de 5 etapas, a partir del mismo mockup "Closer Workspace.html" — el usuario aclaró que esto iba a cambiar bastante el flujo y que corregirlo "poco a poco" después es lo esperado, [CloserWorkflowPage.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/CloserWorkflowPage.jsx) [MODIFY], [index.css](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/index.css) [MODIFY])*:
  - **Cómo se leyó el mockup**: servido con `python -m http.server` y abierto en el navegador (es un bundle de Claude-Design de ~660KB, denso para `Read` directo); resultó ser DOM plano normal (nada de shadow DOM/canvas, solo una búsqueda de texto mal armada al principio hizo parecer lo contrario), así que se pudo interactuar de verdad — tocar las 5 etapas, las pestañas, y ver el estado "ya confirmado" — y extraer `outerHTML`/`getComputedStyle` reales por `javascript_tool` en vez de adivinar desde capturas.
  - **Las 5 etapas y su mapeo al pipeline de 3 estados de siempre**: `CONFIRM_STAGES` (Por contactar/Contactado/Horario/VideoAsk/Testimonio) + `stageToConfirmStatus` deriva `confirm_status` SIEMPRE desde la etapa nueva — "Por contactar" → `por_confirmar`, cualquier etapa intermedia → `conversando`, y `Confirmado` solo lo pone la acción explícita "Listo · 100% confirmado" (llegar a Testimonio por sí solo no confirma). Así el Kanban/`heroLead`/reportes de "Cerrar el día" no necesitaron ningún cambio: siguen leyendo el mismo `result` de siempre.
  - **`saveConfirmStage` — autoguardado real, no un botón "Guardar"**: el mockup dice literalmente "se guarda solo" bajo el stepper, así que cada toque de etapa/chip dispara un `POST /closer/deck/<id>` de una (sin botón intermedio); la nota de texto se guarda al perder el foco. **Bug encontrado y corregido en la propia verificación**: la primera versión solo actualizaba `result` en el `selectedLead`/`agendas` en memoria tras guardar — cerrar la ficha y reabrirla desde la misma tarjeta del Kanban (sin pasar por un `fetchAgendas()` de por medio) hacía que `handleSelectLead` leyera un `lead.confirmation_stage` desactualizado y reiniciara el wizard a "Por contactar" aunque el backend ya tuviera la etapa correcta guardada. Fix: el patch optimista ahora incluye `confirmation_stage`/`confirmation_contact_status`/`confirmation_pain_points`, no solo `result`.
  - **"Listo · 100% confirmado"** (solo habilitado al llegar a Testimonio) reusa intacta la lógica de celebración/racha existente (`confirmadosHoy`, `ORDINALES`, `pcRemaining`/`cvRemaining`, el modal 🏆/✅) — nada de eso se reescribió. Cambio de comportamiento deliberado: **el modal ya no se cierra solo al confirmar** — el mockup deja el wizard abierto mostrando un panel de éxito ("Lead 100% confirmado", con "Deshacer"/"Volver al board"), así que cerrar pasó a ser una elección del closer en vez de automático. "Deshacer" vuelve el `result` a `conversando` sin perder la etapa marcada (sigue en Testimonio, listo para volver a tocar "Listo").
  - **Decisiones/supuestos que el usuario podría querer corregir**:
    - Un lead ya "Confirmado" abierto desde 01 Confirmar entra ahora al wizard (mostrando su estado ya completo) **en vez de saltar directo al reporte de llamada**, como hacía antes — se decidió así porque el mockup abre a un lead ya confirmado (German Pazos) exactamente en esta misma vista, no en el reporte de llamada; el wizard pasa a ser la vista durable de todo el pipeline de confirmación, y "reportar la llamada" se sigue haciendo desde la pestaña 02 Reportar.
    - El botón "Reprogramar" (Reagendar) subió al header como acción siempre visible — antes solo aparecía una vez que el lead pasaba a "conversando" ("si todavía no respondió, no hay nada que reagendar"). Se relajó esa restricción porque el mockup lo muestra siempre en el header, incluso en "Por contactar".
    - "Descargar lead" (botón nuevo del mockup, sin equivalente en la app) se implementó como copiar un resumen de contacto al portapapeles en vez de generar un archivo descargable — pareció más útil para un closer moviéndose entre llamadas y evita la complejidad/permisos de una descarga real. Es la pieza más inventada de todo el cambio; fácil de cambiar si no es lo que se imaginaba.
    - "Descartar lead" y "marcar No show" (acciones de backend existentes que había que preservar sí o sí) NO están dibujadas en el mockup — se agregaron como enlaces chicos y discretos al pie del wizard en vez de recrear la grilla vieja de "Otras acciones", para no romper la estética limpia del mockup pero sin dejarlas inalcanzables.
    - Mientras el lead está en el panel de éxito ("Confirmado"), el stepper y los chips quedan deshabilitados hasta tocar "Deshacer" — el mockup no aclara si deberían seguir editables ahí mismo; se optó por bloquear para que un toque accidental no desarme una confirmación ya hecha.
  - **Qué NO se tocó a propósito**: las pestañas "Formulario" y "Setter" del mismo modal — ya cubrían exactamente lo que el mockup pide (`Client.form_data` vía `getFormDataAnswers`/`renderFormQuestion`, e hilo de comentarios + nota rápida con menciones vía `CommentsSection`/`addLeadNote`) y no eran el pedido explícito de este cambio ("al momento de confirmar"). El mockup sí muestra una reorganización más profunda de "Formulario" (sub-pestañas Antes de llamar/Datos del lead/Calificación, con los datos de `form_data` agrupados por categoría en vez de en lista plana) que se dejó fuera a propósito: hubiera exigido inventar una heurística para clasificar respuestas arbitrarias de `form_data` en esas categorías, un cambio de alcance propio y más riesgoso que el pedido original.
  - **Shell visual**: `.md` (la tarjeta del modal) pasó del degradé índigo v7 (`#111862`→`#0A0E3D`, radio 24px) al navy casi negro del mockup (`rgba(2,6,23,.9)`, radio 32px) — verificado con `getComputedStyle` sobre el mockup real, no asumido a partir del resto de la app (que sí comparte el rosa `#FF3FA4`/verde `#7DEAC0`/ámbar `#D9A441` de siempre, esos si se reusaron tal cual). El stepper viejo de 3 cajas (`.pipe`/`.pstep`) se reemplazó por uno nuevo de 5 nodos (`.cfstages`/`.cfstage`) y quedó sin ningún uso — se borró de `index.css` junto con `.parrow`, en vez de dejarlo muerto.
  - **Verificación en navegador**: sesión de closer simulada (`testcloser`, JWT generado directo con `User.get_auth_token()` e inyectado en `localStorage` como `auth_token`/`user`, sin pasar por `/auth/login` por el CSRF de sesión) contra el `local.db` sembrado. Camino completo: abrir el lead en "Por contactar" → tocar las 5 etapas una por una (cada toque persistido, confirmado en SQLite entre pasos) → marcar "Ansiedad" y "A la espera de respuesta" (colores ámbar/azul distintos, como en el mockup) → escribir una nota → "Listo · 100% confirmado" → se dispara la celebración 🏆 existente → panel de éxito → "Deshacer" (vuelve a conversando, stepper se mantiene completo) → "Listo" de nuevo → "Volver al board" (la tarjeta aparece en la columna "Confirmado" del Kanban con el badge "✓ Listo · espera su fecha" de siempre). Además: pestaña "Formulario" muestra las 8 respuestas del `form_data` sembrado sin cambios; "Reprogramar" abre el flujo de reagendado existente (`reagQ`) correctamente; "Descartar lead" completa el flujo existente y saca al lead del pipeline; "Descargar lead" falló silenciosamente con un toast de error prolijo en este navegador de prueba en particular (permisos de portapapeles del entorno sandboxeado, no del código — `navigator.clipboard.writeText` es la API estándar y el error queda contenido en un try/catch). Screenshots tomados en cada paso. `npm run dev` no mostró errores de compilación ni de consola atribuibles a este cambio.
  - **Nota operativa de infraestructura, no del código**: en este entorno el navegador de verificación no pudo conectar a Vite cuando bindeaba solo a `::1` (default en esta máquina para `vite`/`npm run dev` corridos vía el mecanismo de preview con nombre) — hizo falta levantar `vite` manual (`npx vite --port <N>`, que sí bindeó a `0.0.0.0`) para poder navegar. Ninguno de los archivos de configuración del repo quedó modificado por esto (se probó y revirtió `vite.config.js`/`.claude/launch.json` explícitamente); queda solo como dato para la próxima sesión que necesite levantar el frontend en este entorno.

- **10 de Septiembre de 2026 (12)** *(acción "Eliminar agenda" para citas ya canceladas en Agendas de Mi cartera — pedido explícito del usuario a partir del caso de Mia Sky: "esa agenda realmente no va ahí ese día... quiero eliminarla", [CarteraAgendasPane.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/components/CarteraAgendasPane.jsx) [MODIFY])*:
  - **El pedido, y lo que ya estaba resuelto sin que hiciera falta tocar código**: el usuario reportó que el conteo de agendas de Nerina mostraba 8 en vez de 7 por la duplicada de "Mia Sky" investigada en una entrada anterior — pero al revisar de nuevo, tanto la alerta de "posible duplicado" como el botón "Marcar como duplicada" ya existían en esta misma pestaña (construidos en una sesión previa), y el registro de auditoría mostró que Nerina ya la había marcado ella misma ese mismo día a las 15:46, horas antes de este intercambio. Lo que quedaba pendiente era otra cosa: aunque ya cancelada, la cita seguía contando en "Agendas en el período" (`CloserAgendasService.get_ledger` suma TODAS las citas del período a propósito, sin importar el estado, porque esta pestaña es una auditoría que no esconde nada) — el usuario, al ver que seguía ahí, pidió directamente poder **eliminarla** de verdad, y que cualquier closer pudiera hacerlo cuando hiciera falta, con una confirmación de por medio.
  - **No hizo falta backend nuevo**: ya existía `DELETE /closer/deck/<id>` (usado por "Descartar lead" en el modal de confirmación) — borra la fila de `Appointment` de verdad (no soft-delete), libera las `SurveyAnswer` asociadas sin cascada y borra el evento de Google Calendar si lo hay. Se decidió reusarlo tal cual en vez de escribir un endpoint nuevo o inventar un soft-delete: ya estaba probado en producción y hace exactamente lo que se pidió.
  - **La restricción es del lado del frontend, a propósito**: el endpoint en sí no exige ningún estado (borraría cualquier cita del closer, tenga o no un resultado real reportado). El botón "Eliminar agenda" nuevo solo se ofrece en filas con `estado === 'cancelada'` — nunca sobre un show up, no show, etc. — para que esta lista de auditoría no pueda usarse para borrar un resultado real por error. `window.confirm` con el nombre del lead y la fecha, avisando que no se puede deshacer, mismo patrón que ya usaba "Marcar como duplicada" en este archivo.
  - **Verificación en local, no en producción** (el endpoint borra de verdad, sin vuelta atrás): se sirvió el backend en un puerto alternativo (5050, con el de siempre ocupado por un proceso zombi de otra sesión en esta máquina que no soltaba el puerto) y se apuntó `vite.config.js` ahí temporalmente (revertido al terminar). El `local.db` de esta máquina resultó ser un espejo bastante fiel de producción (mismos nombres: Nerina, Mia Sky, Katherine, hasta la misma deuda de $900) — iniciando sesión como Kerwin y simulando a Nerina, la pestaña Agendas mostró el mismo "8 · Cancelada 1" que en producción. Tocar "Eliminar agenda" en la cita duplicada de Mia Sky disparó `DELETE /closer/deck/6396 → 200`, la fila desapareció de la lista, y "Agendas en el período" pasó de **8 a 7** — confirmando el comportamiento pedido de punta a punta antes de tocar el dato real de producción.

- **10 de Septiembre de 2026 (13)** *(el closer puede configurar cuánto se cobra en cada cuota del plan de un cliente, desde "Ver historial completo del cliente" — pedido explícito del usuario: "ahí debe poder configurar cuánto paga esa persona en cada cuota, la última se calcula sola", [ClientHistoryModal.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/components/shared/ClientHistoryModal.jsx) [MODIFY], [installment_service.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/services/installment_service.py) [MODIFY], [closer_installments.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/api/closer_installments.py) [MODIFY], [installment.py](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/app/models/installment.py) [MODIFY])*:
  - **Lo que ya existía y lo que faltaba**: el sistema de cuotas (`InstallmentPlan`, un plan = varias filas, una por cuota) ya soportaba montos personalizados por cuota del lado del backend — `PATCH /closer/installments/cuota/<id>` ya aceptaba `monto`, y `InstallmentService.create_plan` ya sabía recibir una lista `montos` custom donde la última posición siempre se recalcula como "lo que falta" para que la suma cierre exacto contra el saldo. Pero **nada del frontend usaba ninguna de las dos cosas**: `CuotaRow` (cuota ya creada) solo dejaba editar la fecha de vencimiento, nunca el monto pactado — el campo `montoPagado` que sí existía era para reportar cuánto entró al cobrar, no para ajustar el plan de antemano; y `CreatePlanForm` (armar un plan nuevo) solo mostraba el reparto parejo calculado, sin ningún input para escribirlo distinto ni mandaba nunca `montos` al crear.
  - **El backend sí necesitó un cambio real**: `InstallmentService.update_cuota` (usado al editar una cuota YA EXISTENTE) no tenía ningún ajuste en cascada — cambiar el monto de una cuota puntual la dejaba suelta, sin tocar el resto, así que la suma del plan dejaba de cerrar contra el saldo real. Se le agregó la misma regla que ya aplicaba `create_plan` al armar un plan nuevo: al cambiar el monto de una cuota que NO es la última pendiente del plan (mismo `client_id` + `programa_code`), la diferencia se resta/suma a la última cuota todavía pendiente, para que la suma de lo pendiente no se mueva. Si la cuota editada YA ES esa última, se guarda tal cual (no hay ninguna otra después para absorber nada). El endpoint ahora devuelve `{cuota, ajustada}` en vez de solo `cuota` cuando tuvo que tocar una segunda fila, para que el frontend pueda reflejar las dos sin un refetch completo — se agregó `client_id` a `InstallmentPlan.to_dict()` (no venía) porque el frontend lo necesita para agrupar las cuotas de un mismo cliente por plan.
  - **Frontend — `CuotaRow`**: recibe un nuevo prop `esUltimaPendiente` (calculado en el componente padre agrupando `history.installments` por `client_id + programa_code`, ya que un cliente puede tener más de un plan si compró más de un programa). El monto pasa a ser un input editable con su propio botón de guardar (mismo patrón que ya existía para la fecha) en cualquier cuota pendiente que NO sea la última de su plan; la última se sigue mostrando como texto fijo, con un tooltip que aclara que se ajusta sola. Una cuota ya pagada tampoco se puede editar (no tiene sentido tocar retroactivamente algo que ya se cobró).
  - **Frontend — `CreatePlanForm`**: cada cuota salvo la última pasa a tener un input de monto (precargado con el reparto parejo, editable); la última se muestra ya calculada en vivo (deuda menos la suma de las anteriores) a medida que se escribe, con un aviso si el closer tipeó de más y la última quedaría negativa. Al crear, ahora sí se manda `montos` al backend en vez de dejar que siempre reparta parejo.
  - **Verificación en local, con datos reales**: simulando a Nerina, "Susett Anahí Cochachín Luna" tenía un plan real de 3 cuotas de $300 (suman los $900 que debía). Cambiar la Cuota 1 de $300 a $200 y guardar hizo que la Cuota 3 (última pendiente) pasara de $300 a **$400** automáticamente, sin tocar la Cuota 2 — la suma se mantuvo en $900 en todo momento. Se revirtió el cambio de prueba (Cuota 1 de vuelta a $300, Cuota 3 de vuelta a $300) para no dejar el dato de prueba alterado.

- **10 de Septiembre de 2026 (14)** *(el número "Agendas" del embudo de Ver mis datos ahora muestra al lado, en otro color, cuántas hay agendadas para después de hoy — pedido explícito del usuario tras investigar por qué esa cifra no le cuadraba, [PerformanceFunnel.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceFunnel.jsx) [MODIFY], [CloserDashboard.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/CloserDashboard.jsx) [MODIFY])*:
  - **La investigación previa (agente en worktree aparte, sin cambios de código) descartó un bug**: "Agendas" en el embudo de "Ver mis datos" cuenta únicamente las citas cuya fecha de llamada ya pasó dentro del período — a propósito, no un descuido: mezclar ahí agendas que todavía no tuvieron su llamada había inflado antes la tasa de confirmación a 97.9% contra un 55.8% real (ver docstring de `_confirmations` en `closer_dashboard_service.py`). El usuario entendió la razón pero pidió una forma de ver, aun así, cuántas agendas futuras hay — para poder distinguir de un vistazo "esto es lo que ya generó mis show up/no show/cancelaciones/reagendadas" de "esto todavía está por pasar".
  - **El dato ya existía, solo no estaba en el lugar donde se lo esperaba**: `_confirmations` ya calculaba `agendas_proximas` (cuántas agendas hay con fecha posterior al período), pero solo se mostraba dentro de la tarjeta "Confirmaciones" en la sección 03, lejos del número "Agendas" del embudo en la sección 02. Se hizo pasar `confirmaciones` (que `CloserDashboard.jsx` ya tenía cargado) como prop nueva de `PerformanceFunnel`, y se agregó una pastilla celeste "+N próx." pegada a la barra de "Agendas" (visible solo si `agendas_proximas > 0`) — mismo dato, mostrado también donde el usuario lo estaba buscando, sin duplicar ningún cálculo ni tocar ninguna tasa de conversión existente.
  - **De paso**: se corrigió el tooltip del embudo, que decía que "Slots, agendas, asistencias y presentaciones salen de los reportes diarios" — desactualizado desde que el embudo pasó a calcularse desde la bandeja (`Appointment`/`FinancialSale`, ver `_stats_from_bandeja`); hoy solo Slots depende del reporte diario.
  - **Verificación en local**: simulando a Nerina con "Este mes", la fila "Agendas" mostró "7 · +1 próx. · 39%", coincidiendo exacto con "Próximas agendas — 0 de 1 por venir" de la tarjeta de Confirmaciones más abajo — mismo número, dos lugares.

- **10 de Septiembre de 2026 (15)** *(corrección de diseño de la entrada anterior: las agendas próximas pasan de una pastilla aparte a un tramo de otro color en la punta de la misma barra — el usuario avisó "así no, no me gusta" apenas lo vio, [PerformanceFunnel.jsx](file:///c:/Users/Usuario/Documents/GitHub/NeurOPS/frontend/src/pages/closer/dashboard/components/PerformanceFunnel.jsx) [MODIFY])*:
  - La pastilla "+1 próx." (elemento separado, al lado de la barra) no era lo que el usuario tenía en mente — pidió específicamente "que se vea la misma barra pero con otro color en la punta representando las agendas próximas". Se cambió a eso literal: el contenedor de la barra de "Agendas" pasa a `flex` con dos divs adyacentes — el tramo real (7, color del embudo) seguido del tramo de próximas (1, celeste `#38BDF8`, sin texto adentro por lo angosto que puede quedar, con el detalle en el `title`), en vez de un div único más un `<span>` aparte. El ancho de este segundo tramo tiene un mínimo de 4% para que no desaparezca visualmente cuando `agendas_proximas` es chico contra `Slots`. Nunca llegó a pushearse la versión con pastilla — se corrigió antes del primer deploy de esta funcionalidad.
