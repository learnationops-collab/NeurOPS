# Bitácora - Mayo 2026

- **1 de Mayo de 2026**:
  - **Analítica Conversacional para Anuncios**: Implementación de una vista especializada para medir la efectividad de los mensajes de inicio y seguimientos automáticos de ManyChat.
    - **Base de Datos**: Se extendió el modelo `LeadAnswer` agregando las columnas `id_option` (para registrar el gancho o mensaje inicial) e `id_question` (para registrar el nivel de seguimiento al que se respondió). Migración generada y aplicada.
    - **API & Ingesta**: Actualización del endpoint del webhook (`/manychat-webhook`) para capturar y persistir estas nuevas variables, y de `/manychat-webhook/ad-details/<ad_id>` para realizar la agregación de datos (`option_breakdown` y `question_breakdown`).
    - **Interfaz (Frontend)**: Reestructuración del componente `AdDetailModal.jsx` incorporando un sistema de pestañas ("Rendimiento" y "Conversaciones"). La pestaña de "Conversaciones" presenta un diseño intuitivo con tarjetas y barras de progreso porcentuales para identificar rápidamente la opción inicial ganadora y los seguimientos más efectivos.
    - **Corrección de Métricas**: Se forzó la inicialización de "Opción 1, 2, 3" y "Seguimiento 1, 2, 3" en cero desde la agregación de la API para garantizar su visibilidad constante, además de corregir un error matemático (`NaN%`) en el frontend cuando el total de interacciones era cero.
- **4 de Mayo de 2026**:
  - **Generador de UTMs y Rastreo de Audiencia**: Implementación de una herramienta interna para la creación estandarizada de URLs de campaña para landings en Hostinger.
    - **Interfaz (Frontend)**: Creación del componente `UTMGenerator.jsx` con diseño Dark Glassmorphism. Incluye validación de URLs, formateo automático (minúsculas, guiones) y función de copiado al portapapeles con feedback visual.
    - **Integración**: Incorporación del generador en el "Panel de Control Técnico" (`OperationsSettingsPage.jsx`) bajo la sección "Marketing UTMs".
    - **Base de Datos**: Creación del modelo `UTMLog` para registrar la generación de enlaces, incluyendo usuario, URL base, URL final y parámetros UTM utilizados. Migración generada y aplicada.
    - **API (Backend)**: Implementación de endpoints `POST /api/marketing/utm-logs` para el registro automático y `GET /api/marketing/utm-logs` para consulta del historial.
    - **Refinamientos de UI/UX**:
      - Limpieza visual eliminando elementos decorativos que interferían con la lectura.
      - Implementación de **Tooltips informativos** en `utm_medium` utilizando un sistema de hover con `HelpCircle` y alta visibilidad (`z-index`).
      - Estandarización de la URL base por defecto a `https://institute.thelearnation.com/`.
      - Corrección de estilos y clases de Tailwind para asegurar la consistencia del tema Dark Glassmorphism.
      - **Integración para Administradores**: Incorporación del generador en el **Marketing Intelligence Hub** (`AdminMarketingHubPage.jsx`) mediante un sistema de pestañas.

- **5 de Mayo de 2026**:
  - **Sistema de Rastreo de Campañas Externas**: Centralización de métricas para landings alojadas en Hostinger.
    - **Base de Datos**: Creación del modelo `LandingTracking` para capturar UTMs, ruta de página y referrer. Migración generada y aplicada.
    - **Seguridad (CORS)**: Configuración de autorización para el origen `https://institute.thelearnation.com`, permitiendo peticiones POST con credenciales.
    - **Dashboard Administrativo**: Implementación de una nueva pestaña "Tráfico Landings" en el **Marketing Intelligence Hub**.
    - **API & Gestión**: 
      - Implementación de `POST /api/v1/metrics/track-visit` para la ingesta de datos de tráfico externo.
      - Implementación de `GET /api/v1/metrics/track-visits` para consulta de tráfico histórico.
      - Implementación de `DELETE /api/v1/metrics/track-visit/<id>` para la depuración de registros de prueba.
    - **Interfaz (Frontend)**: Creación del componente `LandingTrafficTable.jsx` con filtrado dinámico, estados de carga y acciones de gestión (borrado) integradas.
  - **Fix: Envío de Reporte de Setter a Discord**: Corrección de un bug que impedía que los reportes diarios de setters llegaran al canal de Discord.
    - **Causa Raíz**: Al iterar sobre `stat.answers` para construir los callouts cualitativos, la clave especial `'frequent_questions'` (almacenada como metadata en el JSON) causaba un `ValueError: invalid literal for int() with base 10` al intentar convertirla a entero. Esto propagaba la excepción hasta el bloque `try/except` que también envolvía el `db.session.commit()`, ejecutando un `rollback()` innecesario y devolviendo un error 500 al frontend.
    - **Archivos Modificados**:
      - `app/api/setter.py`: Adición de un guard `if not str(q_id).isdigit(): continue` para ignorar claves no numéricas en el diccionario de respuestas. Separación del bloque `try/except` del commit del bloque del webhook de Discord para que un fallo en el envío no comprometa el guardado en BD.
      - `app/api/public/closer.py`: Misma separación preventiva del `try/except` aplicada para garantizar consistencia y robustez.
  - **Cleanup: Supresión de errores de D-Bus en logs del servidor**: Los logs del servidor mostraban múltiples líneas `ERROR:dbus/bus.cc` generadas por Chromium (html2image) al intentar conectarse al D-Bus del sistema operativo, inexistente en el contenedor Docker de Railway. Se agregaron los flags `--disable-dbus` y `--disable-extensions` a los `custom_flags` de todos los métodos de `ImageService` para suprimir este ruido en producción.
  - **Feature: Sección "Daily Reflection" en reportes de Setters y Closers**: Rediseño completo de la sección cualitativa de los formularios de reporte diario, reemplazando las preguntas genéricas sin contexto por 6 preguntas estructuradas y detalladas para ambos roles.
    - **Componente compartido**: Creación de `frontend/src/components/reports/DailyReflectionSection.jsx`, componente reutilizable colapsable con header clicable, animación de expand/collapse y listado de 6 preguntas con tooltips de `description` + `info` al hover sobre el icono de info.
    - **Preguntas Closer**: Daily Reflection, Win of the day, Explain why, Opportunity, The Learning Process, The Plan.
    - **Preguntas Setter**: Daily Operations, Win of the day, Explain why, Opportunity, The Learning Process, The Plan.
    - **Estandarización de Daily Reflection (Páginas Públicas):**
      - Integración del componente `DailyReflectionSection` en las rutas `/setter/report` y `/closer/report`.
      - Eliminación de la dependencia de preguntas dinámicas en la BD para estas vistas, fijando las 6 preguntas estándar en el frontend.
    - **Sistema de Reportes Visuales en Discord:**
      - Creación de `app/templates/reports/reflection_image.html` con diseño premium.
      - Nuevo método `generate_reflection_card` en `ImageService`.
      - Actualización de webhooks para enviar dos imágenes (Métricas + Reflexión) en un solo mensaje de Discord.
    - **Rediseño Premium de Setter (Dark Mode):**
      - Transformación completa de `PublicSetterReportPage.jsx` a tema oscuro (`bg-slate-950`).
      - Unificación estética con el reporte de Closers para coherencia visual de marca.
    - **Correcciones Técnicas:**
      - Reparación de sintaxis en `PublicCloserReportPage.jsx`.
      - Restauración de lógica de webhook en `setter.py`.
    - **Base de Datos**: Adición del campo `reflections JSON` a los modelos `CloserDailyStats`, `SetterDailyStats` y `CloserDailyReport`. Tres migraciones generadas y aplicadas.
    - **API (Backend)**: Actualización de los endpoints `POST /closer/daily-report`, `POST /setter/daily-report` y `PUT /public/closer-reports/<id>` para persistir el campo `reflections`. Actualización de los `GET` correspondientes para incluir `reflections` en la respuesta.
    - **Archivos Modificados**: `app/models/report.py`, `app/models/closer_report.py`, `app/api/closer.py`, `app/api/setter.py`, `app/api/public/closer.py`, `CloserDashboard.jsx`, `SetterReportModal.jsx`.

- **7 de Mayo de 2026**:
  - **Fix: Atribución de Ventas en Rendimiento de Anuncios**: Corrección de la lógica de atribución en `GET /api/v1/marketing/ads/performance` para reflejar las ventas correctamente por anuncio.
    - **Causa Raíz (Falla 1)**: Las ventas sin campo `instagram` eran descartadas silenciosamente sin ningún fallback, perdiendo ventas cuyo origen sí era rastreable por otro campo.
    - **Causa Raíz (Falla 2)**: El lookup de `LeadAnswer` estaba restringido a `ad_id.in_(ad_ids)`, donde `ad_ids` solo contenía anuncios activos en el periodo consultado. Esto excluía ventas de leads captados en periodos anteriores (ciclos de conversión largos), nunca atribuyéndolas a ningún anuncio.
    - **Correcciones aplicadas (`app/api/marketing.py`)**:
      - Fallback por `nombre_cliente`: si una venta no tiene instagram o el instagram no matchea ningún `ManychatLead`, se intenta buscar el lead por `nombre_cliente` (normalizado a lowercase).
      - Lookup sin restricción de periodo: se eliminó el filtro `LeadAnswer.ad_id.in_(ad_ids)` y se reemplazó por `LeadAnswer.ad_id != None`, permitiendo atribuir ventas a anuncios de cualquier periodo siempre que el lead tenga un `LeadAnswer` anterior a la fecha de la venta.
  - **Feature: Registro y Gestión Centralizada de Ventas**: Creación de una herramienta de visualización y corrección manual para la base de datos de ventas (Google Sheets / Base de Datos).
    - **Frontend (`PublicFinancialSalesPage.jsx`)**: Nuevo componente en formato Data Table que lista las ventas, destacando visualmente los registros a los que les falta el campo `@ Instagram` (alerta roja). Incluye buscador y funcionalidad de edición "inline" rápida, así como un botón para forzar sincronización con la hoja externa.
    - **Integración de UI**:
      - Se agregó la pestaña **Registro Ventas** en el hub administrativo (`AdminSalesHubPage.jsx`) para control general.
      - Se agregó la pestaña **Registro Ventas** en el panel de Closers (`PublicCloserStatsPage.jsx`) para que ellos mismos auditen sus registros.
    - **Backend (`app/api/public/__init__.py`)**: Ampliación del endpoint `PUT /public/financial-sales/<sale_id>` para permitir la actualización explícita de `instagram`, `nombre_cliente` y `email_vendedor`.
  - **Feature: Obligatoriedad de Instagram en Ventas Manuales**:
    - **Frontend (`NewSalePage.jsx`)**: Se añadió el campo `@ Instagram` marcado como obligatorio en el formulario para registrar nuevas ventas. Al seleccionar un lead previamente existente, se autocompleta el campo `instagram` recuperándolo de los metadatos.
    - **Backend (`app/services/closer_service.py`)**: Se incluyó el retorno del `instagram` del lead dentro de `get_sale_metadata` y se modificó `register_sale` para que, al recibirse el campo, actualice el perfil del cliente en la base de datos antes de registrar la venta.
  - **Refinamiento: Reordenamiento de Columnas en Tabla de Rendimiento**:
    - **Frontend (`AdDashboardTab.jsx`)**: Se reorganizó el orden de las columnas en la tabla de rendimiento de anuncios (vista lista) para presentar la información en la secuencia: Anuncio, Inversión, Leads, CPL, % Cual., CPQL, Agendas, CPA, Ventas, CPV.
  - **Feature: Cartera de Clientes para Closers**: Implementación de una sección dedicada a la gestión de alumnos y pagos activos para optimizar el seguimiento post-venta.
    - **Base de Datos & Backend**:
        - Implementación de `get_customers_pagination` en `CloserService` para listar clientes únicos con inscripciones vinculadas al closer.
        - Nuevos endpoints en `app/api/closer.py`: `GET /customers` para el listado paginado con resumen financiero (total pagado vs precio programa) y `PATCH /customers/<id>` para actualización de datos de perfil.
    - **Interfaz (Frontend)**:
        - Creación de `CloserClientsPage.jsx` con diseño de lista de alta densidad, barras de progreso de pagos dinámicas y acceso directo a la gestión de cuotas.
        - Actualización de `SaleDetailModal.jsx` incorporando un modo de edición inline para corregir datos de contacto (Nombre, Email, Teléfono, Instagram) desde la pestaña de Perfil.
    - **Navegación & UX**:
        - Integración de la sección como una nueva pestaña **"Cartera Clientes"** en el hub principal (`PublicCloserStatsPage.jsx`).
        - Actualización del **Dock** (`useDockNavigation.js`) para incluir accesos directos a "Dashboard", "Reporte" y "Clientes" para closers, y acceso a "Clientes" para administradores.
        - Registro de la ruta `/closer/clients` en `App.jsx`.
        - **Refinamiento UI**: Transición de diseño basado en tarjetas a una tabla de datos premium para optimizar la visualización de grandes volúmenes de clientes.

- **8 de Mayo de 2026**:
  - **Reubicación de Rendimiento Conversacional**: 
    - **Frontend**: Se reubicó la vista de *Rendimiento Conversacional* desde el panel de Marketing (`AdManagementPage.jsx`) hacia la sección de *Setters* (`PublicSetterStatsPage.jsx`). Esto permite separar la analítica de interacciones del bot del panel de control de inversión publicitaria.
    - Se eliminó la pestaña original y se integró como una nueva pestaña en el Performance Center de Setters.
  - **Feature: Estadística de Agendas para Administradores**:
    - **Frontend (`FinancialAgendasPage.jsx`)**: Se mejoró la vista de Agendas (alimentada desde Sheets) para incluir filtros de fecha y un panel de sumario dinámico que consolida la cantidad de agendas generadas por Closer.
    - **Integración (`AdminSalesHubPage.jsx`)**: Se agregó la vista como una nueva pestaña **"Registro Agendas"** dentro del hub principal de Ventas.
    - **Corrección de Layout**: Se ajustó el espaciado superior del contenedor (`pt-24`) para evitar que el selector flotante de pestañas se superpusiera a los filtros de búsqueda.

- **11 de Mayo de 2026**:
  - **Mejora UI: Copiado Rápido de Instagram**: Se agregó un botón interactivo (ícono Copy de lucide-react) en la tabla "Registro Agendas" (`FinancialAgendasPage.jsx`) que permite a los administradores copiar rápidamente al portapapeles el nombre de usuario de Instagram del lead.
  - **Feature: Agendas en Panel de Closers**: Se integró la vista `FinancialAgendasPage` como una nueva pestaña en el hub de rendimiento de Closers (`PublicCloserStatsPage.jsx`), otorgándoles acceso directo al registro histórico y estado de sus citas asignadas.
  - **Limpieza de UI Admin**: Se eliminaron las pestañas redundantes "Registro Ventas" y "Registro Agendas" del menú superior principal en `AdminSalesHubPage.jsx`, ya que ambas ahora están integradas jerárquicamente dentro de la vista de Closers.
  - **Feature: Totales en Rendimiento de Anuncios**: Se incorporó una fila de "Total General" fijada al inicio de la tabla en la vista de lista del Rendimiento por Anuncio (`AdDashboardTab.jsx`), que suma la inversión, leads, agendas y ventas, además de recalcular dinámicamente las métricas de costo (CPL, CPQL, CPA, CPV) y porcentaje de cualificación global.
  - **Feature: Métrica Cash Collect en Rendimiento de Anuncios**: Se agregó la métrica "Cash Collect Promedio" en la tabla de vista de lista del Rendimiento por Anuncio (`AdDashboardTab.jsx`). Esta métrica promedia el monto de ventas reales (primer pago o pago completo > $100) atribuibles a cada anuncio, mostrando también el valor global en la fila de "Total General".
  - **Feature: Métrica de ROI en Rendimiento de Anuncios**: Se añadió la columna de "ROI" (Retorno de Inversión) calculada como la diferencia directa entre el Cash Collect Promedio y el Costo por Venta (CPV) de cada anuncio. Se integró tanto en las filas dinámicas como en la fila de "Total General", incorporando una codificación visual de colores (verde para positivo, rojo para negativo).

- **12 de Mayo de 2026**:
  - **Optimización de Layout (High Density UI)**: Refactorización masiva de los contenedores principales en todos los dashboards para maximizar el uso del espacio horizontal.
    - **Cambio Técnico**: Se reemplazaron los límites de ancho fijo (ej. `max-w-7xl`, `max-w-5xl`) por un ancho fluido del `98%` (`max-w-[98%]`).
    - **Vistas Afectadas**: 
      - Dashboard Administrativo Principal (`AdminDashboard.jsx`).
      - Gestión de Anuncios y Marketing (`AdManagementPage.jsx`, `MarketingPage.jsx`).
      - Reportes Públicos de Setters y Closers (`PublicSetterReportPage.jsx`, `PublicCloserReportPage.jsx`).
      - Performance Centers (Estadísticas) de Setters y Closers (`PublicSetterStatsPage.jsx`, `PublicCloserStatsPage.jsx`).
      - Registro de Agendas Auditables (`FinancialAgendasPage.jsx`).
      - Atribución de Ventas y Triage Stats (`PublicSalesAttributionPage.jsx`, `PublicTriageStatsPage.jsx`).
  - **Resultados**: Se eliminaron los espacios laterales muertos en monitores de alta resolución, permitiendo una visualización de datos mucho más densa y profesional, facilitando la lectura de tablas extensas sin necesidad de scroll horizontal excesivo.
  - **Mejora de Navegación (Scrollbar Dinámico)**: Implementación de barras de desplazamiento inteligentes en `index.css`.
    - **Comportamiento**: Las barras son casi invisibles por defecto para mantener la estética limpia, pero aumentan su opacidad al pasar el mouse por el contenedor (`*:hover`) y se destacan aún más al interactuar directamente con ellas.
    - **Usabilidad**: Soluciona la dificultad de encontrar la barra de scroll en laptops con touchpad, manteniendo un diseño premium y minimalista.
  - **Feature: Persistencia de Filtros (Navegación Sin Costuras)**: Implementación integral del sistema de persistencia de estados en los dashboards principales.
    - **Tecnología**: Uso del hook personalizado `usePersistentFilters` sincronizado con `localStorage`.
    - **Impacto**: Los usuarios ya no pierden sus configuraciones de filtros (fechas, IDs de setter/closer, términos de búsqueda) ni la pestaña activa al navegar entre diferentes secciones de la aplicación o al refrescar el navegador (F5).
    - **Módulos Cubiertos**: Performance Centers (Setters/Closers), Marketing Hub (Rendimiento y CRUD), Atribución de Ventas, Triage Stats y Registros Financieros (Ventas/Agendas).
  - **Refactorización de Filtros y Corrección de Pestañas**:
    - **Problema Crítico**: Se identificó que las pestañas de navegación interna (Rendimiento, Historial, Cartera, etc.) en `/admin/ventas` y `/admin/marketing` no cambiaban de vista. Esto fue causado por una incompatibilidad en el hook `usePersistentFilters.js`, que no soportaba el paso de objetos de estado, corrompiendo el `activeTab`.
    - **Solución Técnica**: Se rediseñó el hook `usePersistentFilters` para ser polimórfico. Ahora soporta nativamente tres modos de actualización: pares `(key, value)`, objetos completos `{ filter: val }` y funciones de actualización `prev => next`. 
    - **Resultado**: Restauración total de la funcionalidad de navegación por pestañas con persistencia de estado garantizada tras recargas.
  - **Rediseño Jerárquico de Marketing Intelligence**: Optimización del flujo visual en `MarketingPage.jsx`.
    - **Orden de Componentes**: Pestañas de Contexto > Tarjetas de KPI (Ventas/Agendas/Leads/Inversión) > Barra de Filtros Unificada > Tabla de Datos.
    - **Beneficio**: Los tomadores de decisiones ahora ven los números globales (KPIs) antes de entrar en el detalle de los filtros, mejorando la velocidad de análisis.
  - **Feature: Columnas Dinámicas y Personalizables (DnD)**: Implementación de un sistema avanzado de gestión de tablas en `AdDashboardTab.jsx`.
    - **Tecnología**: Integración de `@dnd-kit/core` y `@dnd-kit/sortable` para permitir el reordenamiento de columnas mediante arrastrar y soltar.
    - **Interfaz**: Creación del componente `ColumnSettings.jsx` con diseño Glassmorphism, que permite ocultar/mostrar columnas con un solo clic (icono ojo) y reorganizarlas visualmente.
    - **Soporte de Hook**: Se extendió `usePersistentFilters.js` para soportar estados de tipo Array, permitiendo que el orden y visibilidad de las columnas persistan en el navegador del usuario.
    - **Refactorización de Tabla**: Transición de un renderizado de tabla estático a uno dinámico que mapea las columnas activas, manteniendo la coherencia en la fila de "Total General" y en los cálculos de ROI y Cash Collect.

- **13 de Mayo de 2026**:
  - **Rediseño Premium de Estadísticas de Setters (Dark Mode)**: Migración total del Performance Center de Setters a una estética oscura coherente con el resto de la plataforma.
    - **Interfaz (Frontend)**:
      - Transformación de `PublicSetterStatsPage.jsx` a tema `slate-950` con tarjetas en `slate-900` y bordes `slate-800`.
      - Unificación visual de las pestañas "Vista General", "Comparación", "Registros" y "Rendimiento Conversacional".
      - Ajuste de contraste en tipografía y elementos decorativos (glow effects).
    - **Componentes Visuales**: 
      - Actualización de `EvolutionChart.jsx` y `FunnelChart.jsx` para modo oscuro (tooltips, líneas de cuadrícula y etiquetas legibles).
      - Rediseño de `SetterComparisonView.jsx` y `SetterReportsTable.jsx` con paleta de alta densidad.
  - **Optimización de Interfaz Administrativa**:
    - **Simplificación de Navegación**: Se ocultaron las pestañas y secciones de "Triage" y "Workshop" en el `AdminSalesHubPage.jsx` y en el `PublicHubPage.jsx` para reducir la carga cognitiva, priorizando los módulos operativos activos (Closers y Setters).
  - **Fix: Columnas faltantes en vista Lista de Rendimiento por Anuncio**: Las columnas "Cash Collect" y "ROAS" (renombrada desde "ROI") no se renderizaban en la tabla plana de Rendimiento por Anuncio en `/admin/marketing`, aunque la API ya las retornaba y otras vistas/modales sí las mostraban.
    - **Causa Raíz**: El `<thead>` y el `<tbody>` de la tabla de lista en `MarketingPage.jsx` solo tenían 8 columnas definidas (hasta "CP Venta"), sin incluir `cash_collect` ni `roas`.
    - **Correcciones aplicadas (`MarketingPage.jsx`)**:
      - Se agregaron los encabezados `Cash Collect` y `ROAS` al `<thead>`.
      - Se agregaron las celdas en cada `<tr>` del `<tbody>`: `cash_collect` formateado como `$xxx` y `roas` como `x.xxX`, con colores semáforo (ROAS ≥ 3x verde, ≥ 1x ámbar, < 1x rojo).
      - Se corrigió el `colSpan` del estado vacío de `8` a `10`.
  - **Renombrado Estratégico de KPI de Apertura**: Actualización de la terminología de "Tasa de apertura" a "Tasa de cualificación" en toda la plataforma para reflejar con mayor precisión el proceso de filtrado de leads.
    - **Archivos Afectados**: `PublicSetterStatsPage.jsx`, `SetterComparisonView.jsx`, `PublicSetterReportPage.jsx` y `tutorials.js`.
    - **Lógica de Reporte**: Se actualizó el cálculo en el reporte de setters para que la "Tasa de Cualificación" muestre el porcentaje de leads netos sobre el total de entrantes, manteniendo la coherencia con el dashboard principal.
  - **Feature: KPI Unificado de Discovery (Initial Funnel)**: Implementación de una visualización premium y densa que consolida las métricas críticas del inicio del embudo en `PublicSetterStatsPage.jsx`.
    - **Componente**: Creación de `LeadUnifiedKPI.jsx` en `frontend/src/components/shared/`.
    - **Visualización**:
      - **Entrantes**: Destacado en gran formato como métrica de dominancia.
      - **Engagement**: Tasa de respuesta porcentual, conteo de respondidos vs. sin respuesta y barra de progreso de interacción.
      - **Cualificación**: Gráfico circular de leads cualificados, % sobre entrantes, % sobre respuestas y conteo de no cualificados.
      - **UX/Diseño**: Implementación de tooltips detallados con explicaciones matemáticas, animaciones de entrada con `framer-motion` y estética Dark Glassmorphism con efectos de brillo (`glow`).
  - **Feature: KPIs Configurables con Umbrales de Color**: Implementación de un sistema de tarjetas de estadísticas dinámicas que permiten al usuario definir metas visuales.
    - **Componente**: `ConfigurableStatCard.jsx` con persistencia en `localStorage`.
    - **Funcionalidad**: Incluye un menú de configuración (engranaje) para ajustar los límites de color Verde (>máx), Amarillo (medio) y Rojo (<mín).
    - **KPIs Implementados**:
      1. **Leads Cualificados**: % sobre entrantes totales.
      2. **Pasaron a Dolor**: % de avance desde cualificación.
      3. **Tasa Apertura Setting**: Eficacia de apertura excluyendo automáticamente los "No Leads".
  - **Feature: KPIs de Respuesta a Openings**: Adición de métricas detalladas para medir el engagement inicial.
    - **Nuevas Métricas**: Respuesta en etapa de Cualificación, respuesta en etapa de Dolor y Respuesta Total.
    - **Rediseño Estético "Minimalismo Funcional"**: Transición a un diseño más limpio y sobrio.
        - **Paleta de Colores**: Uso estricto de Morado (#8B5CF6), Azul (#3B82F6) y Verde (#10B981).
        - **Estructura por Niveles**: Implementación de filas de Resumen (iconos cuadrados), Análisis (iconos circulares y porcentajes grandes) y Footer (métricas secundarias horizontales).
        - **Tipografía y Jerarquía**: Eliminación de brillos excesivos, uso de iconos vectoriales limpios y tooltips informativos con fórmulas técnicas.
        - **Definición de Tasa de Apertura**: Ajuste de lógica para medir aperturas reales enviadas sobre leads netos (excluyendo descartes automáticos de ManyChat).
        - **Estado**: Adición de píldoras de estado (Óptimo/Alerta/Bajo) basadas en umbrales configurables.

- **14 de Mayo de 2026**:
  - **Optimización de Analítica Conversacional (Medición de Envío)**: Implementación de la métrica `id_option_send` para medir el rendimiento de las preguntas aleatorias enviadas desde Manychat.
    - **Base de Datos**: Se agregó la columna `id_option_send` al modelo `LeadAnswer` para registrar qué opción específica fue enviada al lead. Migración generada y aplicada.
    - **API & Ingesta**: Actualización del endpoint `/manychat-webhook` para capturar `id_option_send` y vincularlo con la respuesta posterior (`id_option`) dentro de una ventana de 24 horas.
    - **Estadísticas Avanzadas**: Mejora de `/manychat-webhook/stats/segmentation` para retornar un desglose comparativo de envíos vs. respuestas y cualificación por cada opción de pregunta.
    - **Interfaz (Frontend)**: Incorporación de la tarjeta **"Efectividad de Envío (Preguntas Aleatorias)"** en `ConversationalStatsTab.jsx`. Presenta un diseño de alta densidad que compara Volumen de Envío vs. Respuestas y Tasa de Cualificación, permitiendo identificar la variante ganadora del bot.

- **15 de Mayo de 2026**:
  - **Feature: Ordenamiento Dinámico en Rendimiento de Anuncios**:
    - **Frontend (`AdDashboardTab.jsx`)**: Se agregó funcionalidad de ordenamiento interactivo (ascendente y descendente) para todas las columnas de la vista de tabla (lista) en la sección de Rendimiento por Anuncio.
    - **UI/UX**: Los encabezados de tabla ahora incluyen indicadores visuales de ordenamiento (flechas tenues que se activan al hacer clic) y gestionan el estado persistiendo el "Total General" fijo en la parte superior.
  - **Feature: Heatmap y Rangos de Color Dinámicos en Tabla de Anuncios**:
    - **Aislamiento de Inactivos**: Los anuncios con todas sus métricas principales en cero (inversión, leads, agendas, ventas) son automáticamente segregados al final de la tabla y marcados con un fondo rojo de baja opacidad para facilitar su omisión durante el análisis, independientemente del orden de la tabla.
    - **Gradiente Dinámico por Columna**: El fondo de las filas ahora adopta un gradiente de tres niveles (verde para óptimo, amarillo para tolerable, rojo para deficiente) basado en el valor matemático de la celda de la columna que esté ordenando la tabla actualmente.
    - **Panel de Configuración (GradientSettings.jsx)**: Se implementó un mini-panel persistente mediante el cual el administrador puede personalizar los umbrales numéricos de "Óptimo" y "Tolerable" para cada KPI. Esto ajusta instantáneamente el gradiente de la tabla y persiste las preferencias mediante `usePersistentFilters`.
  - **Sincronización Automatizada de Google Sheets (Cron Job)**:
    - **Script**: Se creó `scripts/sync_sheets.py`, un script independiente capaz de cargar el contexto de la aplicación y ejecutar la ingesta de datos desde Google Sheets para las tablas de Ventas y Agendas (`Ventas_DB` y `Llamadas_DB`).
    - **Uso**: Diseñado para ser ejecutado mediante Railway Cron Jobs al menos dos veces al día (`python scripts/sync_sheets.py`), garantizando que los dashboards financieros y métricas de desempeño estén actualizados sin intervención manual.
  - **Filtros de Periodo Avanzados**:
    - **Frontend & Backend**: Se incorporó el nuevo filtro "Este mes" (`this_month`) en el panel de Rendimiento de Anuncios (`AdDashboardTab.jsx` y `manychat.py`), el cual limita la consulta estricta desde el primer día del mes en curso hasta la fecha actual. Además, se renombró el filtro anterior a "Últ. 30 días" para clarificar la diferencia entre un mes calendario y los últimos 30 días móviles.
  - **Optimización Visual de Rendimiento por Fuente**:
    - **UI/UX**: Se reemplazó la cuadrícula expansiva de tarjetas individuales por fuente (Setters) por un gráfico de barras apiladas o superpuestas mucho más compacto e interactivo usando `recharts` (`ComposedChart`). Esto reduce drásticamente el espacio vertical (fijado a `192px`) manteniendo toda la comparativa de Agendas vs Ventas accesible mediante *tooltips* interactivos.
  - **Mejora de Visualización de Estadísticas (Registros Setters)**:
    - **Tooltips Informativos**: Implementación de un sistema de tooltips avanzados en la tabla de registros de setters (`SetterReportsTable.jsx`) para clarificar el significado de cada métrica (Entrantes, Leads, Op. Sub/Res, Follow Ups, etc.).
    - **Optimización de UX**: Los tooltips ahora se muestran debajo del encabezado (`top-full`) para evitar recortes visuales por el contenedor de la tabla. El icono de ayuda (`HelpCircle`) solo es visible al pasar el puntero sobre la cabecera, manteniendo la interfaz limpia.
    - **Control de Capas**: Ajuste de `z-index` y `stacking context` en las cabeceras de la tabla para garantizar que los tooltips se superpongan correctamente a cualquier otro elemento de la página.
  - **Feature: Rediseño Visual del Embudo de Setters**:
    - **Visualización (FunnelChart)**: Integración del componente `FunnelChart` en la sección de Funnel de `PublicSetterStatsPage.jsx`, proporcionando una representación gráfica clara de las etapas: Cualificación, Dolor, Oferta, Link y Agenda.
    - **UI Estructurada**: Reorganización de la sección en tres columnas: Standings (lista coloreada), Engagement (métricas de seguimiento y respuesta) y Visualización (gráfico).
    - **Simulación de Embudo**: Las filas de "Standings" ahora incluyen barras de progreso y colores temáticos (Violeta, Azul, Fucsia, Indigo, Esmeralda) que simulan visualmente la caída del embudo, alineándose con la estética del dashboard de Closers.

- **16 de Mayo de 2026**:
  - **Feature: Minimización y Control de Grupos en Inversión de Marketing**:
    - **UI/UX**: En la pestaña de Inversión (Periodos) del panel de Marketing (`AdManagementPage.jsx`), las campañas y los registros del historial de inversión ahora cargan minimizados (colapsados) por defecto para mejorar la lectura y reducir el "ruido visual".
    - **Control Individual y Global**: Se incorporaron iconos interactivos (Chevron) en cada campaña y registro histórico para expandir o contraer la vista individualmente. Además, se implementó un botón global "Expandir Todo / Minimizar Todo" que permite al usuario alternar el estado de todos los grupos y el historial simultáneamente.
    - **Interacciones refinadas**: Se bloqueó la propagación de clics en el campo de `Inversión Total` de la campaña para evitar cierres accidentales al intentar editar el presupuesto.
  - **Feature: Ocultar y Archivar Campañas (Limpieza de Contexto)**:
    - **Gestión Visual**: Las campañas que ya no están activas o no son relevantes pueden ser "archivadas" usando el nuevo botón (icono Ojo) en la cabecera de la campaña. Al archivar una campaña, esta se oculta inmediatamente de la vista principal.
    - **Persistencia**: La selección de campañas archivadas se guarda permanentemente en la memoria del navegador (`localStorage` mediante `usePersistentFilters`).
    - **Modo "Mostrar Archivados"**: Cuando hay campañas ocultas, aparece un botón global que permite revelar estos elementos. En este modo, las campañas archivadas se muestran con un estilo atenuado (escala de grises, menor opacidad) y un indicador visual, permitiendo "desarchivarlas" con un solo clic.
  - **Mejora Visual: Brillo en Gradientes de Rendimiento**:
    - **UI/UX**: Se incrementó significativamente la opacidad y el brillo de los colores semáforo (Verde, Amarillo, Rojo) en la vista de lista del **Rendimiento por Anuncio**. Los fondos base pasaron a utilizar tonos de la paleta `-500` con mayor opacidad (`/10`) y un efecto `hover` mucho más reactivo (`-400/25`), lo que produce un efecto de "brillo" al pasar el puntero para destacar mejor las filas prioritarias.
  - **Fix: Error 401 en Tráfico Landings**:
    - **Causa**: El componente `LandingTrafficTable.jsx` utilizaba `fetch` nativo para consultar las métricas de tráfico, omitiendo los encabezados de autenticación JWT y las credenciales de sesión.
    - **Solución**: Se migró la lógica de peticiones al servicio centralizado `api` (Axios), garantizando la inclusión automática del token de autorización y cookies de sesión necesarias para los endpoints protegidos por `@admin_required`.
  - **Mejora: Analítica de Tráfico Landings (Agregación Específica)**:
    - **UI/UX**: Se reemplazó el panel genérico de Top Páginas por un tablero de **5 KPIs fijos** en `LandingTrafficTable.jsx`, mostrando métricas individuales para las landings clave (`Acceso`, `Bienvenido`, `Live Class`, `Live`) y un acumulado para `Otros`. Se añadió mapeo de nombres amigables también en las filas de la tabla.
    - **Normalización y Reparación Agresiva**: Se implementó una lógica de reparación en el backend que detecta si el script de tracking envía datos incompletos (como solo `/` o el dominio base) y, en esos casos, extrae automáticamente la ruta real desde el `referrer`. Esto garantiza que visitas a subpáginas se cuenten correctamente incluso con scripts de tracking mal configurados.
  - **Fix: Tracking de Landings en Subdominios (Solución Definitiva)**: 
    - **Estandarización de Landings**: Se actualizó el script de rastreo en `acceso/index.html`, `bienvenido/index.html`, `live-class/index.html` y `live/index.html` reemplazando `window.location.pathname` por rutas estáticas hardcodeadas (`/acceso`, `/bienvenido`, etc.). Esto neutraliza el comportamiento de los subdominios de Hostinger que reportaban erróneamente todas las visitas como la raíz `/`.
    - **Backend (`app/api/metrics.py`)**: Se modificó la lógica de ingesta para eliminar la sobrescritura automática basada en el `Referrer`. Ahora el sistema **prioriza estrictamente** el `page_path` enviado explícitamente desde el cliente, permitiendo un control granular del etiquetado de tráfico.
    - **Frontend (`LandingTrafficTable.jsx`)**: Se implementó una normalización de rutas más agresiva y genérica utilizando el constructor `URL`. Esto permite que el dashboard clasifique correctamente las visitas incluso si los registros históricos contienen URLs completas de subdominios en lugar de rutas relativas.

- **18 de Mayo de 2026**:
  - **Optimización de Carga y Scroll Infinito en Registro de Ventas y Agendas**:
    - **Backend (`app/api/public/__init__.py`)**:
      - Modificación de los endpoints `GET /public/financial-sales` y `GET /public/financial-agendas` para admitir parámetros de paginación (`page` y `limit`), búsqueda textual (`search`) y rango de fechas (`start_date`, `end_date`) directamente en las consultas de base de datos.
      - Para agendas, se implementó el cálculo eficiente en el backend de métricas agregadas globales (`total`, `upcoming_count` y conteo por `closer`) basados en el conjunto filtrado, proveyendo consistencia a los KPIs del frontend.
      - Preservación de compatibilidad con llamados heredados sin parámetros de página (retorno de lista plana).
    - **Frontend (`PublicFinancialSalesPage.jsx` y `FinancialAgendasPage.jsx`)**:
      - Reemplazo de la carga total en memoria y filtrado en cliente por scroll infinito automático usando un `IntersectionObserver` de JavaScript, cargando registros en lotes de 10 en 10.
      - Implementación de *debounce* de 300ms para las entradas de búsqueda y rango de fechas para evitar peticiones redundantes.
      - Incorporación de animaciones de carga e indicadores de finalización ("Todas las ventas/agendas cargadas").
  - **Rediseño Premium de Agendas por Closer**:
    - **Frontend (`FinancialAgendasPage.jsx`)**:
      - Remoción de la etiqueta redundante `(Comparativa Calendly)`.
      - Reemplazo de la cuadrícula de tarjetas individuales por una **gráfica superpuesta apilada horizontal (segmented progress bar)** de alta densidad. Cada closer ocupa un segmento porcentual con un degradado de color dinámico y premium.
      - Incorporación de una leyenda horizontal dinámica e interactiva con píldoras de colores y porcentajes calculados en tiempo real que optimiza drásticamente el espacio vertical y mejora la estética del tablero.
  - **Limpieza de Filtros y Configuración por Defecto del Rango de Fechas**:
    - **Frontend (`PublicCloserStatsPage.jsx`)**:
      - Condicioné la visibilidad de la barra de filtros globales ("Closer / Equipo", "Periodo", "Desde", "Hasta", "Agregación") para que únicamente se renderice en las pestañas `general` y `advanced` (Vista General y % Rendimiento). Esto previene la confusión de los closers al visualizar las pestañas de registros (`sales_log`, `agendas_log`, `customers`), las cuales manejan filtros de búsqueda y paginaciones internas independientes.
    - **Frontend (`FinancialAgendasPage.jsx`)**:
      - Inicialización del rango de fechas del filtro de agendas para que comience automáticamente en el **mes actual** (desde el día 1 del mes en curso hasta el día de hoy).
      - Adición de un efecto `useEffect` robusto que detecta si el estado cargado de `localStorage` contiene fechas vacías y realiza el fallback forzando los valores calculados del mes en curso, previniendo visualizaciones sin filtro de fechas.
  - **Rediseño Minimalista y Compacto de Tarjetas KPI**:
    - **Frontend (`FinancialAgendasPage.jsx`)**:
      - Reemplazo del bloque de rejilla de dos tarjetas KPI grandes ("Total Agendados" y "Próximas Citas") que consumían demasiado espacio vertical por dos píldoras horizontales alineadas extremadamente limpias y elegantes con micro-animaciones en hover y un contraste sutil de colores de marca, maximizando la densidad de la interfaz.
  - **Dashboard de Ventas por Fuente (Atribución y Porcentaje Dinámico)**:
    - **Backend (`app/api/public/__init__.py`)**:
      - Agregué soporte para filtrado de fechas (`start_date`, `end_date`) en la consulta de ventas financieras (`GET /public/financial-sales`).
      - Implementé el cálculo de agregados dinámicos en el backend para retornar el monto total acumulado (`total_monto`) y el desglose de ingresos y ventas por setter (`sources_breakdown`) para el rango de fechas seleccionado.
    - **Frontend (`PublicFinancialSalesPage.jsx`)**:
      - Incorporé selectores de fecha en la cabecera del registro de ventas con persistencia y comportamiento por defecto inicializado en el **mes actual** (del 1 de este mes a hoy).
      - Diseñé un componente visual premium que dibuja una **barra de progreso segmentada** (segmented progress bar chart) donde cada segmento representa proporcionalmente la atribución de ingresos de cada fuente (Elias, workshop, VSL, etc.) con sus degradados de color.
      - Añadí una cuadrícula de desglose de métricas detalladas para cada fuente que calcula de forma dinámica y reactiva la participación en comisiones / montos en base a un **porcentaje personalizado** ingresado globalmente por el usuario.
  - **Coherencia Absoluta de Métricas de Venta e Ingresos ("Total Cash")**:
    - **Backend (`app/services/closer_service.py`)**:
      - Modifiqué la lógica de cálculo en `CloserService.get_comprehensive_stats` para que las cifras de ventas (`total_sales`, `total_cash` y desgloses de PIF, Split y Señas) se calculen en tiempo real directamente de la tabla transaccional oficial **`FinancialSale`** (conectada a Stripe/Sheets) en lugar de depender exclusivamente de los reportes diarios manuales de closers (`CloserDailyReport`).
      - Esto garantiza una consistencia matemática absoluta (al 100%) entre lo visualizado en la Vista General de Closers y el Registro de Ventas para cualquier rango de fechas y filtros seleccionados.
      - Implementé una matriz de mapeo robusta que asocia el nombre del usuario (`user.username`) con sus respectivos emails de venta (como `'jeancarlo@thelearnation.com'` para `'Jean Carlo'`), resolviendo el problema de mapeo cuando los correos del modelo `User` son nulos.
    - **Frontend (`PublicCloserStatsPage.jsx`)**:
      - Configuré el estado de los filtros comunes para que, por defecto, se preseleccione al closer **"Jean Carlo"** (ID `3`) en lugar de mostrar los datos agregados de todo el equipo de forma masiva, permitiendo un análisis inmediato, limpio y enfocado en la vista general.

- **19 de Mayo de 2026**:
  - **Edición Interactiva del Historial de Inversión**:
    - **Backend (`app/api/public/marketing.py`)**: Creación del endpoint `PUT /public/ads/period-spend/<int:spend_id>` que permite actualizar el monto gastado (`spend`) de un registro de gasto periódico por ID de manera segura, con validación de tipo y manejo de errores transaccionales.
    - **Interfaz (Frontend) (`AdManagementPage.jsx`)**:
      - Se añadió un botón con icono de lápiz (`Pencil`) al lado del monto de cada registro en el listado del historial de inversión.
      - Al hacer clic, se activa la edición en línea, reemplazando el texto del monto por un input numérico y botones de Guardar (`Save`) y Cancelar (`X`).
      - Soporta confirmación con la tecla **Enter**, cancelación con **Escape** y muestra un spinner de carga (`Loader2`) durante la petición.
  - **Ocultar y Archivar Anuncios en el Dashboard de Rendimiento**:
    - **Interfaz (Frontend) (`AdDashboardTab.jsx`)**:
      - Se implementó un sistema de archivado interactivo para ocultar anuncios innecesarios en la pestaña de "Rendimiento por Anuncio" utilizando hooks de estado persistente (`usePersistentFilters`).
      - Se agregaron iconos de ojo interactivos (`Eye`/`EyeOff`) en las celdas de nombre de anuncio (vista de lista) y en la cabecera de las tarjetas (vista de galería).
      - Las tarjetas archivadas reciben una sutil opacidad y un filtro de escala de grises que se aclara de forma elegante al hacer hover.
      - Se añadió un botón global "Mostrar / Ocultar Archivados" dinámico en la cabecera del dashboard.
      - Las sumas totales del dashboard ("Total General") se recalculan automáticamente en tiempo real excluyendo los anuncios ocultos.

- **20 de Mayo de 2026**:
  - **Optimización de Deduplicación y Protección de Datos en Webhook de ManyChat**:
    - **Backend (`app/api/manychat.py`)**:
      - **Función Helper `is_valid_value`**: Creación de una rutina robusta para verificar que los datos entrantes del webhook no estén vacíos (`""`), sean nulos (`None`) o contengan sintaxis no resuelta de ManyChat (`{{...}}`, `cuf_`, `undefined`).
      - **Deduplicación Permanente sin Límite de 24 Horas**: Eliminación del filtro restrictivo de ventana temporal de 24 horas en `LeadAnswer`. El sistema ahora realiza un lookup histórico integral sobre todo el historial buscando interacciones previas de ese lead con el anuncio (`ad_id` o `keyword`), y actualiza selectivamente el registro existente para evitar duplicar el lead en la base de datos.
      - **Selective Upsert (Protección de Keywords y Perfil de Leads)**: Modificación de la ingesta en `ManychatLead` y `LeadAnswer` para que los datos solo se sobreescriban si el payload entrante de ManyChat trae valores válidos según `is_valid_value`. Esto asegura que si una keyword o cualificación viene vacía en alguna fase del flujo conversacional, no se pisen los datos correctos ya guardados.

- **21 de Mayo de 2026**:
  - **Identificación y Atribución Manual de Leads sin Anuncio**: Implementación de una solución unificada para que los administradores y setters asocien anuncios a ventas y agendas que carecen de atribución.
    - **Base de Datos & API**:
      - Creación del endpoint `GET /api/public/marketing/unattributed-leads` en `app/api/public/marketing.py` para consultar en un rango de fechas las ventas (`FinancialSale`) y agendas (`FinancialAgenda`) no atribuidas, con filtros de búsqueda y paginaciones eficientes.
      - Creación del endpoint `POST /api/public/marketing/manual-attribution-agenda` en `app/api/public/marketing.py` para inyectar interacciones virtuales (`LeadAnswer` y `ManychatLead`) y forzar la atribución de anuncios en agendas de manera análoga a las ventas.
    - **Interfaz (Frontend)**:
      - Creación de la página premium `UnattributedLeadsPage.jsx` con un diseño oscuro de alta densidad (`bg-slate-950`), cards de sumario KPI, tablas estructuradas separadas por pestañas dinámicas (Ventas vs Agendas) y un modal elegante con buscador interactivo de anuncios en tiempo real.
      - Registro de la ruta protegida en `App.jsx` habilitada para los roles `admin` y `setter`.
      - Incorporación de la pestaña de navegación "Sin Anuncio" (icono `Link2Off`) en el panel de navegación de `useDockNavigation.js` para ambos roles.
    - **Configuración del Workspace**:
      - Adición de la directiva `python.defaultInterpreterPath` en `.vscode/settings.json` apuntando al entorno virtual local `env`, resolviendo falsos positivos y advertencias de importación en la IDE.
    - **Refinamiento de Deduplicación y Acumulación**:
      - **Consolidación de Prospectos**: Modificación en el backend (`app/api/public/marketing.py` en `GET /public/marketing/unattributed-leads`) para deduplicar los registros no atribuidos de ventas y agendas basándose en Instagram normalizado (`ig_norm`) o en su defecto por nombre del cliente (`nombre_norm`). Esto garantiza que cada prospecto aparezca exactamente una sola vez en el listado, **registrando y mostrando específicamente la fecha de su primer pago (la más antigua)** para contextualizar el inicio de su relación.
      - **Acumulado de Ingresos**: Se ajustó la lógica del backend para sumar y retornar el **monto total acumulado** de todas las ventas no atribuidas para cada prospecto único en lugar de reportar pagos aislados.
      - **Remoción de Tipo de Pago**: Se eliminó la visualización y persistencia del "tipo de pago" (producto) tanto en la respuesta de la API como en la tabla de ventas de la interfaz frontend (`UnattributedLeadsPage.jsx`), simplificando el proceso de atribución manual.
    - **Acceso para Closers**:
      - **Habilitación en Enrutamiento**: Modificación de `App.jsx` para permitir al rol `closer` visualizar la vista protegida de `/unattributed-leads`.
      - **Integración en Navegación (Dock)**: Actualización de `useDockNavigation.js` incorporando el enlace "Sin Anuncio" (icono `Link2Off`) dentro del Dock de navegación del Closer, facilitando su colaboración directa en la auditoría y atribución manual de leads.
  - **Fusión y Rediseño de la Pestaña de Rendimiento de Closers (Límite 500 Líneas)**:
    - **Fusión de Pestañas**: Unificación de las pestañas "Vista General" y "% Rendimiento" en una sola pestaña llamada **"Rendimiento"** tanto en el panel público (`PublicCloserStatsPage.jsx`) como en el panel privado del closer (`StatisticsPage.jsx`).
    - **Componente Atómico**: Implementación del componente reutilizable `CloserPerformanceTab.jsx` que estructura de manera jerárquica y coherente toda la analítica de rendimiento del Closer (KPIs globales, embudo Recharts, conversión de llamadas, desgloses y distribución circular).
    - **Refactorización de Vistas**: Reducción masiva de la complejidad y tamaño del código. `PublicCloserStatsPage.jsx` se redujo a 286 líneas y `StatisticsPage.jsx` se optimizó a 155 líneas, cumpliendo holgadamente con la directiva técnica de **500 líneas de código máximo por archivo** para control de contexto y eficiencia de tokens.
    - **Remoción de Obsoletos**: Eliminación física del archivo desactualizado `CloserAdvancedStatsView.jsx` tras su total reemplazo y desvinculación.
  - **Mejora del Reporte de Discord de Closer**:
    - **Porcentaje de No Show**: Se implementó el cálculo dinámico de `no_show_rate` (Total No Shows / Total Agendados) en el webhook de backend (`app/api/public/closer.py`).
    - **Renderizado de Plantilla**: Se actualizó la plantilla de imagen premium de Discord (`app/templates/reports/closer_report.html`) para renderizar el porcentaje de inasistencia en formato sutil y alineado al lado de la métrica cuantitativa: `Total No Show: X (Y%)`.
  - **Fix: Robustecimiento de la Atribución Manual de Ventas y Agendas (Casteo de IDs)**:
    - **Causa Raíz**: En entornos de bases de datos estrictos (como PostgreSQL en producción), al buscar registros usando `.query.get()` con strings (ej. `"42"`) cuando la clave primaria es un entero (`Integer`), la base de datos devuelve `None` o falla al realizar la comparación. Esto ocasionaba el error `Error al guardar: No se encontró la venta` al intentar forzar la atribución manual desde el frontend, ya que los IDs se transmitían como strings en el cuerpo JSON de la petición HTTP.
    - **Correcciones aplicadas (`app/api/public/marketing.py`)**:
      - Se implementó un casteo explícito a entero (`int()`) para los IDs recibidos (`sale_id`, `agenda_id` y `ad_id`) dentro de los endpoints `/public/marketing/force-manual-attribution` y `/public/marketing/manual-attribution-agenda`.
      - Se añadió una validación robusta con un bloque `try/except` para atrapar errores de casteo (`ValueError`, `TypeError`), retornando un código de estado `400 Bad Request` en caso de recibir valores no válidos.
      - Se agregó la verificación física de existencia del anuncio (`Ad.query.get(ad_id)`) en el backend antes de proceder con el guardado, devolviendo `404 Not Found` con mensaje claro si el ID del anuncio no existe.
      - Se incorporó `logging.getLogger(__name__)` para registrar detalladamente en los logs del servidor cualquier fallo o excepción en los procesos de atribución.
  - **Feature: Métrica de Porcentaje de Slots Disponibles en Reporte Diario de Closer (Discord)**:
    - **Requerimiento**: Se solicitó mostrar de forma visual y clara en el total de agendas del reporte de Discord qué porcentaje de la capacidad total de slots diarios configurada por el closer quedó libre/disponible.
    - **Backend (`app/api/public/closer.py`)**:
      - En la función helper `_prepare_report_data(report)`, se implementó el cálculo matemático de slots disponibles: `slots_disponibles = max(0, slots_totales - total_scheduled)` y su posterior conversión a tasa porcentual usando `safe_percent` sobre los slots totales.
      - Se inyectó la propiedad `slots_available_pct` dentro del nodo `general` del payload enviado para el renderizado del reporte.
    - **Plantilla HTML (`app/templates/reports/closer_report.html`)**:
      - Se modificó la tarjeta de métricas "Total Agendas" en la grilla superior para añadir la visualización elegante en formato `X (Y% disp.)` (ejemplo: `4 (60% disp.)`) alineada estéticamente al lado de la métrica absoluta, manteniendo coherencia con otras visualizaciones porcentuales como el no-show.
  - **Feature: Porcentajes contextuales en Métricas de Agendas y Llamadas (Discord)**:
    - **Agendas**: Se agregó el porcentaje de Show Rate al lado de "Total Asist." y la tasa de cancelaciones/reprogramaciones al lado de "Total Canc/Rep", con el mismo estilo visual sutil en gris.
    - **Llamadas y Conversiones**: Reestructuración de la sección. El grid inferior se redujo a dos tarjetas (Show Rate y Pitch Rate). Las tarjetas de Close Rate y Offer to Sale fueron movidas a la sección de Ventas, donde conceptualmente tienen mayor relevancia.
    - **Backend (`app/api/public/closer.py`)**: Adición de `canc_rep_rate` (canceladas + reprogramadas sobre total agendadas) al payload de tasas.
  - **Feature: Rediseño de la sección Ventas y doble Close Rate (Discord)**:
    - **Requerimiento**: Separar la visualización de ventas entre "en llamada" y "fuera de llamada", y diferenciar el Close Rate según si incluye o no las señas (depósitos).
    - **Backend (`app/api/public/closer.py`)**:
      - Se calculan `out_call_count` y `out_call_cash` como la diferencia entre el total y los valores en llamada, exponiéndolos en `sales.totals`.
      - Se sustituyó el único `close_rate` por dos métricas: `close_rate_operativo` (solo PIF + Split Pay / asistencias) y `close_rate_promesa` (PIF + Split + Señas / asistencias).
    - **Plantilla HTML (`app/templates/reports/closer_report.html`)**:
      - Se reemplazaron los 4 KPI superiores de Ventas por dos bloques: `Total Ventas + Cash Total` y `🎯 En Llamada (N / $X) | 📤 Fuera de Llamada (N / $X)`.
      - Se actualizaron los encabezados de la tabla de tipos de venta.
      - Al pie de la sección se muestran las dos tarjetas de Close Rate con subtítulo explicativo para cada variante.
  - **Feature: Validación de Seguimientos Mínimos en Formulario de Closer**:
    - **Frontend (`frontend/src/pages/public/PublicCloserReportPage.jsx`)**:
      - Se agregó una validación en `handleSubmit` que bloquea el envío si la suma de seguimientos enviados (Hot + Cold) es cero, mostrando un `alert` descriptivo y abriendo automáticamente la sección de Seguimientos.
      - Se incorporó una advertencia visual ⚠️ dentro de la sección de seguimientos cuando el total de enviados es cero, visible en tiempo real mientras el closer completa el formulario.
  - **Fix: Eliminación de Reflexión Cualitativa del Reporte Discord de Closer**:
    - **Plantilla HTML (`app/templates/reports/closer_report.html`)**: Se eliminó la sección "REFLEXIÓN CUALITATIVA" (Victoria del Día y Oportunidad de Mejora) del template de imagen de Discord. El reporte ahora cierra con la sección de Seguimientos, resultando en una imagen más compacta y enfocada en métricas operativas.

- **22 de Mayo de 2026**:
  - **Feature: Soporte para Anuncio Ficticio "Inbound"**:
    - **Backend (`app/api/public/marketing.py`)**:
      - Creación de la función helper `ensure_inbound_ad_exists()` que garantiza la persistencia en la base de datos de la campaña, conjunto de anuncios y anuncio ficticio `"Inbound"` (de tipo orgánico y keyword `"Inbound"`).
      - Integración de la rutina en los endpoints de listado `/public/ads` y de reportes `/public/reports/sales-attribution` para asegurar su disponibilidad en todo el flujo de marketing.
  - **Feature: Fila "No asignados aún" para Completar Cash Collect**:
    - **Backend (`app/api/public/marketing.py`)**:
      - Modificación del endpoint `/public/reports/sales-attribution` para agrupar todas las ventas sin atribución (`atribucion != "encontrado"`) bajo un anuncio sintético final `"No asignados aún"` con `ad_id: 0` y keyword `"SIN_ANUNCIO"`. Esto permite completar el total de Cash Collect al 100% de los ingresos.
  - **Frontend (Visualización del Ranking)**:
    - **[SalesAttributionPage.jsx](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/frontend/src/pages/admin/reports/SalesAttributionPage.jsx)**:
      - Corrección del cálculo dinámico del porcentaje de las barras del ranking de anuncios usando `Math.max` sobre todo el conjunto de resultados. Esto previene desbordamientos o barras mayores al 100% si el total no asignado es superior a los anuncios individuales reales.
  - **Feature: Exclusión de Cuotas en Cierres Reales de Closers**:
    - **Backend (`app/services/closer_service.py`)**:
      - Modificación del método `get_comprehensive_stats()` para clasificar de manera precisa y estricta los cobros de cuotas de seguimiento (ej. `"RR - Cuota"`, `"AL - Cuota 2"`, `"installment"`, etc.).
      - Las cuotas ahora se segregan a una clave específica `"installment"` y quedan completamente **excluidas** de la métrica de cantidad de nuevos cierres reales (`total_sales`), evitando la duplicación/inflado artificial de cierres.
      - Se mantuvo el efectivo de las cuotas dentro del total de caja recaudado (`total_cash`) para reflejar fielmente la efectividad del Cash Collect del closer.
    - **Frontend (`CloserPerformanceTab.jsx`)**:
      - Integración de la lectura de `installment` (cuotas cobradas) en el componente de rendimiento de closers.
      - Incorporación de una fila interactiva llamada `"Cuotas Cobradas"` en la tabla de **Ventas Breakdown** y adición de este segmento al gráfico circular de ventas para un análisis financiero y operativo del embudo de ventas impecable.
  - **Feature: Medición de Tasa de Conversión de Señas**:
    - **Backend (`app/services/closer_service.py`)**:
      - Implementación del cálculo de la tasa de conversión de señas en el método `get_comprehensive_stats()`.
      - Por cada seña registrada del closer en el periodo, el sistema busca si el mismo lead (por Instagram, email o teléfono) posee al menos un pago posterior de **cualquier otro tipo que no sea una seña** (por ejemplo, cuotas o pagos completos).
      - El total de señas del periodo se divide por las señas convertidas para obtener la tasa porcentual exacta (`deposit_conversions`).
    - **Frontend (`CloserPerformanceTab.jsx`)**:
      - Integración de la métrica en la columna de **Conversiones** mediante una tarjeta KPI compacta que visualiza el porcentaje y el recuento cuantitativo (`X de Y señas convertidas`).
      - Actualización del tooltip en la tarjeta superior de **"Promesas de Venta"** para mostrar también de manera interactiva esta tasa de conversión.

- **23 de Mayo de 2026**:
  - **Desagregación de Close Rates y Remoción de Global Win Rate**:
    - **Frontend (`CloserPerformanceTab.jsx`)**:
      - Se dividió la métrica general de "Close Rate" en tres indicadores más precisos:
        1. **Close Rate Real (Asist.)**: Porcentaje de asistencias totales que concluyeron en una venta real (PIF o Split Pay). Fórmula: `realSalesCount / stats.agendas.totals.attended * 100`. Color de acento `text-amber-500` con etiqueta `Asistencias → Ventas Reales`.
        2. **Close Rate Real (Oferta)**: Conversión de presentaciones u ofertas directas que concluyeron en una venta real (PIF o Split Pay). Fórmula: `realSalesCount / stats.general.offers_made * 100`. Color de acento `text-amber-500` con etiqueta `Ofertas → Ventas Reales`.
        3. **Close Rate Promesa**: Porcentaje de ofertas que concluyeron en reservas o promesas de venta (señas). Fórmula: `depositCount / stats.general.offers_made * 100`. Color de acento `text-fuchsia-500` con etiqueta `Ofertas → Promesas`.
      - Se removió por completo la tarjeta e indicador de **Global Win Rate** (anteriormente Asistencias -> Cierres Reales y Asistencias -> Promesas) en la columna de Conversiones.
      - Se actualizó el StatCard superior de **"Promesas de Venta"** para que su subtítulo de Close Rate Promesa use la nueva fórmula basada en ofertas (`offers_made`) en lugar de asistencias, logrando consistencia conceptual total.
      - Se actualizó el StatCard superior de **"Ingreso Total Cash"** para sumar e incluir las cuotas cobradas (`installmentCash`) y las señas (`depositCash`) junto con las ventas completadas/iniciales (`pifCash` y `splitCash`), mostrando así la caja recaudada real consolidada.
      - **Métricas Financieras Granulares (Breakdown 3x3)**:
        - Implementación de un grid premium de 9 tarjetas informativas al pie de la sección **Ventas Breakdown** para organizar y balancear el análisis:
          1. **New Cash Collect**: Suma de ingresos por PIF y Split (sin cuotas ni señas).
          2. **New Clients**: Cantidad de clientes con ventas reales cerradas (PIF + Split).
          3. **Ticket Promedio PIF**: Promedio obtenido exclusivamente en pagos únicos.
          4. **Ticket Promedio Split**: Promedio obtenido exclusivamente en pagos iniciales de planes de pago.
          5. **Cash Collect por Señas**: Total recaudado por depósitos de reserva.
          6. **Ticket Promedio por Señas**: Promedio por depósito de seña.
          7. **Cantidad de Cuotas**: Conteo de cuotas de seguimiento cobradas.
          8. **Cash Collect por Cuotas**: Ingreso recaudado por concepto de cuotas.
          9. **Ticket Promedio por Cuotas**: Promedio por cuota cobrada.
        - Se sincronizaron las tarjetas superiores (top-row) renombrando "Cierres Reales" a **"Nuevos Clientes"** y añadiendo en los subtítulos/tooltips correspondientes el desglose de "New Cash", "PIF / Split ticket" y "Cash Señas" para garantizar coherencia visual e informativa inmediata.
      - **Corrección de Reschedule Rate**:
        - Se detectó un cálculo incorrecto en la métrica **Reschedule Rate** de la sección de Llamadas y Conversiones.
        - **Fórmula corregida**: `reagendas / (asistencias - decisores)`.
        - **Backend (`app/services/closer_service.py`)**: Se actualizó la propiedad `rescheduled_rate` en la sección de porcentajes para usar la nueva lógica invertida: `div(val(stats.rescheduled_calls), total_attended - val(stats.decision_makers))`. Esto realiza la división y multiplicación por 100 de forma centralizada y segura.
        - **Frontend (`CloserPerformanceTab.jsx`)**: Se modificó la tarjeta visual de `Reschedule Rate` para renderizar el valor mapeado directamente desde el backend (`stats.percentages.rescheduled_rate`), mostrando el valor absoluto correspondiente en la UI: `Reagendas / (Asistencias - Decisores)`.
  - **Feature: Atribución Inbound, Rendimiento y Registro "Sin asignar"**:
    - **Backend (Modularización y Limpieza de Código)**:
      - **[marketing_service.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/services/marketing_service.py) [NEW]**: Creación de este servicio central para procesar de forma centralizada toda la lógica compleja de cálculo de rendimiento por anuncio.
      - **[marketing.py](file:///c:/Users/EQUIPO%20DELL/Documents/GitHub/NeurOPS/app/api/marketing.py) [MODIFY]**: Simplificación radical del endpoint `/ads/performance` delegando el procesamiento al nuevo servicio. Esto redujo el tamaño de `marketing.py` por debajo de las 400 líneas, respetando estrictamente el límite máximo de 500 líneas por archivo.
    - **Lógica de Rendimiento por Anuncio**:
      - **Atribución Inbound**: Se corrigió el listado de rendimiento por anuncio para asegurar que todos los anuncios que tengan leads, spend, o atribuciones manuales dentro del rango (como el canal orgánico `"Inbound"`) aparezcan correctamente en la UI.
      - **Métricas de Cash Collect y ROAS**: Se implementó el cálculo en el backend del monto total recaudado por anuncio (`cash_collect`) y el respectivo factor de retorno de inversión (`roas`) para poblar de forma real y precisa las columnas de la tabla de rendimiento.
      - **Fila "Sin asignar"**: Se diseñó una agrupación especial para calcular de forma matemática y precisa el número exacto de agendas, ventas y el monto consolidado de cash collect de todos los registros en el período que no poseen una atribución publicitaria, devolviéndolo al final de la tabla bajo la etiqueta `"Sin asignar"`.
  - **Feature: Filtro de Fechas y Estadísticas UTM en Tráfico Landings**:
    - **Backend (`app/api/metrics.py`)**: Modificación del endpoint `GET /track-visits` para aceptar parámetros `start_date` y `end_date` en la URL. Se incorporó una lógica de límites adaptables (hasta 5000 registros con filtros) y manejo de fechas robusto mediante SQLAlchemy.
    - **Frontend (`LandingTrafficTable.jsx`)**:
      - Se agregó una barra de filtros de rango de fechas de aspecto premium con presets rápidos (Hoy, Ayer, Últimos 7 días, Este mes) y una vista interactiva de fechas personalizadas.
      - Se implementó un grid analítico de 3 columnas para desglosar el Top de Orígenes (Source), Medios (Medium) y Campañas (Campaign).
      - Cada métrica del top muestra su cantidad de visitas, porcentaje global y una barra de progreso horizontal con degradados visuales de marca.
      - Se optimizó el buscador general en el cliente para abarcar también el campo de Medio (`utm_medium`).

- **25 de Mayo de 2026**:
  - **Feature: Flujo Secuencial de Setters y Closers ("Mazo de Cartas") - Paso 1: Base de Datos y Modelos**:
    - **Modelado en Backend (`app/models/booking.py`) [MODIFY]**:
      - Se agregaron campos específicos a la clase `Appointment` para rastrear el procesamiento secuencial: `setter_processed` (Boolean, default False), `closer_processed` (Boolean, default False), `setter_notes` (Text), `closer_notes` (Text), `ig_chat_link` (String 500) y `keyword` (String 100).
      - Se generó la migración autodetectada y se aplicó con éxito (`flask db upgrade`) incorporando los campos con restricciones `server_default` robustas para no alterar ni romper los agendamientos históricos en la tabla `appointments`.
  - **Feature: Flujo Secuencial de Setters y Closers ("Mazo de Cartas") - Paso 2: Endpoints de Backend**:
    - **Controladores API (`app/api/setter.py` y `app/api/closer.py`) [MODIFY]**:
      - Se implementaron los endpoints `GET /deck` y `POST /deck/<id>` específicos para cada rol para obtener y procesar agendas de forma secuencial (FIFO).
      - Se incorporaron las rutas de búsqueda y salto global `GET /deck/search` y `GET /deck/card/<id>` que permiten saltarse el orden de mazo para cargas y correcciones específicas en caliente.
      - Se validó la carga mediante compilación de Python (`py_compile`) garantizando la ausencia absoluta de errores de sintaxis e imports.
  - **Feature: Flujo Secuencial de Setters y Closers ("Mazo de Cartas") - Paso 3: Componentes e Integración Frontend**:
    - **Componentes Compartidos (`frontend/src/components/deck/`)**:
      - Creación de `MazoCartas.jsx` que renderiza una pila animada de cartas en 3D utilizando `framer-motion` para transiciones fluidas de deslizamiento (swipe).
      - Creación del autocompletador `BuscadorGlobalDeck.jsx` para realizar búsquedas textuales rápidas de leads por nombre, email, teléfono o Instagram en tiempo real y permitir saltarse la secuencia de cola.
    - **Vistas Específicas por Rol (`frontend/src/pages/`) [NEW]**:
      - Creación de `SetterDeckPage.jsx` para gestionar leads entrantes, validando usuario de Instagram, link del chat directo, palabra clave, origen del anuncio, notas y estado de asistencia (Pendiente, Asistió, No Show).
      - Creación de `CloserDeckPage.jsx` que destaca las notas dejadas por el Setter en formato de globo de chat, y captura la palabra clave corregida, link de la grabación de llamada (`linked_call`) y notas de cierre.
    - **Enrutamiento y Menú Dock (`frontend/src/App.jsx` y `useDockNavigation.js`) [MODIFY]**:
      - Registro de las nuevas páginas en las rutas protegidas del enrutador de React de acuerdo a los roles autorizados.
      - Integración de la pestaña "Mazo de Leads" (icono `Layers`) en el Dock de navegación para dar acceso inmediato y elegante a ambos roles.
  - **Feature: Unificación a Interfaz de 3 Columnas ("Gestión de Leads")**:
    - **Interfaz Unificada (Frontend) (`LeadsManagementPage.jsx`) [NEW]**:
      - Creación de una página unificada, robusta y premium que unifica `SetterDeckPage` y `CloserDeckPage` bajo la ruta `/setter/deck` y `/closer/deck`, autoadaptándose dinámicamente según el rol.
      - **Columna 1 (Huérfanos)**: Lista de leads más recientes creados hoy sin asignar (huérfanos), con botón rápido para ir a unattributed-leads.
      - **Columna 2 (Mazo Central)**: Ficha activa del lead del mazo central (nombre, estado, botones de navegación, Closer asignado, fecha de meet), secciones dinámicas de "Setter Data" e "Closer Data", hilo de comentarios y botón de progreso.
      - **Columna 3 (Timeline & Recharts)**: Bitácora interactiva de acciones (Timeline vertical) y gráfica circular interactiva de Recharts con cantidades, estados y porcentajes en tiempo real.
      - **Header & Footer KPIs**: 5 KPIs superiores consolidando agendas totales, mis agendas, sin asignar (huérfanos), realizadas (%) y cancelaciones/No show (%), y 5 KPIs inferiores resumiendo llamadas, confirmadas, cancelaciones, reprogramaciones y leads calificados de hoy.
  - **Feature: Bitácora de Acciones e Historial de Leads**:
    - **Base de Datos (`app/models/crm.py`) [MODIFY]**: Creación del modelo `LeadEventLog` para almacenar registros históricos de acciones sobre las citas (comentarios, notas del setter, notas del closer, asignaciones, etc.).
    - **Migración de Alembic**: Generación y aplicación de la migración `add_lead_event_log_history_model` con éxito en SQLite (`instance/local.db`).
    - **API (Backend)**:
      - Creación de endpoints `GET /deck/events/<appt_id>` en ambos controladores para traer el historial (con parámetro `limit` opcional para el modal de historial completo).
      - Integración de triggers automáticos al añadir comentarios y guardar notas en las citas.
  - **Feature & Fix: Filtro de Fechas Dinámico de Base de Datos y Resolución del Crash (Página Oscura)**:
    - **Causa de la Página Oscura (Crash)**: En el frontend, llamadas directas a `new Date().toLocaleDateString()` y `.toLocaleTimeString()` con fechas nulas o formatos inválidos causaban un RangeError fatal que desmontaba todo el árbol de React, dejando el fondo vacío ("página oscura").
    - **Resolución de Crash**: Implementación de las funciones de parseo de fechas defensivas `formatTime` y `formatTimeOnly` con bloques `try-catch` y validaciones robustas.
    - **Filtro de Fechas Dinámico**:
      - Se modificaron las funciones `fetchQueue` y `fetchStats` en el frontend para enviar el parámetro `date_range` seleccionado en el panel superior de filtros.
      - Se añadió `filterDateRange` como dependencia en el effect de inicialización para gatillar recargas en tiempo real al cambiar el selector de periodo.
      - **Backend (`setter.py` y `closer.py`) [MODIFY]**: Se implementó soporte en los endpoints de deck y KPIs para recibir `date_range` (`today`, `week`, `month`, `all`) y aplicar filtros dinámicos con SQLAlchemy a todas las consultas del servidor (mazo, KPIs superiores y gráfica circular de Recharts).
      - **Fix de Importación**: Se importó `date` y `timedelta` en la cabecera de `app/api/setter.py` para solucionar un `NameError` que arrojaba error 500 al filtrar periodos.

- **27 de Mayo de 2026**:
  - **Optimización de Gestión de Leads (Setters & Closers)**:
    - **Backend (`app/api/setter.py` y `app/api/closer.py`) [MODIFY]**:
      - Reestructuración de la consulta del mazo secuencial (`/deck`) en ambos controladores para restringir estrictamente la visibilidad a citas reales en estado `Agendado` y pendientes de atención (`setter_processed == False` y `closer_processed == False` respectivamente).
      - **Ampliación de filtro de agendados**: Se detectó que las citas agendadas por el calendar se registran con `result == None` o `result == ''`. Se modificó la consulta en `/deck` para incluir estas citas como agendados válidos activos: `or_(Appointment.result == 'Agendado', Appointment.result == None, Appointment.result == '')`, garantizando que Setters y Closers visualicen su carga correspondiente del período.
      - Remoción preventiva de la creación y consulta de prospectos artificiales de ManyChat en estado 'Entrante' en el mazo secuencial del Setter.
      - Supresión de la modificación del estado (`result`) en el procesamiento del Setter para garantizar la consistencia en la fase de precalificación.
      - Garantía de privacidad del Closer, asegurando que el Setter no pueda consultar datos confidenciales del cierre (`closer_notes`, `linked_call`).
    - **Interfaz (Frontend) [MODIFY]**:
      - **`LeadsManagementPage.jsx`**: Ocultación selectiva de la sección de Closing y la selección de estados en el mazo central de 3 columnas para usuarios con rol `setter`. Esto asegura que el Setter no tenga visibilidad sobre las notas de cierre ni grabaciones de llamada, ni pueda alterar el estado de la cita, enfocándose exclusivamente en la precalificación.
      - **`SetterDeckPage.jsx`**: Remoción total del selector visual de estados del lead, adaptando el flujo a la precalificación fija y unificando el botón de acción principal a *"Guardar Precalificación y Avanzar"*.
      - **`CloserDeckPage.jsx`**: Validación y optimización visual del acceso a los datos precalificados provistos por el Setter (Notas, Instagram, Palabra Clave y enlace directo al chat de IG) en formato Glassmorphism.
      - Actualización de las instrucciones del mazo para ambos perfiles para alinearse al nuevo flujo y garantizar una bandeja de entrada limpia.
